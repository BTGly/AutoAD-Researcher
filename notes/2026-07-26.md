# 2026-07-26 开发日志

## UAT round3 修复交接文档

- 目标：将 2026-07-26 UAT round3 的问题、当前实现边界和后续修复顺序交接给下一位开发者/修复 Agent。
- 操作：新增 `docs/uat/2026-07-26_AutoAD_UAT_round3_交接文档.md`；明确资料路径同轮登记、Source intake 实时刷新、仓库候选与用户授权分离、readiness 状态和提示词工程边界。
- 结果：文档明确否定“唯一代码仓库自动成为执行仓库”；唯一仓库只能成为候选，必须由用户以自然语言明确确认后再写入 executable role 并执行 admission。
- 遗留：文档只记录交接和修复计划，没有修改业务代码；round3 问题仍需按文档阶段执行、专项验证和真人 UAT。

## 本地资料路径与后台任务闭环修复

- 目标：继续修复显式本地路径在会话上下文、正式 Source/Job promote、Worker 执行之间的解析和授权不一致，并保留结构证据冲突时的只读确认边界。
- 实现：统一有界本地路径查找结果并持久化 `path_resolution`；支持明确的绝对路径、`workspace/...` 和可唯一解析的相对路径；重复 Job 身份比较忽略仅诊断性的路径查找元数据。结构类型与用户声明冲突时只附加上下文并返回 `confirmation_required`，不登记 Source 或排队 Job。
- Worker：优先使用 intake 时持久化的解析结果，兼容旧 Job 的回退解析；Dataset manifest 成功后同步 Source intake 状态和产物元数据。
- 验证：`tests/test_session_context.py`、`tests/test_v2_task_bridge.py` 通过（29 passed）；资料、通知、聊天路由和 readiness 回归通过（39 passed）；本地后端 `/api/health` 和 Vite 首页均返回成功。
- 遗留：完整门禁与前端构建待本记录后执行；本轮不提交、不推送 GitHub，先交给用户本地启动测试。

## UAT round3 资料接入与执行仓库授权闭环

- 目标：完成 round3 的明确路径同轮登记、Source intake 实时刷新、仓库候选/用户授权/admission/readiness 解耦，并收敛实验确认交互。
- 操作：显式且唯一的本地路径在当轮提升为 Source/Job，结构类型冲突仍停在确认边界；Source registry 写入后追加可重放的 `source.intake_updated` 事件，前端采用按 run 单调请求版本防止旧响应回写；新增候选投影、候选 revision、授权 API 和五态 readiness，唯一候选也不自动获得 `executable` 角色。
- 提示词与 Gate：复用现有 Dialogue Decision/Gate，将自然语言确认、仅作参考和取消映射为 typed action；确定性代码校验 pending task、精确 Source ID、候选集合、revision 和技术证据 SHA。多候选的无指向确认和过期确认均 fail closed；仓库授权不会生成 `input_task.yaml`、Session 或环境 Job。
- 前端：新增独立执行仓库授权弹窗，实验确认窗只展示已冻结的仓库与主指标；支持键盘焦点、错误状态、390x844 移动端和 640x360 等效 200% 可用视口。
- 验证：round3 后端专项 127 passed；前端 `lint` 和 `build` 通过；Playwright mock 7 passed（含旧 `/sources` 响应竞态、过期 revision、仅作参考、移动端和 200% 视口）；真实 FastAPI/Vite 全栈 3 passed，已校验 binding、单一环境 Job 和 Session 持久化；桌面/移动端运行期截图已人工检查，无文本或控件重叠。
- 遗留：代码层面无已知 round3 遗留；最终全量门禁、推送和 GitHub Actions 在本记录后执行，产品环境真人 UAT 仍需用户独立复验。
