# 2026-07-16 Development Log

## Entry 1 - Design V2 orthogonal dialogue decision and state projection

Goal:
- Establish the implementation contract for the 57-case intent-alignment
  findings before changing V2 prompts, action dispatch, permissions, or the
  benchmark.
- Separate interaction mode, action scope, permission, evidence status, and
  conversation transition without restoring the removed Stage 3 pipeline.

Operations:
- Read the current V2 Decision -> Gate -> Reply code path, source/job state,
  TaskBridge, existing PermissionEngine, 57-case report and benchmark runner.
- Reviewed local OpenHands SDK confirmation policy, OpenCode permission
  requests, DeepAgents filesystem permissions, and MiMoCode task gate as
  public architecture references.
- Added `docs/architecture/v2_dialogue_decision_and_state_projection.md` with
  the orthogonal contract, typed candidate-action boundary, permission
  integration, bounded state projection, dispatch order, and benchmark
  requirements.
- Preserved the user-owned changes to
  `configs/benchmarks/user_intent_p0_cases_v1.json` and
  `scripts/bench_user_intent_alignment.py` without staging them.

Result:
- The next implementation steps have a source-of-truth design: an action
  request is no longer synonymous with experiment execution, and infeasible
  objectives, insufficient evidence, and scientific misconduct are distinct
  outcomes.

Leftovers:
- Implement typed source/task candidate actions and route them through the
  existing PermissionEngine.
- Add the persisted V2 state projection and transition audit record.
- Convert the 57-case runner from case-id setup branches and one-mode scoring
  to declarative fixtures and multi-axis assertions after the control-plane
  contract is implemented.

## Entry 2 - Add typed source reparse candidate and V2 permission dispatch

Goal:
- Implement the first bounded typed source action from the V2 decision design:
  request a new immutable parse attempt for an already-registered PDF source.
- Route source actions through the existing PermissionEngine instead of using
  an LLM response as authorization.

Operations:
- Extended `SourceInstruction` with `request_source_reparse`; source removal
  remains a separate destructive action.
- Added a narrow V2 source-action permission adapter. It allows only
  `paper_parse_mineru` for a registered local PDF and returns `ask` for source
  removal. Code, experiment, and system actions remain outside the V2 dialogue
  dispatcher.
- Updated `DialogueGate` to validate an exact source id and reparseable PDF,
  append a permission audit record, and strip denied or unavailable actions.
- Added reparse dispatch after a validated Reply summary. It records a
  `source.reparse_queued` event and suppresses an equivalent queued/running
  parse job for the same source.
- Added Gate and orchestration regressions for allowed reparse, unavailable
  source input, preserved old parse attempt metadata, permission audit, and
  duplicate-job suppression.
- Updated the Reply profile so an authorized source reparse is described as a
  material operation rather than as code or experiment execution.

Result:
- A user-requested reparse can now traverse Decision -> Gate ->
  PermissionEngine -> job ledger without loosening execution boundaries.
- Focused regression suite: 55 passed.

Leftovers:
- Add the full orthogonal decision schema and persisted state projection.
- Convert the 57-case runner to declarative preconditions and multi-axis
  assertions after the state projection is available.

## Entry 3 - Persist bounded V2 dialogue state projection

Goal:
- Replace transcript-only continuity with a bounded projection assembled from
  persisted gated decisions, source jobs, TaskBridge state, and source parse
  state.
- Ensure an invalid decision or an unpersisted Reply never becomes a recorded
  dialogue transition.

Operations:
- Added the V2 transition ledger under `assistant/v2_dialogue_transitions.jsonl`.
  Records include the gated mode/policy, candidate source/task action,
  permission outcome, execution gate, gate notes, and summary hash; no raw
  user text, provider output, or host path is written.
- Added a read-only `dialogue_state` projection to the existing one-per-turn
  context build. It supplies the last persisted decision, bounded pending
  source actions, TaskBridge status, and compact source/parse state to both
  Decision and Reply calls.
- Appended a transition only after the validated Reply summary is saved. A
  schema-invalid Decision continues to preserve the previous summary and now
  also leaves no transition record.
- Added regressions for transition persistence, pending reparse action
  visibility, TaskBridge status precedence, compact source parse state, and
  failed-decision non-persistence.

Result:
- Multi-turn V2 decisions now have a persisted control-plane continuity input
  without making the transcript the authority for source, task, or action
  state.
- Focused regression suite: 75 passed, 2 skipped.

Leftovers:
- Migrate the Decision schema from overloaded mode/policy fields to the
  orthogonal contract in the design document.
- Rebuild the 57-case fixture and scoring layer on the completed decision,
  permission, and state projections.

## Entry 4 - Migrate V2 decisions to orthogonal control-plane axes

Goal:
- Make the V2 runtime emit the orthogonal decision fields defined in the
  design: interaction mode, action scope, policy, evidence status,
  conversation transition, feasibility, and numeric-claim boundary.
- Preserve compatibility for responses that still emit the removed
  `act_request` or `reject` mode values while making the Gate output canonical.

Operations:
- Added the six decision axes to `DialogueDecision`, `GatedDialogueDecision`,
  `OrchestratorResult`, and persisted V2 transition records.
- Made the Gate map legacy `act_request` to `act`; a policy assessment that
  rejects now produces `policy=deny` without assigning a special interaction
  mode.
- Made the Gate derive the authoritative action scope from validated typed
  actions (`source`, `repository`, `experiment`, or `none`) rather than trust
  an unvalidated model label.
- Updated the Decision profile to request the new JSON fields, retaining
  defaults so existing structured responses remain parseable during rollout.
- Updated regression expectations from legacy mode labels to the independent
  policy and canonical mode fields; added coverage for evidence insufficiency,
  feasibility, conversation revision, and numeric-claim restriction.

Result:
- The control plane can now distinguish an infeasible target, an
  evidence-insufficient request, a denied scientific-integrity request, and a
  source-scoped action without conflating them into a single `reject` mode.
- Focused regression suite: 64 passed.

Leftovers:
- Rebuild the 57-case fixture and scoring layer on the completed decision,
  permission, and state projections.

## Backend Experiment Agents Development Plan

Goal:
- Add the experiment agents backend development plan to the notes.

Operations:
- Created `notes/后端开发_实验Agents设计包/` directory with 8 documents
  covering the full experiment agents architecture and development roadmap.
- Documents: architecture framework, 6 development plans (Session/Env,
  Coordinator/IdeaTree, ExecutorAgent, ExperimentJob/GPU, Validity/Reflection,
  Convergence/Budget), plus README index.
- Based on cross-project analysis of 22+ reference repositories.

Result:
- Development plan documents added to notes for reference.

Leftovers:
- Plan documents reference decisions from architecture review; further
  refinement may be needed after reviewer feedback.
