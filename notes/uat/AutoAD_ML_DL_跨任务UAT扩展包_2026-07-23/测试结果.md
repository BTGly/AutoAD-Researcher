# AutoAD ML/DL 跨任务 UAT 测试结果

日期：2026-07-23<br>
分支：`feat/production-candidate-entry`<br>
本轮验证基线：`f3f881e3e97c0473ef8c16ae5554aa3402a370a7`<br>
本轮修复提交：`f5b77d0aeefd2e5c45f33694d4cbec933df3393c`<br>
原始测试包：`AutoAD_ML_DL_跨任务UAT扩展包_2026-07-23`

本轮扩展包复测：官方 fixture oracle `ALL FIXTURES PASSED`；边界、Starter、Baseline、Adapter、patch protocol 和 semantic benchmark 回归 `87 passed`。正式项目门禁与前端验证以本轮发布命令输出为准。

## 执行结果

官方 fixture oracle 在独立 ML 环境中通过：`PASS 01`、`PASS 02`、`PASS 03`、`ALL FIXTURES PASSED`。随后没有修改测试包，在持久化 run `runs/uat_ml_dl_gpu_20260723_round1d/` 中使用真实 Session、Worktree、Attempt、Worker、Finalizer 和 ScientificAssessment 完成三类任务的 B_dev/B_test 闭环。
本次 oracle 使用现有锁定环境 `projects/AutoAD-Researcher/.venv-gpu`：Python 3.12.13、torch 2.5.1+cu124、scikit-learn 1.9.0、numpy 2.4.4；未修改测试包或安装依赖。项目默认 `.venv` 缺少这些 fixture 声明的 ML 依赖，已作为环境前置事实保留。

| 任务 | B_dev baseline -> candidate | B_test baseline -> candidate | 结果 |
|---|---:|---:|---|
| PyTorch XOR 分类，accuracy 最大化 | `0.4124999940395355 -> 1.0` | `0.36250001192092896 -> 1.0` | IMPROVEMENT |
| sklearn Diabetes 回归，RMSE 最小化 | `78.15070522569023 -> 56.26193500117719` | `75.35716006481208 -> 52.657269296257795` | IMPROVEMENT |
| sklearn 无监督聚类，silhouette 最大化 | `0.6028035751712463 -> 0.7337608117858551` | `0.583721676030237 -> 0.7148361214497098` | IMPROVEMENT |

Diabetes 的 MAE/R2 以及聚类的 Davies-Bouldin guardrail 均被保留并完成方向正确的比较。三个任务的命令均保留 fixture 声明的 phase token，未按文件路径猜测 split。

## 故障注入与修复复测

- 分类运行时失败：`MissingModel()` 候选保留为 `attempt_000005=FAILED`，`RUN_COMMAND_FAILED`，无指标，`OutcomeCard.metrics_parsed=false` 且没有 Candidate/Champion。
- 回归重复性：同一 admitted patch、同一 `ComparisonIdentity` 的无固定随机种子候选两次真实 CPU 执行后，第二个 Attempt 持久化 `reproducibility.json`，状态为 `not_reproducible`，`scientific_effect=null`，DecisionEngine 返回 `confirm_seed`；修复前两次均错误报告 `IMPROVEMENT`，登记为 `BUG-20260723-011`。
- 聚类防作弊：尝试通过 `data.py` 引入隐藏标签/ARI 的候选在 Executor 阶段连续收到 `REPAIR_REJECTED_HARD`，未创建 Attempt、指标或 Champion。
- 修复范围：重复性比较只使用历史 Attempt、相同 `final_patch.diff` SHA、相同显式比较身份和 EvaluationContract 声明指标；单次候选保持 `not_checked`，没有为 fixture、任务名或指标名添加特判。

本轮代码与测试门禁：`1968 passed, 2 skipped`；新增科学评估/推广回归 `12 passed`。前端 `npm run lint`、`npm run build`、普通 Playwright `38 passed`、完整栈 Playwright `2 passed`。三 fixture oracle 重新执行为 `ALL FIXTURES PASSED`。

## 产品边界

中文情感分类请求只走 `plan_only`：`input_task.yaml` 已写入，但没有创建 Session、Job 或 Attempt。另有三类完整现实世界设计语义请求通过 `unsupported_domain` policy contract 和 Dialogue Gate 覆盖，均移除 source/task/target 动作；该检查验证的是产品边界和不执行承诺，不是现实世界科学验证。

## 已关闭问题

- P1：显式 phase 命令兼容性。适配器现在支持无 split 文件槽的 `--split b_dev/b_test`，也不会替换 `split_ref_arg_index` 已指向 phase literal 的参数。
- P1：OutcomeCard 元数据兼容性。候选和确认只读取冻结 EvaluationContract 声明的指标，不再把 `split`、`seed`、`sample_count` 等元数据当作指标。
- 聚焦回归：`29 passed`。

## 当前结论

- 流程 1、2、3 的跨任务控制面闭环通过；流程 4 的中文情感 `plan_only` 和三类 `unsupported_domain` 边界复测通过。
- 历史 adapter `BLOCKED`、Flow 4 `COVERAGE_GAP` 和环境探针行保留为历史记录；后续复测行及 `跨任务Bug记录.csv` 已记录对应关闭证据。
- 故障注入要求已完成：失败 Attempt 保留、protected path fail-closed、重复不稳定指标不再被当作稳定提升；`BUG-20260723-011` 已修复并通过真实复测。
- PatchCore/MVTec 是独立收尾包，不作为本扩展包的跨任务通过条件；其当前 GPU 真实执行结果见 `notes/uat/AutoAD_UAT_收尾验收包_2026-07-23/测试结果.md`。

详细逐步证据见 `跨任务UAT观察记录.csv` 和 `跨任务Bug记录.csv`；临时 runs、fixture 副本和测试环境未归档。

## 2026-07-24 GPU 控制面续验

当前代码提交：`4854e1459a84d6cedfa939a19a01cb7c2aa8c4d6`；GitHub Actions verify run：`30075959049`，结果 `success`；项目全量门禁为 `1971 passed, 2 skipped`。

- `runs/uat_gpu_runtime_20260724_r5/`：两个独立 run 同时申请一张 GPU、每个要求 `20000 MB`；中途 lease 分别绑定 GPU `0/1`。两个 CUDA 子进程均 `COMPLETED`，分别报告 RTX 4090 和 `device_count=1`，资源报告均为 `measurement_kind=measured`，GPU utilization 峰值为 `90%/4%`，最终 active lease 为空。
- `runs/uat_patchcore_runtime_smoke_20260724_r1/`：通过当前 AutoAD Worker 执行 runtime-only PatchCore/MVTec bottle `b_dev` smoke。Attempt `attempt_000001` 成功，三项指标均解析，结果为 `1.0 / 0.9793658025353316 / 0.9722287243867787`；保护文件哈希 before/after 一致；资源报告进入 output manifest，峰值显存 `868 MB`、utilization 峰值 `16%`，lease `lease_000001` 已释放。
- 严格边界：PatchCore 工作区的受保护文件哈希与历史冻结证据一致，但当前副本没有 `.git`，冻结 commit `aa646496cd960f30a21eedf1ba97d0371bc26200` 也不在本地或官方 upstream，因此本条仅作为 GPU runtime/control-plane evidence；不替代正式 readiness，也不产生 candidate、B_test、泛化或 Champion 结论。

本节只补充资源控制面和真实 CUDA 执行证据，不改变前三类 ML/DL 任务的既有科学结果或历史观察结论。
