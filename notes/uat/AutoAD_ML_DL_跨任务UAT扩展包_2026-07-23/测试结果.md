# AutoAD ML/DL 跨任务 UAT 测试结果

日期：2026-07-23<br>
分支：`feat/production-candidate-entry`<br>
本轮验证基线：`49eebc70fbfc428b501d12c69591863dd180d29c` 加本轮工作树控制面修复<br>
原始测试包：`AutoAD_ML_DL_跨任务UAT扩展包_2026-07-23`

正式项目门禁：`1957 passed, 2 skipped`。前端 lint/build 通过；普通 Playwright `37 passed`；隔离 full-stack Playwright `2 passed`。

## 执行结果

官方 fixture oracle 在独立 ML 环境中通过：`PASS 01`、`PASS 02`、`PASS 03`、`ALL FIXTURES PASSED`。随后没有修改测试包，在临时副本中使用真实 Session、Worktree、Attempt、Worker、Finalizer 和 ScientificAssessment 完成三类任务的 B_dev/B_test 闭环。
本次 oracle 使用现有锁定环境 `projects/AutoAD-Researcher/.venv-gpu`：Python 3.12.13、torch 2.5.1+cu124、scikit-learn 1.9.0、numpy 2.4.4；未修改测试包或安装依赖。项目默认 `.venv` 缺少这些 fixture 声明的 ML 依赖，已作为环境前置事实保留。

| 任务 | B_dev baseline -> candidate | B_test baseline -> candidate | 结果 |
|---|---:|---:|---|
| PyTorch XOR 分类，accuracy 最大化 | `0.4124999940395355 -> 1.0` | `0.36250001192092896 -> 1.0` | IMPROVEMENT |
| sklearn Diabetes 回归，RMSE 最小化 | `78.15070522569023 -> 56.26193500117719` | `75.35716006481208 -> 52.657269296257795` | IMPROVEMENT |
| sklearn 无监督聚类，silhouette 最大化 | `0.6028035751712463 -> 0.7337608117858551` | `0.583721676030237 -> 0.7148361214497098` | IMPROVEMENT |

Diabetes 的 MAE/R2 以及聚类的 Davies-Bouldin guardrail 均被保留并完成方向正确的比较。三个任务的命令均保留 fixture 声明的 phase token，未按文件路径猜测 split。

## 产品边界

中文情感分类请求只走 `plan_only`：`input_task.yaml` 已写入，但没有创建 Session、Job 或 Attempt。另有三类完整现实世界设计语义请求通过 `unsupported_domain` policy contract 和 Dialogue Gate 覆盖，均移除 source/task/target 动作；该检查验证的是产品边界和不执行承诺，不是现实世界科学验证。

## 已关闭问题

- P1：显式 phase 命令兼容性。适配器现在支持无 split 文件槽的 `--split b_dev/b_test`，也不会替换 `split_ref_arg_index` 已指向 phase literal 的参数。
- P1：OutcomeCard 元数据兼容性。候选和确认只读取冻结 EvaluationContract 声明的指标，不再把 `split`、`seed`、`sample_count` 等元数据当作指标。
- 聚焦回归：`29 passed`。

## 仍未闭环

- PatchCore/MVTec 物理 GPU 验收未启动：当前主机 `nvidia-smi` 返回 `No devices were found`。没有停止其他测试进程、修改共享 PatchCore 工作区或写入共享 runs。

详细逐步证据见 `跨任务UAT观察记录.csv` 和 `跨任务Bug记录.csv`；临时 runs、fixture 副本和测试环境未归档。
