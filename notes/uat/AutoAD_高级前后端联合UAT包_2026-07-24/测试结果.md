# 高级前后端联合 UAT 结果

## 执行边界

- Worktree：`/root/autodl-tmp/AI4S/AutoAD-advanced-uat-fix`
- Branch：`agent/advanced-uat-fix`
- UAT 资料基线：`d92701cc100bafbecd47749b67e4df6406a1f416`
- 第一轮修复提交：`f1e5de2`
- 未修改主 worktree、共享 runs、共享 Worker、GPU 工作区、`main` 或 `v0.9.0-rc1`。

## 已复验

- CPU fixture：baseline B_dev/B_test `0.0`；safe candidate B_dev/B_test `1.0`；Baseline 两个 Attempt 完成，Session `READY`。
- 控制面候选专项：`4 passed, 11 deselected`。
- 后端专项：控制面/故障/修复/Worker 共 `50 passed`；报告协作与 API 共 `69 passed`；语义、安全、executor 共 `54 passed`。
- 前端：lint、build、工作台目标 Playwright `15 passed`、普通 Playwright `37 passed`、full-stack Playwright `2 passed`；报告目标与视觉测试包含在普通套件中。

## 当前结论

- P0/P1：已修复并有回归证据。
- A：CPU 与控制面子集通过；不把 synthetic fixture 当作真实领域结论。
- B/C/D/E：现有后端合同与前端自动化覆盖通过；真人多窗口、屏幕阅读器和完整人工脚本仍需独立验收。
- 07H：历史 Session 2 的 readiness 记录曾阻塞；本轮当前独立 run `autoad-advanced-physical-readiness-20260723-r2` 的 `PhysicalReadinessGate` 已为 `ready`，并完成冻结内部 PatchCore bottle seed 0 baseline smoke。该单 seed 结果不等于 candidate/B_test/泛化结论。
- H：未进行独立盲测，不判通过。
- GitHub Actions：`verify` run `30037988275` 已通过，commit `ba8e45bfd64f181d70b50b26e49cf31c5b503334`，覆盖仓库验证、前端 lint/build、普通浏览器和 full-stack 浏览器测试。
- 发布：不满足创建或移动 `v0.9.0-rc1` 的条件。

## Session 6 补充

- 本轮新增 E 类回归：Baseline 首个错误字段焦点、390px 无横向溢出、报告证据完整元数据按需展示；目标前端 `22 passed`，普通 Playwright `40 passed`，full-stack `2 passed`，lint/build 通过；后端 `verify.sh` 为 `1941 passed, 2 skipped`。
- 07H 当前独立 readiness run `autoad-advanced-physical-readiness-20260723-r2` 为 `ready`，并完成冻结内部 PatchCore bottle seed 0 baseline smoke；Attempt `attempt_000001` / Job `job_000001` 成功，保护文件校验通过，GPU lease 已释放。该证据不等于 candidate/B_test/泛化结论。
- 07H 的 raw `OutcomeCard` 与 derived `scientific_assessment` 状态差异由 `assessment_reconciliation.json` 的双权威设计明确记录，不作为未决冲突。

## Session 7 补充

- 提交 `ba8e45bfd64f181d70b50b26e49cf31c5b503334` 的 GitHub Actions `verify` run `30037988275` 成功；五个门禁步骤全部通过。

## Session 8 补充

- 新增 E 类 200% 布局回归：工作台无横向溢出，Baseline 表单可达，`role=alert` 持久可见，首个错误字段获得焦点且表单通过 `aria-describedby` 关联；专项 1 passed，普通 Playwright `41 passed`。
- 本轮完整门禁：`bash scripts/verify.sh` 为 `1941 passed, 2 skipped`；frontend lint/build 通过；隔离 full-stack Playwright `2 passed`。
- 旧 RareCLIP 独立资料仅作为兼容性近邻探针：当前 Adapter 识别显式 manifest 并拒绝绝对 worktree 路径；该资料生成于 2026-07-19，不能作为当前 HOLDOUT-H。H 仍待独立 tester 生成并运行。
- 本轮最终提交 `d85de29883df251ac2c70aa4e83cf454cee90f27` 的 GitHub Actions `verify` run `30041041292` 成功；仓库验证、frontend lint/build、普通浏览器和 full-stack 浏览器步骤全部通过。
