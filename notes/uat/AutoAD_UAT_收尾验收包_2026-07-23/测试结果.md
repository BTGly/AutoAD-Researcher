# AutoAD 收尾验收 UAT 测试结果

日期：2026-07-23
分支：`feat/production-candidate-entry`
GitHub 基线：`origin/main`=`1c524893b5f81cebe7d6c0e27398d46f42d2ca18`
验收修复 commit：`585a724`

正式项目门禁：`1965 passed, 2 skipped`。前端 `npm run lint`、`npm run build` 通过；普通 Playwright `37 passed`；隔离 full-stack Playwright `2 passed`。

## 流程 A：CPU 微型完整闭环

持久化运行目录：`runs/uat_closeout_20260723/uat_a_cpu_1157_fb98/`。

- success fixture 直接 verifier 通过，baseline `image_auroc=0.28125`（B_dev）。
- 真实控制面完成 baseline、model-only candidate、B_test baseline、candidate confirmation 和人工 Champion promotion。
- baseline 为 B_dev `0.28125`、B_test `0.25`；candidate 为 B_dev/B_test `1.0`；噪声阈值 `0.01`；保护文件校验通过。
- `promotion_000002` journal 为 `committed`，ChampionPointer 指向 `candidate_000002`，Champion event 数量为 `1`；重复推广请求不产生第二次 merge。
- report v1 的 validation、digest、HTML 和 bundle 均已持久化。

结论：CPU 流程 A `CLOSED — ACCEPTED`。

## 流程 B：冲突、失败、恢复

持久化运行目录：`runs/uat_closeout_20260723/uat-b-cpu_1216_7d9c/`；Session：`session_1feb43a6527f5064`。

- baseline `attempt_000001` 保留 `FAILED`、`execution_status=CRASHED`、`execution_result.failure_code=RUN_COMMAND_FAILED`、分类 `UNKNOWN_RUN_FAILURE`、`retryable=false`、`NON_COMPARABLE` 和原始 stderr。
- evaluation.py 固定指标并直接 Champion 的越界请求在 baseline gate 被阻止，没有创建 Candidate 或 Champion。
- 精确 B3 冲突话术通过持久化 HTTP 重放：系统明确说明自动执行与逐步确认不能同时生效，当前不修改代码、不运行实验，并要求在实验工作台确认唯一模式。
- 合法 `model.py` 修复通过 `RETRY_FAILED + BaselineRepairInput` 进入 `baseline_repair` Proposal；`attempt_000002` 以 `attempt_purpose=repair` 完成，改变文件严格为 `model.py`，Session 为 `READY_FOR_BASELINE/b_dev_completed`；原失败 Attempt 未改变。
- report v4 由更新后的报告讨论合同分配，`content_ready` 且 validation/digest/bundle 完整；scientific status 仍为 `EVIDENCE_INSUFFICIENT`，Champion 未物化。
- report v4 的干净 discussion transcript 对精确问题“请列出失败 Attempt 的直接证据和下一步最小修复动作。”准确引用 `execution_status=CRASHED`、`exit_code=1`、`failure_code=RUN_COMMAND_FAILED`、`UNKNOWN_RUN_FAILURE`、stderr 中的 `RuntimeError: intentional UAT model failure; repair model.py only` 及登记 Evidence ID；最小下一步限定为 repair `model.py` 后 retry。

结论：CPU 流程 B 的失败留痕、越界阻止、受控修复、报告和证据讨论通过；修复成功未被包装成论文方法有效性证明。

## 流程 C：PatchCore 真实 GPU

主机 `nvidia-smi` 返回 `No devices were found`，readiness gate fail closed；没有启动物理 PatchCore/MVTec 训练，没有修改共享 checkout 或共享 runs。

结论：流程 C 未通过，原因是当前主机没有 GPU；这是外部容量限制，不是伪造的 CPU 结果。

## 流程 D：现实世界科学边界

蛋白质结构预测与设计、建筑结构设计、材料配比设计三类完整语义请求均通过 `unsupported_domain` policy 和 Dialogue Gate：source、task、target 动作被移除，无 Session、Job 或 Attempt；仅保留材料梳理或明确标注未验证领域的 `plan_only` 路径。

结论：流程 D 通过。这是结构化政策门禁验证，不是现实世界科学验证。

## 正式结论

正式 UAT 的 CPU 流程 A、B、D 已通过，全部权威证据位于持久化 `runs/uat_closeout_20260723/`；正式记录不依赖临时派生夹具。流程 C 仍因 GPU 不可用未执行，因此当前不能宣布整套 UAT 完全通过，也不具备创建新的 RC 标签条件。

历史临时回放和早期错误讨论仍保留在开发日志与旧 report 版本中，但不作为当前正式验收的权威通过证据；report v4 是 B5 讨论的当前权威版本。
