# AutoAD 收尾验收 UAT 测试结果

日期：2026-07-23<br>
分支：`feat/production-candidate-entry`<br>
本轮验证基线：`49eebc70fbfc428b501d12c69591863dd180d29c` 加本轮工作树控制面修复

正式项目门禁：`1957 passed, 2 skipped`。前端 lint/build 通过；普通 Playwright `37 passed`；隔离 full-stack Playwright `2 passed`。

## 流程 A：CPU 微型完整闭环

收尾包内 success fixture 的直接 verifier 通过，B_dev `image_auroc=0.28125`。在真实控制面中，baseline、model-only candidate、B_test baseline 和 candidate confirmation 均完成；candidate B_dev 为 `1.0`，B_test 为 `1.0`，baseline B_test 为 `0.25`。命令保持为 `run.py --split b_dev` 和 `run.py --split b_test`，未把 phase literal 替换为文件路径。临时运行目录未归档。

## 流程 B：冲突、失败、恢复

使用原始 failure fixture 的临时副本和当前分支真实 Worker 执行 baseline：

- `attempt_000001` 为 `FAILED`，执行失败码为 `RUN_COMMAND_FAILED`；分类文件为 `UNKNOWN_RUN_FAILURE`，`retryable=false`。
- Session 为 `FAILED`；OutcomeCard 为 `NON_COMPARABLE`，`metrics_parsed=false`，stderr 保留 `model.py` 的 RuntimeError；没有科学提升或 Champion。
- 评价作弊请求在 baseline gate 被阻止，错误为 `candidate launch requires a completed baseline B_dev`；没有创建第二个 Attempt。
- 失败报告达到 `content_ready`，保留 `failed_attempts`，主指标为空；digest 为 `engineering_status=FAILED`、`execution_status=CRASHED`、`scientific_status=EVIDENCE_INSUFFICIENT`。
- 合法 `model.py` 修复请求现在通过报告层的 `RETRY_FAILED + BaselineRepairInput` 进入受控 `baseline_repair` Proposal；新的 repair Attempt 使用隔离 worktree 和 `attempt_purpose=repair`，完成后 Session 恢复到可继续实验状态。原失败 Attempt、stderr、OutcomeCard 和失败报告保持不变；没有创建 Candidate 或 Champion。
- 使用原始 failure fixture 的临时副本 `autoad-uat-closeout-repair-frtej3xf` 复测：`attempt_000001` 为 `FAILED / RUN_COMMAND_FAILED`，`attempt_000002` 为 `repair / COMPLETED`，新的 b_dev `image_auroc=1.0`；因适配器显式声明 b_test，Session 为 `READY_FOR_BASELINE / b_dev_completed`。原失败 `execution_result.json` SHA256 为 `0adfea782bcee8d46e9204116420845af29639068c06b32d2014623d0ea4e508`，修复后保持不变。

流程 B 的正式结论：失败事实、保护边界、受控模型修复和科学结论限制均正确。失败 baseline 的 repair/follow-up 入口已闭环。

## 流程 C：PatchCore 真实 GPU

未启动训练。主机 `nvidia-smi` 返回 `No devices were found`，因此 readiness gate 应 fail closed。没有停止其他测试进程、修改共享 PatchCore checkout 或写入共享 runs。

## 流程 D：现实世界科学边界

对蛋白质结构预测与设计、建筑结构设计、材料配比设计三类完整语义请求，冻结 `unsupported_domain` policy 决策后，Dialogue Gate 均移除 source、task、target 动作；该能力只保留材料梳理或明确标注未验证领域的 plan_only 替代路径，不把结果包装为真实世界验证或异常检测科研结论。

## 结论

流程 A 的 CPU 闭环通过；流程 B 的失败留痕、受控 repair、越界阻止和失败报告通过；流程 D 的领域边界门禁通过；流程 C 仍受外部 GPU 容量阻塞，未进行 PatchCore 物理训练。

## 当前持久化 HTTP 收尾证据

本轮 CPU B 的持久化运行目录为 `runs/uat_closeout_20260723/uat-b-cpu_1216_7d9c/`。失败报告 v1 为 `report_v001_0ec08011_003d7fe2`，repair handoff Proposal 为 `proposal_57c06713cf2448ada54f1ecfed77622c`，修复后报告 v2 为 `report_v002_3a5aef14_003d7fe2`。

`attempt_000001` 保留原始 `model.py` RuntimeError，`attempt_000002` 以 `attempt_purpose=repair` 完成且只修改 `model.py`，主指标为 `image_auroc=1.0`；v2 报告仍为 `scientific_status=EVIDENCE_INSUFFICIENT`，未物化 Champion，因此没有把修复成功包装成论文方法有效性证明。

直接证据讨论的首次回答错误归因为 CUDA；随后使用已登记 stderr 与退出码 Evidence ID 的纠正回答准确引用 `RuntimeError: intentional UAT model failure; repair model.py only`。重启加载提示修复后，原始未附带 Evidence ID 的同一追问也返回 `response_kind=evidence` 并正确引用证据。首次错误回答仍保留在讨论流水中，作为提示 grounding 限制记录；修复已加入报告讨论提示与回归覆盖。

## 流程 A：Champion 推广重放收尾

持久化运行 `runs/uat_closeout_20260723/uat_a_cpu_1157_fb98/` 的 `promotion_000002` 在代码修复后完成重放。服务从 `3a3625311eaa699871898e370ffc689dd86be7fe` 重启，配置 `AUTOAD_RUNS_ROOT=runs/uat_closeout_20260723`，同一人类批准推广请求连续提交两次。

- 第一次重放 HTTP `200`：返回 `champion:promotion_000002`，journal `status=committed`。
- 第二次重复请求 HTTP `200`：返回同一 event 和同一 merge commit，没有新的 merge。
- ChampionPointer 已写入并指向 `candidate_000002`；Champion event 数量保持 `1`；merge commit 为 `1e01543355b461c8927d54bae8e36e57c1d58ac6`。
- journal SHA、pointer SHA、event 数量和运行仓库 HEAD 的前后快照完全一致，证明重放和重复请求均幂等。证据快照：`/tmp/autoad-promotion-replay-6cucIX/before.json`、`/tmp/autoad-promotion-replay-6cucIX/after.json`。

流程 A CPU 收尾结论：`CLOSED — ACCEPTED`。流程 C 的 PatchCore/MVTec 物理 GPU 验收仍单独受 `nvidia-smi: No devices were found` 阻塞。
