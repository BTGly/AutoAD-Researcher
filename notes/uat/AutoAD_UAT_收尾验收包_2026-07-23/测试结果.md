# AutoAD 收尾验收 UAT 测试结果

日期：2026-07-23
分支：`feat/production-candidate-entry`
GitHub 基线：`origin/main`=`1c524893b5f81cebe7d6c0e27398d46f42d2ca18`
主要验收修复 commit：`f90bc0848172678a059d8a2e0ed9f563a778693a`
最终审计 HEAD：`6a34ed3f4845e400b42988d19bf81810d97e36a4`
GitHub Actions verify run：`30022727711`
证据哈希审计：`50 references, 0 missing, 0 mismatch`

正式项目门禁：`1965 passed, 2 skipped`。前端 `npm run lint`、`npm run build` 通过；普通 Playwright `38 passed`；隔离 full-stack Playwright `2 passed`。真实浏览器重放持久化 B run 通过：`Session：B_dev 已完成`、`基线状态：B_dev 已完成`、失败 Attempt 和 report v4 证据仍可见。

## 流程 A：CPU 微型完整闭环

持久化运行目录：`runs/uat_closeout_20260723/uat_a_cpu_1157_fb98/`。

- success fixture 直接 verifier 通过，baseline `image_auroc=0.28125`（B_dev）。
- 真实控制面完成 baseline、model-only candidate、B_test baseline、candidate confirmation 和人工 Champion promotion。
- baseline 为 B_dev `0.28125`、B_test `0.25`；candidate 为 B_dev/B_test `1.0`；噪声阈值 `0.01`；保护文件校验通过。
- `promotion_000002` journal 为 `committed`，ChampionPointer 指向 `candidate_000002`，Champion event 数量为 `1`；重复推广请求不产生第二次 merge。
- report v1 的 validation、digest、HTML 和 bundle 均已持久化。

结论：CPU 流程 A `CLOSED — ACCEPTED`。

## 流程 B：冲突、失败、恢复

持久化运行目录：`runs/uat_closeout_20260723/uat-b-cpu_1216_7d9c/`；Session：`session_1feb43a6527f5064`。

- baseline `attempt_000001` 保留 `FAILED`、`execution_status=CRASHED`、`execution_result.failure_code=RUN_COMMAND_FAILED`、分类 `UNKNOWN_RUN_FAILURE`、`retryable=false`、`NON_COMPARABLE` 和原始 stderr。
- evaluation.py 固定指标并直接 Champion 的越界请求在 baseline gate 被阻止，没有创建 Candidate 或 Champion。
- 精确 B3 冲突话术通过持久化 HTTP 重放：系统明确说明自动执行与逐步确认不能同时生效，当前不修改代码、不运行实验，并要求在实验工作台确认唯一模式。
- 合法 `model.py` 修复通过 `RETRY_FAILED + BaselineRepairInput` 进入 `baseline_repair` Proposal；`attempt_000002` 以 `attempt_purpose=repair` 完成，改变文件严格为 `model.py`，Session 为 `READY_FOR_BASELINE/b_dev_completed`；原失败 Attempt 未改变。
- report v4 由更新后的报告讨论合同分配，`content_ready` 且 validation/digest/bundle 完整；scientific status 仍为 `EVIDENCE_INSUFFICIENT`，Champion 未物化。
- report v4 的干净 discussion transcript 对精确问题“请列出失败 Attempt 的直接证据和下一步最小修复动作。”准确引用 `execution_status=CRASHED`、`exit_code=1`、`failure_code=RUN_COMMAND_FAILED`、`UNKNOWN_RUN_FAILURE`、stderr 中的 `RuntimeError: intentional UAT model failure; repair model.py only` 及登记 Evidence ID；当前 transcript SHA256 为 `ddfe516c231a9f1dcc0f4d79cb18ea4c3f01f7c3ea074037dd08f83426626bab`；最小下一步限定为 repair `model.py` 后 retry。

结论：CPU 流程 B 的失败留痕、越界阻止、受控修复、报告和证据讨论通过；修复成功未被包装成论文方法有效性证明。

## 流程 C：PatchCore 真实 GPU

历史 C0 记录曾因主机 `nvidia-smi` 返回 `No devices were found` 而 fail closed；该历史记录保留，不代表当前主机状态。当前重测已确认 RTX 4090、CUDA 12.4 和 CUDA-enabled PatchCore 环境可用；用户提供的 `data/mvtec_ad` 已完成只读数据准备，train/B_dev/B_test manifest SHA 分别为 `f4436ea1742f1723292eba4118b6c2725d3e54139b80866f537f92ec28470ad9`、`2b980a11b6aa6658bbfd274b35cd64013e635a53e7e16fe7727ee99d7eb4de40`、`0927df5d4d288f1958f27a38ad18b64d172e9e736eb8171d66cfec248b377204`；项目 preflight 也已通过，证据位于 `runs/uat_closeout_20260723/physical_preflight_20260723_r1/`。

当前 C0 已有新的持久化 `ready` artifact：`runs/uat_patchcore_07h_gpu_20260723_r2/artifacts/07h/physical_readiness.json`，固定 case、数据 manifest、PatchCore commit、锁文件、离线权重、RTX 4090 和 protected hashes 均匹配。随后 C1 已通过正常控制面完成真实 baseline：Session `session_faad414c83732280`、Attempt `attempt_000001`、PipelineJob `job_000001`，GPU lease 已释放，指标已解析且 protected hashes 未变化。

C2 仍 BLOCKED：正式验收包要求两份非空、用户审阅的科学 `cycle_1.patch` / `cycle_2.patch`；本地没有这些精确输入。没有把历史 07H 记录、fixture patch 或其他不相干 diff 当作当前 C2 patch，因此没有启动 candidate、B_test 或 Champion。

## 流程 D：现实世界科学边界

蛋白质结构预测与设计、建筑结构设计、材料配比设计三类完整语义请求均通过 `unsupported_domain` policy 和 Dialogue Gate：source、task、target 动作被移除，无 Session、Job 或 Attempt；仅保留材料梳理或明确标注未验证领域的 `plan_only` 路径。

结论：流程 D 通过。这是结构化政策门禁验证，不是现实世界科学验证。

## 正式结论（C2 重测前历史快照）

正式 UAT 的 CPU 流程 A、B、D 已通过，Flow C 已完成 C0 readiness 和 C1 真实 GPU baseline，但 C2 candidate、B_test、报告和 Champion 收尾仍缺少两份用户审阅 patch；当前不能宣布整套 UAT 完全通过，也不具备创建新的 RC 标签条件。

历史临时回放和早期错误讨论仍保留在开发日志与旧 report 版本中，但不作为当前正式验收的权威通过证据；report v4 是 B5 讨论的当前权威版本。

## 本轮发布

预算文案修复发布于 `196c0a5bbc7699d706f3c8fd4c9205027cb49db5`，BUG-20260723-009 已关闭。最终验证：后端 `1965 passed, 2 skipped`，前端 lint/build 通过，普通 Playwright `38 passed`，full-stack Playwright `2 passed`。
## 当前重测补充：预算文案与 PatchCore C2

预算补全交互已复现并修复：CPU-only 合同使用 `20000, 0`，即 `max_wall_seconds=20000`、`max_gpu_seconds=0`，GPU 数量和显存为 `0`；浏览器回归现在明确断言字段顺序、单位、CPU/GPU 约束和提交 payload。

当前 GPU C0 readiness 已通过，C1 baseline、候选 B_dev、baseline B_test 和 candidate B_test 均通过正常 Session/Attempt/PipelineJob/Worker 控制面完成。候选仅修改 `coreset_sampling_ratio`，B_dev/B_test 主指标均为 `1.0`，候选科学评估为 `NO_EFFECT`，因此没有 CandidateSnapshot、Champion 或不当推广结论。两次启动器路径错误作为操作记录保留，不作为 PatchCore 产品缺陷。

## 当前正式结论

上面的“C2 重测前历史快照”仅保留当时的阻塞状态，已被本节和 `UAT观察记录.csv` 中的 C2 复验记录取代。当前正式 UAT 结论为：流程 A `CLOSED — ACCEPTED`，流程 B `CLOSED — ACCEPTED`，流程 C `CLOSED — ACCEPTED`（真实 GPU baseline、候选 B_dev、独立 B_test baseline 和候选 B_test 均完成；候选为 `NO_EFFECT`，因此不生成 CandidateSnapshot 或 Champion），流程 D `CLOSED — ACCEPTED`。

流程 C 的权威产物为 `runs/uat_patchcore_07h_gpu_20260723_r2/attempts/attempt_000003/`、`attempt_000005/` 和 `attempt_000006/`；其中 `attempt_000002=WORKER_LOST` 仅作为操作失败保留，未被当作科学结果。候选 B_test 使用 held-out manifest，所有 required metrics 均解析，protected artifact validation 均为 `changed_paths=[]`。报告与 Champion 要求已由流程 A 的真实闭环完成；流程 C 的 `NO_EFFECT` 结果不会被包装为候选成功或 Champion 资格。

因此，当前登记的 `BUG-20260723-003` 至 `BUG-20260723-009` 均为 `fixed`，正式 UAT 不再有产品或硬件阻塞项，满足创建新的 RC 标签的条件。本次仅更新验收记录，不自动创建或移动 RC 标签。
