# 2026-07-23 开发日志

### Session 1: 高级联合 UAT 第一轮修复

- 目标：在 `agent/advanced-uat-fix` 独立 worktree 中执行当前高级前后端联合 UAT，不触碰主 worktree、共享 runs、共享 Worker、GPU 工作区或发布标签。
- 基线：资料分支 `d92701cc100bafbecd47749b67e4df6406a1f416`；`origin/main` 为 `1c52489`；工作分支初始 HEAD 为 `d92701c`。
- 复现：用 `docs/uat/AutoAD_高级前后端联合UAT包_2026-07-24/materialize_fixture.py` 生成 CPU fixture。首次接入 Baseline 控制面时，两个进程退出码为 0，但 Attempt 均因 `missing expected outputs: ['outputs/b_dev_metrics.json']` / `['outputs/b_test_metrics.json']` 失败，Session 为 `FAILED`。
- 根因：Worker 在 `run_dir/attempts/{attempt_id}` 校验 `ExperimentCommandPlan.expected_outputs`；fixture 将 metrics 写到 worktree 的 `outputs/`，没有使用 Worker 注入的 `AUTOAD_ATTEMPT_DIR`。这不是进程成功即可替代 artifact 成功的语义。
- 修复：fixture 的 metrics 合同改为 attempt-relative `metrics.json`；有 `AUTOAD_ATTEMPT_DIR` 时写入该 artifact 目录，standalone fixture 校验无该环境变量时仍写入当前工作目录；manifest 的 split 绑定改为空 argv 槽位。
- 适配器复现：`--split=` 被错误变形成 `['run.py', 'SPLIT']`；两个空 split 槽位只替换第一个。修复要求显式 binding 指向唯一空 argv 槽位，未支持的 equals/ambiguous 形态 fail closed。
- 科学复现：EvaluationContract 含 `score` 与 `f1` 时，candidate 只返回 `score` 仍产生 `IMPROVEMENT`。修复要求合同每个 metric 都有 finite numeric candidate/baseline 值，否则返回 `INCONCLUSIVE`。
- 前端复现：报告讨论请求在网络响应丢失、pending 或普通错误后清空 retry 记录；同一问题再次发送会生成新 request id。修复保留 durable request id，成功协调后清除旧错误。
- 验证：`tests/test_executor_adapters.py` 与 `tests/test_validity.py` 共 10 项通过；扩展后端专项共 26 项通过；fixture oracle 通过（baseline B_dev/B_test 为 0，safe candidate B_dev/B_test 为 1）；真实 Baseline 控制面复验两个 Attempt 均 `COMPLETED`，Session `READY/completed`，两张 OutcomeCard 均 `metrics_parsed=true`。
- 待办：运行本轮完整后端门禁、前端 lint/build、普通与 full-stack Playwright；完成 FLOW-A/B/C/D/E 证据和 unseen holdout；检查报告 UI 的 raw enum/P2 状态与外部 PatchCore/MVTec 前置条件。

### Session 2: 高级联合 UAT 第二轮复验与报告状态修复

- 基线与隔离：继续使用独立 worktree `/root/autodl-tmp/AI4S/AutoAD-advanced-uat-fix`、分支 `agent/advanced-uat-fix`；没有停止或修改共享 `127.0.0.1:17828` 服务、共享 runs、GPU 工作区、主 worktree、`main` 或 `v0.9.0-rc1`。
- 前端修复：在 `frontend/src/lib/reportLabels.ts` 集中映射当前 Schema 中的报告生成、审阅、格式、Job、摘要、Proposal、证据和 handoff 状态；报告页不再把这些原始 enum/job type 作为用户主要可见文本。未知值仍保留原始值诊断，避免静默误标。
- 第二轮发现：不可读报告的状态提示仍直接显示 `generation_status`，人工 handoff 的 `kind` 也直接显示。已改为精确映射，并新增报告页断言。
- 回归：`npm run lint` 通过；`npm run build` 通过（Vite 1810 modules）；目标报告与视觉测试 `10 passed`；普通 Playwright `36 passed`；隔离 full-stack Playwright `2 passed`；候选控制面精确测试 `4 passed, 11 deselected`。
- 后端专项结果：控制面、Attempt、repair、failure、retry、worker health、report handoff 共 `50 passed`；报告协作/lifecycle/optional artifacts/API/projection 共 `69 passed`；研究语义边界、UI 安全、executor/patch/handoff 共 `54 passed`。报告依赖失败保持后端 `queued` 并在 projection 提供 `blocked_reason`，待前置 Job 重试后可继续。
- FLOW-A：CPU fixture oracle 为 baseline B_dev/B_test `0.0`、safe candidate B_dev/B_test `1.0`；真实 Baseline 控制面两个 Attempt 均 `COMPLETED`，Session `READY`，`baseline_status=completed`，OutcomeCard `metrics_parsed=true`。候选派生、B_test 确认、immutable candidate、幂等重放、promotion 的现有测试通过。没有把 mock 对话或 synthetic fixture 宣称为真实科学领域结论。
- FLOW-B/C/D/E：现有失败分类、append-only repair、新 Attempt、stale lease/worker restart/WS stale refresh、受保护路径/metric/evaluator、B_dev/B_test 与报告局部失败测试通过；讨论响应丢失回归确认两次 POST 复用同一 `request_id`；报告页状态映射和目标视觉测试通过。390px/键盘/辅助技术的自动化覆盖已随现有套件运行，但未进行独立屏幕阅读器真人验收。
- 07H 物理 readiness：只运行独立临时 run root 下的只读 `PhysicalReadinessGate`，未启动 GPU benchmark、未下载/展开数据。精确阻塞为：07H data manifests/projections 无效（`ValueError`）；PatchCore 当前 commit `fcaa92f124fb1ad74a7acf56726decd4b27cbcad` 不等于冻结的 `aa646496cd960f30a21eedf1ba97d0371bc26200`；锁定 benchmark Python 缺失或不可执行。MVTec archive、权重和 RTX 4090 的存在不能替代这些合同条件。
- HOLDOUT-H：本轮修复 Agent 已看到 package 的公开 fixture 和测试，因此没有独立 unseen tester 生成的盲测证据；H 不判通过，也不据此宣称泛化。
- 结论：P0/P1 代码问题已修复并有回归证据；第二轮新增的 P2 raw-state 可读性问题已修复。完整高级 UAT、07H 物理 readiness 和 HOLDOUT-H 仍不能宣称通过；不具备创建/移动发布标签的条件。GitHub Actions 的 `verify` run `30033599709` 已通过，包含仓库验证、前端 lint/build、普通浏览器和 full-stack 浏览器测试。

### Session 3: post-push CI 结论

- 远端 `verify` run：`30033599709`，commit `70fdcb5376cbcd10f08413340216972f822a2ab9`，状态 `success`，job 用时约 3 分 16 秒。
- CI 步骤全部通过：`Run verification`、`Lint frontend`、`Build frontend`、`Run frontend browser tests`、`Run full-stack browser tests`。
- CI 通过不改变 UAT 边界：HOLDOUT-H 仍无独立盲测，07H 仍受外部 data manifest、冻结 PatchCore commit 和 locked Python 前置条件阻塞。

### Session 4: 工作台刷新失败与枚举可读性复验

- 隔离边界保持不变：继续使用独立 worktree `/root/autodl-tmp/AI4S/AutoAD-advanced-uat-fix` 和 `agent/advanced-uat-fix`；未停止或修改共享 `127.0.0.1:17828` 服务、共享 runs、共享 Worker、GPU 工作区、主 worktree、`main` 或 `v0.9.0-rc1`。
- 新复现：Baseline POST 已返回成功，但随后 projection 刷新失败时，`BaselineLaunchForm` 没有结束 `busy` 状态，按钮会永久显示排队并阻止用户继续；用户填写的契约输入也必须保留。
- 修复：projection 加载返回是否接受快照；动作完成后等待刷新结果，失败显示动作级可操作提示；Baseline 表单在成功或失败路径都释放 busy，保留当前输入；普通刷新仍显示上一份快照和全局错误。
- 新增 UI 可读性修复：Attempt 任务类型、OutcomeCard 类别、reconciliation 权威来源和活动事件类型统一通过当前仓库精确枚举映射；未知值继续带原始值诊断。Baseline 指标方向和角色显示中文含义并保留协议值。
- 回归：工作台目标 Playwright `15 passed`；普通 Playwright `37 passed`；隔离 full-stack Playwright `2 passed`；frontend lint 通过；frontend build 通过（Vite 1810 modules）。新增断言覆盖启动成功后刷新失败时按钮可用、输入保留以及 `experiment_attempt` 不作为用户主文本展示。
- Fixture：按入口要求重新生成 CPU fixture，oracle 保持 baseline B_dev/B_test `0.0`、safe candidate B_dev/B_test `1.0`、sample_count `8`，model 为 `mean-v1` 与 `max-v2`。
- 当前边界：本轮仍没有独立真人多窗口/屏幕阅读器验收；HOLDOUT-H 仍不能判通过；07H 仍受 data manifests/projections、冻结 PatchCore commit 和 locked benchmark Python 前置条件阻塞；不创建或移动发布标签。

### Session 5: 本轮提交 CI 结论

- 提交：`d67472691d73663165cefc9ac2c88a711903ac48`，已推送到 `origin/agent/advanced-uat-fix`。
- GitHub Actions：`verify` run `30035671917` 成功；`Run verification`、`Lint frontend`、`Build frontend`、`Run frontend browser tests` 和 `Run full-stack browser tests` 全部通过，用时约 2 分 20 秒。
- CI 通过不扩大 UAT 结论：HOLDOUT-H 仍无独立盲测，07H 仍受外部物理前置条件阻塞；不创建或移动 `v0.9.0-rc1`。

### Session 6: E 类交互合同与 07H 物理 smoke

- 隔离边界保持不变：继续使用独立 worktree `/root/autodl-tmp/AI4S/AutoAD-advanced-uat-fix` 和独立 run root `runs/autoad-advanced-physical-readiness-20260723-r2`；仅启动并随后停止本轮 run 的临时 Worker，未停止或修改共享 `127.0.0.1:17828` 服务、共享 runs、共享 Worker、主 worktree、`main` 或 `v0.9.0-rc1`。
- E 类复现与修复：Baseline 表单校验原本只显示 `role=alert`，未把焦点移到首个无效字段；报告证据详情原本没有完整显示当前 `ArtifactReferenceV2` 的制品类型、大小、来源和技术引用。修复后分别使用首个错误字段焦点、`aria-describedby`、精确当前 Schema 标签及按需展开的技术引用；未知枚举仍保留原始值诊断。
- 前端回归：新增 Baseline 首个错误字段焦点和 390px 无横向溢出测试；新增报告证据元数据展示测试。完整计数以本轮门禁输出为准。
- 07H readiness：当前只读 readiness run 为 `autoad-advanced-physical-readiness-20260723-r2`，`physical_readiness.json` 状态为 `ready`，blockers 为空；MVTec bottle、冻结 PatchCore commit `aa646496cd960f30a21eedf1ba97d0371bc26200`、locked benchmark Python、权重和 RTX 4090 前置条件均已通过。没有修改外部数据、PatchCore 仓库或环境。
- 07H 实际执行：仅运行冻结内部 PatchCore bottle、seed `0` 的 baseline smoke，Attempt `attempt_000001` / Job `job_000001` 退出码 `0`，Session `READY` / `baseline_status=completed`，GPU lease 已释放，protected artifact validation 为 `passed`，metrics 为 `instance_auroc=1.0`、`full_pixel_auroc=0.9793658025353316`、`anomaly_pixel_auroc=0.9722287243867787`。本次是单 seed baseline 证据，不是 candidate 对比、B_test 结论或领域泛化结论。
- 状态权威说明：该 run 的 `OutcomeCard` 保留无 candidate patch 的执行原始事实 `NON_COMPARABLE`；显式输入生成的 `scientific_assessment` 为 `COMPARABLE` 但 `patch_applied=false`、`smoke_passed=false` 且不产生 scientific effect；`assessment_reconciliation.json` 明确记录两种状态和各自权威来源。这是当前合同设计，不是未处理的状态冲突。
- HOLDOUT-H：本轮仍没有独立 tester 在修复 Agent 不可见的工程形态上生成 unseen fixture；H 不判通过。
- 发布：不创建或移动 `v0.9.0-rc1`；07H readiness 已从历史阻塞转为当前环境 ready，但完整人工 E/A-H 仍不据此自动通过。

### Session 7: ba8e45b post-push CI 结论

- 提交：`ba8e45bfd64f181d70b50b26e49cf31c5b503334`，已推送到 `origin/agent/advanced-uat-fix`。
- 本地门禁：`bash scripts/verify.sh` 为 `1941 passed, 2 skipped`；前端目标套件 `22 passed`，普通 Playwright `40 passed`，full-stack Playwright `2 passed`，lint/build 通过。
- GitHub Actions：`verify` run `30037988275` 成功；`Run verification`、`Lint frontend`、`Build frontend`、`Run frontend browser tests` 和 `Run full-stack browser tests` 全部通过，run 用时约 2 分 46 秒。
- CI 通过不扩大 UAT 结论：HOLDOUT-H 仍无独立盲测，07H 只有当前冻结环境的单 seed baseline smoke，不是 candidate/B_test/泛化结论；不创建或移动 `v0.9.0-rc1`。

### Session 8: 近邻 Adapter 探针与 200% UI 回归补强

- 独立历史 RareCLIP micro repo 资料时间为 2026-07-19，不能作为本轮 HOLDOUT-H；只作为旧工程形态近邻探针，不写入 H 通过结论。
- 当前 `ExecutorAdapter` 能识别其显式 `generic_python` manifest，并生成相对 worktree 的执行计划；使用绝对 worktree 路径按 `ExperimentCommandPlan` 合同拒绝。该仓库自身隔离 CPU baseline 输出 `image_auroc=0.843611`，没有写入当前正式 fixture 或共享 runs。
- 新增工作台 200% 布局缩放回归，覆盖表单可达性、持久 `role=alert`、首个错误字段焦点和 `aria-describedby`；该自动化证据不替代独立真人屏幕阅读器/人工验收，也不替代 HOLDOUT-H。
- 提交 `d85de29883df251ac2c70aa4e83cf454cee90f27` 已推送到 `origin/agent/advanced-uat-fix`；GitHub Actions `verify` run `30041041292` 成功，五个门禁步骤全部通过。
