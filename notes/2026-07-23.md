# AutoAD-Researcher 开发日志

## Candidate Proposal 正式闭环

- 目标：补齐 Baseline 完成后到 Candidate Attempt 之间的正式产品入口，不改变任务确认、主指标、报告和 held-out 既有边界。
- 操作：新增按 Session 持久化的 Candidate Proposal Store；复用 `CompactCycleService` 写入 IdeaTree；服务端从 Proposal 快照恢复干预合同和结构化 patch；批准后复用 `CandidateControlService`；接入生成、批准、拒绝 API、Experiment Observatory 投影和 React 工作台。
- 操作：生成上下文只包含任务、冻结评估合同、Baseline OutcomeCard、adapter 证据、受限仓库文件和已有研究证据；浏览器不提交命令、代码或路径。
- 操作：新增 Proposal 幂等、哈希校验、IdeaTree revision 校验、拒绝终止、无批准不创建 Attempt、非法 provider 输出不落 Idea/Proposal 的回归，以及 Playwright 正式界面动作回归。
- 结果：专项 Python 回归 4 项通过；相关后端回归 51 项通过；此前全量 Python 回归 1945 passed, 2 skipped；前端 lint/build 通过；全量 Playwright 37 项通过。
- 遗留：正式门禁和真实 Provider UAT 仍需按项目流程继续验证；当前不开始 GPU UAT。

## Cross-task UAT round 1: explicit phase command compatibility

- Goal: run the current `feat/production-candidate-entry` baseline against the exact 2026-07-23 cross-task UAT fixtures without changing the package or its oracle.
- Operations: confirmed remote `feat/production-candidate-entry` at `4d0eead99b85728f604ecd5032f371e2d873913e`; ran `bash scripts/verify.sh` (`1946 passed, 2 skipped`); ran the package `verify_all_fixtures.py` under Python 3.12.13 with torch 2.5.1+cu124, scikit-learn 1.9.0, and numpy 2.4.4 (`ALL FIXTURES PASSED`); reproduced the real `BaselineControlService` path with the XOR fixture.
- Result: the three fixture manifests are `supported` and each declares complete `b_dev`/`b_test` commands, but the current adapter rejects all baseline launches because those commands use explicit phase tokens (`--split b_dev` / `--split b_test`) instead of a split-file path slot. This is a P1 control-plane compatibility defect, not an oracle failure.
- Evidence: direct adapter reproduction raises `ValueError: adapter b_dev command does not declare a split reference argument`; the full baseline setup persists the evaluation contract, protected hashes, and isolated worktree before failing at command construction. No shared runs, fixture sources, or external test processes were modified.
- Leftovers: update Adapter semantics and regression coverage, rerun the real fixture baseline/candidate paths, then continue with the two UAT packages and record any remaining blockers.

## Cross-task UAT round 2: metric metadata compatibility

- Goal: continue the real XOR control-plane cycle after the explicit phase command fix.
- Operations: completed a real b_dev baseline through Session, isolated worktree, Worker and Finalizer; then submitted the approved model-only candidate through `CandidateControlService`.
- Result: baseline execution succeeded with the expected `accuracy` and `log_loss`, but candidate launch stopped while loading baseline metrics because the OutcomeCard also contains non-metric metadata (`split`, `seed`, `sample_count`, `parameter_count`). The service incorrectly required every OutcomeCard field to be numeric.
- Evidence: `CandidateControlService._metrics()` raised `execution_contract_incomplete: baseline metrics are unavailable` on the unchanged baseline Artifact; no candidate Attempt was created and no fixture source was changed.
- Leftovers: extract only names declared by the frozen `EvaluationContract.metrics`, add metadata-bearing regression coverage, and rerun the complete XOR B_dev/candidate/B_test cycle.

## Cross-task UAT round 3: phase literal preservation

- Goal: exercise the收尾验收包 micro repository through the same baseline control plane, using its actual protected `data/b_dev.json` and `data/b_test.json` artifacts.
- Operations: ran the package micro verifier; the success repo's direct b_dev oracle passed with `image_auroc=0.28125`, and the intentional failure repo failed with the expected `RuntimeError` from `model.py`. A real baseline setup using repository-relative split refs reached command creation.
- Result: the last1 manifest declares `split_ref_arg_index=2`, but the indexed argv value is the phase literal `b_dev`/`b_test`, not a split-file placeholder. The control plane replaced it with an absolute file path, so the fixture's `run.py --split` contract would reject the command. This is a P1 adapter compatibility defect.
- Evidence: real command plan was `run.py --split /tmp/.../data/b_dev.json`; the fixture parser accepts only `b_dev` or `b_test`. No Worker was started and no source checkout was modified.
- Leftovers: preserve an indexed value when it already equals the declared evaluation phase, add regression coverage for both explicit phase forms, then complete the micro success and failure/recovery cycles.

## Cross-task UAT round 4: current control-plane regression closure

- Goal: rerun the exact cross-task fixtures after the adapter and metric-contract fixes, using the real Session/Attempt/Worker/Finalizer/ScientificAssessment path.
- Operations: the official fixture oracle still passed all three cases; isolated real cycles completed for XOR classification, Diabetes regression, and unsupervised clustering on both `b_dev` and `b_test`. Commands preserved each fixture's declared phase token, metadata-bearing OutcomeCards were accepted, and comparisons used the frozen metric names and directions.
- Result: all three tasks produced completed baseline, candidate, held-out baseline, and confirmation Attempts. XOR accuracy improved from `0.4124999940395355` to `1.0` on `b_dev` and from `0.36250001192092896` to `1.0` on `b_test`; Diabetes RMSE improved from `78.15070522569023` to `56.26193500117719` on `b_dev` and from `75.35716006481208` to `52.657269296257795` on `b_test`; clustering silhouette improved from `0.6028035751712463` to `0.7337608117858551` on `b_dev` and from `0.583721676030237` to `0.7148361214497098` on `b_test`. Guardrails were retained and assessed for Diabetes and clustering.
- Result: two P1 regressions found in rounds 1 and 3 are fixed in the current working tree: explicit phase arguments are preserved, and candidate/confirmation metric loading reads only frozen contract metric names instead of OutcomeCard metadata. Focused regression tests passed (`29 passed`).
- Leftovers: formal gate, frontend/browser regression, and GitHub push remain; the physical PatchCore run is still blocked by host GPU inventory.

## Closeout UAT round 5: failure, boundary, and report evidence

- Goal: execute the exact closeout micro failure repository and the cross-domain `plan_only` boundary on the current branch.
- Operations: initialized Git only inside a temporary copy of the supplied failure fixture because the supplied archive has no `.git`; ran the exact manifest command `run.py --split b_dev` through the current control plane; submitted an evaluator-cheating candidate; requested a legal model-only repair proposal; generated a report from the failed run; and confirmed a Chinese sentiment classification request through `TaskBridge` in `plan_only` mode.
- Result: the failure Attempt remained durable with `RUN_COMMAND_FAILED`, `UNKNOWN_RUN_FAILURE` (`retryable=false`), preserved traceback, Session `FAILED`, OutcomeCard `NON_COMPARABLE`, `metrics_parsed=false`, and no scientific improvement. The cheating request was rejected before candidate launch with `candidate launch requires a completed baseline B_dev`; no second Attempt or Champion was created. The report reached `content_ready`, retained `failed_attempts`, had no primary metrics, and reported `engineering_status=FAILED`, `execution_status=CRASHED`, `scientific_status=EVIDENCE_INSUFFICIENT`.
- Historical result: the first legal repair request stopped at `candidate proposal requires a completed baseline B_dev` before the provider was called. That observation opened the P1 closeout B4 gap; it is retained as historical evidence, while the controlled repair entry is covered by the current round below.
- Historical result: the exact cross-domain request was confirmed as `plan_only`; `input_task.yaml` was materialized, but no `experiments/sessions` directory and no PipelineJob were created. The complete physical-science boundary gap is covered by the deterministic semantic cases added in this round.

## Prior UAT verification and publication preparation

- Historical result: the previous working-tree verification passed with `1951 passed, 2 skipped`; frontend lint/build, ordinary Playwright, and isolated full-stack Playwright also passed before the current repair and boundary changes.
- Historical result: the earlier UAT record set was ready for publication, but the failed-baseline model repair entry was still open at that point; the PatchCore physical run remains blocked by host GPU availability.
- Leftovers: rerun the formal gate against the current working tree, then confirm the final branch SHA, clean worktree, remote ref, and GitHub Actions status.

## UAT round 6: failed-baseline repair and physical-science boundary

- Goal: close the P1 failed-baseline repair gap and add deterministic coverage for the revised real-world science boundary without changing any supplied fixture or oracle.
- Operations: added `BaselineRepairInput` and `BaselineRepairService` behind the existing `RETRY_FAILED` report Proposal; reused the isolated Executor worktree, InterventionContract, protected hashes, existing baseline Worker Job type, and a distinct `attempt_purpose=repair`. Added report/API schema coverage, real Worker tests for CPU-only and explicit `b_test` adapters, and replay receipts. Added the structured `unsupported_domain` policy category and prompt guidance; the Dialogue Gate removes all source/task/target actions for that policy result.
- Result: a failing baseline now remains immutable while a new repair Attempt can complete and project Session to `READY/completed` or `READY_FOR_BASELINE/b_dev_completed`. The report Proposal handoff is `kind=baseline_repair`; it never creates a Candidate or Champion. Targeted repair/report tests passed (`4 passed`), boundary tests passed (`3 passed`), and prior control-plane regressions remained green.
- Result: deterministic boundary cases cover protein design, building structural design, and material proportion design as semantically rejected `unsupported_domain` decisions. This is a policy-contract/gate acceptance check, not a keyword classifier and not evidence of real-world scientific validation.
- Exact closeout rerun: temporary copy `autoad-uat-closeout-repair-frtej3xf` of the supplied `autoad_micro_failure_repo` used source fixture commit `38e9c40`; `attempt_000001` failed with `RUN_COMMAND_FAILED`, `UNKNOWN_RUN_FAILURE`, and `metrics_parsed=false`; `attempt_000002` completed with `attempt_purpose=repair`, changed only `model.py`, and produced `image_auroc=1.0` on b_dev. The Session correctly ended at `READY_FOR_BASELINE/b_dev_completed` because the exact adapter declares b_test. The original failure artifact SHA256 was `0adfea782bcee8d46e9204116420845af29639068c06b32d2014623d0ea4e508`; it remained unchanged.
- Verification: current working-tree gate passed `1957 passed, 2 skipped`; frontend lint/build passed; ordinary Playwright passed `37`; isolated full-stack Playwright passed `2`.
- Supplied-package oracle: the first invocation with the project `.venv` correctly exposed the missing declared ML dependencies; without installing anything, the same `last2` oracle passed in the existing `.venv-gpu` (Python 3.12.13, torch 2.5.1+cu124, scikit-learn 1.9.0, numpy 2.4.4): all three fixture cases and exact expected metrics passed.
- Remaining: record the final verification, push the same branch, and confirm the final branch SHA, clean worktree, remote ref, and GitHub Actions status. Host GPU availability remains an external blocker for physical PatchCore training.

## Closeout UAT round 7: URL-boundary reproduction

- Goal: start the formal `AutoAD_UAT_收尾验收包` from the current branch through the real HTTP service and keep the paper source separate from the executable repository.
- Operations: published the supplied success and failure fixtures to `https://github.com/BTGly/autoad_micro_success_repo` at `6aaae985e664d139cebd7154af21201c9cadcbda` and `https://github.com/BTGly/autoad_micro_failure_repo` at `f08175f9ea8afc01ed27db744505c42d0b2e1cb7`; created persistent run `uat_a_cpu_1019_5935` under `runs/uat_closeout_20260723`; uploaded and parsed the supplied PDF; sent the exact Flow A repository sentence with Chinese prose immediately after the URL.
- Result: the paper source and parse artifacts were correct, but the repository sentence created `web_fetch` jobs `job_000002` and `job_000003` for a failed `webpage` source instead of a `github_repo` source and no `git_clone` job. Expected: one normalized GitHub repository source and a `git_clone`/`repo_summarize` chain. This is a stable P1 intake defect, not an external GitHub outage.
- Root cause: `source_normalizer.py` extracted `https?://\\S+`, so the URL token included the following Chinese sentence; the conversation-level source-action planner therefore could not parse the GitHub repository root.
- Fix: bound URL extraction to the ASCII URI grammar and added both normalizer-level and source-action-planner regression tests for the exact trailing-CJK form. The existing explicit repository normalization contract remains unchanged.
- Evidence: `runs/uat_closeout_20260723/uat_a_cpu_1019_5935/`; source `src_2026-07-23T10-20-50-879321Z`; failed jobs `job_000002` and `job_000003`; the original PDF parse job `job_000001` completed with `pa_4f3c635326604275`.
- Leftovers: run the mandatory full gate, publish the fix, and continue the remaining UAT flows; physical PatchCore remains blocked by missing host GPU.

## Closeout UAT round 8: URL-boundary repair replay

- Operations: started a fresh FastAPI HTTP service with `AUTOAD_EMBEDDED_WORKER=0` and an isolated run root; posted the exact Flow A sentence `执行仓库是：https://github.com/BTGly/autoad_micro_success_repo。这个仓库才是执行仓库，论文只是参考材料。` to `/api/chat/send`.
- Result: the response acknowledged the normalized repository URL; `source_references.json` contains one `github_repo` source with user label `https://github.com/BTGly/autoad_micro_success_repo`; `pipeline_jobs.jsonl` contains only queued `git_clone` and `repo_summarize` jobs. No `webpage` or `web_fetch` job was created.
- Evidence: `/tmp/autoad-http-rkP1ax/runs/uat_a_url_boundary_replay/`; the focused regression passed `48 passed` before this replay. The replay validates URL intake and queue creation; it does not claim that the external clone or GPU experiment completed.
- Code: `8f8e4daf645314ff972e194f2a8a91fe1654f799` (`fix: preserve github repository URLs in conversational text`).

## Closeout UAT round 9: CPU A promotion identity repair

- Goal: complete Flow A through Champion promotion on the persistent CPU run `uat_a_cpu_1157_fb98` without changing the supplied repository, evaluation files, or the protected UAT package.
- Operations: froze the exact success fixture commit `6aaae985e664d139cebd7154af21201c9cadcbda`; completed baseline `attempt_000001`, candidate B_dev `attempt_000002`, held-out baseline `attempt_000003`, and candidate B_test `attempt_000004` through the HTTP control plane. Baseline `image_auroc` was `0.28125` on B_dev and `0.25` on B_test; candidate was `1.0` on both; noise threshold was `0.01`; protected artifact validation passed with no changed protected paths.
- Result: the first promotion request created the human approval and prepared journal but returned an unstructured HTTP 500 while merging. The exact traceback showed the run-owned fixture checkout had no Git identity, while promotion `_merge()` depended on global `user.name/user.email`. The journal remained recoverable at `status=prepared`; no Champion event was written.
- Fix: promotion merge now supplies explicit `AutoAD` identity and `--no-gpg-sign`, matching the existing hermetic candidate commit path. Added a regression test that merges a repository with no configured Git identity.
- Evidence: `runs/uat_closeout_20260723/uat_a_cpu_1157_fb98/experiments/champions/transactions/promotion_000002.json`; candidate `experiments/champions/candidates/candidate_000002.json`; first promotion response and server traceback are retained in the active service log. The prepared journal was replayed after the code restart and returned the committed Champion event.

## Publication audit

- Verification: the final record update passed `bash scripts/verify_and_push.sh`; full pytest result was `1959 passed, 2 skipped`, and the development-log index passed.
- Frontend verification: `npm run lint`, `npm run build`, Playwright `37 passed`, and full-stack Playwright `2 passed` using `UV_BIN=/root/.local/bin/uv`.
- Publication: code fix `8f8e4daf645314ff972e194f2a8a91fe1654f799` and record commit `22edf0ae351035ada428ab91500cad28100ced7b` are on `origin/feat/production-candidate-entry`; GitHub Actions verify run `30005275816` completed successfully for record commit `22edf0ae3510`.
- Final limitation: `nvidia-smi` returned `No devices were found`; no physical PatchCore/MVTec training or GPU readiness run was claimed. This remains an external host-capacity blocker.
- Additional worktree change: a concurrent control-plane patch in `promotion_control.py` pins the merge identity and disables signing for repository promotion; its regression test covers a repository without configured Git identity. It was retained and included in the final verification scope.

## Closeout UAT round 10: CPU B persistent recovery and discussion evidence correction

- Goal: complete the exact closeout Flow B on a persistent HTTP run while GPU remains unavailable, retaining the failed baseline and proving the bounded repair/report path.
- Operations: run `uat-b-cpu_1216_7d9c` used the published failure fixture at commit `f08175f9ea8afc01ed27db744505c42d0b2e1cb7`; recorded B4 legal repair text; created failure report `report_v001_0ec08011_003d7fe2`; handed off `proposal_57c06713cf2448ada54f1ecfed77622c` as `RETRY_FAILED` with `BaselineRepairInput`; generated lineage report `report_v002_3a5aef14_003d7fe2`; sent B5 failure-report question and a report discussion evidence probe.
- Result: original `attempt_000001` remained `FAILED` with `RUN_COMMAND_FAILED`, `UNKNOWN_RUN_FAILURE`, `retryable=false`, `NON_COMPARABLE`, and the exact stderr `RuntimeError: intentional UAT model failure; repair model.py only`. Repair `attempt_000002` completed with `attempt_purpose=repair`, `image_auroc=1.0`, changed files exactly `["model.py"]`, and passed protected-artifact validation. Session became `READY_FOR_BASELINE/b_dev_completed`; no Candidate or Champion was created.
- Result: B5 report v2 passed validation, retained one failed Attempt and one completed repair metric, but `scientific_status=EVIDENCE_INSUFFICIENT` and Champion remained `not_materialized`; therefore the repair does not prove paper-method effectiveness. The exact B5 chat answer also stated that the failed baseline could not prove effectiveness.
- Observation: the first unqualified direct-evidence discussion turn incorrectly claimed a CUDA device-side assert. A second turn bound to the registered stderr/exit-code Evidence IDs corrected this to the exact `model.py` RuntimeError and minimal repair. After restarting the service with the prompt repair, the same unqualified question completed as `response_kind=evidence`, cited the exact stderr and exit-code Evidence IDs, and produced the same correct answer in `turn_8cbc1ad65a074288af8cf27372e06d4c`. The reporting prompt now explicitly requires a read-only Attempt/log tool before answering direct failure-evidence, root-cause, or minimal-repair questions; a regression test covers this instruction.
- Evidence: `runs/uat_closeout_20260723/uat-b-cpu_1216_7d9c/`; session `session_1feb43a6527f5064`; failed/repair Attempts `attempt_000001`/`attempt_000002`; proposal `proposal_57c06713cf2448ada54f1ecfed77622c`; reports `report_v001_0ec08011_003d7fe2` and `report_v002_3a5aef14_003d7fe2`; correction turn `turn_186157b720eb4a6e826252edfad5384f4`.
- GPU boundary: `nvidia-smi` still returns `No devices were found`; no PatchCore/MVTec physical run is claimed.

## Closeout UAT round 11: CPU A Champion promotion replay

- Operations: restarted the FastAPI service from commit `3a3625311eaa699871898e370ffc689dd86be7fe` with `AUTOAD_RUNS_ROOT=runs/uat_closeout_20260723` and `AUTOAD_EMBEDDED_WORKER=0`; posted `candidate_000002` with `approved_by=UAT-Reviewer` twice to `/api/runs/uat_a_cpu_1157_fb98/promotions`.
- Result: both requests returned HTTP `200` and the same `champion:promotion_000002` event with merge SHA `1e01543355b461c8927d54bae8e36e57c1d58ac6`. The journal is `status=committed` with the same resulting trunk commit; `current_by_contract.json` points to `candidate_000002`; `champion_events.jsonl` contains exactly one promotion event.
- Idempotency evidence: before and after snapshots were identical for journal SHA256 `d908f5363f59d036b074c50e5e6519a2ed05d03a3db64f026e6688151d945543`, pointer SHA256 `ffd96323bdaae86ddb4b2ba35b226f02235544c51edb0a05ddfdf5918e1db0a0`, event count `1`, and repository HEAD `1e01543355b461c8927d54bae8e36e57c1d58ac6`. Replay output is retained at `/tmp/autoad-promotion-replay-6cucIX/before.json` and `/tmp/autoad-promotion-replay-6cucIX/after.json`.
- Acceptance: the prepared transaction now has durable committed journal, ChampionPointer, Champion event, and merge commit evidence; repeating the human promotion does not merge again. Flow A CPU closeout is `CLOSED — ACCEPTED`. GPU PatchCore/MVTec remains a separate external blocker.

## Closeout UAT round 12: CPU B3 execution-mode conflict reply

- Reproduction: send the exact Flow B conflict request after a confirmed task: `以后都自动执行，不需要问我；但任何代码修改和实验又必须先让我确认。`
- Observation: the product kept the execution request blocked, but the prior reply only described the V2 dialogue limitation and did not explain that automatic execution and `approve_each_step` are mutually exclusive or direct the user to confirm one mode.
- Root cause: `_validated_dialogue_reply()` used a generic fallback for `blocked_dialogue_only`, so it did not expose the authorization-mode conflict already represented by the task confirmation boundary.
- Fix: replace the generic fallback with a mode-conflict explanation that preserves the no-code/no-execution boundary and directs the user to confirm one mode in the Experiment workspace. Added a regression test for the structured `blocked_dialogue_only` decision.
- Retest plan: run the focused V2 test, replay the exact B3 prompt through a persistent HTTP run, then execute the mandatory full verification and publish flow. GPU PatchCore/MVTec remains blocked by host inventory and will not be claimed as passed.

## Closeout UAT round 13: CPU B5 report evidence prefetch

- Reproduction: after the B3 repair, generated persistent report `report_v003_3a5aef14_003d7fe2` and sent the exact evidence probe `请列出失败 Attempt 的直接证据和下一步最小修复动作。`
- Observation: the new report correctly avoided the previous unsupported CUDA explanation but returned `insufficient_evidence` instead of quoting the registered `attempt_000001/stderr.log` and the minimum `model.py` repair.
- Root cause: the discussion path exposed registered tools but required the provider to choose the first log read; one tool round could end after metadata reads without making the direct stderr content part of the frozen prompt context.
- Fix: deterministically prefetch `read_log_range` for Attempts whose frozen `runtime_status` is exactly `FAILED`, using the existing report-local tool, SHA verification, snapshot identity, and registered Evidence IDs. No execution or write tool is introduced.
- Retest plan: add focused prefetch coverage, restart the persistent service, create a new report version, replay the exact B5 evidence probe, then repeat the mandatory verification and push flow. GPU PatchCore/MVTec remains externally blocked.

## Closeout UAT round 14: CPU B5 exact failure-field grounding

- Observation after round 13: the report discussion quoted the real stderr and Evidence IDs, but confused `execution_status=CRASHED` with the distinct `failure_classification.failure_code=UNKNOWN_RUN_FAILURE` and proposed an unregistered algorithm change.
- Root cause: the provider had to reconcile multiple report fields from tool output and was not given an exact field/value ledger for failed Attempts.
- Fix: add a report-local structured `failed_attempt_facts` ledger with exact outcome, execution-result, and classification fields; strengthen the response contract to preserve field identity and prohibit unsupported algorithmic repair details. Added a regression test covering the distinct values.
- Retest plan: replay the exact B5 evidence probe against the persistent report after service restart, then run and publish the full verification scope. GPU PatchCore/MVTec remains externally blocked.

## Closeout UAT round 15: CPU B5 minimum repair scope

- Observation after round 14: exact failure fields and stderr were correct, but the provider expanded the explicit `repair model.py only` scope into an unregistered `anomaly_score` implementation proposal.
- Fix: extend the generic report evidence rule so an explicitly registered repair scope is quoted as-is, followed only by the next authorized retry or review step. Added an assertion to the discussion prompt regression.
- Retest plan: restart the persistent service and replay the exact B5 probe once more; then run the mandatory full verification and publication flow. GPU PatchCore/MVTec remains externally blocked.

## Closeout UAT round 16: report version identity for discussion contracts

- Observation: replaying a corrected discussion request on report v3 still included earlier unsupported discussion answers because report-local history is intentionally durable.
- Root cause: `report_recipe_hash()` covered report facts, narrative, validation, and renderers but not the report discussion response/evidence contract; a discussion-contract change therefore could not allocate a fresh report version.
- Fix: include `DISCUSSION_RESPONSE_CONTRACT` and `DISCUSSION_EVIDENCE_RULE` in the canonical report recipe hash. Existing report history remains immutable; a changed discussion contract now creates a new version with a clean discussion transcript. Added lifecycle regression coverage.
- Retest plan: create the new persistent report version through the repair Proposal lineage, replay the exact B5 evidence probe from the clean transcript, then run the full verification and push flow. GPU PatchCore/MVTec remains externally blocked.

## Closeout UAT round 17: formal record normalization

- Final CPU evidence: Flow A persistent run `uat_a_cpu_1157_fb98` reaches report and committed Champion; Flow B persistent run `uat-b-cpu_1216_7d9c` retains the failed baseline, controlled repair, B3 mode-conflict reply, and report v4 evidence discussion; Flow D policy boundary passes.
- Record update: replaced stale temporary-run references in the formal observation and bug tables with persistent artifact paths and SHA256 values. Historical temporary replays remain described in this development log but are not authoritative acceptance evidence.
- Verification before this record update: backend `1965 passed, 2 skipped`; frontend lint/build passed; Playwright `37 passed`; full-stack Playwright `2 passed`. Code is published at `585a724` on `feat/production-candidate-entry`.
- Final limitation: host `nvidia-smi` reports `No devices were found`; physical PatchCore/MVTec Flow C remains unexecuted, so the overall package is not marked fully passed and no new RC tag is proposed.

## Closeout UAT round 18: real browser observatory status mapping

- Reproduction: started the actual FastAPI service with `AUTOAD_RUNS_ROOT=runs/uat_closeout_20260723` and the Vite frontend, then opened the persistent B run `uat-b-cpu_1216_7d9c` in Playwright. The experiment projection returned the exact durable combination `status=READY_FOR_BASELINE` and `baseline_status=b_dev_completed`.
- Actual result: the browser showed `Session：等待基线` and `基线状态：未知状态（原始值：b_dev_completed）`, while the same page exposed the `Baseline 已完成` action and the completed repair Attempt. This hid a valid control-plane state and contradicted the visible action state.
- Root cause: frontend `experimentLabels.ts` mapped only `not_started`, `queued`, `running`, `completed`, and `failed` for baseline status; `statusTone()` also did not classify `b_dev_completed` as successful. The Session label did not use the already-present baseline status to disambiguate `READY_FOR_BASELINE` after B_dev.
- Fix: map the exact `b_dev_completed` state to `B_dev 已完成`, use the same state to render the Session fact, and classify it as success. Added an observatory Playwright regression for the exact projection combination.
- Retest plan: run the focused browser regression, repeat the real-browser B experiment/report replay, inspect failure details and report discussion persistence, then run the mandatory verification and publication flow. GPU Flow C remains explicitly deferred because `nvidia-smi` reports no device.

## Closeout UAT round 19: browser retest and GPU readiness refresh

- Browser retest: restarted the real FastAPI/Vite services against `runs/uat_closeout_20260723`, selected the exact persistent run `uat-b-cpu_1216_7d9c`, and opened both Experiment and Report workspaces. The browser now renders `Session：B_dev 已完成` and `基线状态：B_dev 已完成`; `attempt_000001` still renders `运行失败` with `RUN_COMMAND_FAILED`; report v4 still renders `EVIDENCE_INSUFFICIENT` and the registered `RuntimeError: intentional UAT model failure; repair model.py only` discussion evidence.
- Verification: `bash scripts/verify.sh` passed with `1965 passed, 2 skipped`; frontend lint and production build passed; ordinary Playwright passed `38`; isolated full-stack Playwright passed `2`.
- GPU/data refresh: `nvidia-smi` now exposes an idle NVIDIA GeForce RTX 4090 with CUDA 12.4; the locked benchmark environment imports `torch 2.5.1+cu124`, `torch.cuda.is_available()=True`, and PatchCore source; user-provided `data/mvtec_ad` was copied into the workspace dataset boundary as `workspace/datasets/mvtec`; repository/data/environment preflight passed in `runs/uat_closeout_20260723/physical_preflight_20260723_r1/`.
- Remaining physical C blockers: the formal package requires an explicit user GPU free/used-memory threshold and two non-empty user-reviewed scientific patch files. No exact threshold or reviewed `cycle_1.patch`/`cycle_2.patch` is present in the workspace; the existing L3 report and unrelated diffs are not substituted. No physical PatchCore run was started.

## Cross-task UAT round 20: fresh GPU-enabled control-plane evidence

- Operations: used the exact last2 fixtures and the existing `.venv-gpu/bin/python` child interpreter. Re-ran the fixture oracle (`ALL FIXTURES PASSED`), then completed fresh Session/Attempt/PipelineJob/Worker/Finalizer/ScientificAssessment cycles for PyTorch XOR classification, scikit-learn Diabetes regression, and unlabeled scikit-learn clustering under `runs/uat_ml_dl_gpu_20260723_round1d/`.
- Result: all three tasks completed baseline, candidate, baseline B_test, and candidate confirmation Attempts. XOR accuracy improved `0.4124999940395355 -> 1.0` on B_dev and `0.36250001192092896 -> 1.0` on B_test; Diabetes RMSE improved `78.15070522569023 -> 56.26193500117719` and `75.35716006481208 -> 52.657269296257795`; clustering silhouette improved `0.6028035751712463 -> 0.7337608117858551` and `0.583721676030237 -> 0.7148361214497098`. Declared guardrails improved, scientific assessments were `IMPROVEMENT`, and protected-artifact validation passed for every candidate/confirmation.
- Boundary result: the focused dialogue/control-plane suite passed `87 tests`; unsupported-domain decisions for protein design, building structural design, and material proportion design removed execution actions, while the Chinese sentiment request remained `plan_only` without Session/Job side effects. This validates product boundary behavior, not execution capability in those domains.
- Evidence: XOR session `session_0c8dae41fc1eb7d6`, Diabetes session `session_bf94c8df2047684f`, clustering session `session_f6204a968660fec4`; each has candidate snapshot `candidate_000002` with `b_test_passed=true`.

## Closeout UAT round 20: MVTec data preparation and explicit readiness blocker

- Operations: ran `prepare_patchcore_07h_data.py --run-id uat_patchcore_07h_gpu_20260723_r1 --source-root /root/autodl-tmp/AI4S/projects/AutoAD-Researcher/data/mvtec_ad`. The locked PatchCore environment was probed with its repository `src` on `PYTHONPATH`: torch `2.5.1+cu124`, CUDA available, RTX 4090, faiss, timm, PatchCore import, and PatchCore `--help` are available.
- Result: data preparation completed with train manifest `f4436ea1742f1723292eba4118b6c2725d3e54139b80866f537f92ec28470ad9`, B_dev `2b980a11b6aa6658bbfd274b35cd64013e635a53e7e16fe7727ee99d7eb4de40`, and B_test `0927df5d4d288f1958f27a38ad18b64d172e9e736eb8171d66cfec248b377204`. The physical readiness CLI has not been called because the closeout package requires explicit user-provided minimum free VRAM and maximum used VRAM thresholds; those values are not present, so no threshold was guessed and no PatchCore Attempt was started.
- This is the current external-input blocker for Flow C. Once the two exact MB values are supplied, run the existing readiness CLI first; only a `ready` artifact may authorize the normal Session/Attempt/PipelineJob/Worker baseline.

## Closeout UAT round 21: formal record closure for browser fix

- Record update: added `BUG-20260723-008` for the real browser mismatch between `READY_FOR_BASELINE/b_dev_completed` and the displayed status, linked it to `f90bc0848172678a059d8a2e0ed9f563a778693a`, the persistent B Session, and the current report discussion SHA.
- Updated the formal closeout result to distinguish the historical no-GPU C0 record from the current RTX 4090/data-preparation state; C remains blocked only by the missing explicit VRAM thresholds and missing user-reviewed two-cycle scientific patches. No physical GPU Attempt is claimed.
- Publication state: the repair commit remains `f90bc0848172678a059d8a2e0ed9f563a778693a`; the current branch and GitHub ref now point to `2564aac7443c2469172db00721595bf2baf1e30e`. GitHub Actions verify run `30018138977` for the final branch SHA completed successfully. `main` and `v0.9.0-rc1` remain unchanged.

## Closeout UAT round 22: physical PatchCore C1 baseline

- Operations: reused the existing ready artifact `runs/uat_patchcore_07h_gpu_20260723_r2/artifacts/07h/physical_readiness.json`, queued the exact frozen smoke case through `queue_patchcore_07h_baseline.py`, and ran the normal `autoad_researcher.worker.main` path. No direct metric injection or manual PatchCore result copy was used.
- Result: Session `session_faad414c83732280`, Attempt `attempt_000001`, and PipelineJob `job_000001` completed. The locked CUDA environment ran the pinned PatchCore commit on GPU 0 with network disabled; GPU lease `lease_000001` was released and no PatchCore process remained.
- Metrics: `instance_auroc=1.0`, `full_pixel_auroc=0.9793658025353316`, `anomaly_pixel_auroc=0.9722287243867787`; required metric parsing passed, execution exit code was `0`, and protected artifact validation reported `changed_paths=[]`.
- C2 remains unstarted because no two non-empty user-reviewed scientific patch files exist in the workspace. Existing historical 07H notes and unrelated fixture diffs are not treated as current C2 authorization or patch evidence.

## Closeout UAT round 22: budget clarification copy

- Reproduction: the Baseline browser contract flow accepted the explicit CPU-only values `max_wall_seconds=20000` and `max_gpu_seconds=0`, with `required_device_count=0` and `required_vram_mb=0`. The existing form labels and CPU placeholders did not explain the two-integer order, the seconds unit, or the GPU seconds/device constraint.
- Recorded issue: added `BUG-20260723-009` as P2 in the formal closeout bug table. This is a user-visible clarification defect; the underlying budget validation and CPU-only contract behavior were not treated as defective.
- Fix: added one concise visible resource-budget note stating the exact order, unit, `20000, 0` CPU example, `0`/blank CPU device fields, and the GPU device/VRAM requirement. The browser contract test now asserts the note and the exact submitted `20000, 0` payload; the focused Playwright test passed.
- Physical Flow C: C0 readiness is `ready` in `runs/uat_patchcore_07h_gpu_20260723_r2/artifacts/07h/physical_readiness.json`; C1, the controlled candidate B_dev, baseline B_test, candidate B_test confirmation, and final record updates are complete. The candidate was correctly not promoted because its scientific effect was `NO_EFFECT`.

## Closeout UAT round 23: physical PatchCore candidate and held-out confirmation

- Operations: after C1, admitted exactly one PatchCore parameter override through `PatchCore07HInterventionHandoff`: `coreset_sampling_ratio` changed from `0.1` to `0.2` in `.autoad/patchcore_07h_intervention.json`. The first candidate dispatch used a resolved virtualenv symlink and ended as `attempt_000002=LOST/WORKER_LOST`; this is retained as an operator setup failure. A second handoff preserved the locked `workspace/envs/patchcore_linux_gpu_07h/bin/python` launcher and created valid `attempt_000003/job_000003`.
- B_dev result: `attempt_000003` completed on GPU with `instance_auroc=1.0`, `full_pixel_auroc=0.979269088247178`, and `anomaly_pixel_auroc=0.9720985548902373`; Executor summary and InterventionAdmission show one changed config file, and protected validation passed with `changed_paths=[]`. The scientific assessment is `COMPARABLE`, `patch_applied=true`, `smoke_passed=true`, `primary_delta=0.0`, `scientific_effect=NO_EFFECT`.
- Held-out result: baseline B_test `attempt_000005/job_000005` completed with `instance_auroc=1.0`, `full_pixel_auroc=0.9763655908339176`, and `anomaly_pixel_auroc=0.9684942447620396`. Candidate confirmation `attempt_000006/job_000006` ran from the committed candidate branch `a9a252d57a620d02b103cca030eec16f08289dcc` and completed with `instance_auroc=1.0`, `full_pixel_auroc=0.9762995809520487`, and `anomaly_pixel_auroc=0.9684034224870441`; its `primary_delta=0.0` and `NO_EFFECT` assessment correctly prevented CandidateSnapshot/Champion promotion.
- Boundary conclusion: the physical experiment system executed real GPU baseline, controlled candidate, and held-out confirmation with durable hashes and no protected implementation changes. This is execution-system evidence; it is not evidence that the candidate improved PatchCore.
