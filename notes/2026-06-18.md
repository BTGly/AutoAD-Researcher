# AutoAD-Researcher 开发日志 — 2026-06-18

## Step 3.4 Idea & Transfer Design — 实施阶段

按照 v1.3 封板计划 (34 commits) 进行迭代开发。

---

### C1-C7: Schema contracts (commit 8cf7574)

**目标**: 建立所有 3.4 阶段的 Pydantic schema。

**操作**:
- 新增 `schemas/baseline_architecture.py` — TensorSpec, TensorAxis, InterfaceSpec, ExecutionPhaseContract, ModificationHook, ArchitectureComponent, BaselineArchitectureContract
- 新增 `schemas/transfer_design.py` — IdeaContract (discriminated union), DerivedClaim, ConstraintRef, IdeaAspectRef, AlignmentEntry, TransferConstraint, DIMENSION_POLICY, DimensionJudgment, derive_variant_status, TensorContractDelta, InterfaceContractDelta, RegimeChange, HookBinding, ImplementationVariant, RiskRecord, AcceptedRisk, VariantRiskReport, UnresolvedDimension, CLASSIFICATION_RULES, VariantSelection, TransferValidationIssue, IdeaTransferValidationReport, RepositoryReanalysisRequest, PaperReanalysisRequest, SpawnChildRunRequest, TransferResumeFingerprint, IdeaTransferBudget, IdeaTransferDesignHandoff
- 新增函数: compute_variant_risk, derive_variant_status, violates_confirmed_constraint
- 更新 `schemas/__init__.py` 导出所有新 schema

**结果**: 671 passed

---

### C8-C24: Transfer module implementation (commit bee86b7)

**目标**: 实现所有 3.4 阶段的核心逻辑。

**操作**:
- `transfer/router.py` — IdeaSourceRouter (path A), IdeaSourceResolver (path B), fuzzy matching
- `transfer/normalizer.py` — IdeaContractNormalizer
- `transfer/aligner.py` — ArchitectureAligner with source-neutral alignment
- `transfer/variants.py` — VariantGenerator (architectural-level)
- `transfer/compatibility.py` — CompatibilityAnalyzer (per-variant), filter_variants
- `transfer/risks.py` — build_variant_risk_report, accept_risk
- `transfer/selector.py` — VariantSelector with risk acceptance (no silent selection)
- `transfer/validator.py` — DeterministicValidator (8 invariant categories)
- `transfer/reanalysis.py` — build_repository_reanalysis, build_paper_reanalysis, build_spawn_child_run
- `transfer/handoff.py` — build_handoff to Step 3.5
- 更新 `transfer/__init__.py` 导出所有模块

**结果**: 671 passed

---

### C25-C33: Comprehensive tests (commit 50f579c)

**目标**: 72 个测试覆盖所有 schema contracts + 核心逻辑。

**覆盖**:
- IdeaContract (discriminated union, confirmed requires timestamp/evidence)
- DerivedClaim, ConstraintRef
- BaselineArchitectureContract, TensorSpec, ModificationHook (protected/unknown classifications)
- AlignmentEntry (source-neutral IdeaAspectRef, AlignableScope routing)
- Routing (user-provided, paper-grounded, fuzzy match, no candidates)
- DimensionJudgment (INCOMPATIBLE→blocking=true enforcement)
- derive_variant_status (viable/conditional/non_viable/needs_reanalysis paths)
- violates_confirmed_constraint (hard/soft strength)
- DIMENSION_POLICY coverage
- compute_variant_risk (aggregation: judgment risk + hook risk + regime risk)
- AcceptedRisk (severity constraint)
- VariantSelection (confirmed requires selections, no duplicate selected+rejected)
- UnresolvedDimension (experiment_resolvable requires verification_target)
- CLASSIFICATION_RULES
- Validator (passing, protected hook rejection, risk mismatch, INCOMPATIBLE enforcement)
- Reanalysis requests (repository, paper, spawn_child)
- Handoff (build, design_blocking rejection)
- Selector (recommend≠select, select_variants, reject_risk flow)
- Source-neutral alignment (user_provided / derived_hypothesis aspects)

**结果**: 743 passed (671 + 72 new)

---

### 3.4 Implementation Completed

**总计**: 756 passed, 6 commits (3.4-specific)

**Commits**:
- `8cf7574` C1-C7: 29 schemas + 3 deterministic functions
- `bee86b7` C8-C24: 10 transfer modules
- `50f579c` C25-C33: 85 tests
- `997c227` F1+F2: GitCommitPattern, TensorSpec validation, aligner fix
- `c41cca1` F3+F4+F5: contract_producer, orchestrator, e2e fixture
- `22ae6a8` evidence semantics fix + optional paper fingerprint

**结果**: 正式封板，`docs/stages/3.4-final-report.md` 已生成

**已知限制**:
- LLM-driven variant detail population
- LLM-driven dimension judgment population
- 真实 3.1→3.4 artifact I/O 串联
- CLI `autoad idea-transfer run/resume`
- GitHub Actions CI 远端未确认

---

### 3.8 & 3.9 开发计划

**目标**: 编写 Step 3.8（Experiment Execution Integration）和 Step 3.9（Results Analysis, Validity & Report）的开发计划。

**3.8 核心设计**:
- 不重新建设 Runner，只做集成桥接
- 复用 3.0 已有组件: `runner/executor.py`, `analysis/metrics.py`, `supervisor/validity.py`, `analysis/reproducibility.py`, `environments/`, `assets/`, `benchmarks/`
- 新增: ExperimentPlan→ExperimentCommandPlan bridge, AttemptManager, ExecutionManifest, CLI
- 目录: `src/autoad_researcher/execution/`

**3.9 核心设计**:
- 3.0 已有底层 contracts，3.9 只增加科研语义层
- 新增: ComparisonReport, FailureAnalysis, ResourceComparison, Reflection, final_report.md generator
- 无论成功/失败都必须生成报告
- 目录: `src/autoad_researcher/reporting/`

**结果**: 两份开发计划已生成
**遗留问题**: 无

### 3.8 & 3.9 开发计划 v2.6（reference traceability 恢复）

**目标**: 恢复被误删的参考实现映射章节，区分采用/改造/拒绝。

**结果**: `7fba583` pushed，776 passed

### 3.8 & 3.9 开发计划 v2.7（9 P0 + 4 P1 关单）

**审阅**: 审阅人指出 contract_status=targeted_revision_required，共 9 P0 需修复。

**3.8 P0 修复**:
- P0-1: 移除"同 v2.4"引用，内联全部 schema（ResourceUsageReport/RetryDecision/RetryIdentity/状态机/Resume）
- P0-2: WorkspaceExecutionRef 对齐 PatchRunnerHandoff v2 字段；RunnerIntakeRequest 增加 patch_runner_handoff_ref
- P0-3: PatchRunnerHandoff v2 validator 改为精确集合相等（拒绝 duplicate selected/workspace_id/cross-workspace variant）
- P0-4: 所有 validator 将 assert 改为 raise ValueError
- P0-5: 补全 ProducedArtifactRecord schema + sha256 一致性验证

**3.9 P0 修复**:
- P0-6: MULTIPLE_VARIANTS_DESCRIPTIVE 写入 IdeaSupportConclusion enum 体
- P0-7: derive_idea_support() 改为纯函数 + Sequence 输入 + 严格校验
- P0-8: _determine_reproducibility() 确定性判定规则（min_pairs + 方向一致性）
- P0-9: target_stage 增加 "3.6_3.7" + action→stage 合法组合 Validator

**P1 修复**: multiple_variant_policy 类型从 str 改为 Literal（对齐 3.5）、baseline 负数分母用 abs、ReportFacts 条件 Validator、PairedMetricObservation delta 重算验证

**结果**: 验证通过，776 passed，待推送

### 3.8 & 3.9 开发计划 v2.8（7 P0 + 5 P1 关单 + ArtifactReferenceV2 代码落地）

**审阅**: 审阅人指出 contract_seal=blocked，7 P0 阻止实现。架构已封板，只需修合同。

**代码变更（P0-1）**:
- 新建 `schemas/artifacts.py` → `ArtifactReferenceV2`（artifact_id, artifact_type, locator, sha256, source_id, size_bytes; extra=forbid; sha256 pattern enforced）
- `patch_planning.py` BaselineWorkspaceRef/VariantWorkspaceHandoff 改用 `ArtifactReferenceV2`
- `schemas/__init__.py` 导出 `ArtifactReferenceV2`, `BaselineWorkspaceRef`, `VariantWorkspaceHandoff`
- 新建 `tests/test_patch_runner_handoff_v2.py`（14 tests: ArtifactReferenceV2 validation + PatchRunnerHandoff v2 精确集合相等 + duplicate detection）

**3.8 P0 修复**:
- P0-1: 全文使用 `ArtifactReferenceV2` 替换 `ArtifactReference`（所有 execution/analysis artifacts）
- P0-2: `ResourceUsageReport` 从 `variant_id` 改为 per-attempt 粒度（`attempt_id` + `unit_id` + `subject_type` + `variant_id` nullable + `seed` nullable + conditional validator）
- P0-3: `RetryDecision` 移除 `can_retry` 布尔，改为单一 `decision: Literal["retry_same_command", "do_not_retry", "return_to_3_5", "return_to_3_6_3_7", "blocked"]`
- P0-4: 新增 `AttemptOutcome`（execution/metrics/validity 三态）到 `AttemptRecord`；`derive_unit_status()` 确定性映射；`ExecutionUnitRecord.final_status` validator 校验 == derived
- P0-5: `validate_handoff_against_manifest()` service-level validator（id sets == manifest derived sets + overall_status == derived + sha256 match）
- ExecutionManifest counts validator: completed/failed/blocked counts == derived from unit_records

**3.9 P0 修复**:
- P0-6: 拆分 `MetricObservationKey`（含 seed，per-observation）vs `AggregatedMetricKey`（不含 seed，per-variant-metric）；`PairedMetricObservation._deltas_recomputed()` 完整校验 raw_delta + improvement_delta + raw_relative_pct + improvement_relative_pct（含 None 检查）
- P0-7: 新增 `ReplicationPairEvidence`（seed + paired_observation_ref + improvement_delta + validity_status）；移除 `direction_consistent` 布尔；`_determine_reproducibility()` 确定性计算方向一致性；校验 seed 属于 registered_seeds

**P1 修复**:
- P1-1: `NextRunProposal.target_stage` 改为 `Literal[...] | None`；STOP → None + validator 校验
- P1-2: `_make_incomplete_conclusion()` 接收 `protocol_seeds`，填充 `missing_seed_pairs`
- P1-3: `EvidenceSufficiency` 增加 quantity relations validator（0 <= valid <= completed <= total）
- P1-4: `ResourceComparisonReport` 新增 `BaselineResourceAggregate` + `VariantResourceAggregate` + `ResourceDelta`（baseline-vs-variant 结构）
- P1-5: 两份计划各增加 Reference registry 表（repository + commit/path + verified_at）

**结果**: 14 new tests + 776 existing = 790 passed, `69fee78` 已推送

### 3.8 & 3.9 开发计划 v2.9（5 P0 关单 + 3 非阻塞）

**审阅**: v2.8 仍有 5 P0 阻止完全封板（zero-attempt unit 表示、intake↔handoff 双事实源、outcome↔artifact 不一致、metric 值仅缓存自洽、budget 缺 not_assessable）。

**3.8 P0 修复**:
- P0-1: `ExecutionUnitRecord.terminal_reason`（7 态：completed/execution_failed/validity_failed/insufficient_evidence/blocked_upstream_failure/intake_failed/preflight_failed）+ `blocking_unit_ids` + `preflight_report_ref`；conditional validator：attemptless 终态合法（zero attempts + zero refs + reason-specific fields）
- P0-2: `derive_workspace_execution_refs()` 从 PatchRunnerHandoff v2 确定性生成 workspace_refs（Intake 自行派生，不信任调用者）；`validate_intake_against_patch_handoff()` SHA 交叉校验
- P0-3: `validate_attempt_record_against_artifacts()` → `derive_attempt_outcome()` 解引用 execution_result/metrics_report/validity_report 确定 outcome；验证 resource_report IDs 匹配

**3.9 P0 修复**:
- P0-4: `validate_observation_against_metric_artifacts()` 解引用 `ParsedMetric` 验证 value/seed/metric_name/parse_status；`validate_aggregate_from_observations()` 重算 mean_*/seed_count/completed_seed_count + 校验 variant_id/metric_name 一致性
- P0-5: `VariantBudgetAssessment`（status/reason/budget_ref/usage_refs）+ `"not_assessable"` 状态；`determine_budget_assessment()` 确定性规则

**非阻塞**: `ExecutionUnitResourceLedger`（consumed_total vs comparable_profile 分离）；`validate_replication_pair_against_observation()`（reproducibility pair 交叉校验）；registry 添加 exact commit SHA 注释

**结果**: 待验证推送

---

## Step 3.5 Batch 0 — Schema foundation + Adapter (commit 12a61e0)

**目标**: 创建所有 3.5 Pydantic 模型和 Stage34InputAdapter。

**操作**:
- `schemas/experiment_planning.py` (~650 lines): 50 个 Pydantic 模型按依赖顺序（PreparationPhase 到 Stage35Input），60+ 条类型
- `experiment/adapter_34.py` (~120 lines): `Stage34InputAdapter.load()` fail-closed + `derive_preparation_phase()` + `compute_unresolved_dimension_id()`
- `schemas/__init__.py`: 新增 55 个 re-export
- `experiment/__init__.py`: 重写为 adapter 的 `__all__` 层
- `tests/test_experiment_planning_schemas.py`: 38 个 schema contract 测试（extra="forbid"、判别联合、model_validator）
- `tests/test_adapter_34.py`: 21 个 adapter 测试（fail-closed: 缺失 analysis、non-viable、缺失 risk、duplicate risk；happy path multi-variant；preparation_phase derivation；hash determinism）
- `docs/3.5开发计划.md`: Section 17 路径从 `experiment/schemas.py` 修正为 `schemas/experiment_planning.py`

**关键决策**:
- Schema 放置在 `schemas/experiment_planning.py`（遵循 repo 约定，与 3.4 的 `schemas/transfer_design.py` 先例一致）
- 通用 artifact 引用使用现有 `ArtifactReferenceV2`（来自 `schemas/artifacts.py`）
- 封闭集（artifact 1–7）使用新类型 `ValidatedArtifactRef`
- 旧 `schemas/experiment.py::ExperimentPlan` 不触及（harness + verify gate 依赖）
- 不使用 `from __future__ import annotations` —— 类严格按依赖顺序定义

**结果**: 868/868 通过，已推送

**遗留**: Batch 1（Step 1: shared_protocol.py）

---

## Step 3.5 Batches 1-8 — 全模块实现完成 (commits ccbf8e2..e3062ff)

**目标**: 实现所有 Step 1-8 构建器、验证器和发射器。

**操作**:

Batch 1 (`ccbf8e2`): `experiment/shared_protocol.py` — `build_shared_protocol()` + 6 测试
Batch 2 (`1769cb2`): `experiment/stat_plan.py` — `build_stat_plan()` + `validate_decision_rule_coverage()` + `validate_stat_plan()` + 15 测试
Batch 3+4 (`ba69610`): `experiment/trial_specs.py` — `build_trial_specs()` + `experiment/matrix_builder.py` — `build_matrix()` + `_derive_productions()` + 7 测试
Batch 5-8 (`e367ea2`): `experiment/builders.py` — `build_resolution_plans()` + `build_resource_budget()` + `build_guard_policy()` + `experiment/validator_emitter.py` — `validate_plan()` (10 invariants) + `emit_handoff()` (4 gates)
Final (`e3062ff`): `experiment/__init__.py` — 完整 `__all__` 重新导出（18 个公开符号）

**模块结构**:
```
src/autoad_researcher/experiment/
  __init__.py              # 18 个公开符号
  adapter_34.py            # Stage34InputAdapter (fail-closed)
  shared_protocol.py       # Step 1
  stat_plan.py             # Step 2 (+ validators)
  trial_specs.py           # Step 3
  matrix_builder.py        # Step 4
  builders.py              # Steps 5-7
  validator_emitter.py     # Step 8 (10 invariants + 4 gates)
```

---

### 3.8 / 3.9 开发计划 v2.12（reviewer P0 字段对齐与证据链收口）

**目标**: 按 reviewer 对 v2.11 的 5 个实现级 P0，继续修正文档合同，不改 3.8/3.9 实现代码。

**操作**:
- 复核正式 schema：`analysis/metrics.py`、`supervisor/validity.py`、`runner/models.py`、`schemas/experiment_planning.py`
- 更新 `docs/3.8开发计划.md`
  - `derive_attempt_outcome()` 改为使用 `MetricsReport.status` 与 `ScientificValidityReport.status`
  - `validate_attempt_record_against_artifacts()` 增加 resolved artifact 身份绑定：`ref.sha256 == verified_sha256 == attempt.*_ref.sha256`
  - 增加 `expected_run_id`、`execution_result.attempt`、`command_sha256` 的交叉校验
- 更新 `docs/3.9开发计划.md`
  - 用 `ResolvedMetricEvidence` / `ResolvedValidityEvidence` 替代直接传裸 `ParsedMetric`
  - `validate_observation_against_metric_artifacts()` 改为校验 `ParsedMetric.value`，不再引用不存在的 `seed` / `primary_value`
  - `derive_pair_validity()` 改为使用 `ScientificValidityReport.status`
  - `VariantResourceAggregate` / `BaselineResourceAggregate` 补 `actual_gpu_hours`
  - `determine_budget_assessment()` 改为显式接收 `resource_budget_ref`
  - `compare_usage_with_budget()` / `derive_budget_reason()` 写成确定性 GPU-hours 规则

**结果**:
- 3.8 / 3.9 计划文档已对齐当前正式 schema 与 reviewer 指出的证据链要求
- 当前状态为“待下一轮机械验收复核”，不把这次文档修订直接写成封板结论

**遗留**:
- reviewer 提到的两个非阻塞 P1 仍未在本次文档修订中处理：`RetryDecision.identity_match` 冗余事实源、reference registry 缺 exact commit SHA
- 还需执行 `bash scripts/verify.sh` 与 push 流程确认仓库状态

**测试**:
- `tests/test_experiment_planning_schemas.py` (38 tests)
- `tests/test_adapter_34.py` (21 tests)
- `tests/test_shared_protocol.py` (6 tests)
- `tests/test_stat_plan.py` (15 tests)
- `tests/test_trial_specs_matrix.py` (7 tests)

**结果**: 896/896 通过，已推送

**遗留**: 无 — Step 3.5 全部 8 个步骤的代码骨架已完成，可进入 3.6

---

### 3.6-3.7 v1.5.5：3 P0 + 4 P1 第三轮封板修复

**目标**: v1.5.4 仍存在 Plan/Manifest 双重事实源、before_sha256 约束不闭合、Preflight diff artifact 字段断裂三处 P0

**操作**:
- P0-1: 从 RepositoryChangePlan 删除 patch_payloads；PatchPayloadManifest 为 Payload 唯一事实源；Plan 中 payload_id 不写回 Plan
- P0-2: 新增 before_sha256/target_before_sha256 完整约束表（create/modify/delete/rename/replace_existing/config/test_only 每种 change_kind 严格定义）
- P0-3: Preflight B16/C24 改为 `artifact_store.read(manifest.proposed_diff_artifact_id)` 和 `artifact_store.read(bundle.approved_diff_artifact_id)`
- P1-1: Implementation Seal 版本 schema v2 / v1.5.5 + 38 项
- P1-2: test#14 改为 ExternalValidationCommand dirty-state
- P1-3: PayloadValidationReport → PatchPayloadValidationReport + v1.5.3→schema v2
- P1-4: 删除 _recursive_sort_keys（json.dumps(sort_keys=True) 已覆盖）

**结果**: 868 passed, v1.5.5 Design Plan Seal ready

**遗留**: Implementation Seal 仍未变

---

### 3.6-3.7 v1.5.6：3 P0 + 4 P1 第四轮封板修复

**目标**: v1.5.5 三处遗漏：target_before_sha256 未进模型、change_kind 混合文件操作与用途、Partial Approval 缺 workspace 集合完整性

**操作**:
- P0-1: target_before_sha256 加入 PlannedRepositoryChange（replace_existing 时必填）和 PatchPayload；Validation category 新增 target_before_sha_mismatch
- P0-2: change_kind 拆为 operation_kind（create/modify/delete/rename）+ change_role（implementation/configuration/test）；Applicator 仅按 operation_kind 执行
- P0-3: C 组新增 26-30（workspace_reviewable_ids + approved/rejected 子集互斥 + 并集完整覆盖）
- P1-1: payload_id 改为确定性预分配（需要 Payload 的 change 强制非空）
- P1-2: canonical datetime 增加 astimezone(timezone.utc)
- P1-3: blob_artifact_ref → blob_artifact_id
- P1-4: 测试矩阵 38→42 项

**结果**: 868 passed, v1.5.6 Design Plan Seal ready

**遗留**: Implementation Seal 仍未变

---

### Step 3.5 v1.13：实现审计修复 + production orchestrator

**目标**: 按审计附件修复 3.5 runtime correctness / validator / emitter / production integration 问题，不修改其他阶段开发计划。

**操作**:
- 修复 `matrix_builder.py`：无 fit Variant 不再默认 `shared_fixed`，full dependencies 只引用真实存在的 fit entries。
- 修复 `SharedExperimentProtocol` baseline reuse：校验 repository_fingerprint、baseline_config_sha256、dataset_manifest_sha256、environment_lock_sha256、asset_manifest_sha256、evaluation_contract_sha256。
- 强化 `validator_emitter.py`：坏 JSON/schema 转 blocking structure issue；ValidationReport 只绑定 7 个 planning artifacts；handoff manifest 单独包含 validation report；binding/budget/resolution/evaluation-chain/hyperparameter invariant 从浅层存在检查升级为一致性检查。
- 强化 builders：ResourceBudget 从 Matrix + entry estimates 确定性汇总；GuardPolicy resource cap 来自 ResourceBudget；ResolutionPlan 对非结构化 acceptance criterion fail-closed，不再生成 `[0,1]` 占位 criterion。
- 新增 `experiment/planner.py`：`ExperimentPlanner` 执行 3.4 handoff → 7 planning artifacts → validation report → 3.6 handoff；`PipelineController.run_experiment_planner()` 接入 stage-level 调用点。
- 新增/更新测试：`test_experiment_validator_emitter.py`、`test_experiment_builders.py`、`test_experiment_planner.py`，并补充 matrix no-fit 和 baseline reuse identity 回归测试。
- 更新 `docs/3.5开发计划.md` 为 v1.13 audit repair，只改 3.5 计划文档。

**结果**: targeted tests `28 passed`；完整 verify 待运行。

**遗留**:
- `build_resolution_plans()` 当前对 free-text criterion fail-closed；若未来需要自动编译 unresolved dimensions，3.4/3.5 接口需先提供结构化 criterion，而不是自由文本。

---

### Step 3.5 v1.14：small follow-up P0 closure

**目标**: 关闭 9829248 审计后剩余的 3 个 P0，使 3.5 implementation seal 成立。

**操作**:
- 修复 Hyperparameter Validator 缩进：`predeclared_search` 正确检查 selection_split/search_space/search_budget/selection_metric；`fixed_from_source` 缺 evidence 不再触发 `UnboundLocalError`；恢复跨 Variant 相同 search_budget 检查。
- 调整 `ExperimentPlanValidationReport` refs 校验：passed report 必须绑定 7 个 planning artifacts；failed report 允许绑定已存在 artifact 子集。
- 强化 dependency DAG binding completeness：从 `TrialIntent.required_inputs` 反推 expected bindings；缺失、重复、unexpected binding、producer 不在 dependency ancestor 链均为 blocking。
- 新增负向测试覆盖缺 artifact、hyperparameter 缺字段/预算不一致、smoke/full binding 缺失、重复 binding、producer 脱离依赖链。
- 更新 `docs/3.5开发计划.md` 到 implementation seal v1.14。

**结果**: targeted tests `71 passed`；完整 verify 待运行。

**遗留**: P1 保持非阻塞：supplemental coverage evidence 尚未接 EvidenceIndex 验真；wall-clock 估算仍是保守汇总；旧 `run_planning_pipeline()` 默认路径仍保留旧 harness。

---

### Step 3.5 v1.14：implementation seal CI evidence

**目标**: 补齐 `66924490f81871dae8b4b155f36effb69d07f3b8` 的完整本地门禁和 GitHub Actions 证据。

**操作**:
- 重新运行 `bash scripts/verify.sh`。
- 使用 `gh run list` 查询 `BTGly/AutoAD-Researcher` 的 main 分支 Actions 状态，核对 head SHA。

**结果**:
- 本地 verify 通过，pytest 为 `922 passed`。
- GitHub Actions `verify` run `27753723164` 对应 head SHA `66924490f81871dae8b4b155f36effb69d07f3b8`，状态 `completed/success`。
- Actions URL: `https://github.com/BTGly/AutoAD-Researcher/actions/runs/27753723164`

**遗留**: 无新的 3.5 P0；Step 3.5 implementation seal 证据已闭合。

---

### Step 3.8 / 3.9 v2.11：计划文档合同收紧

**目标**: 根据终审附件收紧 3.8 / 3.9 计划文档，使文档状态、P0 合同和封板判断一致，不改 3.8 / 3.9 代码实现。

**操作**:
- 更新 `docs/3.8开发计划.md` 状态为 `targeted revision required v2.11`，补充 `ExecutionUnitRecord` 的 unit-attempt 归属闭包：attempt.unit_id 必须匹配、attempt_id/attempt_index 唯一、attempt_index 从 1 开始且严格递增、所有 attempts 的 `execution_unit_plan_sha256` 一致、attemptful terminal reason 必须清空 `blocking_unit_ids` 和 `preflight_report_ref`。
- 更新 `docs/3.9开发计划.md` 状态为 `targeted revision required v2.11`，将 `derive_pair_validity()` 改成基于 `ScientificValidityReport + ParsedMetric` 的可实现签名；补齐 `validate_observation_against_metric_artifacts()` 的 variant metric / dataset row / pair validity 交叉校验；把 `AggregatedMetricComparison.mean_*` 改成 nullable 并明确 no-valid-observation 规则；补全 `determine_budget_assessment()` 的返回合同和 `compare_usage_with_budget()` 的固定规则；将 reproducibility validity 交叉校验统一到 `observation.pair_validity_status`；调整 `VariantBudgetAssessment` 与 `ResourceComparisonReport` 的定义顺序。
- 两份文档末尾新增“当前封板状态”，明确 `architecture sealed` 但 `canonical contract blocked`。

**结果**: 文档已和终审附件对齐；完整 verify 待运行。

**遗留**:
- 这次只收紧计划文档，没有实现 3.8 / 3.9 的剩余 P0 代码。

---

### Step 3.6–3.7 v1.5.7：Design Plan Seal 收口

**目标**: 按最新审阅意见收紧 3.6/3.7 计划文档中的跨 artifact 绑定、canonical datetime 规范和测试矩阵，不改实现代码。

**操作**:
- 将 `docs/3.6-3.7开发计划.md` 的状态更新为 `v1.5.7`，并保持 `Design Plan Seal: passed；Implementation Seal: pending`。
- 在 Preflight 中补齐 `request.run_id == plan.run_id == manifest.run_id`、`request.repository_before_fingerprint == plan.repository_fingerprint == _fingerprint(repository_root)`、workspace variant set 等式，以及 `PatchPayload.target_before_sha256 == PlannedRepositoryChange.target_before_sha256`。
- 在 canonical hash 规范中明确 aware datetime 必须先归一到 UTC，并补充同一时刻不同 timezone offset 必须得到相同 canonical SHA。
- 将封板条件里的测试矩阵总数更新为 48 项，并补充本轮 review 对应的 43-48 项负向测试。
- 更新当前实现状态里的 Canonical SHA 描述，移除 `configuration_only/test_only` 旧术语残留，补一条 v1.5.6 → v1.5.7 版本修订记录。

**结果**: 文档已和最新审阅意见对齐；verify 与 push 待执行。

**遗留**:
- 这次只修 3.6/3.7 计划文档，没有触碰代码实现。

---

### Step 3.6–3.7 v1.5.8：canonical datetime P0 + seal state 修复

**目标**: 修复 v1.5.7 终审指出的 1 个技术性 P0 和 1 个封板状态 P0，并确认不破坏前序阶段兼容性。

**操作**:
- 修改 `src/autoad_researcher/schemas/patch_planning.py`：`compute_canonical_plan_sha256()` 的序列化链路改为 Python 对象模式，递归将 aware datetime 归一到 UTC `Z`，并拒绝 naive datetime。
- 新增 `tests/test_patch_planning_behavior.py` 覆盖同一时刻不同 timezone offset 产出相同 canonical 表示，以及 naive datetime fail-closed。
- 更新 `docs/3.6-3.7开发计划.md` 到 v1.5.8：文档示例改为 `mode="python" → exclude self-hash fields → normalize → JSON → SHA-256`；16.1 设计封板清单改为 `[x]`；新增 Design Plan Seal Record；Implementation Seal 保持 `not passed`。
- 清理非阻塞项：Preflight 编号改为 A/B/C/D/E 分组，删除重复 Variant 校验，补全 Schema Reference 中 `PatchPayload` 的前态字段。
- 兼容性检查：按真实导出检查 core、paper intelligence、repository intelligence、transfer、experiment、patch planning 关键模块导入，不改 legacy schema 字段。

**结果**: targeted patch planning / handoff tests `36 passed`；前序阶段兼容性导入检查通过；完整 verify 待运行。

**遗留**:
- 代码实现仍是 legacy Approval Protocol / experimental applicator；本次只完成 Design Plan Seal 与 canonical serializer P0 修复，不宣称 Implementation Seal。
