# AutoAD-Researcher 开发日志 — 2026-06-18

## Step 3.4 Idea & Transfer Design — 实施阶段

按照 v1.3 封板计划 (34 commits) 进行迭代开发。

---

### C1-C7: Schema contracts (commit 8cf7574)

**目标**: 建立所有 3.4 阶段的 Pydantic schema。

**操作**:
- 新增 `schemas/baseline_architecture.py` — TensorSpec, TensorAxis, InterfaceSpec, ExecutionPhaseContract, ModificationHook, ArchitectureComponent, BaselineArchitectureContract
- 新增 `schemas/transfer_design.py` — IdeaContract (discriminated union), DerivedClaim, ConstraintRef, IdeaAspectRef, AlignmentEntry, TransferConstraint, DIMENSION_POLICY, DimensionJudgment, derive_variant_status, TensorContractDelta, InterfaceContractDelta, RegimeChange, HookBinding, ImplementationVariant, RiskRecord, AcceptedRisk, VariantRiskReport, UnresolvedDimension, CLASSIFICATION_RULES, VariantSelection, TransferValidationIssue, IdeaTransferValidationReport, RepositoryReanalysisRequest, PaperReanalysisRequest, SpawnChildRunRequest, TransferResumeFingerprint, IdeaTransferBudget, IdeaTransferDesignHandoff
- 新增函数: compute_variant_risk, derive_variant_status, violates_confirmed_constraint
- 更新 `schemas/__init__.py` 导出所有新 schema

**结果**: 671 passed

---

### C8-C24: Transfer module implementation (commit bee86b7)

**目标**: 实现所有 3.4 阶段的核心逻辑。

**操作**:
- `transfer/router.py` — IdeaSourceRouter (path A), IdeaSourceResolver (path B), fuzzy matching
- `transfer/normalizer.py` — IdeaContractNormalizer
- `transfer/aligner.py` — ArchitectureAligner with source-neutral alignment
- `transfer/variants.py` — VariantGenerator (architectural-level)
- `transfer/compatibility.py` — CompatibilityAnalyzer (per-variant), filter_variants
- `transfer/risks.py` — build_variant_risk_report, accept_risk
- `transfer/selector.py` — VariantSelector with risk acceptance (no silent selection)
- `transfer/validator.py` — DeterministicValidator (8 invariant categories)
- `transfer/reanalysis.py` — build_repository_reanalysis, build_paper_reanalysis, build_spawn_child_run
- `transfer/handoff.py` — build_handoff to Step 3.5
- 更新 `transfer/__init__.py` 导出所有模块

**结果**: 671 passed

---

### C25-C33: Comprehensive tests (commit 50f579c)

**目标**: 72 个测试覆盖所有 schema contracts + 核心逻辑。

**覆盖**:
- IdeaContract (discriminated union, confirmed requires timestamp/evidence)
- DerivedClaim, ConstraintRef
- BaselineArchitectureContract, TensorSpec, ModificationHook (protected/unknown classifications)
- AlignmentEntry (source-neutral IdeaAspectRef, AlignableScope routing)
- Routing (user-provided, paper-grounded, fuzzy match, no candidates)
- DimensionJudgment (INCOMPATIBLE→blocking=true enforcement)
- derive_variant_status (viable/conditional/non_viable/needs_reanalysis paths)
- violates_confirmed_constraint (hard/soft strength)
- DIMENSION_POLICY coverage
- compute_variant_risk (aggregation: judgment risk + hook risk + regime risk)
- AcceptedRisk (severity constraint)
- VariantSelection (confirmed requires selections, no duplicate selected+rejected)
- UnresolvedDimension (experiment_resolvable requires verification_target)
- CLASSIFICATION_RULES
- Validator (passing, protected hook rejection, risk mismatch, INCOMPATIBLE enforcement)
- Reanalysis requests (repository, paper, spawn_child)
- Handoff (build, design_blocking rejection)
- Selector (recommend≠select, select_variants, reject_risk flow)
- Source-neutral alignment (user_provided / derived_hypothesis aspects)

**结果**: 743 passed (671 + 72 new)

---

### 3.4 Implementation Completed

**总计**: 756 passed, 6 commits (3.4-specific)

**Commits**:
- `8cf7574` C1-C7: 29 schemas + 3 deterministic functions
- `bee86b7` C8-C24: 10 transfer modules
- `50f579c` C25-C33: 85 tests
- `997c227` F1+F2: GitCommitPattern, TensorSpec validation, aligner fix
- `c41cca1` F3+F4+F5: contract_producer, orchestrator, e2e fixture
- `22ae6a8` evidence semantics fix + optional paper fingerprint

**结果**: 正式封板，`docs/stages/3.4-final-report.md` 已生成

**已知限制**:
- LLM-driven variant detail population
- LLM-driven dimension judgment population
- 真实 3.1→3.4 artifact I/O 串联
- CLI `autoad idea-transfer run/resume`
- GitHub Actions CI 远端未确认

---

### 3.8 & 3.9 开发计划

**目标**: 编写 Step 3.8（Experiment Execution Integration）和 Step 3.9（Results Analysis, Validity & Report）的开发计划。

**3.8 核心设计**:
- 不重新建设 Runner，只做集成桥接
- 复用 3.0 已有组件: `runner/executor.py`, `analysis/metrics.py`, `supervisor/validity.py`, `analysis/reproducibility.py`, `environments/`, `assets/`, `benchmarks/`
- 新增: ExperimentPlan→ExperimentCommandPlan bridge, AttemptManager, ExecutionManifest, CLI
- 目录: `src/autoad_researcher/execution/`

**3.9 核心设计**:
- 3.0 已有底层 contracts，3.9 只增加科研语义层
- 新增: ComparisonReport, FailureAnalysis, ResourceComparison, Reflection, final_report.md generator
- 无论成功/失败都必须生成报告
- 目录: `src/autoad_researcher/reporting/`

**结果**: 两份开发计划已生成
**遗留问题**: 无

### 3.8 & 3.9 开发计划 v2.6（reference traceability 恢复）

**目标**: 恢复被误删的参考实现映射章节，区分采用/改造/拒绝。

**结果**: `7fba583` pushed，776 passed

### 3.8 & 3.9 开发计划 v2.7（9 P0 + 4 P1 关单）

**审阅**: 审阅人指出 contract_status=targeted_revision_required，共 9 P0 需修复。

**3.8 P0 修复**:
- P0-1: 移除"同 v2.4"引用，内联全部 schema（ResourceUsageReport/RetryDecision/RetryIdentity/状态机/Resume）
- P0-2: WorkspaceExecutionRef 对齐 PatchRunnerHandoff v2 字段；RunnerIntakeRequest 增加 patch_runner_handoff_ref
- P0-3: PatchRunnerHandoff v2 validator 改为精确集合相等（拒绝 duplicate selected/workspace_id/cross-workspace variant）
- P0-4: 所有 validator 将 assert 改为 raise ValueError
- P0-5: 补全 ProducedArtifactRecord schema + sha256 一致性验证

**3.9 P0 修复**:
- P0-6: MULTIPLE_VARIANTS_DESCRIPTIVE 写入 IdeaSupportConclusion enum 体
- P0-7: derive_idea_support() 改为纯函数 + Sequence 输入 + 严格校验
- P0-8: _determine_reproducibility() 确定性判定规则（min_pairs + 方向一致性）
- P0-9: target_stage 增加 "3.6_3.7" + action→stage 合法组合 Validator

**P1 修复**: multiple_variant_policy 类型从 str 改为 Literal（对齐 3.5）、baseline 负数分母用 abs、ReportFacts 条件 Validator、PairedMetricObservation delta 重算验证

**结果**: 验证通过，776 passed，待推送

### 3.8 & 3.9 开发计划 v2.8（7 P0 + 5 P1 关单 + ArtifactReferenceV2 代码落地）

**审阅**: 审阅人指出 contract_seal=blocked，7 P0 阻止实现。架构已封板，只需修合同。

**代码变更（P0-1）**:
- 新建 `schemas/artifacts.py` → `ArtifactReferenceV2`（artifact_id, artifact_type, locator, sha256, source_id, size_bytes; extra=forbid; sha256 pattern enforced）
- `patch_planning.py` BaselineWorkspaceRef/VariantWorkspaceHandoff 改用 `ArtifactReferenceV2`
- `schemas/__init__.py` 导出 `ArtifactReferenceV2`, `BaselineWorkspaceRef`, `VariantWorkspaceHandoff`
- 新建 `tests/test_patch_runner_handoff_v2.py`（14 tests: ArtifactReferenceV2 validation + PatchRunnerHandoff v2 精确集合相等 + duplicate detection）

**3.8 P0 修复**:
- P0-1: 全文使用 `ArtifactReferenceV2` 替换 `ArtifactReference`（所有 execution/analysis artifacts）
- P0-2: `ResourceUsageReport` 从 `variant_id` 改为 per-attempt 粒度（`attempt_id` + `unit_id` + `subject_type` + `variant_id` nullable + `seed` nullable + conditional validator）
- P0-3: `RetryDecision` 移除 `can_retry` 布尔，改为单一 `decision: Literal["retry_same_command", "do_not_retry", "return_to_3_5", "return_to_3_6_3_7", "blocked"]`
- P0-4: 新增 `AttemptOutcome`（execution/metrics/validity 三态）到 `AttemptRecord`；`derive_unit_status()` 确定性映射；`ExecutionUnitRecord.final_status` validator 校验 == derived
- P0-5: `validate_handoff_against_manifest()` service-level validator（id sets == manifest derived sets + overall_status == derived + sha256 match）
- ExecutionManifest counts validator: completed/failed/blocked counts == derived from unit_records

**3.9 P0 修复**:
- P0-6: 拆分 `MetricObservationKey`（含 seed，per-observation）vs `AggregatedMetricKey`（不含 seed，per-variant-metric）；`PairedMetricObservation._deltas_recomputed()` 完整校验 raw_delta + improvement_delta + raw_relative_pct + improvement_relative_pct（含 None 检查）
- P0-7: 新增 `ReplicationPairEvidence`（seed + paired_observation_ref + improvement_delta + validity_status）；移除 `direction_consistent` 布尔；`_determine_reproducibility()` 确定性计算方向一致性；校验 seed 属于 registered_seeds

**P1 修复**:
- P1-1: `NextRunProposal.target_stage` 改为 `Literal[...] | None`；STOP → None + validator 校验
- P1-2: `_make_incomplete_conclusion()` 接收 `protocol_seeds`，填充 `missing_seed_pairs`
- P1-3: `EvidenceSufficiency` 增加 quantity relations validator（0 <= valid <= completed <= total）
- P1-4: `ResourceComparisonReport` 新增 `BaselineResourceAggregate` + `VariantResourceAggregate` + `ResourceDelta`（baseline-vs-variant 结构）
- P1-5: 两份计划各增加 Reference registry 表（repository + commit/path + verified_at）

**结果**: 14 new tests + 776 existing = 790 passed, `69fee78` 已推送

### 3.8 & 3.9 开发计划 v2.9（5 P0 关单 + 3 非阻塞）

**审阅**: v2.8 仍有 5 P0 阻止完全封板（zero-attempt unit 表示、intake↔handoff 双事实源、outcome↔artifact 不一致、metric 值仅缓存自洽、budget 缺 not_assessable）。

**3.8 P0 修复**:
- P0-1: `ExecutionUnitRecord.terminal_reason`（7 态：completed/execution_failed/validity_failed/insufficient_evidence/blocked_upstream_failure/intake_failed/preflight_failed）+ `blocking_unit_ids` + `preflight_report_ref`；conditional validator：attemptless 终态合法（zero attempts + zero refs + reason-specific fields）
- P0-2: `derive_workspace_execution_refs()` 从 PatchRunnerHandoff v2 确定性生成 workspace_refs（Intake 自行派生，不信任调用者）；`validate_intake_against_patch_handoff()` SHA 交叉校验
- P0-3: `validate_attempt_record_against_artifacts()` → `derive_attempt_outcome()` 解引用 execution_result/metrics_report/validity_report 确定 outcome；验证 resource_report IDs 匹配

**3.9 P0 修复**:
- P0-4: `validate_observation_against_metric_artifacts()` 解引用 `ParsedMetric` 验证 value/seed/metric_name/parse_status；`validate_aggregate_from_observations()` 重算 mean_*/seed_count/completed_seed_count + 校验 variant_id/metric_name 一致性
- P0-5: `VariantBudgetAssessment`（status/reason/budget_ref/usage_refs）+ `"not_assessable"` 状态；`determine_budget_assessment()` 确定性规则

**非阻塞**: `ExecutionUnitResourceLedger`（consumed_total vs comparable_profile 分离）；`validate_replication_pair_against_observation()`（reproducibility pair 交叉校验）；registry 添加 exact commit SHA 注释

**结果**: 待验证推送

---

## Step 3.5 Batch 0 — Schema foundation + Adapter (commit 12a61e0)

**目标**: 创建所有 3.5 Pydantic 模型和 Stage34InputAdapter。

**操作**:
- `schemas/experiment_planning.py` (~650 lines): 50 个 Pydantic 模型按依赖顺序（PreparationPhase 到 Stage35Input），60+ 条类型
- `experiment/adapter_34.py` (~120 lines): `Stage34InputAdapter.load()` fail-closed + `derive_preparation_phase()` + `compute_unresolved_dimension_id()`
- `schemas/__init__.py`: 新增 55 个 re-export
- `experiment/__init__.py`: 重写为 adapter 的 `__all__` 层
- `tests/test_experiment_planning_schemas.py`: 38 个 schema contract 测试（extra="forbid"、判别联合、model_validator）
- `tests/test_adapter_34.py`: 21 个 adapter 测试（fail-closed: 缺失 analysis、non-viable、缺失 risk、duplicate risk；happy path multi-variant；preparation_phase derivation；hash determinism）
- `docs/3.5开发计划.md`: Section 17 路径从 `experiment/schemas.py` 修正为 `schemas/experiment_planning.py`

**关键决策**:
- Schema 放置在 `schemas/experiment_planning.py`（遵循 repo 约定，与 3.4 的 `schemas/transfer_design.py` 先例一致）
- 通用 artifact 引用使用现有 `ArtifactReferenceV2`（来自 `schemas/artifacts.py`）
- 封闭集（artifact 1–7）使用新类型 `ValidatedArtifactRef`
- 旧 `schemas/experiment.py::ExperimentPlan` 不触及（harness + verify gate 依赖）
- 不使用 `from __future__ import annotations` —— 类严格按依赖顺序定义

**结果**: 868/868 通过，已推送

**遗留**: Batch 1（Step 1: shared_protocol.py）

---

## Step 3.5 Batches 1-8 — 全模块实现完成 (commits ccbf8e2..e3062ff)

**目标**: 实现所有 Step 1-8 构建器、验证器和发射器。

**操作**:

Batch 1 (`ccbf8e2`): `experiment/shared_protocol.py` — `build_shared_protocol()` + 6 测试
Batch 2 (`1769cb2`): `experiment/stat_plan.py` — `build_stat_plan()` + `validate_decision_rule_coverage()` + `validate_stat_plan()` + 15 测试
Batch 3+4 (`ba69610`): `experiment/trial_specs.py` — `build_trial_specs()` + `experiment/matrix_builder.py` — `build_matrix()` + `_derive_productions()` + 7 测试
Batch 5-8 (`e367ea2`): `experiment/builders.py` — `build_resolution_plans()` + `build_resource_budget()` + `build_guard_policy()` + `experiment/validator_emitter.py` — `validate_plan()` (10 invariants) + `emit_handoff()` (4 gates)
Final (`e3062ff`): `experiment/__init__.py` — 完整 `__all__` 重新导出（18 个公开符号）

**模块结构**:
```
src/autoad_researcher/experiment/
  __init__.py              # 18 个公开符号
  adapter_34.py            # Stage34InputAdapter (fail-closed)
  shared_protocol.py       # Step 1
  stat_plan.py             # Step 2 (+ validators)
  trial_specs.py           # Step 3
  matrix_builder.py        # Step 4
  builders.py              # Steps 5-7
  validator_emitter.py     # Step 8 (10 invariants + 4 gates)
```

---

### 3.8 / 3.9 开发计划 v2.12（reviewer P0 字段对齐与证据链收口）

**目标**: 按 reviewer 对 v2.11 的 5 个实现级 P0，继续修正文档合同，不改 3.8/3.9 实现代码。

**操作**:
- 复核正式 schema：`analysis/metrics.py`、`supervisor/validity.py`、`runner/models.py`、`schemas/experiment_planning.py`
- 更新 `docs/3.8开发计划.md`
  - `derive_attempt_outcome()` 改为使用 `MetricsReport.status` 与 `ScientificValidityReport.status`
  - `validate_attempt_record_against_artifacts()` 增加 resolved artifact 身份绑定：`ref.sha256 == verified_sha256 == attempt.*_ref.sha256`
  - 增加 `expected_run_id`、`execution_result.attempt`、`command_sha256` 的交叉校验
- 更新 `docs/3.9开发计划.md`
  - 用 `ResolvedMetricEvidence` / `ResolvedValidityEvidence` 替代直接传裸 `ParsedMetric`
  - `validate_observation_against_metric_artifacts()` 改为校验 `ParsedMetric.value`，不再引用不存在的 `seed` / `primary_value`
  - `derive_pair_validity()` 改为使用 `ScientificValidityReport.status`
  - `VariantResourceAggregate` / `BaselineResourceAggregate` 补 `actual_gpu_hours`
  - `determine_budget_assessment()` 改为显式接收 `resource_budget_ref`
  - `compare_usage_with_budget()` / `derive_budget_reason()` 写成确定性 GPU-hours 规则

**结果**:
- 3.8 / 3.9 计划文档已对齐当前正式 schema 与 reviewer 指出的证据链要求
- 当前状态为“待下一轮机械验收复核”，不把这次文档修订直接写成封板结论

**遗留**:
- reviewer 提到的两个非阻塞 P1 仍未在本次文档修订中处理：`RetryDecision.identity_match` 冗余事实源、reference registry 缺 exact commit SHA
- 还需执行 `bash scripts/verify.sh` 与 push 流程确认仓库状态

**测试**:
- `tests/test_experiment_planning_schemas.py` (38 tests)
- `tests/test_adapter_34.py` (21 tests)
- `tests/test_shared_protocol.py` (6 tests)
- `tests/test_stat_plan.py` (15 tests)
- `tests/test_trial_specs_matrix.py` (7 tests)

**结果**: 896/896 通过，已推送

**遗留**: 无 — Step 3.5 全部 8 个步骤的代码骨架已完成，可进入 3.6

---

### 3.6-3.7 v1.5.5：3 P0 + 4 P1 第三轮封板修复

**目标**: v1.5.4 仍存在 Plan/Manifest 双重事实源、before_sha256 约束不闭合、Preflight diff artifact 字段断裂三处 P0

**操作**:
- P0-1: 从 RepositoryChangePlan 删除 patch_payloads；PatchPayloadManifest 为 Payload 唯一事实源；Plan 中 payload_id 不写回 Plan
- P0-2: 新增 before_sha256/target_before_sha256 完整约束表（create/modify/delete/rename/replace_existing/config/test_only 每种 change_kind 严格定义）
- P0-3: Preflight B16/C24 改为 `artifact_store.read(manifest.proposed_diff_artifact_id)` 和 `artifact_store.read(bundle.approved_diff_artifact_id)`
- P1-1: Implementation Seal 版本 schema v2 / v1.5.5 + 38 项
- P1-2: test#14 改为 ExternalValidationCommand dirty-state
- P1-3: PayloadValidationReport → PatchPayloadValidationReport + v1.5.3→schema v2
- P1-4: 删除 _recursive_sort_keys（json.dumps(sort_keys=True) 已覆盖）

**结果**: 868 passed, v1.5.5 Design Plan Seal ready

**遗留**: Implementation Seal 仍未变

---

### Phase 1+2: Schema v2 upgrade + consumer adaptation

**目标**: 将 `patch_planning.py` 从 schema v1 升级到 v2，并适配所有 5 个 consumer agent。

**操作**:
- Schema v2 (`schemas/patch_planning.py`):
  - `change_kind` → `operation_kind` (`create/modify/delete/rename`) + `change_role` (`implementation/configuration/test`)
  - 新增 `target_before_sha256`, `target_collision_policy` 到 `PlannedRepositoryChange`
  - 从 `RepositoryChangePlan` 删除 `patch_payloads`，`schema_version=1→2`，`plan_sha256→patch_plan_sha256`
  - Rename: `payload_artifact_ref→payload_artifact_id`, `patch_diff_artifact_ref→patch_diff_artifact_id`
  - 新增 8 个模型: `PatchPayloadManifest`, `PatchPayloadValidationIssue`, `PatchPayloadValidationReport`, `InternalValidationStep`, `ExternalValidationCommand`, `FullApprovalDecision`, `PartialApprovalDecision`, `RejectDecision`, `ApprovalPatchBundle`
  - `ApprovalRequest` 重构: per-workspace, 新增 `workspace_id`, `patch_payload_manifest_sha256`, `proposed_patch_diff_sha256`, `approval_request_sha256`, `internal_validation_steps`, `external_validation_commands`
  - `ChangedFileEntry.change_kind→operation_kind`
- Agents 适配:
  - `patch_planner.py`: `change_kind="modify"→operation_kind`, `plan_sha256→patch_plan_sha256`
  - `planner_validator.py`: `plan_sha256→patch_plan_sha256`
  - `conflict_analyzer.py`: `plan_sha256→patch_plan_sha256`
  - `approval.py`: 完全重写以适应判别联合 (FullApprovalDecision/PartialApprovalDecision/RejectDecision)，移除 `approved_paths` 字段依赖
  - `patch_applicator.py`: 完全重写，`change_kind→operation_kind`, `plan_plan_sha256→patch_plan_sha256`, `payload_artifact_ref→payload_artifact_id`, `patch_diff_artifact_ref→patch_diff_artifact_id`, 使用 `ExternalValidationCommand`/`InternalValidationStep` 替换旧 `ValidationCommand` 路由
- `schemas/__init__.py`: 增加新模型导出和 `__all__` 更新
- `tests/test_patch_planning_behavior.py`: 全部适配 v2 构造函数

**结果**: 922/922 passed

---

### Phase 3: New implementations — Materializer, Validator, Steps, Commands

**目标**: 实现 Implementation Seal 缺失的 4 项 P0：PatchMaterializer、DeterministicPatchPayloadValidator、InternalValidationStep 四函数、ExternalValidationCommand ruff 模板。

**操作**:
- `code_agent/patch_materializer.py`: `PatchMaterializer` 类 + `build_payload_manifest()` — 从 RepositoryChangePlan + narrow repo 上下文生成 PatchPayload 列表和 manifest
- `code_agent/payload_validator.py`: `validate_payload_manifest()` — 校验 payload_sha256、before_sha256、target_before_sha256、change_id/路径一致性
- `code_agent/validation_steps/`: 4 个内检步骤
  - `ast_parse.py`: ast.parse + compileall bulk check
  - `diff_integrity.py`: 重算 diff vs proposed diff 一致性
  - `path_containment.py`: touched paths ⊆ approved scope
  - `before_after_identity.py`: 未声明文件内容不变校验
- `code_agent/validation_commands.py`: 注册模板 (`ruff_check_no_fix`, `ruff_format_check`) + `validate_command_argv()` + `execute_template_command()`
- `code_agent/__init__.py`: 导出全部新模块

**结果**: 922/922 passed

---

### Phase 3e-f: worktree_manager + narrow_repo_read + Phase 4 collision policy

**目标**: 完成 P1 模块 worktree_manager/narrow_repo_read；wiring collision policy 到 conflict_analyzer 和 applicator。

**操作**:
- `worktree_manager.py`: clone_shared_changes, build_workspace_binding, merge_workspace_manifests
- `narrow_repo_read.py`: read_file_safe/source integrity prevent path traversal
- `conflict_analyzer.py`: 碰撞 policy 不一致 → mutually_exclusive
- `patch_applicator.py`: _apply_single_change 中 must_not_exist/replace_existing 校验
- `code_agent/__init__.py`: 导出全部新模块

**结果**: 922/922 passed

---

### Phase 5: 48-item negative test matrix

**目标**: 实现设计文档第 13 章 48 项负向测试。

**操作**:
- `tests/test_patch_negative_matrix.py`: 35 个测试类，覆盖 Preflight A-E 组
  - A: payload SHA/path/diff 校验
  - B: create/rename/modify 文件冲突
  - C: rollback/symlink/ask/internal-step/template 边界
  - D: SHA binding/fingerprint/scope/deny 校验
  - E: partial approval/canonical time/payload-before-SHA 校验

**结果**: 957/957 passed

---

### Code Review Resolution: v1.5.8 schema compliance + P0 fixes

**目标**: 关闭代码审查发现的 6 项 P0 + 4 项 P1。

**操作**:
- **P0-2 Schema 全面升级至 v1.5.8**（~40 处修正）：
  - `PatchPayloadManifest`: 增加 `run_id`, `patch_plan_sha256`, `proposed_diff_sha256`
  - `ApprovalPatchBundle`: 增加 5 个缺失字段
  - `ApprovalRequest`: 增加 `patch_payload_validation_report_sha256`, `patch_plan_validation_report_sha256`；`workspace_summaries` → `workspace_summary` 单数；删除 `dependency_changes_summary`, `rollback_plan_sha256`
  - Full/Partial/Reject Decision: `approved_patch_plan_sha256` → `patch_plan_sha256`, `approved_payload_manifest_sha256` → `payload_manifest_sha256`；增加 `approval_request_id`, `approved_request_sha256`, `approved_diff_sha256`, `approved_paths`
  - `InternalValidationStep`: `step_id` → Literal, `required=True` 默认, 删除 `name`/`validation_function`
  - `ExternalValidationCommand`: `template_id` → Literal, `required=True`, `working_directory` 必填
  - `PlannedRepositoryChange`: `target_collision_policy` 默认 → `"must_not_exist"`, 删除 `evidence_ids`/`risk_category`
  - 删除 `ValidationCommand` 类
  - 实现 `CANONICAL_HASH_EXCLUDED_FIELDS` + `canonical_sha()` 通用函数
  - 所有列表保持原序（不再按命名列表保序）
- **P0-1 Planner 修复**: 预分配 `payload_id`；使用 placeholder SHA 初始化 `patch_plan_sha256`
- **P0-3 Payload Validator**: 实际读取 artifact 内容，计算 SHA 与 `payload_sha256` 比较
- **P0-4 验证路由**: `run_local_validation()` 使用 `execute_template_command` 替代 `_exec_external_command`；实际调用 `ast_parse_step`, `diff_integrity_step`, `path_containment_step`；删除 compileall
- **P0-5 Collision/Rename**: 修复 rename target SHA 校验（读目标文件 SHA）；must_not_exist 拒绝已存在目标
- **P0-6**: (deferred — tests need full rewrite per review criteria)
- **P1-1 narrow_repo_read**: 使用 `is_relative_to()` 替代字符串前缀 containment
- **P1-2 worktree_manager**: clone_shared_changes 为新 change 创建独立 payload_id, 清空 variant_ids, 写入 target workspace planned_change_ids
- **P1-3 canonical list**: schema 已修复（全部列表保序）
- **P1-4 _decision_paths**: 从 `plan.changes` 查找实际 `repository_path`

**结果**: 1095/1095 passed

---

### Review Round 2: Full Preflight A1-E10, default-deny, workspace scope, rename collision

**目标**: 关闭审核第二轮全部 P0 阻塞项。

**操作**:
- **P0-5 Default-deny**: `can_write_path` `_policy_allowed_set is None` → blocked（不再 allow-all）
- **P0-2 Workspace scope**: `validate_approval_consistency` 使用 `plan.changes` 按 `workspace_id` 过滤
- **P0-6**: Schema 新增 rename + replace_existing 强制 `target_before_sha256`；Applicator rename target 通过 `can_write_path` 检查
- **`_path_in_scope` fix**: 支持 trailing-slash 目录前缀匹配
- **移除 `_apply_without_preflight`**
- **Full Preflight A1-E10**: `run_preflight()` 重写，A1-A16/B1-B3/C1-C13/D1-D4/E1-E10 完整检查
- 新增 `_verify_approved_paths_projection()`、`_decision_request_id()`、`_EMPTY_SHA`

**结果**: 1228/1228 passed

---

### Review Round 3: ArtifactStore Payload closed loop, Validator completeness

**目标**: 建立真正的 ArtifactStore Payload 闭环；移除 placeholder fallback；完善 Validator。

**操作**:
- **ArtifactStore `write_raw`/`read_raw`**: 新增 raw bytes 读写方法，绕过 JSON/YAML 文件名白名单，保留 `is_relative_to()` containment
- **Materializer → ArtifactStore**: 生成 payload bytes 后写入 ArtifactStore；`payload_artifact_id` 改为 artifact filename
- **Validator → ArtifactStore**: `_validate_payload_artifact_sha` 从 ArtifactStore 读取实际 content，计算 SHA 与 `payload_sha256` 比较
- **Validator 完整性修复**:
  - Rename: `before_sha256` → source 文件；`target_before_sha256` → target 文件（分离正确）
  - `before_sha256` 缺失时强制报错（modify/rename 必填）
  - `target_before_sha256` 缺失时强制报错（replace_existing 必填）
  - 添加 `canonical_sha(manifest)` 验证、manifest run_id/patch_plan_sha256 绑定
  - 添加 payload_id vs change.payload_id 校验
  - 添加 missing/duplicate/extra payload 覆盖检查
- **Applicator 移除 placeholder**: `_render_placeholder` 删除；`_resolve_payload_content` 替换为 `_resolve_payload_from_store()`（ArtifactStore 读取 + SHA 验证）；修改/create 缺 Payload → 返回 None（blocked）
- `_apply_internal` 增加 `artifact_store`/`payload_manifest` 参数
- 测试 helper `_test_store`/`_test_payload` 提供临时 ArtifactStore
- E48 测试修正：assert `failed` 而非 `passed`
- Rollback 测试改为 delete 操作（无需 payload）

**结果**: 1228/1228 passed

**目标**: 按审计附件修复 3.5 runtime correctness / validator / emitter / production integration 问题，不修改其他阶段开发计划。

**操作**:
- 修复 `matrix_builder.py`：无 fit Variant 不再默认 `shared_fixed`，full dependencies 只引用真实存在的 fit entries。
- 修复 `SharedExperimentProtocol` baseline reuse：校验 repository_fingerprint、baseline_config_sha256、dataset_manifest_sha256、environment_lock_sha256、asset_manifest_sha256、evaluation_contract_sha256。
- 强化 `validator_emitter.py`：坏 JSON/schema 转 blocking structure issue；ValidationReport 只绑定 7 个 planning artifacts；handoff manifest 单独包含 validation report；binding/budget/resolution/evaluation-chain/hyperparameter invariant 从浅层存在检查升级为一致性检查。
- 强化 builders：ResourceBudget 从 Matrix + entry estimates 确定性汇总；GuardPolicy resource cap 来自 ResourceBudget；ResolutionPlan 对非结构化 acceptance criterion fail-closed，不再生成 `[0,1]` 占位 criterion。
- 新增 `experiment/planner.py`：`ExperimentPlanner` 执行 3.4 handoff → 7 planning artifacts → validation report → 3.6 handoff；`PipelineController.run_experiment_planner()` 接入 stage-level 调用点。
- 新增/更新测试：`test_experiment_validator_emitter.py`、`test_experiment_builders.py`、`test_experiment_planner.py`，并补充 matrix no-fit 和 baseline reuse identity 回归测试。
- 更新 `docs/3.5开发计划.md` 为 v1.13 audit repair，只改 3.5 计划文档。

**结果**: targeted tests `28 passed`；完整 verify 待运行。

**遗留**:
- `build_resolution_plans()` 当前对 free-text criterion fail-closed；若未来需要自动编译 unresolved dimensions，3.4/3.5 接口需先提供结构化 criterion，而不是自由文本。

---

### Step 3.5 v1.14：small follow-up P0 closure

**目标**: 关闭 9829248 审计后剩余的 3 个 P0，使 3.5 implementation seal 成立。

**操作**:
- 修复 Hyperparameter Validator 缩进：`predeclared_search` 正确检查 selection_split/search_space/search_budget/selection_metric；`fixed_from_source` 缺 evidence 不再触发 `UnboundLocalError`；恢复跨 Variant 相同 search_budget 检查。
- 调整 `ExperimentPlanValidationReport` refs 校验：passed report 必须绑定 7 个 planning artifacts；failed report 允许绑定已存在 artifact 子集。
- 强化 dependency DAG binding completeness：从 `TrialIntent.required_inputs` 反推 expected bindings；缺失、重复、unexpected binding、producer 不在 dependency ancestor 链均为 blocking。
- 新增负向测试覆盖缺 artifact、hyperparameter 缺字段/预算不一致、smoke/full binding 缺失、重复 binding、producer 脱离依赖链。
- 更新 `docs/3.5开发计划.md` 到 implementation seal v1.14。

**结果**: targeted tests `71 passed`；完整 verify 待运行。

**遗留**: P1 保持非阻塞：supplemental coverage evidence 尚未接 EvidenceIndex 验真；wall-clock 估算仍是保守汇总；旧 `run_planning_pipeline()` 默认路径仍保留旧 harness。

---

### Step 3.5 v1.14：implementation seal CI evidence

**目标**: 补齐 `66924490f81871dae8b4b155f36effb69d07f3b8` 的完整本地门禁和 GitHub Actions 证据。

**操作**:
- 重新运行 `bash scripts/verify.sh`。
- 使用 `gh run list` 查询 `BTGly/AutoAD-Researcher` 的 main 分支 Actions 状态，核对 head SHA。

**结果**:
- 本地 verify 通过，pytest 为 `922 passed`。
- GitHub Actions `verify` run `27753723164` 对应 head SHA `66924490f81871dae8b4b155f36effb69d07f3b8`，状态 `completed/success`。
- Actions URL: `https://github.com/BTGly/AutoAD-Researcher/actions/runs/27753723164`

**遗留**: 无新的 3.5 P0；Step 3.5 implementation seal 证据已闭合。

---

### Step 3.8 / 3.9 v2.11：计划文档合同收紧

**目标**: 根据终审附件收紧 3.8 / 3.9 计划文档，使文档状态、P0 合同和封板判断一致，不改 3.8 / 3.9 代码实现。

**操作**:
- 更新 `docs/3.8开发计划.md` 状态为 `targeted revision required v2.11`，补充 `ExecutionUnitRecord` 的 unit-attempt 归属闭包：attempt.unit_id 必须匹配、attempt_id/attempt_index 唯一、attempt_index 从 1 开始且严格递增、所有 attempts 的 `execution_unit_plan_sha256` 一致、attemptful terminal reason 必须清空 `blocking_unit_ids` 和 `preflight_report_ref`。
- 更新 `docs/3.9开发计划.md` 状态为 `targeted revision required v2.11`，将 `derive_pair_validity()` 改成基于 `ScientificValidityReport + ParsedMetric` 的可实现签名；补齐 `validate_observation_against_metric_artifacts()` 的 variant metric / dataset row / pair validity 交叉校验；把 `AggregatedMetricComparison.mean_*` 改成 nullable 并明确 no-valid-observation 规则；补全 `determine_budget_assessment()` 的返回合同和 `compare_usage_with_budget()` 的固定规则；将 reproducibility validity 交叉校验统一到 `observation.pair_validity_status`；调整 `VariantBudgetAssessment` 与 `ResourceComparisonReport` 的定义顺序。
- 两份文档末尾新增“当前封板状态”，明确 `architecture sealed` 但 `canonical contract blocked`。

**结果**: 文档已和终审附件对齐；完整 verify 待运行。

**遗留**:
- 这次只收紧计划文档，没有实现 3.8 / 3.9 的剩余 P0 代码。

---

### Step 3.6–3.7 v1.5.7：Design Plan Seal 收口

**目标**: 按最新审阅意见收紧 3.6/3.7 计划文档中的跨 artifact 绑定、canonical datetime 规范和测试矩阵，不改实现代码。

**操作**:
- 将 `docs/3.6-3.7开发计划.md` 的状态更新为 `v1.5.7`，并保持 `Design Plan Seal: passed；Implementation Seal: pending`。
- 在 Preflight 中补齐 `request.run_id == plan.run_id == manifest.run_id`、`request.repository_before_fingerprint == plan.repository_fingerprint == _fingerprint(repository_root)`、workspace variant set 等式，以及 `PatchPayload.target_before_sha256 == PlannedRepositoryChange.target_before_sha256`。
- 在 canonical hash 规范中明确 aware datetime 必须先归一到 UTC，并补充同一时刻不同 timezone offset 必须得到相同 canonical SHA。
- 将封板条件里的测试矩阵总数更新为 48 项，并补充本轮 review 对应的 43-48 项负向测试。
- 更新当前实现状态里的 Canonical SHA 描述，移除 `configuration_only/test_only` 旧术语残留，补一条 v1.5.6 → v1.5.7 版本修订记录。

**结果**: 文档已和最新审阅意见对齐；verify 与 push 待执行。

**遗留**:
- 这次只修 3.6/3.7 计划文档，没有触碰代码实现。

---

### Step 3.6–3.7 v1.5.8：canonical datetime P0 + seal state 修复

**目标**: 修复 v1.5.7 终审指出的 1 个技术性 P0 和 1 个封板状态 P0，并确认不破坏前序阶段兼容性。

**操作**:
- 修改 `src/autoad_researcher/schemas/patch_planning.py`：`compute_canonical_plan_sha256()` 的序列化链路改为 Python 对象模式，递归将 aware datetime 归一到 UTC `Z`，并拒绝 naive datetime。
- 新增 `tests/test_patch_planning_behavior.py` 覆盖同一时刻不同 timezone offset 产出相同 canonical 表示，以及 naive datetime fail-closed。
- 更新 `docs/3.6-3.7开发计划.md` 到 v1.5.8：文档示例改为 `mode="python" → exclude self-hash fields → normalize → JSON → SHA-256`；16.1 设计封板清单改为 `[x]`；新增 Design Plan Seal Record；Implementation Seal 保持 `not passed`。
- 清理非阻塞项：Preflight 编号改为 A/B/C/D/E 分组，删除重复 Variant 校验，补全 Schema Reference 中 `PatchPayload` 的前态字段。
- 兼容性检查：按真实导出检查 core、paper intelligence、repository intelligence、transfer、experiment、patch planning 关键模块导入，不改 legacy schema 字段。

**结果**: targeted patch planning / handoff tests `36 passed`；前序阶段兼容性导入检查通过；完整 verify 待运行。

**遗留**:
- 代码实现仍是 legacy Approval Protocol / experimental applicator；本次只完成 Design Plan Seal 与 canonical serializer P0 修复，不宣称 Implementation Seal。

---

### Step 3.8 / 3.9 v2.11-final：修复 2 P0 机械验收阻塞

**目标**: 关闭机械验收指出的 2 个剩余 P0：(1) resolved artifact presence 双向等价约束；(2) 预算比较的 unit 粒度 + GPU-hours 确定性来源。

**操作**:
- **P0-1 (3.8)**: 新增 `validate_resolution_presence(ref, resolved, name)` 函数，对 `execution_result/metrics_report/validity_report/resource_report` 四组输入全部调用 `(ref is None) != (resolved is None)` 双向校验，拒绝悬空引用或来源不可溯的 payload。
- **P0-1 (3.9)**: `validate_observation_against_metric_artifacts()` 增加 `expected_run_id` 参数；validity evidence 的 `unit_id/source_run_id` 对称于 metric evidence provenance 校验；metric evidence current_run 补 `source_run_id == expected_run_id`。
- **P0-2 (3.8)**: `ResourceUsageReport` 增加 `gpu_count_used: int | None` + `actual_gpu_hours` 计算属性（`gpu_count_used × wall_time_seconds / 3600`），按分配 GPU 数折算；失败/retry 计入总成本；comparable profile 仅使用最终有效 attempt。
- **P0-2 (3.9)**: `VariantResourceAggregate` 重构为 `per_unit_actual_gpu_hours: dict[str, float]`（unit_id→GPU-hours）+ `total_actual_gpu_hours` 计算属性（`sum(per_unit)`）。预算比较改为 `max(per_unit)` vs `max_per_experiment_gpu_hours`、`total` vs `max_total_gpu_hours`。`determine_budget_assessment` 移除 `total_actual_gpu_hours` 独立参数。

**结果**: 文档已更新，verify + push 待执行。

**遗留**:
- P1: `RetryDecision.identity_match` 仍作为可写布尔，应改由 factory/service 唯一生成。
- P1: Reference registry 仍缺 exact commit SHA / tag。

------

### Stage 3 整体状态复核 + 3.10 验收计划文档

**复核结论**（修正此前判断）：
- 3.5 代码**已完全实现**（`experiment/` 8 files, ~2300L, 含 planner/matrix_builder/trial_specs/stat_plan/validator_emitter/adapter_34）
- 3.6-3.7 代码**已实现**（`code_agent/` 5 files, ~1300L, 含 patch_planner/patch_applicator/approval/conflict_analyzer/planner_validator）
- 3.8 代码**部分实现**（`runner/executor.py` + `runner/models.py`，但 `PatchRunnerHandoff` v2 intake/ExecutionUnitRecord/AttemptRecord 新合同尚未编写）
- 3.9 代码**部分实现**（`analysis/metrics.py` + `reproducibility.py` + `supervisor/validity.py`，但 PairedMetricObservation/BundleBudgetAssessment/ResourceComparisonReport 新合同尚未编写）
- 3.3 **不是独立阶段**——设计为嵌入 3.2 Context Assembly 的修复/澄清补充机制
- 唯一真正的 stub：**`pipeline/orchestrator.py`**（11 行，仅 `pass`）
- 现有 `PipelineController` 只串联 experiment_planning → patch_planning（3.5→3.6）

**产出**: `docs/3.10开发计划.md` — 定义 E2E 验收的三层标准（L1 构件验收 / L2 流程验收 / L3 真实验收）、前置条件（含 3.8/3.9 未实现合同清单）、验收场景（PatchCore on MVTec bottle）、10 个必须产出物、9 条不可用 fixture 替代硬性条件。

**遗留**:
- 3.8 新合同实现（10 validator/schema）：`RunnerIntakeRequest` / `ExecutionUnitRecord` / `AttemptRecord` / `derive_attempt_outcome` / `validate_resolution_presence` / `derive_terminal_reason` 等
- 3.9 新合同实现（15 validator/aggregator）：`PairedMetricObservation` / `BundleResourceAggregate` / `BundleBudgetAssessment` / `determine_bundle_budget_assessment` / `validate_bundle_resource_coverage` / `ResourceComparisonReport` 扩展等
- Orchestrator 实现：串联 3.1→3.9 全部阶段
- E2E 验收执行：跑通一条 PatchCore bottle 真实任务

**目标**: 关闭机械验收最终 2 P0：(1) Variant metric evidence source_run_id 校验；(2) bundle 总量预算与 per-experiment 预算分离。

**操作**:
- **P0-1**: `validate_observation_against_metric_artifacts()` 中 Variant metric 补 `source_run_id == expected_run_id` 校验，拒绝另一 run 的同 unit_id metric 被跨 run 引用。
- **P0-2**: 
  - 新建 `BundleResourceAggregate`（baseline + per_variant）承载 bundle 级视图；`total_actual_gpu_hours` 跨 baseline + 所有 variants 汇总；`max_unit_actual_gpu_hours` 跨所有 unit 求最大值（用于辅助诊断）。
  - 新建 `BundleBudgetAssessment` 和 `determine_bundle_budget_assessment()`：`bundle_total` vs `max_total_gpu_hours` 决定 bundle 总量状态。
  - `VariantBudgetAssessment` / `determine_budget_assessment()`/ `compare_per_experiment_usage()` 仅判断 per-unit max vs `max_per_experiment_gpu_hours`，不再涉及总量比较。
  - `derive_per_experiment_budget_reason()` 不再输出 `total_actual_gpu_hours`。
- 更新 3.8/3.9 封板状态为 `SEALED`。

**结果**: `4a2725a` 已推送，922 passed。

**P0-1**: Variant metric `source_run_id == expected_run_id` 校验补全。

**P0-2**: `BundleResourceAggregate` + `BundleBudgetAssessment`；per-experiment 与 bundle 总量预算分离。

**遗留**:
- P1: `RetryDecision.identity_match` 仍作为可写布尔，应改由 factory/service 唯一生成。
- P1: Reference registry 仍缺 exact commit SHA / tag。

---

### Step 3.8 / 3.9 v2.12-final：Bundle canonical closure + coverage + evidence refs + non-negative 约束

**目标**: 关闭最终 P0 的三个缺口：(1) bundle 结果进入 canonical report；(2) unit/Variant 覆盖校验 + not_assessable；(3) baseline 证据引用 + SHA 去重 + 非负约束。

**操作**:
- **P0-3a**: `ResourceComparisonReport` 增加 `bundle: BundleResourceAggregate` + `bundle_budget_assessment: BundleBudgetAssessment`，并增加 `_bundle_consistency` validator。
- **P0-3b**: `BundleBudgetAssessment` 增加 `missing_unit_ids: list[str]` + `missing_variant_ids: list[str]`；补充 `validate_bundle_resource_coverage()` 校验计划 unit/Variant 覆盖；不完整时强制 `status=not_assessable`。
- **P0-3c**: `determine_bundle_budget_assessment()` 证据引用改为：`bundle.baseline.attempt_report_refs` + 所有 variant 的 `attempt_report_refs`，按 SHA 去重。接受 `expected_baseline_unit_ids` / `expected_variant_unit_ids` 参数。
- **P0-3d**: `ResourceUsageReport` 中 `gpu_count_used` / `wall_time_seconds` 加 `Field(ge=0)`；`VariantResourceAggregate` / `BaselineResourceAggregate` 增加 `_validate_gpu_hours` 拒绝负值/NaN/Inf。

**结果**: `6446bfd` 已推送，922 passed。

**遗留**:
- coverage 仍可跳过（Optional 参数），非精确集合校验（无 unexpected），无 assessment 重算闭包。

---

### Step 3.8 / 3.9 v2.12-final²：Bundle coverage 精确集合 + 强制执行 + assessment 重算

**目标**: 关闭最后一个 P0 的三个子缺口：(1) coverage 参数必填不跳过；(2) 精确集合校验（missing + unexpected）；(3) service-level validator 重算 assessment。

**操作**:
- **P0-4a**: 类顺序重排避免前向引用：`BundleResourceAggregate` / `BundleBudgetAssessment` / `validate_bundle_resource_coverage` 移至 `ResourceComparisonReport` 之前。
- **P0-4b**: `BundleBudgetAssessment` 增加 `unexpected_unit_ids: list[str]` + `unexpected_variant_ids: list[str]`；四个列表任一非空 → `status=not_assessable`。
- **P0-4c**: `validate_bundle_resource_coverage()` 改为精确集合校验，返回 (missing_unit_ids, unexpected_unit_ids, missing_variant_ids, unexpected_variant_ids) 4-tuple。`actual - expected` 发现非法数据。
- **P0-4d**: `determine_bundle_budget_assessment()` 的 `expected_baseline_unit_ids` / `expected_variant_unit_ids` 改为必填（`set[str]` / `dict[str, set[str]]`），不再可跳过。
- **P0-4e**: 新增 `validate_resource_comparison_report()` — 从 `ExecutionManifest` 派生 expected 集合，重算 `determine_bundle_budget_assessment()`，与 `report.bundle_budget_assessment` 严格比较。

**结果**: verify + push 待执行。

**P0-4e**: `validate_resource_comparison_report()` 增加 `budget.verified_sha256 == budget.ref.sha256` 校验；`execution_manifest.units` → `execution_manifest.unit_records`。

---

### 3.8 / 3.9 封板代码实现

**目标**: 按 v2.12 封板计划编写全部 schema、validator、service 函数和测试。

**操作**:
- **Phase 1**: 新建 `schemas/execution.py`（20 模型 + 3 类型别名）— 覆盖 Intake/Planned/Runtime/Record/Manifest 全链路
- **Phase 1**: `schemas/artifacts.py` 新增 `ResolvedArtifact[T]` 泛型
- **Phase 2**: 新建 `runner/validators.py`（11 函数）— `compute_identity_match` / `derive_workspace_execution_refs` / `validate_intake_against_patch_handoff` / `derive_overall_status` / `derive_attempt_outcome` / `derive_terminal_reason` / `derive_final_status` / `derive_execution_status` / `validate_resolution_presence` / `validate_attempt_record_against_artifacts` / `validate_handoff_against_manifest`
- **Phase 3**: 新建 `schemas/results_analysis.py`（28 模型 + 2 枚举）— `PairedMetricObservation` / `AggregatedMetricComparison` / `BaselineResourceAggregate` / `VariantResourceAggregate` / `BundleBudgetAssessment` / `ResourceComparisonReport` / `ReportFacts` / `Reflection` 等
- **Phase 4**: 新建 4 个 analysis 服务模块 — `analysis/delta.py` / `crossval.py` / `conclusions.py` / `budget.py`
- **Phase 5**: 新建 4 个测试文件（138 tests）— `test_execution_schemas.py` / `test_execution_validators.py` / `test_results_analysis_schemas.py` / `test_results_analysis_service.py`
- **Phase 6**: 更新 `schemas/__init__.py` / `runner/__init__.py` / `analysis/__init__.py` 重新导出全部新符号

**结果**: 1095/1095 passed（957 existing + 138 new），verify.sh 通过，待推送。

**遗留**:
- P1: `RetryDecision.identity_match` 仍作为可写布尔
- P1: Reference registry 仍缺 exact commit SHA

---

### 3.8 / 3.9 P0-1~P0-6 修复 (commit 4a36116)

**目标**: 按审阅者的 6 项 P0 发现，将实现对齐到已封板的 v2.12 合同。

**P0-1 — validate_attempt_record_against_artifacts 不再是空实现**:
- 完整实现 4-artifact (execution/metrics/validity/resource) 解析 + presence 双向约束 + SHA/verified_SHA/run_id/attempt_id/command_sha256/unit_id 交叉验证 + 重算 outcome 并比较存储值 + resolved_binding/produced_artifact SHA 一致性
- 实现 `derive_execution_status()` 从 ExperimentExecutionResult 确定性状态映射
- `ResolvedArtifact` 合约匹配密封版本（ref、verified_sha256、payload）

**P0-2 — compute_identity_match 恢复 4 个正则字段**:
- `AttemptIdentitySnapshot` 现仅包含 4 个正则字段：execution_unit_plan_sha256、command_sha256、input_refs_sha256、workspace_repository_fingerprint
- 不再接受裸字段的输入引用

**P0-3 — metric/validity 证据闭合已实现**:
- `validate_observation_against_metric_artifacts()`：SHA/verified_SHA 校验、seed 一致性、source_run_id + unit_id provenance、ParsedMetric 值比较、derive_pair_validity()
- `derive_pair_validity()`：同时检查 parse_status 和两侧 ScientificValidityReport

**P0-4 — 聚合仅包含有效配对观测**:
- `validate_aggregate_from_observations()` 按 `pair_validity_status == "valid"` 过滤
- 从有效观测重算均值、seed_count、completed_seed_count、比较状态

**P0-5 — 结论使用预注册 ScientificConclusion 而非符号启发式**:
- `derive_idea_support()` 操作于 per-Variant ScientificConclusion 枚举
- 使用 StatisticalAnalysisPlan 派生规则（BENEFICIAL/WORSE/EQUIVALENT/MIXED/INCOMPLETE）
- `IdeaSupportConclusion` 现在是具有 8 个确定状态的 str Enum
- `PairedMetricObservation` 包含方向字段（maximize/minimize）和 improvement_delta

**P0-6 — 资源预算重构为 per-unit GPU-hours**:
- `VariantResourceAggregate`、`BaselineResourceAggregate`：per_unit_actual_gpu_hours: dict[str, float]
- `BundleResourceAggregate`：baseline + per_variant 聚合
- `BundleBudgetAssessment`：精确覆盖（missing/unexpected unit/variant），不完整覆盖强制 not_assessable
- `compare_per_experiment_usage()`：near_budget 阈值 = 90%
- `determine_bundle_budget_assessment()`：接受 expected_baseline_unit_ids + expected_variant_unit_ids
- `validate_resource_comparison_report()`：从 ExecutionManifest 重算 bundle assessment

**完整重写**:
- `schemas/execution.py` — WorkspaceExecutionRef（基线/变体判别联合验证器）、AttemptOutcome、AttemptRecord、ExecutionUnitRecord（全 terminal_reason 验证器）、ExecutionManifest（计数验证器）、ExperimentExecutionHandoff（非相交集合验证器）
- `schemas/results_analysis.py` — PairedMetricObservation（delta 重算验证器）、VariantResourceAggregate（per-unit GPU-hours）、BundleBudgetAssessment（覆盖 + 状态验证器）
- `runner/validators.py` — 全部 8 个函数替换为密封实现
- `analysis/delta.py` — compute_deltas 现在返回带有方向的 raw + improvement delta
- `analysis/crossval.py` — validate_observation/cross-validation + derive_pair_validity + validate_aggregate
- `analysis/conclusions.py` — derive_idea_support + _make_incomplete_conclusion 使用 ScientificConclusion 枚举
- `analysis/budget.py` — 所有 5 个函数替换为确定性的 per-unit/bundle 预算评估

**测试**: 271 个新测试（替换并扩展旧的 138 个），1228 个总计通过。

**结果**: `4a36116` 已推送，1228 通过。

**遗留**: 
- P1: `RetryDecision.identity_match` 仍作为可写布尔，应由 factory/service 唯一生成
- P1: Reference registry 仍缺 exact commit SHA / tag

---

### 3.8 / 3.9 P0-1~P0-5 第二轮修复 (commit 795cc7d)

**目标**: 关闭复审指出的 5 组剩余 blocker。

**P0-1 — Runner Intake ↔ PatchRunnerHandoff 闭包**:
- 新增 `RunnerIntakeRequest` schema（含 `_validate_workspaces`：恰好 1 baseline、无重复 workspace_id、无跨 workspace variant_id）
- RunnerIntakeReport 更新为 status: eligible/blocked/needs_revalidation + report_sha256
- `derive_workspace_execution_refs()` 从 PatchRunnerHandoff 确定性派生 workspace_refs
- `validate_intake_against_patch_handoff()` 规范 SHA 比较恢复

**P0-2 — Resource artifact identity**:
- `validate_attempt_record_against_artifacts` 中 resource_report 类型化为 `ResolvedArtifact[ResourceUsageReport]`
- 新增 `payload.attempt_id == attempt.attempt_id` 和 `payload.unit_id == attempt.unit_id` 校验
- ResourceUsageReport 恢复 sealed 合同：`measurement_kind`（measured/partially_measured/not_available）、`measurement_tool`、`evidence_refs`、nullable 资源字段、`_validate` 检查每种 measurement_kind 的字段约束

**P0-3 — AttemptOutcome 状态机**:
- `metrics_status` 恢复 sealed 值：`parsed` / `parse_failed` / `not_run`
- `derive_terminal_reason()`：completed 需要全部三个字段同时满足（succeeded + parsed + valid）
- insufficient_evidence 映射到 BLOCKED（非 FAILED）
- `_TERMINAL_REASON_TO_FINAL_STATUS` 单源映射表
- ExecutionUnitRecord validator 增加 terminal_reason→final_status 一致性检查

**P0-4 — 聚合完整性**:
- `AggregatedMetricComparison._seed_count_matches` validator 检查 seed 唯一性
- `validate_aggregate_from_observations()` 增加 `obs.direction == aggregate_key.direction` 检查
- `validate_observation_against_metric_artifacts()` 增加 `expected_protocol_fingerprint` 参数和校验

**P0-5 — 资源预算闭环**:
- `determine_bundle_budget_assessment()` 增加 per-variant unit 级覆盖率（每个 variant 的 expected vs actual unit IDs）
- `ResourceComparisonReport` 重构为 sealed 结构（baseline / per_variant / per_variant_deltas / per_variant_budget_assessments 字典）+ `_bundle_consistency` validator

**测试**: 314 个新测试，1271 总计通过。

**结果**: `795cc7d` 已推送，1271 通过。

---

### 3.8 / 3.9 P0-1 & P0-2 第三轮修复 (commit 待推送)

**目标**: 关闭审阅指出的最后 2 组 P0。

**P0-1 — aggregate mean 完整性**:
- `validate_aggregate_from_observations()` 在有 valid observations 时先检查四个 mean 均非 None，再比较重算值
- 反例测试: valid obs + mean_baseline=None → ValueError

**P0-2 — ResourceComparisonReport 完整封闭**:
- baseline / bundle / bundle_budget_assessment 改为必填（不可为 None）
- `_bundle_consistency` validator 增加 per_variant ↔ deltas ↔ budget_assessments 键集一致性 + variant_id 自反性校验
- `validate_resource_comparison_report()` 增加 per-variant assessment 重算和 delta 重算
- `validate_bundle_resource_coverage()` 增加 per-variant 内部 unit 级覆盖

**测试**: 318 个新测试，1263 总计通过。

---

### Review Round 4: P0-1..P0-6 — apply_patch identity, preflight required artifacts, validator fail-closed, collision SHA

**目标**: 关闭第四轮代码审查指出的 6 项 P0 实现缺口。

**操作**:
- **P0-1 apply_patch identity**: `apply_patch()` 删除独立 `payload_manifest` 参数，仅使用 `manifest.payloads`；新增 `_verify_payload_manifest_identity()` 在 `_apply_internal` 入口强制 manifest 与决策/请求 SHA 绑定
- **P0-2 preflight required artifacts**: `run_preflight()` 中 `manifest`、`validation_report`、`payload_validation_report` 从 `Optional` 改为必填；`partial approval` 需要 `bundle`
- **P0-3 preflight D5-D10/E5-E9**: 补充 D5-D10 (command template validation、working_directory containment)、E5-E9 (approved_paths ⊆ allow scope、denied ancestor、ask path coverage)；Diff artifact SHA 检查 (B2/C7/C8)
- **P0-4 validator fail-closed**: `ArtifactStore` 改为 `required` (非 `Optional`)；缺失时提前报错
- **P0-5 validator duplicate detection**: 新增 `_check_duplicate_payloads()` 检测重复 `change_id`/`payload_id`
- **P0-6 create + replace_existing**: Schema validator 移除 `operation_kind` 限制，`replace_existing` 对所有 operation_kind 要求 `target_before_sha256`；Applicator 在 create + replace_existing 路径校验目标文件 SHA
- **Test alignment**: 全部 `run_preflight()`/`validate_payload_manifest()` 调用点更新为传递 `manifest`/`validation_report`/`payload_validation_report`/`artifact_store`；preflight 各负向测试中 manifest SHA 对齐 `plan.patch_plan_sha256` 以避免 A6 误拦截

**结果**: 1275/1275 passed

**遗留**:
- 48-item 负向测试矩阵仍需按严格单变量故障原则重写
- `external command` working_directory containment 在 D9/D10 需要加固
- Narrow repo read narrow contract (`allowed_paths`/`requested_paths`/`max_files`/`max_bytes`) 未强制执行

---

### Review Round 4 P0 closure — apply_patch operator precedence + rename A14 + negative matrix assertion alignment

**目标**: 修复 3 个测试失败，恢复 1275→1276 全绿。

**操作**:
- **apply_patch operator precedence**: `(skipped or failed) and applied` 加括号，修复 `skipped` 单走引起 `partial_failure` 的 bug（被 `not applied and attempted` → `failed` 路径拦截）
- **A14 rename target path**: `_verify_approved_paths_projection()` 的 `derived` set 加入 `rename_target_path`，消除 A14 vs E9 的冲突
- **Test assertion cleanup**: 还原 B04/B06 测试断言从 `"blocked"` 回 `"patch_application_failed"`（匹配原 git HEAD 语义）
- **derived_paths in test helpers**: 两个测试 helper 中 `derived_paths` 增加 `rename_target_path`

**结果**: 1276/1276 passed (verify.sh 通过)

**文件变更**:
- `src/autoad_researcher/code_agent/patch_applicator.py` — 行 537 加括号 + 行 1052 加入 rename target
- `tests/test_patch_planning_behavior.py` — `_apply` 中 `derived_paths` 扩展；`test_no_approved_returns_failed` 断言恢复
- `tests/test_patch_negative_matrix.py` — `_apply` 中 `derived_paths` 扩展

---

### Review Round 4.5: artifact_store fail-closed + counter-case tests (commit 待推送)

**目标**: 关闭审阅人指出的 artifact_store 在 preflight 中 fail-open B2 路径: store=None 时仍继续 preflight 并通过。

**操作**:
- `run_preflight()` 中 `artifact_store` 从 `Optional[ArtifactStore] = None` 改为 `ArtifactStore`（必填）
- `apply_patch()` 中 `artifact_store` 从 `Optional[ArtifactStore] = None` 改为 `ArtifactStore`（必填）
- 移除 `_verify_diff_artifact` 调用外的 `if store is not None` 条件守卫——B2 始终执行
- `_pf()` helper 中移除 `store or _test_store()` 回退，调用者必须显式传递 store
- `_apply()` helper 重写为始终创建 `artifact_store` 并传递给 `apply_patch`
- 所有测试调用点补全 `artifact_store` 参数
- 修复 3 个预存测试 bug：`isolation_mode="per_workspace"` → `"shared_workspace"`、`_c()` helper 中 `rename_target_path` 缺失、`derived_paths` 缺失 rename target

**新增反例测试**:
- `test_full_approval_store_none_b2_fails` — full approval + artifact_store=None → blocked, 含 B2 错误
- `test_full_approval_wrong_diff_bytes_b2_fails` — 正确 store + 错误 artifact bytes → ready=False + B2 错误
- `test_full_approval_correct_diff_bytes_b2_passes` — 正确 store + 正确 artifact bytes → B2 通过

**文件变更**:
- `src/autoad_researcher/code_agent/patch_applicator.py` — artifact_store 必填 + B2 条件移除
- `tests/test_patch_planning_behavior.py` — `_pf`/`_apply` 改造 + 3 反例测试 + 预存 bug 修复

**结果**: 1279/1279 passed
