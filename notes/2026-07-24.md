# 2026-07-24 开发日志

## GPU 运行测试与资源控制面复验

- 目标：在当前双 RTX 4090 主机上验证独立 run 的 GPU 分配、CUDA 实际执行、Attempt 运行期资源报告和 terminal 释放；修复跨 run 共享 lease 账本缺失的问题。
- 实现：将 `GpuAllocator` 的 lease ledger/lock 提升到 run 的父级主机资源作用域，`ResourceLease` 持久化 `run_id`，并增加跨 run 所有权校验；Attempt 生命周期接入 `nvidia-smi` 与 `/proc` 的可恢复采样，终态写入 `resource_samples.jsonl`、`resource_usage_report.json` 和 output manifest。
- 回归：新增跨 run 互斥、双卡不足、错误 run 释放/heartbeat、telemetry schema 和资源报告测试。专项结果为 `tests/test_gpu_allocator.py` 与 `tests/test_resource_telemetry.py` 共 `8 passed`；Attempt/report/schema 相关回归 `162 passed`；PatchCore readiness、Attempt、GPU allocator 和 telemetry 组合回归 `27 passed`。
- 真实双卡 run：`runs/uat_gpu_runtime_20260724_r2/` 中两个独立 run 同时申请单 GPU、`required_vram_mb=20000`；运行期间分别持有 `lease_000001 -> GPU 0`、`lease_000002 -> GPU 1`。两个子进程均报告 `torch.cuda.is_available=true`、单张 RTX 4090、可见设备分别为 `0` 和 `1`，并完成 CUDA 矩阵计算；两个 Attempt 均 `COMPLETED`，资源报告 `measurement_kind=measured`，output manifest 含资源报告，最终 active lease 为空。
- 真实资源报告：双卡 Attempt 分别持久化约 `0.0016 GPU-hours`、约 `5.75s wall time`、峰值显存约 `428 MB`。本机 `nvidia-smi` 采样到的 GPU utilization 为 `0.0`，因此本轮只把子进程 CUDA 计算产物和显存/可见设备证据作为实际执行证明，不扩大为持续利用率证明。
- 失败注入记录：`runs/uat_gpu_runtime_20260724_r1/` 保留了较大矩阵 workload 在框架超时后的失败 Attempt；其 child 产物、失败状态、资源报告和 lease 释放均被保留。随后用较小且显式释放 CUDA 缓存的 workload 完成 r2，未删除历史失败证据。
- PatchCore 边界：新的 `uat_patchcore_gpu_regression_20260724_r1` readiness 使用既有操作阈值 `required_free_vram_mb=20000`、`maximum_used_vram_mb=100`，GPU、数据、锁定 Python 和保护文件哈希均可通过，但当前 `workspace/repos/patchcore-inspection-07h` 没有 `.git` 元数据，`PhysicalReadinessGate` 按合同返回 `blocked`，因此没有伪造新的 PatchCore ready 证据或启动未经授权的正式 Attempt。既有 `uat_patchcore_07h_gpu_20260723_r2` 的历史单 seed PatchCore 结果仍保持独立，不作为本轮新回归结果。
- 结果：双 GPU 分配、CUDA 实际运行、跨 run 互斥、资源报告持久化和 terminal 释放通过；PatchCore 新鲜 readiness 仍受冻结仓库元数据缺失阻塞。完整 `scripts/verify.sh` 尚待执行，必须在当前日志写入后进行。
- 遗留：恢复带冻结 commit `aa646496cd960f30a21eedf1ba97d0371bc26200` 的可验证 PatchCore 仓库副本后，重新运行 readiness 和正式 PatchCore regression；继续保留 utilization 为 `0.0` 的采样边界说明。
