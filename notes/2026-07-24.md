# 2026-07-24

## 高级联合 UAT H1 P1 修复

- 目标：修复独立 H1 发现的 Candidate `.pyc` 运行时噪声阻断和 Adapter equals-form split 参数问题。
- 操作：在 `agent/advanced-uat-fix-h1` 隔离分支中，为 Python Adapter 强制设置 `PYTHONDONTWRITEBYTECODE=1`；候选确认只清理并审计未跟踪普通 `.pyc`/`.pyo` 运行时文件；收紧 split reference 空槽位校验；新增集成与 fail-closed 回归测试。提交前审查又补充了 protected path 显式拒绝及对应回归测试。
- 结果：定向回归测试 `28 passed`；完整项目门禁上一版为 `1945 passed, 2 skipped`，protected path 补强后需重新执行完整门禁。
- 遗留：H1 只能进行修复后回归复验；正式 UAT 仍需要独立 H2、真人 200% 缩放和屏幕阅读器记录。
