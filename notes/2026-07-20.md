## 2026-07-20 — non-Git repository attestation round-trip

- Goal: restore admission for acquired non-Git executable repositories without changing
  the canonical artifact serialization used by existing attestations.
- Operations: made `RepositoryAttestation.detached_head` optional at field presence,
  added persisted local-acquisition round-trip/hash coverage, and made the execution
  admission fixture serialize with production `exclude_none=True` semantics.
- Result: targeted acquisition/admission coverage and the full verification gate pass.
- Leftovers: frontend repository binding, execution metric contract, and remote PDF
  best-effort handling remain separate follow-up changes.

## 2026-07-20 — explicit execution repository confirmation UI

- Goal: make the repository selected for non-plan execution visible to the user and
  transmit its exact source ID through the existing typed confirmation API.
- Operations: replaced the execution-mode prompt with a small confirmation dialog;
  it lists acquired `github_repo`/`local_repo` sources, requires one explicit selection
  for execution modes, displays the final source ID before submission, and leaves
  confirmed-task recovery unchanged. The API client now sends
  `execution_repository_source_id` only for non-plan confirmation.
- Result: frontend production build and the full verification gate pass.
- Leftovers: no browser runtime is installed on this host, so browser interaction
  acceptance remains a manual final check rather than a new browser-test dependency.

## 2026-07-20 — execution metric contract and typed confirmation

- Goal: preserve metric-free plan drafts while making execution fail closed until at
  least one user-confirmed primary metric exists.
- Operations: added the stable `execution_contract_incomplete` confirmation conflict
  before repository binding/materialization; added a narrow typed metric endpoint and
  task-card input that persists `ConfirmedDecision` provenance and refreshes the
  pending draft under the existing confirmation lock.
- Result: targeted backend tests, frontend production build, and the full verification
  gate pass.
- Leftovers: remote PDF best-effort degradation remains separate follow-up work.

## 2026-07-20 — remote paper URL best-effort degradation

- Goal: make uploaded PDFs the dependable paper-evidence path while treating remote
  paper URLs as an optional acquisition attempt that never blocks the rest of intake.
- Operations: bounded the generic HTTP(S) fetcher to three validated redirects and
  50 MiB, mapped transport, HTTP, redirect, and size failures to the stable
  `remote_source_unavailable` outcome, kept the original URL source record, and
  added UI guidance to upload a PDF. Payload classification remains MIME-or-PDF-magic
  only; URL suffixes do not decide the paper pipeline.
- Result: deterministic provider/worker regression coverage passes (`48 passed,
  2 skipped`) and the frontend production build passes. The next full project gate
  will cover this change together with the earlier execution repairs.
- Leftovers: real-site availability, including CVF TLS reachability, is external
  best-effort acceptance rather than a required product gate.

## 2026-07-20 — baseline Attempt control-plane vertical slice

- Goal: connect one frozen, environment-ready Session to a real baseline Attempt
  without allowing a browser client to supply a command plan or any execution hash.
- Operations: added a server-owned baseline control service and route. It accepts
  only explicit scientific-contract choices, validates the confirmed primary metric,
  frozen repository binding, observed environment and registered input sources, then
  derives an isolated baseline worktree, EvaluationContract, execution-input record,
  protected-artifact hashes, command plan and idempotent Attempt/Job. Baseline
  finalization now projects Session state to `READY` only after successful completion.
- Result: a real subprocess Worker regression creates metrics and OutcomeCard, and
  verifies an idempotent replay creates neither a second Attempt nor a second Job.
- Leftovers: candidate/Reflection/Champion control and browser control-surface
  acceptance remain the next two independent changes.

## 2026-07-20 — experiment observatory plan follow-up patches

- Goal: incorporate the second review's small implementation safeguards without changing
  the read-only observatory architecture or pushing a new commit.
- Operations: recorded fixed revisions, URLs, licenses, exact reference files, and
  adoption boundaries for Arbor, MLflow, MiMo-Code, and the current AutoAD baseline;
  separated Champion registration from missing/invalid scientific assessment; capped
  Activity at the newest 100 reliable cards; aligned frontend acceptance with the
  existing build/lint toolchain plus manual UAT; and required exact candidate-file
  matching before loading an external `session_id`.
- Result: five observatory plan documents and this log were patched; `git diff --check`,
  frontend build/lint, and the project verification gate passed (`1798 passed, 2 skipped,
  4 warnings`). No commit or push was performed, and existing uncommitted frontend
  changes were left untouched.
- Leftovers: frontend interaction items remain manual UAT until a frontend test framework
  is intentionally introduced.
## Candidate Attempt control-plane vertical slice

- Added the Session-owned candidate launch service and REST route.  It accepts a concrete, reviewed `ExecutorProposal` and `InterventionContract`, while deriving the repository binding, observed Python/environment hash, frozen evaluation contract, baseline metrics, protected-artifact report, and command plan from durable artifacts.
- It reuses the existing `ScientificExecutorHandoffService`, isolated worktrees, intervention admission, Worker finalization, and `ScientificAssessmentService`; no new execution loop or raw command API was added.
- Added a real-worker regression: baseline → IdeaTree child → candidate → OutcomeCard/scientific assessment, plus idempotent replay.

## Browser confirmation E2E

- Added Playwright as a frontend development/CI dependency only.  The browser suite runs the actual Vite/React app with controlled API responses rather than creating a production browser dependency.
- It checks typed execution-repository binding, cancellation with no confirmation request, and visible handling of the stable `summary_changed` conflict.  Fixtures include multiple candidates and Chinese/long labels.

## Launch-request idempotency integrity

- Goal: prevent a same-key baseline or candidate replay from silently accepting a changed scientific contract, seed, or reviewed patch.
- Operations: baseline now records a canonical request hash in its existing execution-input artifact; candidate writes an immutable request receipt beside its Attempt. A replay reuses only an exact hash match; old/missing receipts and mismatches fail closed with `idempotency_conflict`.
- Result: baseline and candidate conflict regressions prove no new PipelineJob is created on a mismatched replay.
- Leftovers: split/input artifact snapshots, promotion control, and action UI remain separate changes.

## Frozen split and selected-material inputs

- Goal: make the selected split and material artifacts part of the same protocol evidence Finalizer verifies, rather than only recording human-readable references.
- Operations: baseline now requires `b_dev`/`b_test` files, includes them in the EvaluationContract protected-hash set, records their SHA-256 in execution inputs, and accepts selected materials only after successful intake with a run-local file artifact. The material source ID, artifact reference, and SHA-256 are frozen into that record.
- Result: pending material selection is rejected before any Job; mutating a split after freezing yields a protocol-violated OutcomeCard.
- Leftovers: the existing local-directory dataset registration is intentionally not treated as an acquired immutable artifact; it must be materialized into a run-local artifact before selection.

## Explicit B_dev/B_test adapter commands and baseline evidence

- Goal: remove the false implication that a frozen split path alone makes a Worker
  command a dev or held-out evaluation.
- Reference: Arbor keeps a distinct dev/test evaluation command and only considers
  held-out evidence for promotion; the same separation is adopted here without a
  new execution framework.
- Operations: adapter evidence can now declare exact `b_dev` and `b_test` command
  profiles. Legacy adapters remain B_dev-only; a B_test command is never guessed.
  When an adapter explicitly declares B_test, baseline control queues a matching
  B_test baseline Attempt and waits for both baseline records before making the
  Session ready for candidates.
- Result: targeted adapter and real Worker control tests cover the explicit B_test
  profile, two baseline Attempts, and the delayed `READY` transition.
- Leftovers: candidate confirmation must consume this B_test baseline, create a
  committed candidate snapshot, and remain separately approved before promotion.

## Candidate B_test confirmation and immutable candidate registration

- Goal: make held-out evaluation a real, explicitly approved product action rather
  than a manual test-only construction.
- Reference: Arbor keeps implementation worktrees isolated, evaluates a held-out
  command separately, and only then retains an improvement. This change reuses the
  existing Attempt/Worker/ScientificAssessment/CandidateRegistry components rather
  than adding an experiment loop.
- Operations: added a narrow confirmation endpoint/service. It derives the bound
  repository, B_test baseline, environment, contract, adapter command, candidate
  workspace and source evidence from stored artifacts; the caller supplies only the
  candidate Attempt, a declared noise threshold, and an idempotency key. It commits
  precisely the admitted diff in the run-owned worktree, queues the explicit B_test
  command, links the confirmation to the original implementation evidence, and has
  Worker finalization register an immutable CandidateSnapshot only after the B_test
  scientific decision is ready for promotion. Exact replays reuse the same Attempt
  and snapshot; conflicting requests fail closed.
- Result: real Worker coverage proves B_dev candidate → confirmed B_test → immutable
  snapshot and replay reuse.
- Leftovers: a CandidateSnapshot is eligible evidence, not a Champion. Promotion
  approval and merge remain a separate explicit action.

## Explicit Champion promotion action

- Goal: close the distinction between a best observed candidate and a durable
  Champion without introducing automatic promotion.
- Operations: added a promotion control action that accepts only a candidate ID and
  named human approver. It derives the run-owned repository from the candidate's
  recorded worktree, writes/reuses an immutable human approval policy, and delegates
  merge, journal recovery, ChampionEvent, and ChampionPointer updates to the existing
  `PromotionService`.
- Result: the real-worker chain now verifies candidate B_test registration followed by
  explicit promotion and a matching ChampionPointer.

## Promotion checkout correction

- Corrected promotion-target resolution: a candidate worktree is not the baseline
  checkout to merge into. The control service now reads Git's worktree registry and
  uses its run-owned primary checkout; it refuses a self-merge target.
- The worker regression now verifies the promoted source checkout actually contains
  the candidate change, in addition to the ChampionPointer.

## Approve-each-step browser actions

- Goal: preserve the read-only observatory while giving `approve_each_step` users a
  real product control surface for the two already-safe post-execution actions.
- Operations: the projection now exposes only durable candidate action facts. The
  workbench renders B_test confirmation only for an assessed, unregistered B_dev
  improvement and Champion promotion only for a B_test-passed, guardrail-passed
  candidate. It sends typed APIs; repository paths, commands, hashes, and merge
  targets remain unavailable to the browser.
- Result: projection regression, production TypeScript build, and all five browser
  tests pass. Local browser readiness now explicitly bypasses the external HTTP proxy
  for `127.0.0.1`, avoiding its 502 on the Vite probe.

## Durable task recovery and real-browser confirmation smoke

- Goal: validate the browser path against FastAPI and durable control-plane files,
  rather than treating intercepted Playwright responses as end-to-end coverage.
- Operations: added a read-only pending-task endpoint and UI reload recovery. The
  dedicated smoke seeds a disposable run through the same TaskBridge and repository
  attestation models used in production, then starts a real FastAPI instance and
  Vite proxy on isolated ports. It performs an `approve_each_step` confirmation in
  Chromium without route interception and verifies the durable binding, queued
  environment Job, and Session authorization.
- Result: the real smoke passes alongside the five mocked UI contract tests and the
  production frontend build. The isolated ports avoid touching a pre-existing local
  product server; `UV_BIN` remains an optional local override for environments whose
  non-login npm shell does not expose `uv` on PATH.

## Experiment Observatory repair 1 — refreshed detail and assessment semantics

- Goal: prevent a read-only projection refresh from showing a stale selected object,
  and keep invalid scientific-assessment artifacts distinct from a not-yet-materialized
  assessment.
- Operations: changed the page-local selection state to stable entity identifiers and
  derive the drawer object from the newest projection; made the attempt drawer render
  a state-specific scientific-assessment explanation instead of inferring it from a
  nullable object; added browser regressions for refreshed selection and invalid
  assessment rendering.
- Result: targeted production build and browser coverage pass before the mandatory
  full verification gate.
- Leftovers: refresh scheduling, durable-record degradation, activity tail reading,
  and remaining research-view formatting are addressed by subsequent isolated repairs.

## Experiment Observatory repair 2 — bounded activity and durable-state degradation

- Goal: keep the read-only projection responsive as its event log grows, and never
  turn corruption in an unrelated Session or Champion control record into a false
  absence claim.
- Operations: added a projection-only reverse JSONL iterator while retaining the
  WebSocket replay reader; stopped activity projection after its newest 100 relevant
  cards; made named Session reads exact before inventory discovery; return a typed
  conflict for an invalid unselected inventory; distinguish invalid Champion control
  records; removed refresh-effect identity dependencies; and rendered research-view
  facts rather than raw artifact JSON where the projection exposes stable fields.
- Result: focused projection tests and all Observatory browser tests pass locally;
  the first CI browser run exposed an assertion ambiguity after the structured
  insight display, which is corrected in the follow-up test-only commit.
- Leftovers: none for the observatory review repairs.

## HF-2.2 commit 1 — Experiment Observatory navigation shell

### Goal

Replace the former Settings navigation destination with the Experiment
Observatory while preserving the distinct general API configuration and the
existing experiment configuration form.

### Operations

- Added an `experiment` page identity and the “实验工作台” left-navigation entry.
- Added the initial `ExperimentPage` shell with explicit no-run and no-session
  empty states. It does not create, cache, or infer experiment state.
- Kept the top-level `ConfigModal` path unchanged. The Observatory’s
  “实验配置” action renders the existing `SettingsPage` with its existing save
  callback rather than duplicating or merging the two configuration forms.

### Result

- `npm run build`, `npm run lint`, and the mandatory `bash scripts/verify.sh`
  gate passed. The lint output contains only the pre-existing dynamic dependency
  warning from `useAutoScroll.ts`.

### Leftovers

- The next commit adds the backend read-only durable-artifact projection; the
  shell intentionally has no data request before that API exists.

## HF-2.2 commit 2 — read-only Experiment Observatory projection

### Goal

Expose one typed, short-lived view of the durable Session, IdeaTree, Attempt,
assessment, Champion, CognitiveCommit, and event artifacts without creating a
second experiment state or writing during a GET request.

### Operations

- Added `ExperimentProjection` and its API route at
  `GET /api/runs/{run_id}/experiment/projection`.
- Session discovery is private to the projection, does not add a Store listing
  API, never chooses by ordering, and accepts an external `session_id` only
  after exact matching against the current candidate filename stem.
- Input-task, OutcomeCard, scientific-assessment, reconciliation, Champion,
  and Activity reads are validated from already-existing files. Missing or
  invalid assessment details never trigger `assess()`/`effective_assessment()`
  and do not hide a valid Champion registration.
- Activity is a deterministic, session-bound, newest-first projection capped
  at 100 cards. Idea-tree mutations expose only the durable revision, never an
  invented historical node mutation.
- Added focused empty/selected/ambiguous selection, path-escape, read-only
  assessment, activity-cap, and configured-run-root regressions.

### Result

- Focused projection and run-path tests passed: `7 passed`; the final focused
  projection rerun passed `6 passed`. Full `bash scripts/verify.sh` passed with
  `1815 passed, 2 skipped, 4 warnings`.

### Leftovers

- Pending the full verification gate and publication of this isolated
  projection commit; the next commit consumes this API in the React page.

## HF-2.2 commit 3 — Experiment Observatory data display

### Goal

Render the typed read-only projection as an experiment overview, current Idea
Tree, reliable activity feed, and selected-object detail panel without turning
browser state or WebSocket payloads into a second scientific state.

### Operations

- Added typed frontend projection contracts and one API client reader.
- Added a nested React Idea Tree, Activity Feed, and detail panel. Attempt
  details explicitly separate OutcomeCard execution facts, scientific
  assessment, and reconciliation. The discussion action only pre-fills the
  existing chat composer; it never sends a message.
- Kept ordinary UI free of run IDs and artifact paths; they are exposed only by
  an explicit developer-details disclosure. Unknown runtime values render with
  their raw value rather than a guessed Chinese status.
- Added a Playwright observatory snapshot regression covering navigation, real
  projection rendering, insight display, and no-auto-send discussion handoff.

### Result

- `npm run build` and `npm run lint` passed (only the existing
  `useAutoScroll.ts` lint warning remains).
- The new Playwright test passed. On this proxied host it requires local
  `NO_PROXY=127.0.0.1,localhost` so Playwright can reach its Vite server; CI
  already provides a direct local connection.
- Full `bash scripts/verify.sh` passed with `1815 passed, 2 skipped, 4 warnings`.

### Leftovers

- Pending the full project gate and push. The last planned commit will make
  existing `experiment.*` WebSocket messages invalidate this snapshot.

## HF-2.2 commit 4 — WebSocket projection invalidation

### Goal

Refresh the Observatory from durable state when existing experiment events are
replayed or polled, without changing the WebSocket protocol or interpreting
event payloads in the browser.

### Operations

- `App.tsx` now treats the exact `experiment.` prefix only as an invalidation
  signal and increments a refresh tick before all existing source/job/chat/
  toast handling.
- `ExperimentPage` debounces that tick by 300 ms and rereads the projection.
  It aborts superseded requests and uses a monotonic request ID so an old
  run/session response cannot overwrite the current snapshot. It also clears
  stale selection and projection immediately when run/session changes.
- Added a direct refresh affordance that invokes the same projection reader;
  WebSocket envelopes and backend event writers remain unchanged.

### Result

- Frontend build, lint, and the Observatory Playwright regression pass. Full
  `bash scripts/verify.sh` passed with `1815 passed, 2 skipped, 4 warnings`.

### Leftovers

- Pending final project verification, push, and GitHub Actions confirmation.

## PR-R1 — Snapshot, Facts, Evidence, and Digest

- Goal: freeze report inputs and assemble evidence-bound facts without allowing report code to derive or invent scientific results.
- Operations: extended the typed snapshot inventory across the existing Session, contract/environment, IdeaTree, Attempt, OutcomeCard, ScientificAssessment, Candidate/Champion, StopDecision, and persisted cost artifacts; added SHA verification, stable Evidence IDs, immutable Facts/Evidence/Digest report artifacts, and the persistent Facts Worker stage.
- Result: report lifecycle/Facts coverage plus existing Worker and legacy route regressions pass (48 passed, 2 skipped). Failed, missing, and non-comparable facts remain explicit rather than being converted into improvement claims.
- Leftovers: PR-R2 will add structured Narrative, deterministic validation, and Markdown publication over these frozen artifacts.

## PR-R2 — Narrative, Validator, and Markdown

- Goal: publish a trustworthy Markdown report only after deterministic validation over frozen Facts and Evidence.
- Operations: added structured narrative sections, a safe deterministic fallback narrative, fixed-section Markdown rendering, evidence/attempt-aware validation, persistent Narrative Job dispatch, validation artifacts, and content-ready state transition.
- Result: report chain and legacy Worker/route regressions pass; publication never lets narrative text create metrics, attempts, status, Champion, or evidence facts.
- Leftovers: PR-R3 will expose the immutable content through versioned APIs, self-contained HTML, download, and the minimal ReportPage.

## PR-R3 — Versioned API, HTML, and ReportPage

- Goal: expose immutable report versions through fixed report IDs while preserving the legacy read-only report endpoint.
- Operations: added versioned report API, content/evidence/download allow-lists, self-contained HTML generation, FastAPI registration, and a ReportPage/API client upgrade that prefers version-bound latest content with legacy fallback.
- Result: report API regression tests pass and the frontend production build passes using existing project dependencies.
- Leftovers: PR-R4/R5 will add isolated read-only discussion, structured Proposal/Review, and user-confirmed handoff.

## PR-R4/R5 — Report Discussion, Review, and Confirmed Handoff

- Goal: keep report discussion read-only and turn follow-up requests into durable, user-confirmed control-plane handoffs.
- Operations: added a bounded report-bound transcript; typed proposals with EvidenceIndex-backed artifact references and validation; independent review decisions; confirmation/rejection APIs; event records; idempotent human-queue, failed-Attempt retry, and existing candidate-confirmation handoffs.
- Safety boundary: missing reviewed inputs for refinement or a new task context for pivot leave the proposal in DRAFT and create no Job, Attempt, Session, or contract mutation.
- Result: focused collaboration and handoff regression tests pass (4 passed), including no pre-confirmation execution, rejection blocking handoff, and confirmation replay.
- Leftovers: PR-R6 will add optional PDF generation and reproducible bundle packaging without making either a prerequisite for content review.

## PR-R6 — Optional PDF and Reproducible Bundle

- Goal: provide portable report artifacts without making PDF or ZIP success a condition for reading a validated report.
- Operations: registered Markdown/HTML bytes in the immutable manifest; added finite ZIP packaging with in-archive checksums and post-write verification; added a XeLaTeX-only optional PDF worker with timeout, captured diagnostic tails, non-empty output validation, and no pdflatex fallback; exposed enqueue-only PDF/Bundle requests and manifest-validated downloads.
- Compatibility: legacy `/api/runs/{run_id}/report` now prefers a content-ready versioned report, then preserves its prior read-only fallback order.
- Result: focused optional-artifact/API/legacy-route regressions pass (10 passed). A missing PDF capability records `format_status.pdf=failed` while generation remains `content_ready`.
- Leftovers: none for this PR; PDF remains intentionally optional and is not embedded in the browser.

## PR-R5 follow-up — Review idempotency and explicit refine/pivot handoff

- Goal: complete the report review and follow-up boundaries that were intentionally
  left in draft state when no reviewed candidate input or new task context existed.
- Operations: added stable narrative claim IDs; made ReviewDecision append-only with
  request-id replay protection, claim validation, and deterministic review-status
  projection; persisted Proposal evidence IDs alongside resolved references. A
  confirmed `REFINE_CURRENT` now delegates its reviewed CandidateLaunchInput to the
  existing CandidateControlService. A confirmed `PIVOT` creates a separate run with
  a new pending TaskBridge draft and immutable parent report/session lineage, without
  copying the old contract, repository, Session, or `input_task.yaml`.
- Result: focused reporting collaboration/handoff/narrative regressions pass
  (`8 passed`). No report path gains command construction, direct executor access,
  or automatic follow-up execution.
- Leftovers: the new pivot run still requires its own explicit task confirmation and
  repository selection before Session/environment creation, matching the existing
  TaskBridge confirmation boundary.

## PR-R3 follow-up — Explicit report reads and report workspace

- Goal: make the UI distinguish an updating report version from the last readable
  version, while binding every content, summary, evidence, and download action to a
  fixed report ID.
- Operations: added explicit latest-created/latest-content-ready, state, digest, and
  evidence-index reads; restored manifest to immutable identity only; unified report
  artifact SHA and directory checks for content, download, and availability. Rebuilt
  ReportPage around those APIs with version selection, status, summary, evidence, and
  HTML/new-window plus PDF/Bundle download actions.
- Result: report API regression and frontend production build pass.
- Leftovers: report retry and the bounded Discussion responder are handled as separate
  control-plane stages; this UI does not execute render or experiment work in-browser.

## PR-R4 follow-up — Replay-safe discussion turns

- Goal: make report discussion durable across request retries and interrupted writes
  without coupling it to the main Chat transcript or to an agent filesystem.
- Operations: replaced isolated message appends with report-bound DiscussionTurn JSONL
  records. A request first persists `pending`, later appends the same turn as completed
  or failed; request IDs replay under a discussion lock, completed turns flatten to
  user/assistant display messages, corrupt physical tail data is ignored only at EOF,
  and a corrupted middle record is explicit failure.
- Result: focused collaboration and handoff regressions pass (`8 passed`).
- Leftovers: response generation remains a separate bounded service over Facts/Evidence;
  this persistence stage does not expose arbitrary file or execution tools.

## PR-R4 follow-up — Frozen-context discussion responder

- Goal: complete report-bound discussion without giving a model generic filesystem,
  shell, task, or experiment-control capabilities.
- Operations: added a bounded structured LLM call over the current turn, report digest,
  snapshot identity, and a capped EvidenceIndex projection. Response evidence IDs are
  revalidated before the persisted turn becomes completed; missing model configuration,
  provider failures, or invalid JSON complete the same turn as failed.
- Result: focused Discussion regressions pass (`5 passed`), including schema output and
  report-bound turn completion.
- Leftovers: the responder intentionally does not create Proposals. Propose remains the
  separately validated API path already implemented by the report control plane.

## PR-R4 follow-up — ReportPage discussion surface

- Goal: expose the already report-bound Discussion API in the report workspace without
  coupling it to main Chat or adding an execution control surface.
- Operations: added typed discussion API clients and a compact ReportPage transcript
  with per-request browser IDs. The component reloads only the selected report's turns
  after sending and retains the existing evidence, version, and download binding.
- Result: frontend production build passes.
- Leftovers: follow-up proposals stay in their explicit validated API flow; the reading
  workspace does not turn discussion text into an execution request.

## Report agents contract completion

- Goal: align the report control plane with the current report-agent handoff without
  altering other in-flight features or adding a second executor/runtime.
- Operations: added canonical report-recipe and snapshot-policy identity, optimistic
  double-read Snapshot freezing, frozen control-plane copies, an immutable Manifest
  with State-owned artifacts/delivery records, and report-only failed-Job requeue.
  The durable DAG is now Facts -> Narrative -> Validate -> HTML -> Bundle; PDF stays
  independent. Validation now uses structured paragraphs/claims/fact placeholders
  and emits a claim-evidence map. Bundle delivery includes fixed state/exclusion
  snapshots, stable ZIP metadata, checksums, and no mutable State/PDF.
- Safety and UX: report API reads verify State-bound SHA references, explicit delivery
  metadata supplies MIME/filename/disposition, and report Discussion has a bounded
  structured responder with a per-report response slot. ReportPage keeps State visible
  for updating versions and enables discussion only after validated content is ready.
- Result: focused reporting regressions pass (`26 passed`), the frontend production
  build passes, and `bash scripts/verify.sh` completes successfully.

## Report agents completion audit follow-up

- Goal: close the remaining report-plan observability gaps after the first contract
  delivery without expanding report control into an execution surface.
- Operations: separated frozen cognitive-cost and compute-resource Facts (the latter
  explicitly records an unknown state when no ResourceUsageReport is frozen), exposed
  report-owned Job projections from `/state`, and rendered Champion/stop facts already
  present in the Digest. The state API stays read-only and all values remain bound to
  the selected `report_id`.
- Result: focused reporting regressions pass (`27 passed`) and frontend production
  build passes. Full verification and the follow-up push are next.

## Report agents remediation: provenance, identity, deep reads, and delivery

### Goal

Close the remaining report-agent contract gaps without adding a second execution
runtime or generic filesystem access to report discussion.

### Operations

- Added explicit Fact-path projections to field-level Evidence and made Narrative
  Claim validation require matching Facts and the corresponding registered Evidence;
  removed root-Evidence fallback binding. Scientific effect checks now prefer the
  frozen ScientificAssessment projection over OutcomeCard comparison fields.
- Captured a non-secret Narrative generation profile at report creation and included
  its mode, provider endpoint, model, and prompt hash in the recipe identity. API
  keys remain runtime-only and are never persisted.
- Bound CognitiveCostSummary to a locked `llm_usage.jsonl` SHA-256 window. Resource
  aggregation now discovers only `ResourceUsageReport` JSON files registered by an
  execution OutputManifest and otherwise reports explicit unknown state.
- Replaced escaped-Markdown HTML with offline Markdown table rendering and escaped
  Evidence metadata. `/state` now projects report Job dependency status and blocking
  reasons for ReportPage.
- Added bounded typed deep-read tools for frozen digest, sections, attempts,
  outcome/assessment/metrics, contract/environment, champion, budget, and Evidence.
  Patch/log requests remain unavailable unless a compatible artifact was registered;
  no tool accepts paths or can write, execute, or create experiment work.

### Result

- Focused reporting regression suite passes (`33 passed`) and the frontend production
  build passes. Full project verification and push remain pending.

### Leftovers

- None within the approved report remediation scope. Runtime verification, commit,
  push, and GitHub Actions confirmation remain required before handoff.
