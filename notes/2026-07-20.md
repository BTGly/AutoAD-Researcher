# 2026-07-20

## 报告 Agents 计划复核修订

- 根据审阅报告与本地成熟项目固定版本，修订 `notes/报告agents/`。
- 采用 Arbor/ARIS 的原子状态与有限恢复思路，补充 Snapshot 来源的 optimistic double-read；不增加跨 Store 全局锁。
- 明确 Facts 只读取冻结 Snapshot，补充段落级 claim/evidence 绑定、DiscussionTurn 恢复、Proposal evidence ID 和 report recipe identity。
- 将 Bundle 的 live state 改为打包时 delivery state snapshot，v1 Bundle 与 PDF 解耦。
- 修正复用清单的真实 source files、完整 local_target 路径和 Claw 许可证核验状态。

验证：manifest YAML 解析、参考文件存在性、文档交叉字段检查、`git diff --check`。

## 报告 Agents follow-up 契约修订

- 冻结 `AttemptSnapshotV1` 和报告创建时的 `CognitiveCostSummary`，避免报告回读会变化的运行时 JSON。
- 增加报告 Job 的显式 failed requeue，并将 HTML/Bundle/PDF 调整为固定依赖 DAG。
- 固定 package Job 的 `created_at` 作为 `packaged_at`，补充 exclusions、checksums 和外部 ZIP SHA 语义。
- 增加 State API，修正 `/latest`、request_id、ReviewDecision 和 Session 审阅字段表述。
- 给 Narrative paragraph 增加结构化 `paragraph_kind`，并将复用清单目标改为 `planned_local_targets`。
