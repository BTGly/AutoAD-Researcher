# 2026-07-21

## Report Agent provenance and discussion closure

- Goal: close the reviewed report-agent provenance, model-failure, multi-turn discussion, and package-contract gaps without modifying the parallel reporting worktree.
- Operations: in the isolated reporting worktree, made selected Narrative model failures fail the durable Job for retry; included the actual system prompt, agent profile, and JSON schema in report identity; completed Facts-to-Evidence projections; exposed engineering/execution/scientific status and evidence-insufficient uncertainty; published interpretation and limitation text only through bound Claims; added bounded completed-turn history and typed-tool provenance; verified OutputManifest self-hashes; and admitted only existing handoff patch artifacts. Pre-commit review also made the four-field typed-tool provenance envelope consistent while preserving existing result fields. Synced the reporting plans and index to these exact contracts.
- Result: focused report tests, production frontend build, real FastAPI/Vite E2E, and the full project verification gate pass after the final typed-tool envelope correction. Commit, push, and remote Actions confirmation remain.
- Leftovers: `get_patch_diff` intentionally returns unavailable without an existing registered patch artifact; it does not manufacture a Git diff. Broader artifact contracts and external-model operations remain later product work.

## Report Agent completion-audit correction

- Goal: prove the remaining patch-diff and deep-read provenance contracts instead of relying on indirect coverage.
- Operations: changed `get_patch_diff` to return the standard unavailable result when no unique registered patch Evidence exists, while keeping registered patch reads on the SHA-bound reader; added direct OutcomeCard, ScientificAssessment, and Metrics provenance regressions; added Worker-level coverage that a frozen model profile failure persists a failed Narrative Job and can be requeued; added Digest and ReportPage browser coverage for separate engineering/execution/scientific state, core metrics, and HTML delivery; added precise Candidate, Attempt, baseline, metric, validity, failure, and non-comparable Evidence projection assertions. The new Candidate assertion found and corrected a `candidate_snapshot:{candidate_id}` parser collision.
- Result: `40` focused report regressions, the full frontend E2E suite (`8 passed`), the real FastAPI/Vite E2E (`1 passed`), the production frontend build, and the project verification gate pass after the final projection-coverage correction.
- Leftovers: no Git worktree diff is generated or inspected by reporting.

## Report plan conformance batch 1 - proposal budgets and lineage

- Goal: close the planned deterministic Proposal budget gate and generic report lineage without inferring parents from report order or idempotency strings.
- Operations: replaced free-text `estimated_budget` with unit-bearing limits; execution-producing Proposal validation now compares estimated wall/GPU time to the frozen EvaluationContract and optional cognitive usage to the frozen CognitiveCostSummary remainder. New reports can name one already handed-off source Proposal, which writes immutable parent/previous/proposal lineage into the manifest.
- Result: focused handoff, lifecycle, and collaboration coverage passes (`26 passed`), and the full project verification gate passes. Push remains pending for this batch.
- Leftovers: PIVOT retains its existing cross-run lineage artifact and is not forced into a same-Session parent relationship.

## Report plan conformance batch 2 - environment and recipe identity

- Goal: make the registered observed environment available through SHA-bound Facts, Evidence, and Discussion without creating a second environment collection path; bind Discussion deep reads to the persisted turn snapshot; and account for all delivery behavior versions in report identity.
- Operations: parsed the existing `EnvironmentSnapshot` source during Facts enrichment, copied every observed field except the local `environment_path`, mapped its retained projection to Evidence, and returned that projection from the typed environment tool. The tool context now verifies the persisted turn's snapshot hash against the manifest, saved Snapshot, and Evidence Index. Added explicit build/format versions for Evidence, Digest, Bundle, and PDF to the recipe hash.
- Result: focused report-tool, lifecycle, and handoff regression coverage passes (`27 passed`). Full verification and push remain pending for this batch.
- Leftovers: the report retains the full observed package inventory as frozen data; only the local absolute environment path is intentionally excluded from its user-facing Facts projection.

## Report plan conformance batch 3 - minimal report review and handoff UI

- Goal: expose the existing report review and human follow-up contracts in ReportPage without duplicating execution controls in the browser.
- Operations: added a read-only proposal list endpoint and ReportPage controls for ReviewDecision plus `REQUEST_HUMAN` Proposal creation, confirmation, and rejection. Execution-producing Proposal types remain backend-only because their typed candidate/retry inputs belong to the existing control plane. Synced the bundled reporting plans and workspace plan documents to the actual turn path, bounded-history behavior, recipe boundaries, and this UI scope.
- Result: focused reporting regressions pass (`27 passed`) and the production frontend build passes after restoring the worktree's lockfile dependencies. Full verification and push remain pending for this batch.
- Leftovers: the UI does not offer a generic execution-form builder; `REFINE_CURRENT`, retry, confirmation, and pivot stay available only through their already typed server-side workflows.

## Report plan conformance batch 3 regression correction

- Goal: preserve ReportPage compatibility when an older mock or server does not return the new Proposal-list envelope.
- Operations: normalized an absent or non-array `proposals` payload to an empty list instead of allowing it to interrupt the digest/content load.
- Result: the focused ReportPage Playwright regression passes (`1 passed`). Full verification and push remain pending for this correction.
- Leftovers: none.

## Report plan conformance completion-audit documentation correction

- Goal: align the actual reporting plans with the verified Narrative boundary and avoid documenting a prose-keyword lint that the approved structure-first design intentionally excludes.
- Operations: clarified that Digest is a deterministic UI/Discussion projection rather than a duplicate Narrative model input; replaced the planned free-prose lint with the implemented Claim/Facts/Evidence/Attempt/scientific-assessment validator contract. Applied the same correction to the workspace plan source.
- Result: documentation now matches the frozen report implementation and does not prescribe keyword or numeric-pattern scanning.
- Leftovers: full verification and push remain pending for this documentation correction.

## Report plan conformance final corrections

- Goal: close the final review findings without expanding the report runtime or introducing prose keyword rules.
- Operations: added one SHA-bound Facts reader reused by Proposal budget validation and the first Discussion model context; rebuilt budget fixtures from a frozen EvaluationContract and CognitiveCostSummary instead of editing report Facts; refreshed the selected ReportPage state after ReviewDecision and cleared proposal state for unreadable versions; added browser coverage for Review, human Proposal creation/confirmation/rejection, and version isolation; synced R3, R8, and the remaining outline transcript path.
- Result: focused reporting tests pass (`22 passed`), the frontend production build passes, and the ReportPage browser suite passes (`2 passed`). Full verification and push remain pending for this batch.
- Leftovers: execution-producing Proposal forms intentionally remain in the existing typed server-side control plane.

## Report Agents and Experiment Observatory integration

- Goal: integrate the verified Report Agents implementation with the current
  Experiment Observatory, Idea Tree, and Activity Feed mainline without
  restoring the removed experiment-settings UI contract.
- Operations: merged exact report commit `7043c3f` into exact main commit
  `bc49c73` in an isolated integration worktree; preserved both branches'
  2026-07-20 records; verified the three-way `api.ts` and `types.ts` result
  retains the current Observatory fields while adding the Report APIs and
  types; added a real FastAPI/Vite report smoke that generates a disposable
  content-ready report and bundle, persists Review and `REQUEST_HUMAN`
  handoff state across reloads, and proves those interactions do not alter the
  PipelineJob file. Extended CI to run frontend lint, production build, and
  the full-stack browser suite in addition to the existing gates.
- Result: the preliminary real full-stack suite passes both the existing
  repository-confirmation case and the new report persistence case. Final-SHA
  local gates and remote Actions confirmation remain pending.
- Leftovers: advance `main` only if its remote SHA remains `bc49c73` and the
  exact integration SHA passes every local and remote gate. The existing UI
  redesign branches remain outside this integration.
