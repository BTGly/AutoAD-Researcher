# 2026-07-14 Development Log

## Entry 1 - Reconcile control-plane crash windows

Goal:
- Repair recoverable tears between terminal experiment attempts, PipelineJob state, ExperimentSession, and materialization-request records before lease expiry or a new claim can corrupt the run.

Operations:
- Added a recovery-only completion fence for `published`/`no_op` AttemptResults. It requires matching Job/claim/result identity and canonical readiness hash, but deliberately permits an expired lease and never creates another AttemptResult or readiness revision.
- Added materialization-request reconciliation with one scheduled request per prepare Job, exact `AttemptResult.control_request_id` matching, deterministic terminal-ledger repair, missing-requeue repair, and fail-closed handling for duplicate or invalid request bindings.
- Split pre-recovery file/schema validation from post-recovery cross-store invariant validation, and reordered Worker startup so repairable tears are reconciled before the strict invariant gate and lease recovery.
- Added one-lock experiment-control GET snapshots and aligned standalone Worker per-run exception isolation with the embedded Worker.
- Added crash-injection and consistency tests for both request/Job write directions, expired-lease publication recovery without revision growth, duplicate scheduled requests, structural-versus-invariant validation, and unexpected single-run Worker failures.

Result:
- Focused control-plane, readiness, request/API, Worker, and store regression suite passed: `33 passed`.

Leftovers:
- Run the full verification gate, precisely stage all Commit 1 files except the existing user-owned `AGENTS.md` modification, push `p0.3-control-plane`, and confirm GitHub Actions before starting the readiness trust-invariant commit.

## Entry 2 - Enforce readiness trust invariants

Goal:
- Make execution readiness depend on implementation readiness, bind every configured Resolver decision including negative facts to hashed producer inputs, and prevent arbitrary Resolver callbacks from running under the per-run mutation lock.

Operations:
- Added `ResolverSnapshot.observed_inputs` and advanced the fact policy fingerprint to `readiness_fact_policy:v2`; every configured Resolver snapshot must declare unique SHA256-bound component inputs.
- Enforced evidence-backed `verified` facts, two distinct hashed references for `conflict`, and no fabricated value for `unavailable_due_to_dependency`; the materializer's no-Resolver fail-closed default remains evidence-free because it is derived from configuration and policy version.
- Made execution readiness fail with `implementation_not_ready` whenever implementation is blocked, even when an execution-only Resolver reports passing facts.
- Moved both materialization collections and `assert_readiness_current()` collection outside RunMutationLock. The final locked section now revalidates the immutable contract hash, every component input, and every Fact evidence hash before publication.
- Updated Resolver fixtures to use bounded local artifacts, added lock-state assertions, and added coverage for execution-only bypass attempts and invalid verified/conflict/dependency snapshots.

Result:
- Focused readiness, Worker, and experiment-control regression suite passed: `30 passed`.

Leftovers:
- Run the full verification gate, precisely stage Commit 2 without `AGENTS.md`, push, and confirm GitHub Actions before changing confirmation projection or UI behavior.

## Entry 3 - Bind confirmation display to authorization semantics

Goal:
- Ensure the user confirmation modal displays exactly the normalized semantic fields bound by the draft hash, while keeping transcript/evidence-derived suggestions advisory and making the durable Draft safe under crashes and concurrent confirmation reads.

Operations:
- Added public `ConfirmationSemanticProjection` and `build_confirmation_semantic_projection()`. The existing flat versioned draft/contract Hash payloads and the confirmation display now share this projection without exposing private Hash models or changing the existing hash domains.
- Made `save_contract_draft()` use the run UnitOfWork and atomic fsync/replace persistence, with the draft-updated Event appended only after releasing the control-plane lock. Confirmation requests now hash and compare the durable Draft under the same lock.
- Added Hash-bound confirmation fields to the Draft API, kept enriched sidebar fields separate as `advisory_enrichment`, and changed the modal to consume only authorization fields. Stale pending projections are not exposed as actionable confirmations.
- Updated modal, chat confirmation, and confirmed reply boundary text to state that approval saves the contract and creates an experiment-preparation task without modifying code, creating a worktree, running a baseline, or occupying a GPU.
- Added `draft_sha256` and `contract_sha256` to canonical resolved Events and resolved API state, including an explicit null contract hash for rejection.
- Added coverage for durable-draft/request agreement, atomic replace failure preserving the prior Draft, Hash-bound display versus advisory enrichment, and resolved Event hashes.

Result:
- Focused confirmation, intent-contract, experiment-control, and store regression suite passed: `72 passed`.
- Frontend `tsc -b && vite build` passed. The build retained the existing ineffective dynamic-import chunking warning for `src/lib/mock.ts`.

Leftovers:
- Run the full verification gate, precisely stage Commit 3 without `AGENTS.md`, push, confirm GitHub Actions, and perform the final branch/status audit.

## Entry 4 - Recover all terminal attempt tears

Goal:
- Close the remaining crash windows where an immutable terminal AttemptResult is durable but the corresponding PipelineJob transition has not been written.

Operations:
- Replaced publication-only startup recovery with `reconcile_incomplete_terminal_attempts()`, dispatching exact running Job/claim/result identities across `published`, `no_op`, `completed`, `failed`, `stale_input`, and `lease_lost`; incompatible Job/result combinations fail closed as authoritative corruption.
- Added backward-compatible `AttemptResult.outputs` persistence and made ordinary Job completion record outputs before updating the Job Store, so restart recovery preserves the exact output list without creating a second AttemptResult.
- Reused the same deterministic stale-input and lease-loss transition builders in live execution and restart recovery, preserving recovery counters, bounded retry decisions, original `finished_at`-based backoff, and terminal errors.
- Repaired experiment Session state from the recovered Job and left exact materialization-request terminal repair to the existing request reconciler that immediately follows terminal recovery during Worker startup.
- Added crash-injection coverage for ordinary completion, technical failure, stale input, lease loss, fourth-stale terminal failure, invalid result/job combinations, immutable-result reuse, output restoration, and failed Session/request coordination.
- Added Node 22 setup, npm lockfile caching, `npm ci`, and the Vite/TypeScript production build to the GitHub Actions verification job.

Result:
- Focused terminal-recovery, Worker, and readiness regression suite passed: `28 passed`.
- Full `bash scripts/verify.sh` gate passed: `2068 passed, 48 skipped`.
- Frontend clean install and production build passed with `npm ci && npm run build`; the existing ineffective dynamic-import warning for `src/lib/mock.ts` remains unchanged.

Leftovers:
- Precisely stage this commit without the existing user-owned `AGENTS.md` modification, push `p0.3-control-plane`, and confirm GitHub Actions.

## Entry 5 - Isolate internal reply control data

Goal:
- Prevent Reply Planner control envelopes, meta-instructions, internal state labels, and PipelineJob identifiers from reaching user-visible chat or WebSocket deltas.

Operations:
- Added a strict Pydantic `V2ReplyPlan` envelope and removed free-text passthrough plus loose key/value parsing. Reply Planner output is now fully buffered and is emitted only after complete schema and authorization validation.
- Kept raw `next_reply_instruction` outside Reply Planner context, converted parse/schema/provider failures to user-language fallbacks, and removed internal state, Job ID/type, and artifact-stage wording from fallback replies.
- Added regression coverage for the four observed meta-instruction leaks, incomplete structured output, post-validation single-delta emission, key-order independence, and sanitized repository/parse failure explanations.
- Added backward-compatible `--staged-only` support to `verify_and_push.sh` so explicitly staged feature files can be committed without staging the user-owned `AGENTS.md` working-tree change.

Result:
- Focused V2 intent/reply regression suite passed: `57 passed`.
- Full `bash scripts/verify.sh` gate passed: `2084 passed, 48 skipped`.

Leftovers:
- Run the full verification gate, explicitly stage only Entry 5 files, push `p0.3-control-plane`, confirm GitHub Actions, and only then begin task-aware contract readiness.

## Entry 6 - Enforce task-aware contract readiness

Goal:
- Replace the anomaly-detection-only planning gate with evidence-validated, task-aware requirements while preserving byte-stable v1 authorization hashes and confirmed-contract recovery.

Operations:
- Added task profile, source, evidence, research object, target platform, workload, and per-need evidence quotes to the Need/Contract path. Need Discovery now proposes a profile, while the validator verifies user quotes, conservatively downgrades unsupported profiles, restores the profile's required needs, and recomputes every blocking/readiness field.
- Added separate requirement sets for empirical model research, systems/operator optimization, code diagnosis, and general research. The safe `plan_only` fallback is marked as a default, optional method/module hints remain non-blocking, and incomplete generic/system tasks cannot become ready from model-supplied readiness flags or placeholder success text.
- Added authorization schema version 2 and a separate v2 semantic Hash domain binding the task profile fields and reserved tri-state evaluation constraints. Authorization v1 retains its original projection and domains; fixed draft/contract Hash fixtures guard byte stability.
- Updated the Need Discovery prompt contract and added regressions for the reported AI-operator prompt, completed systems evidence, unsupported profile downgrade, missing user evidence, PatchCore/MVTec continuity, v1 Hash compatibility, and v2 field binding.

Result:
- Focused Need Discovery, intent-contract, confirmation, V2, Draft, and readiness suites passed: `185 passed, 2 skipped`.
- Full `bash scripts/verify.sh` gate passed: `2091 passed, 48 skipped`.

Leftovers:
- Run the full verification gate, explicitly stage only Entry 6 files, push `p0.3-control-plane`, confirm GitHub Actions, and only then begin structured evaluation constraints and confirmation lifecycle work.

## Entry 7 - Preserve explicit constraints and confirmation lifecycle

Goal:
- Bind explicit evaluation/execution authorization into the v2 contract Hash and move suspend/resume/supersede decisions behind a deterministic, auditable confirmation state machine.

Operations:
- Activated the reserved tri-state evaluation constraint model for test-set, metric-definition, and dataset-split preservation. Missing statements remain `null/unknown`; exact user evidence records true or false, conflicting statements add a blocking clarification need, and the readable evaluation protocol is generated from the structured values.
- Made explicit current-turn execution authorization override defaults and older drafts, added the reported “代码修改需要逐步确认” phrasing, retained persisted authorization when later transcript history is contradictory, and made same-turn conflicting modes block confirmation.
- Extended Turn Gate with a proposal-only `confirmation_action_proposal`. The confirmation service independently validates exact current-turn evidence, confirmation ID, Draft Hash, current persisted status, legal transition, confirmed immutability, and idempotency before applying `suspend`, `resume`, or `supersede`.
- Added `needs_clarification` and `superseded` projection states, lifecycle revisions, audit events for suspended/resumed/superseded transitions, automatic supersession when a pending Draft Hash changes, and active-confirmation Draft API state without exposing suspended confirmations as pending.
- Standardized confirmation conflicts as HTTP 409 bodies with `confirmation_stale`, `confirmation_state_conflict`, or `confirmed_contract_immutable`. Confirmed contracts reject in-run topic replacement and direct the user to the top-level new-task entry while preserving the old contract and ExperimentSession.
- Added regressions for tri-state/null/false/conflict semantics, v2 Hash binding, current authorization priority, suspend/resume identity preservation, invalid evidence, supersede auditing, stale/state API errors, confirmed immutability, and the original reported PatchCore constraint wording.

Result:
- Focused V2, confirmation, Draft, readiness, prompt-integrity, and control-plane suites passed: `206 passed, 2 skipped`.
- Full `bash scripts/verify.sh` gate passed: `2103 passed, 48 skipped`.

Leftovers:
- Run the full verification gate, explicitly stage only Entry 7 files, push `p0.3-control-plane`, confirm GitHub Actions, and only then begin the frontend task/Draft/readiness interaction commit.

## Entry 8 - Align task, Draft, and readiness interactions

Goal:
- Make current-task renaming directly usable and durable in the Web UI, align Draft/readiness language with actual control-plane behavior, and put frontend interaction tests into the required verification gates.

Operations:
- Added a persistent pencil button beside the current title and an inline editor with Enter/blur save, Escape cancel, blank/unchanged suppression, and an in-flight fence that prevents duplicate Enter-plus-blur PATCH requests. Successful responses immediately update the displayed title and task list without changing the run identity.
- Added structured `ApiError` parsing that retains HTTP status and backend `detail`; rename failures keep the editor and user input open while showing the backend message.
- Removed the fixed PatchCore next-step suggestion. Draft questions now prefer the validated NeedSpec clarification and dynamically refer to the known baseline or research object.
- Changed the Draft API to return every canonical PipelineJob, including completed jobs, and added sidebar counts for pending, failed, and completed work.
- Added suspended-confirmation UI semantics (`需要澄清`, no modal, old Draft retained), user-language preparation-check actions, and an explicit notice that a check only rereads the contract and registered facts without changing code or running experiments. The manual check remains visible independently of freshness.
- Added Vitest, Testing Library, and jsdom tests for title visibility, Enter/blur/Escape behavior, duplicate-submit prevention, immediate and remounted title display, backend error detail, and API error metadata. Extended Python coverage to prove refresh persistence, stable `run_id`/directory, dynamic questions, and all-job Draft responses.
- Added `npm run test` to local verification and GitHub Actions before the existing TypeScript/Vite production build.

Result:
- Frontend tests passed: `7 passed`; TypeScript/Vite production build passed with the existing ineffective dynamic-import warning for `src/lib/mock.ts`.
- Focused run-route and V2 intent/Draft regression suite passed: `69 passed`.
- Full `bash scripts/verify.sh` gate passed: `2105 passed, 48 skipped`; the gate also reran all 7 frontend tests.

Leftovers:
- Run the full verification gate, explicitly stage only Entry 8 files, push `p0.3-control-plane`, confirm GitHub Actions, then run the two original-dialogue regressions and start the services for manual testing.

## Entry 9 - Prevent deleted Runs from being resurrected

Goal:
- Eliminate the deletion/in-flight-write race that recreated a removed Run as a profile-less `run_xxx` ghost task.

Operations:
- Added durable lifecycle records and cross-process operation locks under `runs/.control`, with non-reusable generations and `active`, `deleting`, and `deleted` states stored outside the deletable Run directory.
- Made Run creation persist lifecycle state, made deletion logical and idempotent before asynchronous physical cleanup, recovered interrupted deletions at server startup, and excluded deleting/deleted Runs plus the `.control` directory from task and Worker scans.
- Wrapped chat, upload, source deletion, task rename/archive/restore, contract confirmation, experiment control/config writes, and all Worker processing in shared operation leases. Chat and upload no longer create missing Run directories; deleted Runs fail with HTTP 410.
- Migrated pre-lifecycle directories lazily and replaced raw run-id fallback titles with a safe legacy title/profile so profile damage cannot expose another `run_xxx` task label.
- Added frontend abort controllers for chat/upload, optimistic task hiding, explicit running-task deletion language, asynchronous deletion response support, and suppression of late WebSocket events for deleted Runs.
- Added lifecycle concurrency/restart/tombstone tests, updated source-ingress fixtures to create Runs explicitly, and extended frontend API tests for AbortSignal and HTTP 202 deletion behavior.

Result:
- Focused lifecycle, run-route, task-profile, chat, upload, and source-ingress regressions passed.
- Full Python suite passed: `2108 passed, 48 skipped`.
- Frontend tests passed: `9 passed`; the initial production build found and then fixed a test-only TypeScript tuple typing error.

Leftovers:
- Run the mandatory full verification gate after the TypeScript fix, explicitly stage Entry 9 without `AGENTS.md`, push, confirm GitHub Actions, then delete the confirmed ghost Run through the new lifecycle path.

## Entry 10 - Eliminate avoidable Turn Gate repairs

Goal:
- Remove the systematic second Turn Gate provider call while preserving soft semantic routing and fail-closed contract authorization.

Operations:
- Added the exact `TurnGateDecision.model_json_schema()` and two valid examples to every Turn Gate request while retaining the reviewed prompt profile and its natural-language intent boundaries.
- Added proposal-only task profile, exact profile evidence, and Need-enrichment fields. Unsupported profile evidence is downgraded to `general_research` and marked for enrichment rather than trusted as authorization.
- Limited local recovery to stripping unknown fields and converting an exact evidence string into a one-item list. Illegal enums, missing required semantic fields, malformed JSON, and provider errors now fall back to no contract update after one call.
- Removed the provider-side Repair call path and its second 30-second timeout. Updated the former crash-window regression to assert that invalid routing output cannot create or confirm a contract.
- Added a fixed replay fixture covering ordinary chat, scalar evidence recovery, extra-field recovery, and illegal semantic enums; updated the reviewed Turn Gate prompt hash.

Result:
- Focused Turn Gate, intent-contract, source planner, typed-event, and prompt-registry suites passed: `108 passed`.
- Full Python suite passed: `2110 passed, 48 skipped`.

Leftovers:
- Run the mandatory full verification gate, explicitly stage Entry 10 without `AGENTS.md`, push, and confirm GitHub Actions before changing Need Discovery.

## Entry 11 - Make Need Discovery deterministic first

Goal:
- Remove avoidable Need Discovery model calls for already explicit research facts while retaining LLM enrichment for genuine semantic ambiguity.

Operations:
- Reordered Need Discovery to build and validate the deterministic requirement specification first. The Turn Gate's schema-validated enrichment proposal is now the only normal trigger for an LLM enrichment call; clear PatchCore, MVTec AD, AUROC, and numeric-target evidence stays local.
- Passed the Turn Gate task-profile proposal and exact user evidence into the Validator. Unsupported evidence remains conservative, while supported proposals select the profile before required needs and readiness are recomputed.
- Bounded the temporary enrichment call to eight seconds and made every provider, parse, and schema failure return the already validated deterministic specification immediately.
- Added an atomic success-only enrichment cache under the Run assistant state. Its canonical key covers user input, transcript, contract draft, source registry, evidence, jobs, stage goal, answerability, artifact summary, and the validated routing proposal; invalid or failed responses are never cached.
- Added regressions proving zero Need Discovery calls for an explicit PatchCore/MVTec/AUROC contract, one call across repeated identical ambiguous requests, the eight-second budget, and preservation of existing enriched non-hardcoded task behavior.

Result:
- Focused Need Discovery, intent-contract, typed-event, and source-planner suites passed: `106 passed`.
- Full Python suite passed: `2112 passed, 48 skipped`.

Leftovers:
- Run the mandatory full verification gate, explicitly stage Entry 11 without `AGENTS.md`, push, and confirm GitHub Actions before introducing the shared LLM runtime.

## Entry 12 - Add the shared LLM call runtime

Goal:
- Bound cross-Run model concurrency and total chat latency while reusing provider connections and recording enough redacted timing data to distinguish queue, provider, generation, and fallback delays.

Operations:
- Added a synchronous shared `LLMCallBroker` with one thread-safe `httpx.Client` pool per normalized provider, configurable in-flight and reserved-interactive semaphores, and a 30-second conversation deadline propagated across `asyncio.to_thread()` calls.
- Assigned eight-second routing budgets, a six-second Need enrichment budget, and the remaining turn budget to the interactive Reply call. The still-temporary task-naming call also uses the broker as low-priority work so it cannot bypass the deadline before its removal in the next commit.
- Limited retries to one connection failure or HTTP 429/502/503/504 response, honored bounded `Retry-After`, kept timeout and response/schema failures non-retryable, and added a three-failure/60-second provider circuit breaker with one half-open probe.
- Added JSON response-format requests for structured calls with a budget-bound compatibility retry only when the provider explicitly identifies `response_format` as unsupported.
- Expanded redacted traces with sanitized provider request ID, HTTP status, error type, queue wait, response-header latency, first streaming token, total latency, retry metadata, circuit state, provider fallback, and compatibility reason. Provider bodies, headers, credentials, and unprocessed errors remain excluded.
- Enforced one active chat turn per Run while allowing different Runs to proceed concurrently. Cancellation now waits for the non-cancellable worker thread before releasing the Run lease and same-Run guard, preventing an aborted HTTP request from reopening a late-write race.
- Closed shared provider clients during server shutdown and retained the existing chat-client API as a broker-backed compatibility facade.
- Added deterministic tests for connection reuse (including equivalent `/v1` bases), provider concurrency, reserved interactive capacity, deadline-bound queueing, retry eligibility, circuit open/half-open recovery, response-format compatibility, runtime trace propagation, same-Run rejection, and cancellation-safe guard retention.

Result:
- Focused runtime, chat-client, trace, chat-route, task-profile, routing, Need, source-planner, and intent-contract suites passed: `184 passed`.
- Full Python suite passed: `2122 passed, 48 skipped`.

Leftovers:
- Run the mandatory full verification gate, explicitly stage Entry 12 without `AGENTS.md`, push, and confirm GitHub Actions before replacing the synchronous naming call with Router/deterministic task naming and progress events.

## Entry 13 - Make task naming non-blocking and observable

Goal:
- Remove the standalone naming model call, update task identity from the already validated conversation route, and give users immediate progress without exposing internal pipeline terms.

Operations:
- Extended the Turn Gate compatibility schema with optional task-title and summary suggestions. The generated JSON Schema and examples now require ordinary chat to leave naming empty and allow recognizable research topics to return a concise user-facing suggestion for the future unified Router.
- Removed the standalone Task Naming prompt, provider request, 15-second timeout, route helper, and eligibility flag. Automatic naming now adds zero model calls.
- Added validated `router_suggested` and `deterministic_projection` task-profile sources while retaining historical source values. Router titles are rejected if generic, secret-like, path-like, too long, or containing the exact Run ID; invalid suggestions fall back to a projection made only from the validated task profile and contract fields.
- Enforced title priority atomically: manual and historical titles are immutable to automation, Router suggestions can improve deterministic projections, and deterministic projections can only replace the original UI placeholder.
- Applied automatic naming after contract validation but before reply generation, preserving `run_id`, directory, contract, and session identity. A successful update immediately schedules a `task.updated` WebSocket message with the persisted title, summary, source, and timestamp.
- Added `assistant.progress` messages. The first server-authored “正在理解你的任务……” event is broadcast before config/transcript loading; later route-aware messages use only user-facing wording. Progress is displayed in the pending assistant bubble and is never appended to the formal transcript.
- Added frontend WebSocket reducers and tests for progress display and in-place task-list updates, plus backend regressions for the 300 ms initial event, transcript exclusion, route naming, ordinary-chat exclusion, deterministic fallback, source priority, manual-title protection, and stable Run identity.

Result:
- Focused task-profile, chat-route, Turn Gate, intent-contract, and typed-event suites passed: `139 passed`.
- Full Python suite passed: `2123 passed, 48 skipped`.
- Frontend tests passed: `11 passed`; the TypeScript/Vite production build passed with the existing ineffective dynamic-import warning for `src/lib/mock.ts`.

Leftovers:
- Run the mandatory full verification gate, explicitly stage Entry 13 without `AGENTS.md`, push, and confirm GitHub Actions before unifying Source Action and Turn Gate into the Conversation Router.

## Entry 14 - Unify conversation routing and add end-to-end regressions

Goal:
- Replace the ordinary natural-language Source Planner plus Turn Gate sequence with one strict Conversation Router while preserving deterministic source ingress, contract authorization boundaries, compatibility entry points, and observable performance limits.

Operations:
- Inherited the partially implemented `ConversationRouteDecision`, Orchestrator integration, explicit-source helpers, and in-progress test migration after Entries 9-13 were pushed and passed GitHub Actions.
- Confirmed that the working tree contains only the in-progress Router files plus the user-owned `AGENTS.md` change, that the staging area is empty, and that the current local and remote branch SHA is `319434718fd1dd110152739bb509108ee6c829c7`.
- Confirmed that `run_20260714_1052_adc4` still exists without a lifecycle record and that no old backend, frontend, or Worker process is running; cleanup remains deferred until the unified Router commit passes CI and the current lifecycle-enabled service is started.
- Reproduced the inherited focused suite as `9 failed, 76 passed`. Eight failures were migrated Router stubs whose mutation evidence did not quote the actual current turn, and one typed-event regression still expected separate Source Planner and Turn Gate events.
- Removed the proposed global `ordinary conversation model calls <= 2` acceptance limit at the user's direction. Entry 14 will enforce only the architectural invariant that one natural-language turn cannot invoke both legacy semantic planners; total model calls remain observable benchmark data rather than a product or CI cap.
- Searched the current configuration and found no existing user-visible LLM call-count or cost-limit field. The existing shared 30-second conversation deadline and per-request `max_tokens` remain service-runtime controls; this commit will not invent a new configuration key.
- Registered `assistant.v2.conversation_route.v1` and routed its prompt through `PromptSelector`, composing the reviewed Source Action and Turn Gate rules without removing either compatibility profile. Traces now reference an existing reviewed profile and retain redacted provider/runtime metadata.
- Completed the strict Router envelope and Orchestrator integration. Pure URLs and attachments bypass semantic model calls, URL-plus-natural-language turns register the exact source plan before one Router decision, deterministic plans cannot be replaced by model output, and mutating decisions without exact current-turn evidence fail closed.
- Added Router replay, schema recovery, trace redaction, task-profile consistency, structural call-deduplication, source-boundary, URL-shape, typed-event, prompt registry, and prompt selector regressions. The broad focused suite passed: `253 passed, 2 skipped`.
- Added a 20-case observational chat benchmark and an in-process FastAPI benchmark runner. The benchmark records call sites/counts, queue delay, fallbacks, route schema status, total latency, and first server progress without storing raw prompts, credentials, or provider errors; no total call-count threshold is enforced.
- Ran the configured default-model live corpus with concurrency 2. All 20 HTTP turns succeeded; 19/20 routes succeeded on the first deterministic/schema path, including 16/17 model-Router first-schema successes; legacy Source Planner/Turn Gate calls were zero; 31 total model calls were observed; first progress was at most about 18 ms; the slowest turn was about 22.25 seconds. The one Router miss was the `code_diagnosis` case after provider queueing and it failed closed without contract mutation.

Result:
- Broad focused regression passed: `253 passed, 2 skipped`.
- Live chat benchmark met the planned 95% route-success target with no legacy semantic-planner calls; total calls remained observational only as requested by the user.
- Full `bash scripts/verify.sh` passed: `2135 passed, 48 skipped`; frontend tests passed: `11 passed`.

Leftovers:
- Explicitly stage Entry 14 without `AGENTS.md`, rerun the staged-only verification/push gate, confirm local/remote SHA and GitHub Actions, then perform service-level ghost-Run deletion and original-dialogue acceptance.

## Entry 15 - Preserve exact Router profile evidence for task naming

Goal:
- Make the original AI infra/operator-optimization request reliably enter the validated contract/naming path without adding keyword routing or weakening exact-evidence authorization.

Operations:
- Completed Entry 14 push and CI at `3f008711a5eda775cdb7267ecbb6e17a66368cdd` before starting this follow-up.
- Started the lifecycle-enabled backend with the embedded Worker disabled and the Vite frontend. Verified the Vite 502 seen by the first curl was inherited proxy routing; direct localhost access returned HTTP 200.
- Migrated and deleted `run_20260714_1052_adc4` through `DELETE /api/runs/{run_id}`. The endpoint returned 202, the directory was removed, the tombstone became `deleted`, new writes returned 410, and a backend restart left it absent and hidden.
- Ran live HTTP acceptance on new disposable Runs. PatchCore contract drafting, casual chat, suspension/resumption, numeric 5% update, Router-suggested PatchCore title, manual-title priority, internal-field non-leakage, and concurrent deletion all passed. The original AI infra request still retained the UI placeholder, exposing a remaining acceptance defect.
- Reproduced the AI infra request without deleting its diagnostic Run. The redacted typed event showed a conservative `missing_exact_current_turn_evidence` fallback; no contract mutation or naming was authorized. A separate one-shot structured Router diagnostic showed that the provider can supply `systems_optimization` plus a top-level `task_profile_evidence` copied verbatim from the user input.
- The first exact-profile-evidence recovery passed deterministic tests but a fresh service-level repetition still retained the placeholder, which proves the provider can paraphrase both evidence locations. Kept the fail-closed validator and added a schema-adjacent prompt instruction requiring the complete current user message to be copied verbatim for every mutating action, without normalizing spaces, case, or punctuation.
- Three repetitions after the evidence-copy instruction still retained the placeholder. A preserved fourth diagnostic Run showed no schema or evidence fallback: the Router classified the correction as ordinary chat with confidence 0.9. The merged frustration rule was over-dominant for “不对啊” and failed to distinguish a state complaint from a correction that immediately supplies a replacement research direction. Added a soft pragmatic distinction plus a full valid correction-route example; no keyword classifier or deterministic intent mapping was introduced.
- The pragmatic correction example then named the AI infra task in 3/3 service-level repetitions. A full 20-case rerun fluctuated to 17/20 first-route successes, while a concurrency-1 diagnostic recovered two of the three misses and isolated one Router non-JSON response. Compressed the new correction example to required schema fields and naming evidence to avoid unnecessary prompt-token growth; runtime budgets, retries, and acceptance logic remain unchanged.

Result:
- Focused Router, intent-contract, naming, chat-route, Prompt Registry, and Prompt Selector regressions passed: `159 passed`.
- The final compressed correction prompt named the original AI infra request in `3/3` fresh service-level repetitions as `AI Infra 与算子优化研究` with source `router_suggested`.
- Live 20-case runs remained provider-variable: the pre-fix Entry 14 run reached 19/20; follow-up concurrency-2 runs reached 17/20 and 18/20; the concurrency-1 run reached 17/20. HTTP success stayed 20/20, legacy semantic-planner calls stayed zero, first progress stayed below 7 ms in the follow-up runs, and total turns stayed below 25 seconds. The varying failed case IDs show why this metric remains observational rather than a CI gate.
- Full `bash scripts/verify.sh` passed: `2136 passed, 48 skipped`; frontend tests passed: `11 passed`.

Leftovers:
- Explicitly stage Entry 15 without `AGENTS.md`, rerun the staged-only gate, push and confirm CI, then restart the delivered services and confirm the ghost tombstone plus clean task list one final time.

## Entry 16 - Separate naming evidence from mutation authorization

Goal:
- Prevent task-classification or naming evidence from authorizing contract and confirmation mutations, while retaining LLM-first semantic routing and fail-closed state changes.

Operations:
- Added the dedicated `mutation_evidence_from_current_turn` field to the strict Turn Gate schema. `update_contract`, `confirm_contract`, `suspend`, `resume`, and `supersede` now require that field, after outer whitespace removal, to equal the complete current user message.
- Kept `task_profile_evidence` available only for task classification and automatic naming. Removed the Router recovery that appended a matching profile substring to general current-turn evidence, and stopped the Orchestrator from deriving confirmation-action authorization from arbitrary current-message substrings.
- Kept general `evidence_from_current_turn` and `evidence_from_context` as audit/classification evidence rather than mutation authority. Non-mutating routes explicitly discard any supplied mutation evidence.
- Updated the Router and compatibility Turn Gate prompts, JSON-schema examples, replay fixtures, reviewed prompt hashes, and all mutating test doubles to use the independent field. No domain keyword or deterministic semantic-intent classifier was added.
- Added negative Router, Turn Gate, and Orchestrator regressions proving that `PatchCore` profile evidence cannot authorize a contract update and that `你是 PatchCore 战神哈哈哈` cannot create a contract or update the task title when a model misroutes it without complete mutation evidence.

Result:
- Focused Router, Turn Gate, intent-contract, typed-event, prompt-registry, source-planner, and benchmark regressions passed: `125 passed`.
- Full `bash scripts/verify.sh` passed: `2140 passed, 48 skipped`; frontend tests passed: `11 passed`.

Leftovers:
- Stage Entry 16 without the user-owned `AGENTS.md`, run the staged-only push gate, and confirm GitHub Actions before starting modal-only approval.

## Entry 17 - Require modal approval for contract confirmation

Goal:
- Make the pending confirmation modal and its hash-bound API the only path that can create the immutable confirmed contract, ExperimentSession, and experiment-prepare Job.

Operations:
- Removed the Orchestrator's chat-side call that resolved a pending confirmation as approved. A Router `confirm_contract` decision now persists or resumes the ready draft's pending confirmation and returns `intent_contract_confirmation` with `intent_contract_confirmed=false`.
- Kept chat confirmation semantic and model-driven. The implementation does not match confirmation phrases or domain keywords; tests supply schema-valid Router decisions for `确认`, `可以`, `按这个来`, and `开始吧` and assert that none can approve state.
- Removed the pending-confirmation approval convenience function. The confirmation Saga and compatibility resolver now require the caller's `draft_sha256`, compare it with the persisted confirmation projection, and re-hash the durable draft before either approval or rejection.
- Extended the POST confirmation request schema with a required 64-character lowercase SHA-256. The frontend reads the exact `draft_hash` displayed by the current modal and submits it with `confirmation_id` and the explicit approved/rejected decision.
- Added backend regressions for pending-only chat, absent confirmed contract/session/prepare Job before modal approval, wrong-hash rejection, and successful modal approval. Added frontend request-body and visible pending-modal regressions.

Result:
- Focused confirmation, intent-contract, experiment-control, Router, and Turn Gate regressions passed: `113 passed`.
- Frontend tests passed: `13 passed`.
- Full `bash scripts/verify.sh` passed: `2144 passed, 48 skipped`; frontend tests passed again: `13 passed`.

Leftovers:
- Stage only Entry 17 while preserving concurrent user-owned workspace and index changes, run the staged-only push gate, and confirm GitHub Actions before generalizing corrected-direction routing.

## Entry 18 - Generalize corrected research direction routing

Goal:
- Remove the acceptance-sentence overfit from the production Router prompt while preserving semantic correction handling and deriving task titles from validated structured fields whenever possible.

Operations:
- Removed the complete AI infra acceptance sentence and fixed `AI Infra 与算子优化研究` title from production code. The original sentence remains only in the Router replay regression fixture.
- Replaced the schema-adjacent correction example with a different-domain Rust service diagnosis example. It demonstrates the general pragmatic shape: reject an earlier framing, supply a concrete replacement direction, carry complete mutation evidence, and leave title generation out of the example.
- Kept the production correction rule domain-neutral and LLM-first. Added a second replay case for `不是异常检测，我要做推理引擎、CUDA kernel 和算子融合` without introducing a keyword allowlist or deterministic intent classifier.
- Reordered automatic naming so validated contract fields project the title before a Router free-text suggestion is considered. Systems and diagnosis titles now require the structured `research_object`; Router suggestions remain a validated fallback when structured fields cannot yet produce a title.
- Reversed automatic title upgrade priority so later structured contract information can replace an earlier Router fallback, and deterministic projections can improve as contract fields become more specific without overwriting manual titles.
- Removed the legacy compatibility Turn Gate's offline confirmation phrase table. Without a semantic model decision it now fails closed; confirmation phrasing is handled only by the unified Router. Updated the Turn Gate prompt to state that chat requests a pending modal and only the hash-bound modal API approves.
- Updated Router, naming, chat-route, prompt-content, replay, and reviewed prompt-hash regressions.

Result:
- Focused Router, Turn Gate, task-profile, chat-route, prompt-registry, selector, intent-contract, and Need Discovery regressions passed: `199 passed`.
- Full `bash scripts/verify.sh` passed: `2146 passed, 48 skipped`; frontend tests passed: `13 passed`.

Leftovers:
- Stage only Entry 18 while preserving user-owned workspace/index changes, run the staged-only push gate, and confirm GitHub Actions before adding benchmark semantic oracles.

## Entry 19 - Add semantic oracles to the chat benchmark

Goal:
- Separate Router first-schema/topology reliability from semantic intent alignment and make valid-but-wrong routes visible without turning live provider variance into a CI gate.

Operations:
- Upgraded the 20-case corpus to schema version 2. Every case now declares the exact reviewed fields `expected_turn_type`, `expected_contract_action`, `expected_confirmation_action`, `expected_task_profile`, `expected_source_action_types`, and `expected_contract_mutation` in addition to whether a model Router call is expected.
- Evaluated semantics from the durable `planner.conversation_route.decided` typed event rather than reply text. Contract mutation is observed through `contract.draft.updated`; raw prompts, user text, credentials, and provider errors remain absent from the report.
- Renamed the former success fields to `router_first_schema_topology_success` and `route_first_schema_topology_success` with separate counts/rates. These names now state exactly that they measure call topology plus first Router schema validation, not intent correctness.
- Added independent `semantic_oracle_success`, field-level mismatch records, and aggregate semantic case/success counts and rate. The live benchmark remains observational and applies no semantic, call-count, or provider-performance CI threshold.
- Added a negative evaluator regression where a schema-valid Router JSON maps ordinary chat to `update_contract`: schema/topology succeeds while semantic oracle evaluation fails on turn type, contract action, task profile, and mutation.
- Updated dry-run metadata and in-process live benchmark tests for the v2 corpus and both metric families.

Result:
- Focused benchmark, Router, and typed-event regressions passed: `22 passed`.
- Benchmark dry-run validated schema v2 with `20` semantic-oracle cases and `17` model-Router cases.
- Full `bash scripts/verify.sh` passed: `2147 passed, 48 skipped`; frontend tests passed: `13 passed`.

Leftovers:
- Stage only Entry 19 while preserving user-owned workspace/index changes, run the staged-only push gate, and confirm GitHub Actions before atomic Run publication.
