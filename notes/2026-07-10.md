# 2026-07-10 Development Log

## Entry 1 - Simplify queued-message withdrawal

Goal:
- Reduce the follow-up queue to one safe action: withdraw an unsent item back into the composer for editing.

Reference alignment:
- Rechecked Claude Code's `messageQueueManager.popAllEditable()` and PromptInput integration. Its queued-input edit path removes editable queued input and combines it with the current prompt; historical conversation/code rewind is a separate subsystem.
- AutoAD adopts only the queued-input behavior. It does not imitate historical fork, transcript rewind, or code rewind without matching backend checkpoints.

Changes:
- Replaced separate pencil and trash actions with one `Undo2` icon labelled `撤回到输入框`.
- Removed the queue collapse control and the normal-state resume button, leaving a compact count, state summary, message preview, and one action per row.
- Withdrawing removes only the selected unsent item and restores its exact content to the controlled composer.
- If the composer already contains a draft, the withdrawn content is prepended with a newline instead of overwriting the draft, matching Claude Code's lossless queue-to-input principle.
- A manual resend after a queue failure clears the paused flag and resumes FIFO drain; no persistent extra resume control is needed.

Safety and boundaries:
- Sent transcript messages, the active request, contract state, sources, jobs, typed events, and run artifacts are untouched.
- No historical rewind/fork, cancellation protocol, `ChatResponse`, WS event shape, prompt, permission, memory, tool registry, lifecycle, hook, or multi-agent change was introduced.

Verification before full gate:
- `npm run build` in `frontend/` -> passed, 1797 modules transformed.
- `npm run lint` in `frontend/` -> passed with the pre-existing `useAutoScroll.ts` exhaustive-deps warning.
- `git diff --check` -> passed.

Result:
- The common queue UI now exposes one unambiguous, state-safe return action and preserves any in-progress composer draft.

Leftovers:
- Run full verification, commit/push, confirm GitHub Actions, and keep port 8000 available for manual refresh testing.
