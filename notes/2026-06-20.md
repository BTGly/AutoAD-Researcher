# 2026-06-20 开发日志

## 目标

实现 Stage 3.8 runner_execute 真实执行链，填补 3.7→3.9 之间的管线缺口。

## 背景

上一轮（6/19）修复了 3.6 empty hook_bindings 阻塞点，打通了 3.1–3.7 的结构闭环（commit `cf4c339`）。但 3.8 在 orchestrator 中仅返回 `blocked` 占位符。所有 3.8 基础设施（schemas v2.12、validators、executor、handoff_bridge）已提前实现，缺的是 `pipeline/runner_execute_stage.py` 和 orchestrator wiring。

## 操作

### 1. 3.7 patch artifact 审计

执行审计命令验证 `run_l3_bottle_001` 的 patch artifacts：

| 项目 | 结果 |
|---|---|
| `patch_plan_validation_status` | `"passed"` |
| `patch_payload_validation_status` | `"passed"` |
| `changes` | 1（`src/patchcore/sampler.py`） |
| `payloads` | 1 |
| `proposed_patch.diff` | **0 bytes — no-op** |
| `before_sha256 == after_sha256` | 相同，内容未变 |
| `patch_execution_result.overall_status` | `"patch_applied_but_local_validation_failed"` |
| `patch_runner_handoff` | 存在，`patch_diff_sha256 == null` |
| workspace repo (`patchcore-inspection`) | ✅ clean, HEAD = `fcaa92f12` |

结论：3.7 管线闭环已验证（artifact chain、validation、approval、handoff），但 PatchMaterializer 生成 no-op patch。3.8 需要在 plumbing 模式下验证执行链。

### 2. 创建 `pipeline/runner_execute_stage.py`

新增 247 行 stage runner，实现完整的 3.8 执行链：

- ✅ Resume check（`experiment_execution_handoff.json` 存在时跳过）
- ✅ 加载 3.7 `PatchRunnerHandoff` + 3.5 experiment artifacts
- ✅ **No-op patch gate**：`l3-preflight` 模式下 no-op patch 直接 blocked；`l1-l2` 模式允许（plumbing）
- ✅ `build_runner_intake_request()` 构建 `RunnerIntakeRequest`
- ✅ `validate_intake_against_patch_handoff()` 验证 handoff 一致性
- ✅ `_build_execution_units()` 从 experiment matrix 构造 `ExecutionUnitPlan` 列表
- ✅ 为每个 unit 构建 `ExperimentCommandPlan` + `ExperimentInputRefs`
- ✅ `execute_experiment_attempt()` 执行每个 attempt
- ✅ 收集 `AttemptRecord` → `ExecutionUnitRecord`
- ✅ 构建 `ExecutionManifest`（含 `MatrixCoverageReport`）
- ✅ 构建 `ExperimentExecutionHandoff` → 3.9

### 3. 更新 orchestrator.py

替换 `_stage_runner_execute()` 占位符为 `run_runner_execute_stage()` 调用。

## 产出文件

- `src/autoad_researcher/pipeline/runner_execute_stage.py` — 新文件，247 行
- `src/autoad_researcher/pipeline/orchestrator.py` — 修改 `_stage_runner_execute`（349-356 行）

### 后续修复

执行 `stage3-acceptance --mode l1-l2` 后发现：
- 3 个 execution unit 全部 `execution_failed`（预期内——命令参数不全 + dataset 不可用）
- 修复 FileExistsError 冲突（rmtree 清理 + 不预创建 attempt_dir）
- 修复 RetryDecision prev_identity 类型错误
- 修复 _collect_command_plans 返回 dict 而非 model 的调用方错误
- 修复 blocked_reason 缺失导致的 schema validation error

### 第二轮改进

1. **完整 PatchCore 命令构造** — `_make_command_plan()` 从 benchmark config 读取所有 fixed_parameters 拼接真实 CLI args（背骨、层、dataset、sampler、超参数等）
2. **input refs SHA 填充** — `environment_sha256` 从 `requirements.lock.txt` 计算；`dataset_manifest_sha256` 从实际 MVTec path 递归计算
3. **CLI 子命令** — 新增 `uv run autoad runner-execute --run-id <ID>`（独立运行 3.8）

### 第三轮修复（审阅后）

根据代码审阅指出的四个运行阻断问题：

1. **缺 `import os`** → 顶部添加
2. **脚本路径双重重叠** → `args[0]` 改为相对 `entrypoint`（`bin/run_patchcore.py`），`cwd` 保持不变
3. **expected_outputs 与 outputs 目录不对齐** → 命令构建移到 attempt 循环内，`results_root` 设为 `attempt_dir` 绝对路径（`{attempt_dir}/outputs`），PatchCore 产出可被 executor 在 attempt_dir 下找到
4. **dataset manifest SHA 非确定性** → 改用 `sorted(Path(root).rglob("*"))` Python 内部排序

同时增加 `_cuda_available()` 检测，仅当有 GPU 时才传 `--gpu` 参数。

审计确认：修复后 `runner-execute` 完整跑通，intake eligible，3 units 因无 dataset/GPU 而 `execution_failed`（预期行为）。

### 4. Stage 3.9 results_analysis 实现

新增 `pipeline/results_analysis_stage.py` — 消费 3.8 `ExperimentExecutionHandoff` + `ExecutionManifest`，产出：

- ✅ **Paired metric observations** — 通过 `ExecutionUnitRecord.stage == "baseline"` 识别 baseline，按 `seed` 配对 variant；从 `output_manifest → results.csv` 读取 `instance_auroc` 指标
- ✅ **AggregatedMetricComparisons** — 每 variant 聚合 baseline→variant delta、improvement delta、relative change %
- ✅ **Evidence sufficiency** — 统计计划 seed vs 完成 vs 有效的 pair 数
- ✅ **Scientific conclusion** — no-op patch → `practically_equivalent`；有 Δ → `beneficial`/`worse`；无数据 → `incomplete`
- ✅ **Resource comparison** — 标记为 `not_available`（telemetry 尚未实现）
- ✅ **ReportFacts** — 统计 summary
- ✅ **final_report.md** — 人类可读的报告
- ✅ **CLI 子命令** — `uv run autoad results-analysis --run-id <ID>`

实际运行 `run_l3_bottle_001` 验证：
- 3 units (baseline + 2 variants) → 正确识别 1 variant `idea_run_l3_bottle_001_var_A`
- `instance_auroc = 1.0` 在所有 units 一致 → Δ=0, conclusion=`practically_equivalent` (zero_improvement_delta)
- ✅ 所有 1354 tests pass

### 5. 3.9 CLI wiring

- `cli.py`: 新增 `results-analysis` 子命令（partition 3）
- `orchestrator.py`: 替换 `_stage_results_analysis` 占位符为 `run_results_analysis_stage` 调用

## 未完成/剩余项

- `run_l3_bottle_001` 的 3.7 no-op patch 意味着 3.8 在 plumbing 模式下只验证管线，不验证方法
- `metrics.py`/`ScientificValidityReport` 解析尚未集成到 attempt outcome 中
- `ResourceUsageReport` 生成尚未集成
- `RetryDecision` 的逻辑可以进一步精细化
- 需要真实 MVTec dataset + GPU 环境才能实际执行 PatchCore benchmark
- 缺 3.8 单元测试（test_command_path, test_expected_outputs, test_deterministic_sha）
- GPU telemetry → 3.9 resource comparison 仍为 `not_available`
- 3.10 final_report stage 仍为 `blocked` 占位符
