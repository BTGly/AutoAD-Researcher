# 2026-07-15 Development Log

## Entry 1 - Establish the simplified product baseline

Goal:
- Preserve the current `a6cbf26` control-plane history while restarting product simplification from the exact `f72da8fbb23147be74502caef6c7ce320f506f73` checkpoint.
- Carry the existing user-owned `AGENTS.md` update and 13 obsolete document deletions into the new baseline unchanged.

Operations:
- Created `backup/p0.3-a6cbf26` at `a6cbf262ffa818c44828a7b01e9638d016ffae95`.
- Created `simplify-from-f72` from `f72da8fbb23147be74502caef6c7ce320f506f73`.
- Restored the pre-existing staged `AGENTS.md` content byte-for-byte and the 13 pre-existing document deletions.
- Kept the rollback baseline isolated from the later Conversation Router, Research Intent Interpreter, field mutation protocol, authorization v3, confirmation saga, and experiment control-plane commits.

Result:
- The simplified branch now has the requested historical base and pending workspace cleanup ready for the full verification and push gate.

Leftovers:
- Run the full verification gate and push the baseline commit.
- Remove the draft checklist UI and confirmation requirement for ordinary research-summary persistence in separate verified commits.
- Redesign intent handling around a compact LLM-generated summary with confirmed facts, inferred facts, unresolved conflicts, and at most one blocking question.
- Port only independently justified material-ingestion improvements after checking their dependencies; do not transplant the later routing, mutation, or contract-gating stack.

## Entry 2 - Remove form-style draft gap prompts

Goal:
- Stop presenting the research conversation as a field-completion form before changing the underlying draft model.

Operations:
- Removed the visible `还缺这些` list and `下一步可问` list from the Draft sidebar.
- Replaced the ready-versus-missing badge and gap warning with a neutral automatic-summary status.
- Kept the backend response fields temporarily compatible so this UI-only change remains isolated from the upcoming summary persistence redesign.

Result:
- The Draft panel now presents current research context without exposing a checklist or suggested form questions.

Leftovers:
- Run the frontend build and full project verification, then push this isolated UI change.
- Replace the field-oriented Draft API with the compact research summary model in the next commit.

## Entry 3 - Keep explicit repository intake in the dialogue turn

Goal:
- Recognize bare GitHub repository-root URLs deterministically without relying on an LLM.
- Register material actions without ending the current dialogue turn.

Operations:
- Reused `parse_github_repository_url` in the explicit source planner so exact `github.com/owner/repository` URLs route to repository intake even without a `.git` suffix.
- Tightened strict GitHub parsing to reject subpaths, query strings, fragments, and non-supported owner/repository characters instead of partially matching them.
- Removed both orchestrator branches that replaced the conversational response with a source-intake status after creating sources or jobs.
- Added focused tests for exact GitHub roots and for a turn that both queues clone/summary jobs and returns the dialogue response.

Result:
- Bare GitHub roots now create `github_repo`, `git_clone`, and `repo_summarize` records through the deterministic source path.
- Material registration remains an internal side effect while the user message continues through the existing dialogue path.

Leftovers:
- Replace the remaining multi-call contract/turn-gate/reply path with one `ResearchDialogueAgent` call and a compact persisted summary.
- Run the full verification gate and push this change before starting that architecture replacement.

## Entry 4 - Replace the orchestrator with one dialogue-summary call

Goal:
- Replace the field-oriented contract orchestration path with one LLM call that returns a natural reply and a compact research summary.
- Keep confirmed user facts separate from material-backed inferences and unresolved conflicts.

Operations:
- Added strict `ResearchIntentSummary` and based-statement Pydantic models for `goal`, `confirmed_facts`, `inferred_facts`, `unresolved_conflicts`, and one nullable `blocking_question`.
- Added atomic `summary.json` load/save using a same-directory temporary file, flush, `fsync`, and `os.replace`; invalid or failed model replies do not overwrite the previous summary.
- Added `ResearchDialogueAgent` with the V1 behavior contract, compact Evidence/Job/material-action context, prior summary, recent dialogue, complete-summary semantics, and strict JSON output validation.
- Rewrote `ResearchOrchestratorV2` so the production path runs deterministic explicit material intake followed by exactly one Dialogue Agent call. It no longer calls TurnGate, NeedDiscovery, contract building/merging/confirmation, or ReplyPlanner.
- Exported the explicit source-action function separately. Natural-language tool requests can no longer make a separate planner LLM invent a repository URL or create a WebSearch job on the production path.
- Added focused summary, prompt, single-call, failure-preservation, and source/dialogue regressions. Marked eleven obsolete old-orchestrator tests for deletion with the remaining control-plane code.

Result:
- The reply and complete summary now come from the same validated model response, while source/job registration remains deterministic and user-transparent.
- `confirmed_facts` can only be populated under the prompt contract from explicit user statements; inferred facts and conflicts require a visible basis.

Leftovers:
- Replace the Draft API and frontend panel with the persisted intent-summary projection.
- Remove intent confirmation from the Stage 3 patch-planning gate.
- Delete the obsolete contract/router/planner modules and their skipped tests after downstream consumers move to the summary.
- Run the full verification gate and push this architecture change before editing API/frontend consumers.

## Entry 5 - Replace Draft UI and remove research-summary confirmation

Goal:
- Expose the compact research summary through the V2 API and right sidebar instead of the field-oriented Draft projection.
- Remove ordinary research-summary confirmation as a prerequisite for patch planning while preserving approvals for code application and real execution.

Operations:
- Replaced the mounted `/api/runs/{run_id}/draft` route with `/api/runs/{run_id}/intent-summary`, returning the exact persisted summary schema or an empty schema-valid summary.
- Replaced frontend Draft types, API call, state, tab, status text, and field rows with a summary view for goal, confirmed facts, material-backed inferences, unresolved conflicts, and the single blocking question.
- Removed the `IntentConfirmation` schema, intent confirmation file helpers, intake-bridge confirmation/hash dependency, patch-planner gate, UI gate rows, blocked-reason hints, and HITL verifier checks.
- Kept `patch_approval` before code application and `run_approval` plus the real-execution environment guard before experiments.
- Changed the clarification-to-input bridge to generate `input_task.yaml` directly from the validated clarification artifact and to audit only that source hash.
- Updated backend, pipeline, UI helper, route, and HITL tests to assert the new summary and approval boundaries.

Result:
- The V2 frontend has no field-completion Draft surface or plan-readiness badge.
- Research dialogue state is not presented as something the user must confirm; only a non-null summary `blocking_question` represents a dialogue blocker.
- Patch planning no longer depends on a research-summary approval artifact, while mutation and execution remain separately gated.

Leftovers:
- Build the frontend, run the full verification gate, and push this API/UI/gate replacement.
- Add target-directed repository analysis and artifact-derived Source/parser status.
- Remove the now-unmounted Draft service and the rest of the obsolete V2 control-plane modules.

## Entry 6 - Add target-directed repository evidence and artifact-derived source state

Goal:
- Read an explicitly identified repository task file before reasoning about Case 05 instead of inferring the operation from a benchmark identifier.
- Make Source and PDF parser displays reflect successful artifacts and the active parse attempt.

Operations:
- Added a bounded repository file mapper for the exact `level` and `problem_id` identifiers, with unique-path resolution, content reading, file/content hashes, byte counts, and atomic analysis output.
- Added deterministic extraction of the two explicit identifiers and a target-bearing `repo_analyze` job tied to the repository clone dependency; the same turn still makes exactly one Dialogue Agent call.
- Added target analysis and task-file evidence artifacts so the Dialogue Agent receives the actual selected file path and content preview.
- Changed repository source status projection to distinguish acquisition attestation from supported analysis artifacts, preventing a completed clone from remaining `user_provided_not_ingested`.
- Converted successful PDF fallbacks into first-class parse attempts under `paper/parse/attempts/{parse_attempt_id}`, preserving the original PDF `stored_path` and recording the converter's actual `parser_name` in the attempt, quality report, evidence, and summarization payload.
- Made parse-attempt evidence usable only while its successful attempt remains active, and repository evidence usable only while acquisition attestation exists.
- Added focused tests for exact target selection/ambiguity, target job planning, actual task-file evidence, plan-only non-mutation, repository lifecycle projection, and PDF fallback parser identity.

Result:
- `level=2, problem_id=40` produces an auditable read of the uniquely matching acquired task file; absent or ambiguous mappings do not select a file.
- Source status and fallback parser identity now derive from current artifacts instead of stale registration labels.

Leftovers:
- Run focused tests and the full verification gate, then push this repository/source-state change.
- Delete the obsolete V2 control-plane modules and tests in a separate verified change.
- Run the nine-case semantic acceptance suite against the final single-call architecture.

## Entry 7 - Remove the obsolete V2 control plane

Goal:
- Remove the superseded multi-call routing, requirement-discovery, contract-building, and reply-planning paths after all production consumers moved to `ResearchDialogueAgent`.
- Preserve deterministic material intake and read compatibility for historical run artifacts.

Operations:
- Confirmed that Conversation Router, Research Intent Interpreter, Mutation Protocol, Intent Mutation Service, Contract Confirmation Service, Contract Hashing, and Research Semantics were already absent from the simplified baseline.
- Replaced `source_action_planner.py` with a deterministic `source_actions.py` that accepts only structured attachments and explicit URLs and never calls an LLM or resolves a natural-language repository name.
- Deleted `turn_gate.py`, `need_discovery.py`, `draft_service.py`, `reply_planner.py`, and their obsolete tests.
- Moved the still-used explicit metric-name normalization into `assistant/metric_normalization.py`, independent of requirement discovery.
- Reduced `intent_contract.py` from a builder/mutator/confirmation model to a permissive read-only compatibility layer for old draft and confirmed contract files.
- Removed old fallback-reply tests and the stale LLM source-planner test; retained and extended tests for the active single-call orchestration, deterministic source actions, metric normalization, and legacy contract reading.
- Updated `AGENTS.md` architecture references to the summary API and the remaining V2 modules.

Result:
- The V2 production path has one conversational LLM call and no hidden Router, Interpreter, Need Discovery, Turn Gate, contract mutation/confirmation, Source Planner LLM, or Reply Planner LLM path.
- Historical intent-contract artifacts remain readable but cannot be generated, mutated, confirmed, or written by the compatibility module.

Leftovers:
- Run the full verification gate and push the dead-code deletion.
- Build and run the nine-case semantic acceptance suite against this final architecture, including the three veto cases and plan-only side-effect checks.

## Entry 8 - Adapt the nine-case acceptance gate to dialogue summaries

Goal:
- Execute the retained nine semantic cases against the single-call `reply + summary` architecture without restoring deleted control-plane fields.
- Enforce the user-specified minimum/average/veto thresholds and deterministic evidence/side-effect requirements.

Operations:
- Restored the exact historical nine-case corpus byte-for-byte, including its turns, source URLs, semantic targets, prohibited commitments, required conflicts, veto rules, paraphrases, entity variants, and counterfactuals.
- Added a test-only live acceptance runner that sends every source URL and user turn through `ResearchOrchestratorV2`, preserving one Dialogue Agent call per user message.
- Replaced the obsolete Confirmation Saga score with a `blocking_question` appropriateness score; the retained `expected_pending_confirmation` field remains historical corpus metadata and is not treated as a product requirement.
- Added an independent semantic judge for target coverage, prohibited commitments, conflict handling, execution mode, blocking-question behavior, and the named veto rules.
- Added strict judge transport normalization and bounded retries so malformed evaluator JSON cannot be mistaken for a product failure or score.
- Added deterministic scoring for exact source actions, plan-only code/session/job side effects, Case 05 exact target-file evidence, and Case 06 repository README evidence.
- Limited live material execution to Case 05 and Case 06, where the acceptance criteria require repository-grounded conclusions; all other cases still exercise deterministic source registration and conversational handling.
- Added generic performance-planning guidance requiring reference correctness and a same-condition benchmark, while forbidding guesses about an unread target file.
- Required explicit execution boundaries and negative constraints to remain in confirmed facts, and clarified that missing secondary implementation material blocks source-level mapping rather than an evidence-bounded high-level plan.
- Made the one production call tolerant of transport text around one complete JSON object without relaxing Pydantic response validation.
- Strengthened the generic cross-domain rule to refuse implementation details until compatibility is evidenced, and made null blocking questions incompatible with trailing material requests.
- Bounded the visible reply to a few short prose paragraphs and required resource-overcommitment risks to remain unresolved until offload/checkpointing feasibility is evidenced.
- Added offline tests for corpus shape, deterministic repository URL actions, production-prompt isolation, and the release threshold calculation.

Result:
- The new gate can score natural dialogue state without reintroducing Router/Interpreter/Mutation fields or a second production LLM call.
- Runtime reports and raw observations are written under ignored `runs/semantic_acceptance_*` directories, not committed source files.
- The final live suite passed all nine cases at 100 points each: average 100, minimum 100, veto failures 0.
- Case 05's exact target-file read and Case 06's repository README evidence checks passed; every plan-only case recorded zero code modifications, zero experiment jobs, and zero experiment Sessions.
- Case 09 refused to determine a cross-domain implementation without compatibility evidence and did not emit a fusion, serialization, or score-combination scheme.

Leftovers:
- Run the final full verification gate and push the acceptance tooling and prompt corrections.
- Confirm GitHub Actions, then start the local application server for handoff.

## Entry 9 - Preserve HTTP 404 for removed API routes

Goal:
- Ensure replacing the Draft endpoint is observable at the HTTP boundary, not only in OpenAPI and frontend code.

Operations:
- Traced `GET /api/runs/{run_id}/draft` to the React SPA catch-all after confirming no Draft router remained mounted.
- Changed the catch-all to raise `HTTPException(404)` for unknown `/api/*` paths and for missing frontend assets instead of returning a successful JSON dict.
- Added ASGI-level regressions proving the removed Draft endpoint returns 404, the Intent Summary endpoint returns the exact empty summary schema, and non-API client routes still receive the React application.
- After CI exposed that `frontend/dist` is intentionally absent on a clean runner, extracted the SPA decision into a helper so frontend fallback behavior is tested with an explicit temporary index instead of depending on local build artifacts.

Result:
- `/api/runs/{run_id}/intent-summary` is the only mounted summary API, and the removed `/draft` path now has correct HTTP not-found semantics.

Leftovers:
- Run the full verification and push gate, confirm GitHub Actions, and restart the local server with the embedded worker disabled for handoff.

## Entry 10 - Restore per-run follow-up message suspension

Goal:
- Restore the omitted ability to submit multiple follow-up messages while a dialogue turn is active without starting concurrent turns in the same Run.
- Preserve FIFO delivery and let the user withdraw any unsent message back into the composer.

Operations:
- Restored the controlled chat composer and an in-memory `QueuedChatMessage` collection partitioned by exact Run id.
- Added a compact queued-message panel with the pending count, automatic-send state, and one `Undo2` action that restores the selected unsent message without overwriting an existing composer draft.
- Restored one-at-a-time FIFO draining after the active turn completes, stale Run-load guards, per-Run active-turn ownership, queue cleanup on Run deletion, and pause-on-failure behavior for dequeued requests.
- Restored the validated optional `request_id` chat field and client request correlation so WebSocket delta/done events update only their matching assistant message.
- Removed the historical contract-confirmation dependency from queue draining because the simplified dialogue architecture has no intent-confirmation gate.
- Added backend regressions for request-id reuse, fallback generation, and rejection of unsafe request-id characters.

Result:
- Follow-up submissions remain unsent while a turn is active, are shown per Run, and are sent in FIFO order only after the preceding turn finishes.
- Withdrawing a queued item returns it to the composer for editing; queued state is intentionally not persisted across page reloads, and active requests are not cancellable.
- The frontend production build and request-correlation tests pass; lint reports only the pre-existing dynamic dependency-list warning in `useAutoScroll.ts`.

Leftovers:
- Run the full verification and push gate, confirm GitHub Actions, and restart the local application so the backend loads the restored request-correlation field.
