# 2026-07-22

## DeepSeek role routing and request runtime foundation

- Goal: establish the mature-project-informed model contract before wiring the three AutoAD model zones, while removing the shared conversation deadline from the LLM transport layer.
- Operations: inspected the cloned `DeepSeek-Reasonix` implementation under the workspace references and reused its role-separated model configuration, native DeepSeek thinking/reasoning parameters, tool-call preservation, streaming reasoning, usage, and finish-reason observability. Added a small typed route contract for `research_dialogue`, `report`, and `experiment_agent`; defaulted the first two to V4 Flash without thinking and the experiment role to V4 Pro with `thinking.type=enabled` and `reasoning_effort=max`. Updated the shared broker/client to omit generation limits by default, accept optional DeepSeek controls, preserve reasoning/tool calls/usage/finish reason, and use only individual request timeouts. Removed the V2 orchestrator's shared conversation-deadline wrapper and updated its contract regression.
- Result: focused LLM runtime, model-routing, client, V2 dialogue, task-bridge, and report-collaboration tests pass (`70 passed` across the focused runs). Static compilation passes. The request payload now carries only explicitly selected generation controls; the route snapshot carries capability and provenance fields without credentials.
- Leftovers: legacy model migration, product-level experiment/report budget removal, adaptive report context maintenance, and full verification/push remain for the next stages.

## Three-zone route wiring and provenance

- Goal: connect the role route contract to the real dialogue, report, and experiment entrypoints while keeping browser and server credentials separate.
- Operations: passed the selected dialogue and report routes through V2 and report Discussion calls; recorded non-secret route provenance in assistant/report events, report generation profiles, immutable ReportManifest records, ExperimentSession records, and environment Job payloads. Replaced the frontend's single free-text model plus stale experiment headers with three exact model selectors and a legacy localStorage migration for the former dialogue model. Background report/experiment paths continue to resolve credentials from server configuration rather than browser experiment headers.
- Result: focused route, V2, experiment starter, reporting lifecycle/narrative/discussion tests pass (`51 passed`); frontend TypeScript/build passes. Unsupported model identifiers fail at the route boundary instead of changing thinking behavior implicitly, while the two exact legacy DeepSeek aliases migrate to their V4 route IDs. The first full gate run exposed this migration requirement from the existing server config and was corrected before push.
- Leftovers: product-level cognitive/report budgets, fixed report context windows, legacy model references in tests/docs, and full verification/push for this route batch are pending before the next implementation stage.
