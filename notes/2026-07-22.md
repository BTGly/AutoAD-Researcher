# 2026-07-22

## UI-V2 interaction and backend feedback repair

- Goal: restore the original hover-based left navigation behavior and make
  research chat, source upload, task creation, and task rename failures
  diagnosable while preserving the current backend contracts.
- Operations: removed the explicit Sidebar expand/collapse button and pinned
  state; kept mouse enter/leave and focus expansion with a fixed rail
  footprint. Routed non-2xx responses through the existing structured API
  error parser. Updated App actions to preserve backend errors and distinguish
  connection failures from server validation failures. Added Playwright
  coverage for hover/focus Sidebar behavior and the exact chat/upload/create/
  rename request methods and paths. The new action test also found that the
  TaskMenu rename controls were underneath the clickable session row, so the
  action group now has an explicit stacking context and always-available hit
  area; opacity still keeps secondary actions visually quiet until hover.
- Result: the focused action test passes after fixing the TaskMenu action hit
  area. Frontend lint/build pass; fixture Playwright passes `28 passed`,
  full-stack Playwright passes `2 passed`, and `scripts/verify.sh` passes
  `1907 passed, 2 skipped`.
- Leftovers: push the branch and confirm the corresponding GitHub Actions run.
