# 2026-07-08

## Entry 1 — Remove Streamlit surface and add task switching API/UI

Goal:
- Remove the legacy Streamlit product surface.
- Reuse the existing task profile/archive logic instead of rebuilding it.
- Add task title/list/rename/archive/delete support for the React UI through the existing top "当前任务" header area, not the left sidebar.

Operations:
- Moved task profile/archive implementation to `autoad_researcher.task_workspace.task_profile` and left `autoad_researcher.ui.task_profile` as a compatibility import path.
- Expanded `/api/runs` with task metadata, create, rename, archive, restore, archived delete, and transcript endpoints.
- Updated the React header with a `TaskMenu` component for current-task switching, creation, rename, archive, restore, and delete; switching reloads transcript, sources, jobs, artifacts, and WebSocket state through the active `runId`.
- Removed Streamlit entrypoints, v2 Streamlit helpers, `.streamlit/config.toml`, `scripts/fix_streamlit_cors.sh`, and Streamlit dependencies/lock entries.
- Replaced the backend chat config fallback that imported Streamlit config with direct `~/.autoad/config.json`/environment lookup.
- Updated Streamlit-era tests and docs to the FastAPI/React surface; added server run-task API coverage.

Result:
- Targeted verification passed:
  - `uv run python -m compileall -q src tests`
  - `uv run pytest tests/test_server_runs_api.py tests/test_streamlit_api_key_state.py tests/test_ui_task_profile.py tests/test_ui_research_chat_safety.py tests/test_ui_material_requests.py -q` → 95 passed
  - `npm run build` in `frontend/` passed
- `bun` was not available on the server PATH, so the frontend build was verified with the repository's `package-lock.json` via npm.

Leftovers:
- Run the full project gate with `bash scripts/verify.sh`.
- If the gate passes, run `bash scripts/verify_and_push.sh "<message>"` and confirm remote CI.
- Existing dirty `notes/2026-07-07.md` predates this entry and was not modified.

## Entry 2 — Fix Vite dev API proxy for React testing

Goal:
- Fix the blank React page observed when opening `http://127.0.0.1:5173` against the Vite dev server.

Operations:
- Reproduced that Vite returned the SPA HTML for `/api/health` and `/api/runs` instead of proxying to the FastAPI backend.
- Added a Vite dev-server proxy for `/api` with WebSocket support to `http://127.0.0.1:8000`.
- Restarted the stale backend process so `/api/runs` uses the new task metadata response shape.
- Restarted the Vite dev server.

Result:
- `http://127.0.0.1:8000/api/health` returns JSON 200.
- `http://127.0.0.1:5173/api/health` returns JSON 200 through the proxy.
- `http://127.0.0.1:5173/api/runs` returns JSON with `task_title`/`task_summary`.
- `npm run build` passed.

Leftovers:
- Run full `bash scripts/verify.sh`, then commit and push this proxy fix.

## Entry 3 — Match Claude Code session history controls

Goal:
- Replace the task dropdown/archive UI with Claude Code style session controls: a history icon, a new session icon, searchable session history, hover actions, and direct delete.

Operations:
- Reworked `TaskMenu` into a session-history popover with `Search sessions...`.
- Added right-header icon buttons for session history and new session using `lucide-react`.
- Current session rows expose rename and delete on hover; non-current history rows expose delete only.
- Updated React task deletion flow so deleting the current session switches to the next available session or creates a new one.
- Changed `DELETE /api/runs/{run_id}` to directly remove the run directory instead of requiring archive first.
- Kept archive/restore backend routes for compatibility, but removed them from the primary UI path.

Result:
- `npm run build` passed.
- `uv run pytest tests/test_server_runs_api.py -q` passed.
- Restarted the FastAPI backend so direct delete is active for local testing.

Leftovers:
- Run full `bash scripts/verify.sh`.
- Commit and push if verification passes; existing dirty `notes/2026-07-07.md` predates this work and should remain separate.

## Entry 4 — Session UI closeout and onboarding placeholder

Goal:
- Close out the session/task-switching feature for handoff.
- Add a non-persistent first-screen assistant prompt so empty sessions do not look blank, without polluting chat history.
- Record development notes for future contributors.

Operations:
- Replaced the old centered welcome blurb with a translucent `Assistant` message-style placeholder shown only when `messages.length === 0`.
- Kept the placeholder out of `chat/transcript.jsonl`; it is rendered client-side only.
- Adjusted the session history popover alignment to open from the control's left edge with `width: min(440px, calc(100vw - 24px))`, preventing it from overflowing off the left side of the viewport.
- Reviewed the task/session code paths touched in this feature: React session controls, FastAPI run routes, Vite API proxy, Streamlit removal, and task profile migration.
- Cleaned source-level legacy UI docstrings that still mentioned Streamlit so future contributors do not mistake compatibility helpers for an active product surface.

Result:
- `npm run build` passed after the placeholder/alignment changes.
- Local dev server check passed: `http://127.0.0.1:5173/` returns HTML and `http://127.0.0.1:5173/api/runs` returns JSON.
- `rg -n "Streamlit|streamlit" src pyproject.toml frontend scripts` no longer reports active source, dependency, or entrypoint matches.

Development notes:
- The top header is the canonical session/task surface. Do not add a separate left-sidebar Tasks page unless the product direction changes.
- The current session title is display metadata only; `run_id` remains the artifact key and should stay in Developer Details or backend payloads, not the main user-facing title.
- Empty-session guidance must remain a visual placeholder, not a persisted assistant turn. Persisting it would contaminate transcript-tail prompts and task history.
- The Vite dev server must proxy `/api` and WebSocket requests to FastAPI; otherwise `/api/runs` returns the SPA HTML and the React app can fail while parsing JSON.
- Delete is now the primary UI removal action and physically removes the run directory. Archive/restore routes remain backend-compatible but are not part of the Claude-style session history path.
- Keep `ui_chat/` artifact paths stable for compatibility even though the Streamlit surface is gone.
- Existing `autoad_researcher.ui.task_profile` imports are compatibility shims; new task workspace logic should import from `autoad_researcher.task_workspace`.
- Tests with Streamlit in their names are removal-regression tests; do not delete or rename them solely to remove the word.

Leftovers:
- Run full `bash scripts/verify.sh`.
- Commit and push the closeout if the gate passes.
- Existing dirty `notes/2026-07-07.md` predates this work and should remain separate.

## Entry 5 — Wire text-first evidence for MinerU, MarkItDown, and worker jobs

Goal:
- Add the HF-2.2 material-processing bridge for subagent/worker outputs without regressing the flexible LLM Turn Gate / Reply Planner.
- Keep PDF parsing MinerU-first, use MarkItDown for web/general fallback text conversion, and make right-side Evidence read the same supported evidence index as the main assistant context.
- Preserve the boundary that web_search returns candidate sources only; fetch/parse must happen before anything becomes usable evidence.

Operations:
- Reviewed `references/document-processing/README.md`: PDF depth maps to MinerU, broad text conversion maps to MarkItDown.
- Extended V2 job records with payload support and explicit job types: `web_markitdown`, `paper_parse_mineru`, `paper_parse_markitdown`, `paper_summarize`, and `repo_summarize`.
- Added an optional MarkItDown adapter that returns structured failure when the package is unavailable instead of breaking imports.
- Updated the worker to run `web_fetch`, `web_markitdown`, MinerU-backed paper parsing through the existing `paper-intelligence` command, MarkItDown fallback for paper files, and repository summaries.
- Added V2 artifact evidence indexing under `evidence/evidence_index.jsonl` and merged it with existing `paper/evidence_index.jsonl` plus legacy paper artifacts in `load_usable_evidence`.
- Added `/api/runs/{run_id}/evidence` and changed the React right sidebar Evidence tab to render synced evidence entries, not just a count.
- Made the frontend WebSocket handler accept both snake_case and camelCase job/source fields so existing event payloads update UI state correctly.

Result:
- Targeted Python checks passed with `/root/.local/bin/uv` because the current shell PATH does not include `uv`:
  - `/root/.local/bin/uv run python -m compileall -q src tests`
  - `/root/.local/bin/uv run pytest tests/test_v2_evidence_route.py tests/test_v2_intent_contract.py -q` → 29 passed
- Frontend build passed:
  - `npm run build`

Leftovers:
- Run full `bash scripts/verify.sh` with `PATH=/root/.local/bin:$PATH`.
- Then run `bash scripts/verify_and_push.sh "<message>"` only if pushing is desired and GitHub credentials/network are available.

## Entry 6 — Connect browser file upload to source intake and evidence jobs

Goal:
- Replace the React placeholder upload reply with a real backend upload path.
- Reuse existing source registry helpers and the V2 job/evidence pipeline rather than inventing a separate upload system.

Operations:
- Added `POST /api/runs/{run_id}/sources/upload` using raw request bytes plus `X-AutoAD-Filename`, avoiding a new multipart dependency.
- Reused `save_uploaded_file` so uploaded PDF/txt/md/markdown files are stored under `runs/{run_id}/sources/{source_id}/` and registered in `source_references.json`.
- For uploaded PDFs, the route creates a `paper_parse_mineru` PipelineJob and appends a `job.queued` event.
- For uploaded `.txt`/`.md`/`.markdown`, the route appends direct `uploaded_text` evidence and emits `artifact.created` / `evidence.updated`.
- Updated `PlusMenu`, `App.tsx`, and `api.ts` so the browser sends the actual file content, then refreshes Sources/Jobs/Evidence.
- Added route tests for PDF upload job creation and Markdown upload evidence creation.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run python -m compileall -q src tests`
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_evidence_route.py tests/test_ui_sources.py -q` → 44 passed
  - `npm run build` passed

Leftovers:
- Run full `bash scripts/verify.sh` again.
- Restart the local FastAPI/Vite dev servers so the upload route and new frontend bundle are active.

## Entry 7 — De-duplicate upload source/job events in the React sidebar

Goal:
- Fix the UI showing two sources/jobs after a single PDF upload even though the backend persisted only one source and one PipelineJob.

Operations:
- Inspected `runs/run_20260708_0827_6d36/sources/source_references.json`, `jobs/pipeline_jobs.jsonl`, and `events/events.jsonl`; confirmed the uploaded PDF created exactly one source and one `paper_parse_mineru` job.
- Updated the WebSocket `source.created` and `job.queued` handlers to ignore events whose `source_id` or `job_id` already exists in React state.
- Added `stored_path` / `storedPath` to the frontend WebSocket message type and use it as the source label when available.

Result:
- `npm run build` passed.
- `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_evidence_route.py -q` passed.

Leftovers:
- Run full `bash scripts/verify.sh` again.
- Refresh the browser after Vite reloads so the duplicate in-memory rows disappear.

## Entry 8 — Start queued upload jobs and expose job state to the main agent

Goal:
- Fix uploaded PDFs staying `queued` forever when only the FastAPI/Vite dev servers are running.
- Ensure the main agent can see queued/running/failed PipelineJobs and does not tell the user no PDF parsing task exists.

Operations:
- Added an embedded FastAPI worker loop that polls `runs/*/jobs/pipeline_jobs.jsonl` and reuses the existing worker `_process_pending_jobs`.
- The embedded worker is enabled by default and can be disabled with `AUTOAD_EMBEDDED_WORKER=0`.
- Changed paper parsing subprocess execution from `uv run autoad ...` to `sys.executable -m autoad_researcher.cli ...` so the embedded worker does not depend on shell PATH containing `uv`.
- Extended V2 LLM context with `pending_jobs` and `failed_jobs`.
- Added PipelineJob state to the LLM reply planner system context and fallback text, so queued jobs are reported as queued/running rather than treated as missing evidence.
- Added tests for queued-job context visibility, fallback wording, and embedded-worker disable switch.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run python -m compileall -q src tests`
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_evidence_route.py tests/test_v2_intent_contract.py -q` → 34 passed
  - `npm run build` passed

Leftovers:
- Run full `bash scripts/verify.sh`.
- Restart FastAPI so the embedded worker starts and consumes the currently queued PDF parse job.

## Entry 9 — Stop exposing unreadable PDF parses as supported evidence

Goal:
- Fix the mismatch where the right Evidence panel showed `paper_text supported` while the actual parse artifacts had no readable `paper.md`.
- Make the main agent explicitly know the uploaded PDF parse is unusable for paper-method extraction instead of hallucinating from short evidence labels.

Operations:
- Inspected the current uploaded `2303.15140v2.pdf` run and found `paper.md` was missing; `pages.jsonl` / `blocks.jsonl` were garbled and had 0 readable blocks.
- Confirmed the main agent context only received short evidence summaries such as `Parsed paper text · page 1 · b_0_0`, not readable paper content.
- Tightened `paper_intelligence` quality handling: a parse with no readable `paper.md` now writes `quality_level=unusable`, records fatal errors, and returns `parse_failed` at `parse_quality`.
- Updated V2 evidence loading so it does not expose `paper_text` evidence when the referenced `paper.md` does not exist, and it skips active parse attempts whose quality report is not usable.
- Added `unusable_parsed_sources` to the main LLM context and reply planner, so the assistant can truthfully say a PDF was processed but did not produce usable text evidence.
- Corrected the current run’s stale parse status/quality report from `parsed/usable` to failed/unusable so the UI and agent no longer treat it as supported evidence.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run python -m compileall -q src tests`
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_evidence_route.py tests/test_paper_intelligence_behavior.py::TestOrchestratorBehavior::test_parse_quality_report_has_source_and_attempt_id -q` → 12 passed
- Current context for `run_20260708_0827_6d36` now reports:
  - `usable_evidence=[]`
  - `blocking_next_step=parse_quality`
  - `unusable_parsed_sources=[2303.15140v2.pdf]`

Leftovers:
- Run full `bash scripts/verify.sh`.
- The uploaded PDF still needs a real parser backend or a readable text source before the main agent can extract methods from it.

## Entry 10 — Add anchored paper reading artifacts for main-agent context

Goal:
- Implement the reference-backed text-first paper reading layer: subagent/worker parses produce compact summary artifacts for the main assistant and right-side Evidence, while original `paper.md` remains the detail source of truth.
- Avoid keyword-based routing; preserve the LLM-driven reply planner and add soft instructions plus artifact state.

Operations:
- Added `paper_intelligence.reading_artifacts` to generate `paper_reading_summary.md`, `paper_reading_summary.json`, `paper_method_cards.json`, and `paper_artifact_manifest.json` from readable `paper.md` anchors.
- Wired `paper_summarize` into the worker and made MinerU / MarkItDown paper parsing call it automatically after readable markdown is available.
- Registered `paper_reading_summary` and `paper_artifact_manifest` as supported evidence, with raw anchors back to source markdown.
- Extended V2 LLM context with `paper_reading_summaries` and `artifact_manifests`, and updated the reply planner prompt to treat summaries as routing artifacts rather than final fact sources.
- Added `/api/runs/{run_id}/evidence/state` so the UI can show usable evidence and unusable parsed sources from the same backend state.
- Updated the right Evidence sidebar to show unusable parse cards separately and display detail/source markdown paths for paper summaries.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run python -m compileall -q src tests`
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_evidence_route.py tests/test_v2_intent_contract.py tests/test_paper_intelligence_behavior.py::TestOrchestratorBehavior::test_parse_quality_report_has_source_and_attempt_id -q` → 41 passed
  - `npm run build` passed

Leftovers:
- Run full `bash scripts/verify.sh`.
- This does not make the existing garbled `2303.15140v2.pdf` parse readable; it ensures future usable parses get anchored summaries and current unusable parses are represented honestly.

## Entry 11 — Fix LLM reply leakage, metric correction, and parse-failure wording

Goal:
- Fix the chat reply that exposed `reply_to_user`, `contract_updates`, and other internal structured fields directly to the user.
- Stop stale metric focus from surviving after the user corrects the assistant from VRAM/runtime back to AUROC.
- Make parse-failure explanations grounded in actual warnings/errors instead of speculative causes.

Operations:
- Extended the V2 reply planner parser to accept LLM key/value output such as `reply_to_user: ...` when the model ignores the requested JSON envelope, then render only the visible reply/question.
- Updated chat fact extraction to prefer the latest metric correction/focus user turn over older metric mentions in the transcript tail.
- Fixed AUROC canonicalization so Chinese-adjacent text like `提升AUROC` is recognized.
- Added parse quality fields, fatal errors, and parser fallback errors to `unusable_parsed_sources`.
- Updated reply planner guidance and fallback text to cite known parse warnings/errors and avoid unsupported guesses such as scanned-page or complex-layout explanations.
- Added regression coverage for the three observed failures.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_intent_contract.py tests/test_v2_evidence_route.py -q` → 43 passed
  - `PATH=/root/.local/bin:$PATH uv run python -m compileall -q src tests`
  - `npm run build`
- Local transcript extraction for `run_20260708_0827_6d36` now reports `metrics=['image_level_auroc']` instead of stale `peak_vram`.

Leftovers:
- Run full `bash scripts/verify.sh`.
- The run still has no readable `paper.md`; the assistant should say the known failure is no readable markdown plus MarkItDown unavailable for the second upload fallback.

## Entry 12 — Make intent extraction LLM-first and parse failures evidence-only

Goal:
- Align metric extraction with the intended product design: LLM Need Discovery fills semantic intent; deterministic regex/chat facts only normalize or provide fallback.
- Stop “why did PDF parsing fail” replies from speculating beyond source registry, quality report, and parser error artifacts.

Operations:
- Changed `build_contract_from_context` so validated Need Discovery metrics take precedence over `confirmed_from_user` chat-fact metrics. `chat_facts` now remains a conservative fallback when Need Discovery provides no metrics.
- Kept metric canonicalization as enum normalization of LLM/user strings, not as the primary product decision layer.
- Added a parse-failure-question guard in `reply_planner` that uses local evidence-state fallback when failed jobs or unusable parses exist, bypassing LLM wording that may add unsupported “possible causes”.
- Added deduping for unusable parsed source entries and regression tests for failed parse attempts without `active_parse_attempt_id`.
- Added regression tests proving stale `peak_vram` chat facts do not override LLM-discovered `image_level_auroc`, and proving speculative failure replies are not surfaced.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_intent_contract.py tests/test_v2_evidence_route.py -q` → 46 passed
  - `PATH=/root/.local/bin:$PATH uv run python -m compileall -q src tests`
- Local check for `run_20260708_1048_893f` now answers “为什么失败了” with only artifact-backed reasons:
  - `paper_parse_mineru(job_000001): execution failed`
  - `parse produced no readable paper.md`
  - `markitdown unavailable: No module named 'markitdown'`
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH bash scripts/verify.sh` → 1946 passed, 46 skipped

Leftovers:
- Restart the running FastAPI process if it is still serving old code.
- Push still requires working GitHub credentials/network; this turn did not push.

## Entry 13 — Add readable PDF fallback and surface real job errors

Goal:
- Make uploaded PDFs recover when the MinerU fixture pipeline produces unreadable blocks.
- Avoid a hard dependency on MarkItDown being installed before any fallback text conversion can work.
- Show real job/fallback errors in the right-side UI instead of only `failed`.

Operations:
- Added `tools/pdf_text_adapter.py`, using system `pdftotext` to extract readable PDF text into a `paper.md` fallback artifact.
- Updated the worker fallback chain for `paper_parse_mineru`: after MinerU parse-quality failure it now tries `pdftotext`, then MarkItDown/builtin text fallback, then arXiv abstract fallback when an arXiv id is present.
- Enhanced `markitdown_adapter.py` so missing MarkItDown can still convert local HTML/text files through a small builtin text fallback; it reports structured errors only when all local conversion paths fail.
- Changed failed PipelineJob error messages to include concrete parser errors from `*_error.json` rather than the generic `execution failed`.
- Updated frontend Job and Evidence cards to display job errors, fatal parse errors, and parser fallback errors.
- Manually repaired `run_20260708_1102_1909` through the new `pdftotext` fallback; it now has `paper/parse/pdftotext/.../paper.md`, `paper_reading_summary.md`, and usable Evidence.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_evidence_route.py tests/test_v2_intent_contract.py -q` → 48 passed
  - `PATH=/root/.local/bin:$PATH uv run python -m compileall -q src tests`
  - `cd frontend && npm run build`
- Local context for `run_20260708_1102_1909` now reports:
  - `can_answer=True`
  - basis: `paper_markdown_fallback`, `paper_reading_summary`, `paper_artifact_manifest`
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH bash scripts/verify.sh` → 1948 passed, 46 skipped

Leftovers:
- Restart FastAPI/Vite so the worker fallback and UI error rendering are active in the browser.

## Entry 14 — Harden evidence/upload run_id boundary before closeout

Goal:
- Close a route-level boundary gap found during final review of the evidence/upload feature batch.
- Keep all run artifact access inside `runs/{run_id}` using the shared `core/run_id.py` helper, matching project rules.

Operations:
- Changed the new `/api/runs/{run_id}/evidence` and `/api/runs/{run_id}/evidence/state` routes to resolve run directories through `run_dir_path`.
- Changed source list and upload routes to use `run_dir_path` before reading/writing `source_references.json` or uploaded files.
- Added regression tests proving traversal-like run IDs are rejected with HTTP 400 and do not write outside the configured runs root.

Result:
- Targeted route hardening checks passed:
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_evidence_route.py -q` -> 19 passed
  - `PATH=/root/.local/bin:$PATH uv run python -m compileall -q src tests`
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH bash scripts/verify.sh` -> 1950 passed, 46 skipped
- Frontend build passed:
  - `cd frontend && npm run build`

Leftovers:
- Restart FastAPI/Vite before browser testing if old dev servers are still running.
- Push is still not attempted in this closeout because the last instruction was to stop pushing and hand off clearly.

## Entry 15 — Replace PatchCore clone alias routing with LLM source actions

Goal:
- Fix the chat failure where natural-language requests such as "clone pathcore/PatchCore GitHub repo" were not turned into executable source actions.
- Avoid hard-coded PatchCore/pathcore keyword routing; use an LLM planner with tool capability context and auditable repository hints.
- Reuse existing source intake, PipelineJob, and Repository Acquisition runner for actual execution.

Operations:
- Reviewed AutoAD's repository intelligence plan and implementation: repository acquisition should be an audited job using Git CLI allowlists, detached checkout, attestation artifacts, and run-relative outputs.
- Reviewed reference agent patterns: Claude Code carries git repository source/outcome context in the session and passes tools into the model loop, while Claw/Claude-style tool pools load and filter tools by permission context.
- Removed the temporary `KNOWN_REPOSITORY_ALIASES`/`pathcore` substring resolver from V2 source intake. `source_service.py` now handles only structured upload/explicit URL signals; natural-language tool intent is not classified by local keyword rules.
- Added `assistant/v2/source_action_planner.py`, an LLM-first SourceActionPlanner that receives tool capabilities, source registry state, pending jobs, transcript tail, existing contract draft, and repository hints, then returns structured actions such as `web_search`, `github_discovery`, `git_clone`, and `ask_clarification`.
- Exposed the internal PatchCore benchmark repository as a planner hint from `configs/benchmarks/internal_patchcore_mvtec_bottle_v1.yaml`; code validates selected hint IDs/URLs but does not branch on user text aliases.
- Changed V2 orchestrator to execute planner actions by registering sources and creating existing PipelineJobs. Explicit URLs still register deterministically; natural-language search/clone requires an LLM SourceActionPlan.
- Fixed the worker `git_clone` job path to use the existing `RepositoryAcquisitionRunner` with GitHub metadata/default-branch commit resolution, replacing a stale import of a non-existent `shallow_clone` helper.
- Wrapped PipelineJob `web_search` execution into the material-discovery request shape using `job_id` as `request_id`, so planner-created search jobs produce traceable subagent records.
- Replaced alias-routing tests with planner tests proving natural-language clone/search requests are handled only through mocked LLM SourceActionPlan outputs.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_source_action_planner.py tests/test_v2_turn_gate.py -q` -> 9 passed
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_intent_contract.py tests/test_v2_evidence_route.py tests/test_v2_source_action_planner.py -q` -> 54 passed, 2 skipped
- Final full gate passed:
  - `PATH=/root/.local/bin:$PATH bash scripts/verify.sh` -> 1955 passed, 48 skipped

Leftovers:
- Browser-test the actual live flow after restarting FastAPI so the embedded worker picks up the new clone code and the planner uses the configured API key.
- MarkItDown still needs a separate live experiment; this entry only fixes common GitHub repository acquisition routing.

## Entry 16 — Fix partial GitHub clone and fallback PDF evidence state

Goal:
- Diagnose the live PatchCore clone failure without guessing beyond recorded artifacts.
- Prevent failed clone attempts from producing misleading supported repo evidence.
- Treat a PDF as usable when MinerU fails but a supported text fallback has succeeded.

Operations:
- Inspected `run_20260708_1329_e703` job and acquisition artifacts. The first clone failed at `git fetch --depth=1 --no-tags origin main` with `GnuTLS recv error (-110)`, and the later clone failure was the stale partial repository directory left by that failed fetch.
- Added PipelineJob dependency gating so `repo_summarize` fails immediately when its `git_clone` dependency failed.
- Added cleanup of incomplete repository targets before a clone retry, guarded by absence of `repo_acquisition/{source_id}/repository_attestation.json`.
- Required repository acquisition attestation before repo summarization and filtered stale `repo_summary` evidence without attestation from supported Evidence.
- Suppressed unusable PDF source state when supported fallback text evidence exists, and marked fallback parse success back into the source registry for future runs.
- Added regression tests for supported PDF fallback state, missing clone attestation, stale repo summary evidence, and failed clone dependency propagation.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_evidence_route.py tests/test_v2_source_action_planner.py -q` -> 28 passed, 2 skipped
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH bash scripts/verify.sh` -> 1959 passed, 48 skipped

Leftovers:
- Restart FastAPI before browser retesting.
- The original GitHub TLS interruption is still an environment/network condition; this change fixes the app's retry/state handling around that failure.

## Entry 17 — Probe repository-agent references and MarkItDown conversion

Goal:
- Review coding-agent/system-prompt references for reusable repository analysis patterns.
- Install MarkItDown through `uv` and test project conversion behavior on generated local sample files.

Operations:
- Reviewed local project rules and selected reference files under `references/coding-agents/` and `references/system-prompts/`.
- Extracted repository-agent design patterns: explicit task status/output files, subagent result handoff through durable artifacts, tool/source capability context, and compact system-init summaries rather than raw repository dumps.
- Added `markitdown>=0.1.6` to `pyproject.toml` and regenerated `uv.lock` via `UV_LINK_MODE=copy uv add --no-sync markitdown`.
- Attempted `uv sync` against the default PyPI index; MarkItDown's transitive packages (`magika`, `onnxruntime`, `numpy`) downloaded extremely slowly. Re-ran sync with `UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple`, which completed successfully, then restored dev dependencies with `uv sync --extra dev`.
- Generated local sample files under `/tmp/autoad_markitdown_probe/inputs`: HTML, TXT, MD, CSV, JSON, and XML.
- Invoked `autoad_researcher.tools.markitdown_adapter.convert_local_to_markdown` before and after MarkItDown installation.
- Generated richer samples under `/tmp/autoad_markitdown_probe/rich_inputs`: DOCX, PDF, and ZIP.
- Fixed `markitdown_adapter` so the builtin fallback only handles explicit text-like suffixes and refuses binary-looking content, preventing DOCX/PDF bytes from being recorded as successful Markdown.

Result:
- Builtin fallback conversion succeeded for all generated sample files:
  - `sample.html` -> extracted readable text and stripped script/style content.
  - `sample.txt`, `sample.md`, `sample.csv`, `sample.json`, `sample.xml` -> copied readable text to Markdown outputs.
- MarkItDown 0.1.6 runtime import succeeded.
- MarkItDown conversion succeeded for HTML, TXT, MD, CSV, JSON, XML, and ZIP.
- DOCX/PDF conversion now fails explicitly with MarkItDown `MissingDependencyException` messages indicating optional extras `[docx]` / `[pdf]`; builtin fallback no longer turns these binary files into fake successful evidence.
- Targeted regression passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple uv run pytest tests/test_v2_evidence_route.py::test_markitdown_adapter_missing_module_uses_builtin_html_fallback tests/test_v2_evidence_route.py::test_markitdown_builtin_fallback_refuses_binary_files -q` -> 2 passed
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple bash scripts/verify.sh` -> 1960 passed, 48 skipped

Leftovers:
- Restart FastAPI before browser retesting so the running server sees the MarkItDown dependency and binary-fallback guard.
- Consider adding targeted optional dependencies such as `markitdown[pdf]` and `markitdown[docx]`; `markitdown[all]` was not directly lockable under the current project Python support because of a pre-release `azure-ai-contentunderstanding>=1.2.0b1` constraint.
- Implement repo analysis as a V2 subagent/artifact producer rather than keeping the current thin `repo_brief.md` summarizer.

## Entry 19 — Make source removal real and surface Chinese live draft

Goal:
- Fix the observed mismatch where the assistant said an accidentally uploaded file was cleared, but Sources/Evidence still showed it.
- Add a right-sidebar Chinese Draft view that updates from the current transcript, source registry, evidence, jobs, and intent contract state.

Operations:
- Inspected `run_20260709_0100_00b0` and confirmed `LDRMCC_revision_log.md` remained in `sources/source_references.json`, `evidence/evidence_index.jsonl`, and the source upload directory after the assistant claimed it was removed.
- Added `remove_source()` in `ui/sources.py` to delete the source registry entry, source upload directory, per-source parse/acquisition directories, and evidence index rows keyed by `source_id`.
- Added `DELETE /api/runs/{run_id}/sources/{source_id}` and emits `source.deleted` plus `evidence.updated` events.
- Added a direct orchestrator guard for user turns such as “上传错了 / 不是我们要的”, removing the latest registered source before any LLM planner is called.
- Added `assistant/v2/draft_service.py` and `GET /api/runs/{run_id}/draft`, returning a Chinese research draft state with known fields, missing fields, current Sources/Evidence/Jobs, and next questions.
- Updated the React sidebar to fetch draft state with Sources/Jobs/Evidence, render a Chinese Draft tab, and expose a delete button on Source cards.
- Fixed duplicate SimpleNet method hints in the Draft view when multiple evidence artifacts reference the same paper method.

Result:
- Targeted regression passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple uv run pytest tests/test_v2_evidence_route.py::test_delete_source_removes_registry_evidence_and_files tests/test_v2_evidence_route.py::test_draft_route_returns_chinese_missing_state tests/test_v2_source_action_planner.py::test_orchestrator_removes_latest_source_when_user_rejects_upload -q` -> 3 passed
- Method-hint dedup regression passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple uv run pytest tests/test_v2_evidence_route.py::test_draft_route_deduplicates_method_hints tests/test_v2_evidence_route.py::test_draft_route_returns_chinese_missing_state -q` -> 2 passed
- Frontend build passed:
  - `cd frontend && npm run build`
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple bash scripts/verify.sh` -> 1965 passed, 48 skipped
- Live run cleanup passed:
  - `DELETE /api/runs/run_20260709_0100_00b0/sources/src_2026-07-09T01-01-52-045095Z` -> deleted, removed 1 evidence row.
- Runtime smoke passed after restart:
  - `GET /docs` -> 200.
  - `GET /api/runs/run_20260709_0100_00b0/sources` shows only `2303.15140v2.pdf`.
  - `GET /api/runs/run_20260709_0100_00b0/draft` shows Chinese Draft fields and a single `SimpleNet 论文方法` hint.

Leftovers:
- Browser-test the sidebar after refresh to confirm the visual Draft tab matches the API state.

## Entry 18 — Replace fake chat chunking with provider token streaming

Goal:
- Fix the FastAPI/React chat path where `assistant.delta` was produced by slicing the completed reply after `ResearchOrchestratorV2.handle()` returned.
- Reuse mature coding-agent streaming patterns: provider SSE/token callbacks, transient UI deltas, and final authoritative message reconciliation.
- Avoid streaming internal structured fields such as `contract_updates` or `missing_required_fields`.

Operations:
- Reviewed coding-agent references under `references/coding-agents/`: OpenHands SDK `LLM.acompletion`/token callbacks, Mimo/OpenCode `message.part.delta` event flow, Go opencode provider channels, and aider's litellm stream consumer.
- Added OpenAI-compatible SSE streaming support to `call_research_chat` behind an optional `on_delta` callback, preserving the existing non-streaming default for JSON planners.
- Threaded an `on_reply_delta` callback only into the V2 final Reply Planner, leaving SourceActionPlanner, TurnGate, and NeedDiscovery non-streaming so their internal JSON cannot reach the user.
- Added a `reply_to_user` streaming filter that forwards only user-visible text from streamed structured reply-planner output.
- Changed `/api/chat/send` to run the synchronous orchestrator in a worker thread and schedule WebSocket broadcasts on the main event loop, so provider tokens can reach the browser before the HTTP response finishes.
- Removed the `_reply_chunks` fake-streaming path and changed `assistant.done` to include the final reply content for frontend reconciliation.
- Updated the React WebSocket handler to replace the streaming placeholder with `assistant.done.content`, matching the transient-delta/final-message pattern.
- Added regression tests for OpenAI-compatible SSE parsing and reply-planner streaming field filtering; removed the old chunk-splitting test.

Result:
- Targeted verification passed:
  - `PATH=/root/.local/bin:$PATH uv run python -m compileall -q src tests`
  - `PATH=/root/.local/bin:$PATH uv run pytest tests/test_ui_chat_client.py tests/test_v2_chat_route.py tests/test_v2_intent_contract.py -q` -> 42 passed
  - `cd frontend && npm run build` passed
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple bash scripts/verify.sh` -> 1961 passed, 48 skipped

Leftovers:
- Browser-test with a live provider after restarting FastAPI/Vite to confirm token cadence over WebSocket.

## Entry 20 — Tighten research draft semantics and job failure replies

Goal:
- Fix the live Draft state observed in conversation: success criteria should not be a concatenated transcript, two mainstream AUROC metrics should persist, SimpleNet-derived user improvement ideas should be listed, and backend-only evaluation protocol should not appear as a user-facing missing field.
- Fix failed clone explanation so repo acquisition failures do not end with unrelated PDF parsing conclusions.

Operations:
- Add regression coverage for PatchCore/MVTec/SimpleNet conversation state and repo clone failure fallback.
- Normalize success criteria from conversational text into a compact comparison against the PatchCore baseline.
- Infer the two common anomaly-detection AUROC metrics when the user asks for "AUROC 的两种/主流两种".
- Populate method hints from SimpleNet evidence: feature adaptor, synthetic anomaly features, and discriminator/score calibration.
- Hide backend-auto-fill fields from the right-sidebar Draft user-facing field list.
- Add read-only Draft display cleanup for already-saved legacy polluted contracts, so existing runs do not need another user turn before the sidebar becomes clean.

Result:
- Targeted regression passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple uv run pytest tests/test_v2_intent_contract.py::test_contract_cleans_patchcore_simplenet_conversation_state tests/test_v2_intent_contract.py::test_draft_display_cleans_legacy_polluted_contract tests/test_v2_intent_contract.py::test_repo_failure_question_does_not_append_pdf_conclusion -q` -> 3 passed.
- Related V2 contract/draft tests passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple uv run pytest tests/test_v2_intent_contract.py tests/test_v2_evidence_route.py::test_draft_route_returns_chinese_missing_state tests/test_v2_evidence_route.py::test_draft_route_deduplicates_method_hints -q` -> 37 passed.
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple bash scripts/verify.sh` -> 1968 passed, 48 skipped.

Leftovers:
- Restart FastAPI after verification so the running Draft API picks up the fixes.

## Entry 21 — Humanize GitHub clone failure diagnosis and mirror search fallback

Goal:
- Summarize clone failures in user-facing language instead of echoing raw job artifacts.
- Make explicit "websearch/search mirror repository" requests create a web_search job even when the natural-language LLM planner is unavailable.

Operations:
- Classify `timed_out`, `GnuTLS recv error`, TLS termination, and GitHub access failures as network/TLS transfer failures.
- Keep dependent `repo_summarize` failures separate from root-cause clone failures.
- Add deterministic mirror-search planning only for explicit user search intent plus mirror/repository context, deriving the query from the registered GitHub repository URL when available.

Result:
- Targeted regression passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple uv run pytest tests/test_v2_intent_contract.py::test_repo_failure_question_does_not_append_pdf_conclusion tests/test_v2_source_action_planner.py::test_explicit_mirror_search_uses_registered_github_repo_without_llm -q` -> 2 passed.
- Related V2 source planner / intent tests passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple uv run pytest tests/test_v2_source_action_planner.py tests/test_v2_intent_contract.py -q` -> 43 passed.
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple bash scripts/verify.sh` -> 1969 passed, 48 skipped.
- Runtime smoke passed after FastAPI restart:
  - Chat request `如果是网络问题，能不能 websearch 对应的镜像仓库？` returned `source_intake`.
  - Created `web_search(job_000006)` with query `amazon-science/patchcore-inspection mirror GitCode Gitee AtomGit`.

## Entry 22 — Render assistant Markdown in the React chat surface

Goal:
- Render assistant chat replies as Markdown instead of showing raw markers such as `**PatchCore**`.
- Reuse the coding-agent pattern seen in Claude Code internals: parse user-visible assistant text as Markdown and render structured blocks during normal and streaming updates.

Operations:
- Reviewed `references/coding-agents/claude-code-internals/claude-code-main/src/components/Markdown.tsx`, `src/utils/markdown.ts`, and assistant message usage. Claude Code renders assistant text and streaming assistant text through Markdown components, backed by `marked`.
- Added `marked` to the React frontend dependencies.
- Added a `MarkdownContent` React component that uses `marked.lexer` and renders React elements for paragraphs, headings, lists, blockquotes, code, tables, links, and inline emphasis without `dangerouslySetInnerHTML`.
- Switched assistant messages, Markdown artifact previews, and the report page to use `MarkdownContent`; user messages remain plaintext to avoid reinterpreting pasted paths or command text.
- Added CSS for compact chat-oriented Markdown spacing, inline code, code blocks, blockquotes, and tables.

Result:
- Frontend build passed:
  - `cd frontend && npm run build`
- Frontend lint passed:
  - `cd frontend && npm run lint` -> passed with an existing `useAutoScroll` dependency-list warning.
- Frontend smoke passed:
  - `GET http://127.0.0.1:5173` -> 200.
  - `marked.lexer` on a sample `**PatchCore**` plus bullet-list reply produced paragraph/list tokens and nested `strong` tokens.
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple bash scripts/verify.sh` -> 1969 passed, 48 skipped

Leftovers:
- Visual browser screenshot check was not run because Chromium/Playwright is not installed in this environment.

## Entry 23 — Localize and clarify the right sidebar panels

Goal:
- Improve the right sidebar readability for Sources, Jobs, Evidence, and Draft.
- Replace raw English labels/statuses with Chinese display text while preserving exact backend fields in muted detail text.
- Borrow the coding-agent UI pattern from Claude Code internals: compact status badges, primary human-readable summaries, and secondary dimmed technical details.

Operations:
- Reviewed Claude Code internals `ContextSuggestions`, `CoordinatorAgentStatus`, and `AgentsList` patterns: title plus dim detail, compact status markers, source grouping, and truncation of low-priority metadata.
- Reworked the React `Sidebar` into Chinese tabs: `资料`、`任务`、`证据`、`草案`.
- Added display-layer mappings for source kinds/statuses, job types/statuses, evidence types/support levels, parser names, and common job failure strings.
- Reordered Jobs latest-first and made clone timeout/TLS/dependency failures readable in Chinese while keeping raw error text collapsed as muted detail.
- Grouped Draft fields into `核心信息`、`方法线索`、`执行与来源`, and added concise readiness/source counts.
- Added CSS classes for sidebar cards, badges, summaries, notes, and draft field rows.
- Localized the adjacent status bar and right-sidebar developer details labels so the sidebar area no longer mixes `Sources/Jobs/Evidence/Draft` English labels into the main UI.

Result:
- Frontend build passed:
  - `cd frontend && npm run build`
- Frontend lint passed:
  - `cd frontend && npm run lint` -> passed with the existing `useAutoScroll` dependency-list warning.
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple bash scripts/verify.sh` -> 1969 passed, 48 skipped

Leftovers:
- No push per user instruction.

## Entry 24 — Archive bundle source classification and child evidence

Goal:
- Replace the brittle `zip/tar -> local_repo` assumption with a reusable archive-bundle intake path.
- Support mixed user uploads containing papers, documents, text/Markdown/HTML, and code repositories.
- Preserve the existing repository-analysis path by creating `local_repo` child sources for code directories.

Operations:
- Added `archive_bundle` and `document` source kinds.
- Added parent/child source metadata to the source registry and made source deletion cascade to child sources, derived files, repo artifacts, and evidence rows.
- Changed uploaded `zip/tar/tar.gz/tgz/tar.bz2/tar.xz` files to queue `archive_unpack_classify`.
- Added archive classifier worker logic:
  - safely extracts zip/tar members with path-escape and special-member checks;
  - writes `archive_unpack/{source_id}/archive_manifest.json`;
  - detects repository roots using marker files and code-file signals;
  - creates child sources for `paper_pdf`, `document`, `markdown`, `text`, and `local_repo`;
  - queues child jobs such as `paper_parse_mineru`, `document_markitdown`, `local_repo_acquire`, and `repo_summarize`;
  - appends `archive_manifest` and direct text evidence where appropriate.
- Added `document_markitdown` for doc/docx/html-style sources and `local_repo_acquire` for already-unpacked repository directories.
- Updated right-sidebar labels for `资料包`, `文档`, `解包分类`, `转换文档`, `资料包清单`, and `文档正文`.
- Kept legacy `_run_local_repo_unpack` behavior available so older internal tests and existing job records do not break.

Result:
- Targeted tests passed:
  - `PATH=/root/.local/bin:$PATH uv run pytest -q tests/test_ui_sources.py tests/test_v2_evidence_route.py tests/test_v2_source_action_planner.py` -> 79 passed, 2 skipped.
- Full gate passed:
  - `PATH=/root/.local/bin:$PATH bash scripts/verify.sh` -> 1976 passed, 48 skipped.

Leftovers:
- No push per user instruction.
