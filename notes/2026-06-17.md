# AutoAD-Researcher 开发日志 — 2026-06-17

---

### 3.0C-2 最终封板

**目标**: 建立 repository / dataset / environment 三项 preflight 检查链。

**操作**:

1. 历经多次 review-fix 迭代，最终封板提交 `b7016f2`：
   - repository.py — Git HEAD/commit/remote/dirty 检查、required files SHA
   - dataset.py — MVTec bottle 结构验证、mask 映射、symlink 拒绝
   - environment.py — 固定 probe（torch/torchvision/faiss/timm）、CUDA/GPU 检查
   - preflight.py — 三项非 fail-fast 聚合器
   - CLI — `scripts/benchmark/preflight.py`
   - 测试 — 46 tests（hashing 5 + repository 11 + dataset 20 + environment 9 + preflight 1）
   - 最终 362 passed

2. 关键边界：
   - CLI `project_path()` 使用 `os.path.abspath()` 保留 venv symlink
   - `launcher.parent.resolve()` 防止中间目录 symlink 逃逸
   - `train/good` 自身 symlink 显式拒绝
   - 路径使用 `PurePosixPath.parts` 拒绝任意位置 `..`

---

### 3.0C-3 Commit 1: environment lock contracts

**目标**: 建立环境锁定契约 schema 和 lockfile 校验器。

**操作**:

1. 创建 `benchmarks/environment_lock.py`
   - `BenchmarkEnvironmentSpec` — draft/locked 状态机、SHA256 十六进制、Python 精确三段
   - `BenchmarkEnvironmentBuildEvidence` — 全部 check flags `Literal[True]`
   - `PackageIndexSpec` — https only
   - `validate_lockfile()` — 使用 `packaging.Requirement` 强制 `==` 精确 pin
   - 拒绝：bare、loose(`>=`)、URL、editable、`--requirement`、`--index-url`、`--editable`

2. 安全路径：`PurePosixPath.parts` 拒绝任意位置 `..`

3. verify.sh 实际加载 YAML → `model_validate()`

4. 测试 — 21 tests（spec 7 + lockfile 14）

**结果**: 383 passed

---

### 3.0C-3 Commit 2A: host inspection tooling

**创建**:

- `benchmarks/host_capabilities.py` — `BenchmarkHostCapabilities` + `GpuCapability`
- `scripts/benchmark/inspect_host.py` — 采集 platform/GPU/Python/uv
- 测试 — 3 tests

**结果**: 386 passed

---

### 3.0C-3 Commit 2B: 真实 PatchCore lockfile

**操作**:

1. 在 RTX 4090 + CUDA 12.4 + Python 3.11.15 上运行 `uv pip compile`
2. 生成 `requirements.lock.txt` — 66 packages，全部精确 `==`
   - torch==2.5.1+cu124, torchvision==0.20.1+cu124, timm==1.0.27, faiss-cpu==1.14.3
   - 12 NVIDIA CUDA 12.4 packages
3. `environment.yaml` 切换为 `status: locked`
4. 录入 `package_indexes`（pypi + pytorch-cu124）和 `package_manager_version: 0.11.21`
5. lockfile header 规范化：移除绝对路径，显式 `--extra-index-url`
6. verify.sh 新增 lockfile SHA 验证
7. 保存 `host_capabilities.json` + `lock_generation.json`

**初始 Commit 2B lockfile SHA**: `247dde0087e635536932db0baf7f5b3441475639ba3ccdc210668d2feffe9454`（后续 header finalization 修复会更新 SHA）

**结果**: 386 passed，verify.sh lockfile gate 通过

---

### Codex 项目记忆初始化与仓库巡检

**目标**: 在 Codex 重新参与开发前，将必须遵守的项目记忆写入仓库，并核对当前开发路线、门禁和日志规则。

**操作**:

1. 核对仓库状态
   - 当前 `main` 与 `origin/main` 同步
   - 最新历史已推进到 benchmark lock / evidence traceability 阶段

2. 读取关键入口
   - `CLAUDE.md`
   - `scripts/verify.sh`
   - `scripts/verify_and_push.sh`
   - `notes/development-log.md`
   - `docs/AutoAD_真实纵向闭环开发计划.md`
   - `docs/AutoAD_任务参数决策与来源协议.md`

3. 新增 `AGENTS.md`
   - 固化 Codex 工作流：读文件 → 小步修改 → 写日志 → verify → commit/push → 查 Actions
   - 固化不能猜字段、不能静默决定真实用户任务参数、不能提交密钥或真实 LLM 统计等边界
   - 指向当前主要计划文档和 benchmark 配置

**结果**:
- `bash scripts/verify.sh` 通过，391 passed

**遗留问题**:
- 无

---

### 3.1 R1 Repository Intelligence Contracts

**目标**: 按 `docs/3.1开发计划文档.md` 启动 Step 3.1，先实现可与 Tool Foundation 并行的 R1 contracts，不运行真实外部仓库。

**操作**:

1. 新增 `src/autoad_researcher/repository_intelligence/`
   - `models.py`：`RepositoryIntelligenceRequest`、`RepositoryAgentBudget`、`RepositoryCandidate`、`RepositoryResolution`、`RepositorySource`、`RepositoryClaim`、`RepositoryIntelligenceResult`
   - `evidence_models.py`：`FileEvidenceRef`、`RepositoryIdentityEvidenceRef`、`WebEvidenceRef`、`UserInputEvidenceRef`、discriminated `EvidenceRef`、`EvidenceIndexRecord`
   - `control_models.py`：`AnalysisControlSignal`
   - `ids.py`：identifier、SHA、commit 和 relative path 校验
   - `status.py` / `errors.py` / `__init__.py`

2. 合同边界
   - EvidenceRef 使用 `source_kind` discriminated union
   - budget 区分 analysis 与 repair reserve
   - candidate 包含 `default_branch` 与 `resolved_commit`
   - source path 使用 relative path 校验，拒绝 absolute、backslash 和 `..`
   - result 必须为七个正式 artifact 路径逐一提供 SHA
   - `SubmoduleRecord` 暂不臆造字段，使用 `extra="allow"` 保存后续 `.gitmodules` 解析结构

3. 新增测试 `tests/test_repository_intelligence_contracts.py`
   - custom budget 必填
   - bad commit SHA 拒绝
   - resolved resolution 需要 selected candidate 和 commit
   - absolute `local_path_label` 拒绝
   - repository identity evidence 可进入 Evidence Index
   - file evidence path/SHA 严格校验
   - AnalysisControlSignal coverage literal 校验
   - confirmed claim 必须有 evidence
   - result artifact SHA 集合必须与七个正式 artifact 一致

**结果**:
- targeted tests 通过：`12 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- R2 Skills 和 R3 Evidence Middleware 尚未实现

---

### 3.1 R2 Repository Intelligence Skills

**目标**: 按 3.1 计划新增 Repository Intelligence mandatory Skills，并提供离线 Skill loader / audit 合同；不执行真实工具，不进入真实外部仓库 Full Mode。

**操作**:

1. 新增四个 Skill 文件
   - `skills/repository-discovery/SKILL.md`
   - `skills/repository-acquisition/SKILL.md`
   - `skills/repository-analysis/SKILL.md`
   - `skills/repository-synthesis/SKILL.md`

2. 新增 `repository_intelligence.skills`
   - 解析 YAML frontmatter
   - 校验 mandatory stage → skill 映射
   - 校验 required sections：Purpose、Preconditions、Allowed Tools、Forbidden Actions、Recommended Workflow、Evidence Requirements、Output Contract、Stop Conditions、Failure Handling、Examples
   - 校验 required tools 已注册
   - 校验 permission profile 已注册
   - 计算 `skill_sha256`
   - 生成 `LoadedSkillRecord` / `LoadedSkillsAudit`
   - `write_loaded_skills()` 原子写入并拒绝覆盖
   - 明确 URL/local source 时跳过 discovery skill

3. 新增测试 `tests/test_repository_intelligence_skills.py`
   - analysis mandatory skill 可加载且包含 SHA
   - Skill body 必须包含 required sections
   - required tool 缺失会 blocked
   - permission profile 缺失会 blocked
   - explicit source 跳过 discovery
   - loaded skill audit 拒绝覆盖

**结果**:
- targeted tests 通过：`18 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- R3 Evidence Middleware 尚未实现

---

### 3.1 R3 Repository-aware Evidence Middleware Skeleton

**目标**: 实现 R3 的离线 Evidence Middleware 骨架，让 repository file / identity / web evidence 能进入统一 append-only Evidence Index；仍不运行真实外部仓库 Full Mode。

**操作**:

1. 新增 `repository_intelligence.evidence`
   - `ActiveRepositoryContext`
   - `FileEvidenceRequest`
   - `create_file_evidence()`
   - `create_repository_identity_evidence()`
   - `append_evidence()`
   - `read_evidence_index()`
   - `EvidenceMiddlewareError`

2. Evidence 行为
   - repository path 映射为 repository-relative path
   - 拒绝 absolute、`..`、backslash、symlink path escape
   - 计算 file SHA 和 line-range snippet SHA
   - RepositorySource 可生成 `repository_identity` evidence
   - Evidence Index 使用 JSONL append-only 写入
   - duplicate `evidence_id` 拒绝
   - `.env` 和私钥路径不进入 Evidence Index
   - credential-bearing URL 不进入 Evidence Index

3. 新增测试 `tests/test_repository_intelligence_evidence.py`
   - file evidence path / file SHA / snippet SHA
   - path escape 拒绝
   - symlink path 拒绝
   - identity evidence append / read
   - duplicate evidence 拒绝
   - `.env` evidence 拒绝
   - credential URL evidence 拒绝

**结果**:
- targeted tests 通过：`24 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- R3 当前是 Evidence Middleware skeleton；真实 Filesystem Tool、Permission Engine、ProcessTool 和 State Attestor 仍未实现

---

### 3.0-F T1 Generic Tool Contracts and Registry

**目标**: 按 3.1 计划启动 3.0-F Tool Foundation Addendum，先补 T1 通用 Tool contracts 和 registry。

**操作**:

1. 新增 `src/autoad_researcher/tools/`
   - `ToolSpec`：按计划文档字段固定 name、description、input_schema、output_schema、read_only、destructive、concurrency_safe、deferred、permission_category
   - `ToolRegistry`：注册、查找、require 和 deferred tool 列表
   - `ToolContext` / `ToolResult`：计划仅点名但未封字段，当前使用 `extra="allow"` 的占位合同，避免臆造工具结果结构

2. 安全校验
   - `ToolSpec` 拒绝 extra field
   - `read_only=True` 时拒绝 `destructive=True`
   - registry key 必须与 spec name 一致
   - duplicate tool spec 拒绝

3. 新增测试 `tests/test_tool_contracts.py`
   - ToolSpec 计划字段
   - extra field 拒绝
   - safety flag 冲突拒绝
   - registry register / require / deferred list
   - duplicate spec 拒绝
   - unsealed ToolContext / ToolResult extension fields 保留

**结果**:
- targeted tests 通过：`7 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- T2 deferred loading、T3 PermissionEngine、T4 ProcessTool、T5 Filesystem Tools 尚未实现

---

### 3.0-F T2 Deferred Tool Loading

**目标**: 实现 Tool Foundation 的 deferred tool loading，使 Web/GitHub 等工具 schema 不进入初始上下文，只在允许 stage 加载并记录审计 SHA。

**操作**:

1. 新增 `src/autoad_researcher/tools/deferred.py`
   - `ToolStage`
   - `STAGE_ALLOWED_TOOLS`
   - `LoadedToolRecord`
   - `StageToolLoad`
   - `initial_tool_specs()`
   - `load_stage_tool_specs()`

2. Stage loading 规则
   - initial context 排除 `deferred=True` 工具
   - discovery 加载 `web_search` / `web_fetch` / `github_read`
   - analysis 加载 filesystem tools 和 `process`，不加载 Web/GitHub
   - synthesis 不加载新工具
   - 每个 loaded tool 记录 `tool_name`、`stage`、`trigger_reason`、`schema_sha256`、`loaded_at`

3. 新增测试 `tests/test_deferred_tools.py`
   - deferred tools 不进入 initial context
   - discovery stage 加载 discovery deferred tools
   - analysis stage 不加载 Web/GitHub
   - synthesis stage 不加载新工具

**结果**:
- targeted tests 通过：`11 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- T3 PermissionEngine、T4 ProcessTool、T5 Filesystem Tools 尚未实现

---

### 3.0-F T3 Layered Permission Engine

**目标**: 实现 Tool Foundation 的分层 PermissionEngine 骨架，支持 allow / ask / deny 和 permission decision 审计。

**操作**:

1. 新增 `src/autoad_researcher/tools/permissions.py`
   - `PermissionProfile`
   - `PermissionRequest`
   - `PermissionDecisionRecord`
   - `PermissionEngine`
   - `default_repository_permission_engine()`
   - `append_permission_decision()`

2. 决策顺序
   - global deny
   - missing profile deny
   - profile deny
   - profile allow
   - session grant
   - ask tools / destructive tool ask
   - default deny

3. 默认 repository profiles
   - `repository_discovery`
   - `repository_acquisition`
   - `repository_analysis`
   - `repository_synthesis`

4. 新增测试 `tests/test_permission_engine.py`
   - global deny 优先于 profile allow
   - discovery 阶段拒绝 process
   - analysis 阶段拒绝 Web/GitHub，允许 filesystem read
   - missing profile 拒绝
   - session grant 可允许未列工具
   - destructive tool 默认 ask
   - permission decision JSONL append

**结果**:
- targeted tests 通过：`18 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- T4 ProcessTool、T5 Filesystem Tools、T6 stage-driven skill loading 尚未实现

---

### 3.0-F T4 Audited argv-based ProcessTool

**目标**: 实现 Tool Foundation 的 argv-based `process` tool，保证 `shell=False`、权限先行、stdout/stderr/timeout 可审计。

**操作**:

1. 新增 `src/autoad_researcher/tools/process.py`
   - `ProcessToolRequest`
   - `ProcessToolOutput`
   - `ProcessToolResult`
   - `process_tool_spec()`
   - `run_process_tool()`

2. 行为边界
   - 输入必须是 `argv: list[str]`
   - 拒绝空 argv entry 和 NUL byte
   - `cwd_label` 使用 relative path 校验
   - 执行前调用 `PermissionEngine`
   - permission 非 allow 时返回 `blocked`，不运行 subprocess
   - subprocess 固定 `shell=False`
   - 捕获 stdout/stderr/exit code
   - timeout 返回 `timed_out`

3. 新增测试 `tests/test_process_tool.py`
   - argv 执行并捕获 stdout
   - non-zero exit 返回 failed
   - permission deny 阻止执行
   - timeout
   - shell string / NUL 输入拒绝

**结果**:
- targeted tests 通过：`19 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- Git acquisition/analysis 子命令 allowlist、T5 Filesystem Tools、T6 stage-driven skill loading 尚未实现

---

### 3.0-F T5 Workspace-scoped Filesystem Tools

**目标**: 实现 Tool Foundation 的 workspace-scoped `filesystem_list/read/search/stat`，为后续 active repository context 和 evidence middleware 串联做准备。

**操作**:

1. 新增 `src/autoad_researcher/tools/filesystem.py`
   - `FilesystemRequest`
   - `FilesystemReadRequest`
   - `FilesystemSearchRequest`
   - `FilesystemEntry`
   - `FilesystemStat`
   - `FilesystemSearchMatch`
   - `FilesystemToolResult`
   - `filesystem_list/read/search/stat`

2. 行为边界
   - 所有路径必须是 workspace-relative
   - 拒绝 absolute、`..`、backslash
   - 拒绝 symlink path escape
   - 执行前调用 `PermissionEngine`
   - permission 非 allow 时返回 `blocked`
   - read 限制 `max_bytes`
   - search 使用 literal substring，不引入 regex 行为

3. 新增测试 `tests/test_filesystem_tools.py`
   - list/stat/read 工作区内文件
   - literal search
   - parent traversal 拒绝
   - symlink escape 拒绝
   - permission deny 阻止 read

**结果**:
- targeted tests 通过：`12 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- T6 stage-driven skill loading、T7 active repository tool context、T8 automatic evidence middleware 尚未实现

---

### 3.0-F T6 Stage-driven Skill Loading

**目标**: 将 R2 的 Skill parser/loader 接入 Tool Foundation 的实际 ToolRegistry 与 PermissionEngine，实现 stage-entry mandatory Skill loading。

**操作**:

1. 新增 `repository_intelligence.stage_loading`
   - `StageSkillLoadResult`
   - `load_stage_skill()`

2. 行为边界
   - stage/request 规则决定是否加载 mandatory Skill
   - explicit repository URL / local source 跳过 discovery Skill
   - 使用 `ToolRegistry` 中真实注册工具校验 `required_tools`
   - 使用 `PermissionEngine` 中真实 profile 校验 `permission_profile`
   - 可写入 `loaded_skills.json` audit

3. 新增测试 `tests/test_stage_skill_loading.py`
   - analysis stage 加载 mandatory Skill 并写 audit
   - explicit source 跳过 discovery
   - registry 缺 required tool 时 blocked
   - permission profile 缺失时 blocked

**结果**:
- targeted tests 通过：`10 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- T7 active repository tool context、T8 automatic evidence middleware、T9 providers 尚未实现

---

### 3.0-F T7 Active Repository Tool Context

**目标**: 将 `ToolContext.active_repository` 从占位扩展字段收紧为正式字段，并让 repository analysis 阶段的 filesystem tools 校验 active repository context。

**操作**:

1. 更新 `ToolContext`
   - 新增正式字段 `active_repository: ActiveRepositoryContext | None`
   - `ToolContext` 改为 `extra="forbid"`
   - `ToolResult` 仍保持 `extra="allow"`，等待具体工具结果合同继续收紧

2. 更新 filesystem tools
   - `FilesystemRequest` 新增 `tool_context`
   - 当 `stage=analysis`、`permission_profile=repository_analysis` 且存在 `active_source_id` 时，必须提供 `tool_context.active_repository`
   - active repository 的 `source_id` 必须匹配 `active_source_id`

3. 更新测试
   - ToolContext 接受 active repository
   - ToolContext 拒绝未封字段
   - repository analysis 缺 active repository context 时拒绝
   - active source mismatch 拒绝
   - matching active repository context 允许 filesystem read

**结果**:
- targeted tests 通过：`23 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- T8 automatic evidence middleware、T9 providers 尚未实现

---

### 3.0C-3 Commit 2B 收尾：结构化 lock 校验与可复制重建命令

**目标**: 修复 Commit 2B 封板前的两个阻塞问题：lockfile header 写成字面量 `\n`，以及 `verify.sh` 使用字符串包含检查关键包版本。

**操作**:

1. 修复 `requirements.lock.txt` header
   - 将字面量 `\n` 替换为真实换行和真实 shell 续行 `\`
   - 使 header 中的 `uv pip compile` 命令可以直接复制审阅

2. 同步 lockfile SHA
   - 新 SHA：`4b9699feb20019458c266d751de151686c41e8d30c1c61f0d27e5eb6d697e856`
   - 更新 `environment.yaml`
   - 更新 `evidence/benchmarks/patchcore_linux_gpu/lock_generation.json`

3. 增加结构化 pin 解析
   - 新增 `parse_lockfile_pins(path)`，基于 `packaging.Requirement` 和 canonical package name
   - 重复包名会抛 `ValueError`

4. 修复 `verify.sh`
   - 将字符串包含断言替换为结构化 pin 比较
   - 注释中出现的版本字符串不再能伪造关键版本校验

5. 补测试
   - `test_parse_lockfile_pins`
   - `test_comment_cannot_spoof_required_version`
   - `test_duplicate_package_pin_rejected`
   - `test_duplicate_index_name_rejected`
   - `test_duplicate_index_url_rejected`
   - `test_index_url_fragment_rejected`

**结果**:
- `tests/test_benchmark_environment_lock.py` 通过，31 passed
- 结构化解析确认关键 pin：torch / torchvision / timm / faiss-cpu
- `bash scripts/verify.sh` 通过，397 passed

**遗留问题**:
- 无

---

### Codex 日志日期修正

**目标**: 修正 Codex 本轮记录误用容器本地日期的问题，统一以用户确认的项目日期 `2026-06-17` 作为当天日志日期。

**操作**:

1. 将 Codex 新增的两条记录从 `notes/2026-06-16.md` 移到 `notes/2026-06-17.md`
   - `Codex 项目记忆初始化与仓库巡检`
   - `3.0C-3 Commit 2B 收尾：结构化 lock 校验与可复制重建命令`

2. 更新 `AGENTS.md` 日志规则
   - 当容器 `date` 与用户/会话日期不一致时，优先使用用户/会话日期
   - 必要时在日志中说明日期差异

**结果**:
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 无

---

### 3.0 执行计划文档入库

**目标**: 将 `docs/3.0计划文档.md` 纳入版本控制，作为 3.0 阶段后续开发和验收的执行依据。

**操作**:

1. 读取 `AGENTS.md`、`CLAUDE.md`、`notes/development-log.md` 和当日日志，确认工作流和日志规则
2. 读取 `docs/3.0计划文档.md` 全文，确认 3.0 退出条件和提交顺序
3. 保留文档原始内容，不在本提交中混入代码修改

**结果**:
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 后续按文档第 13 节从 `feat: add generic environment plan contracts` 开始实施

---

### 3.0C-4 Commit 1：通用 EnvironmentPlan 合同

**目标**: 建立 LLM 与后续 Environment Builder 之间的通用环境计划合同，不引入 PatchCore、PyTorch 或 CUDA 专属字段。

**操作**:

1. 新增 `src/autoad_researcher/environments/`
   - `models.py`：`EnvironmentPlan`、`EnvironmentTarget`、`EvidenceReference`、`PlanAssumption`、`CommandStep`、`ValidationStep`、`EnvironmentPermissions`、`EnvironmentPlanRevision`
   - `io.py`：`load_environment_plan()`、`write_environment_plan()`、`environment_plan_sha256()`
   - `__init__.py`：导出通用环境计划合同

2. 合同边界
   - `extra="forbid"`
   - `validation_steps.network` 固定为 `False`
   - `EnvironmentPermissions.max_revision_count` 限制在 `0..2`
   - 命令参数、cwd、环境变量拒绝 NUL byte
   - revision 与 parent plan 关系做结构化校验

3. 新增测试 `tests/test_environment_plan_models.py`
   - 合法 plan
   - extra field 拒绝
   - 空 evidence 拒绝
   - validation network 拒绝
   - NUL arg 拒绝
   - revision parent 规则
   - revision replacement parentage
   - SHA 稳定
   - JSON roundtrip
   - YAML load

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_plan_models.py -q` 通过，10 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 下一提交实现 policy gate 与三类 EnvironmentPlan fixture

---

### 3.0C-4 Commit 2：EnvironmentPlan Policy Gate 与三类 fixture

**目标**: 将 EnvironmentPlan 从“可解析 schema”推进到“可审查、可拒绝危险动作、可由 CI 离线校验”的计划合同。

**操作**:

1. 新增 `src/autoad_researcher/environments/policy.py`
   - `evaluate_environment_plan_policy()` 返回 `EnvironmentPlanPolicyReport`
   - `validate_environment_plan_policy()` 在 denied 时抛 `EnvironmentPlanPolicyError`
   - policy report 保存 `plan_sha256`

2. Policy Gate 覆盖
   - 环境目录必须位于 `workspace/envs`
   - repository 路径必须位于 `workspace/repos`
   - build cwd 必须位于 `workspace/repos` 或 `runs/{run_id}`
   - 拒绝 `sudo`、`su`、apt/yum/dnf/apk/brew/pacman 等系统安装入口
   - 拒绝 shell 元字符、cwd traversal、环境变量非白名单
   - repository modification 需要权限与审批
   - high risk assumption 必须绑定 validation
   - step id、validation id、assumption id 唯一
   - 至少一个 required validation

3. 新增三类 fixture
   - `fixtures/environment_plans/python_cpu_uv.yaml`
   - `fixtures/environment_plans/python_cuda_uv.yaml`
   - `fixtures/environment_plans/existing_python.yaml`

4. 更新 `scripts/verify.sh`
   - 检查环境计划模块和三类 fixture 存在
   - 离线加载每个 fixture 并运行 policy gate

5. 新增 `tests/test_environment_plan_policy.py`
   - 合法 plan
   - 三类 fixture
   - duplicate step id
   - no required validation
   - shell metacharacter
   - cwd traversal
   - environment path escape
   - system install
   - repository modification
   - build network permission mismatch
   - unapproved environment variable
   - high risk assumption without validation

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_plan_models.py tests/test_environment_plan_policy.py -q` 通过，22 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 下一阶段进入 3.0C-5：Environment Builder、executor 与 adapter

---

### 3.0C-5 Commit 1：Environment Executor 与 Build Step 证据

**目标**: 建立通用 Environment Builder 的底层执行与证据模型，使已通过 policy 的 build steps 可以用 `shell=False` 语义执行并保存 stdout/stderr、exit code、duration 和失败分类。

**操作**:

1. 新增 `src/autoad_researcher/environments/result.py`
   - `ResolvedCommand`
   - `CommandExecutionOutput`
   - `CommandStepResult`
   - `EnvironmentBuildResult`

2. 新增 `src/autoad_researcher/environments/executor.py`
   - `execute_resolved_command()`
   - 默认使用 `subprocess.run(..., shell=False)`
   - 支持 injected runner 便于离线测试
   - stdout/stderr 分文件保存
   - timeout 与非零退出码分别映射到 `ENV_COMMAND_TIMEOUT` / `ENV_COMMAND_FAILED`
   - 对 secret-like 环境变量值做日志 redaction

3. 新增 `src/autoad_researcher/environments/builder.py`
   - `run_environment_build_steps()`
   - 执行前调用 `validate_environment_plan_policy()`
   - 顺序执行 build steps
   - 遇到首个失败立即停止
   - 写出 `step_results.json` 与 `build_result.json`

4. 更新 `src/autoad_researcher/environments/__init__.py`
   - 导出 executor、result 和 build-step orchestration API

5. 新增测试
   - `tests/test_environment_executor.py`
   - `tests/test_environment_builder.py`

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_executor.py tests/test_environment_builder.py -q` 通过，8 passed
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_*.py -q` 通过，30 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 下一提交继续实现 adapter contract 与 uv/pip/existing/conda adapter 命令生成

---

### 3.0C-5 Commit 2：uv/pip/existing-python Adapter Contract

**目标**: 将 Environment Builder 从直接执行 `build_steps` 改为通过 adapter 翻译计划，为后续不同环境类型提供统一扩展点。

**操作**:

1. 新增 adapter 包
   - `src/autoad_researcher/environments/adapters/base.py`
   - `src/autoad_researcher/environments/adapters/uv_venv.py`
   - `src/autoad_researcher/environments/adapters/pip_venv.py`
   - `src/autoad_researcher/environments/adapters/existing_python.py`
   - `src/autoad_researcher/environments/adapters/__init__.py`

2. 新增 adapter registry
   - `get_environment_adapter("python_uv_venv")`
   - `get_environment_adapter("python_pip_venv")`
   - `get_environment_adapter("existing_python")`

3. 更新 Builder
   - `run_environment_build_steps()` 通过 adapter 准备 `ResolvedCommand`
   - `EnvironmentBuildResult.adapter` 使用 adapter kind

4. adapter 行为
   - `UvVenvAdapter` 要求 uv build step 和 environment_path
   - `PipVenvAdapter` 要求 python venv step 和 pip step
   - `ExistingPythonAdapter` 禁止 environment_path、联网、repo 修改和安装动作

5. 更新 `scripts/verify.sh`
   - 检查 builder、executor、result 和 adapter 文件存在

6. 新增测试 `tests/test_environment_adapters.py`
   - adapter 选择
   - unsupported kind 拒绝
   - uv 命令翻译
   - uv 缺失拒绝
   - pip 命令翻译
   - existing python 只读检查
   - existing python 安装拒绝

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_adapters.py tests/test_environment_builder.py tests/test_environment_executor.py tests/test_environment_plan_policy.py -q` 通过，27 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- Conda adapter contract 仍未实现
- EnvironmentSnapshot、真实 adapter smoke、validation registry 和 revision loop 仍待后续提交

---

### 3.0C-5 Commit 3：Conda Adapter Contract

**目标**: 补齐 3.0C-5 要求的 conda adapter 合同，但不要求 CI 或当前机器真实安装 conda/mamba/micromamba。

**操作**:

1. 新增 `src/autoad_researcher/environments/adapters/conda.py`
   - `CondaAdapter`
   - 支持 `conda` / `mamba` / `micromamba` 形式的显式 build step
   - 只验证和翻译 plan 中的命令，不做包版本或环境修复决策

2. 更新 adapter registry
   - `get_environment_adapter("conda")`

3. 更新导出
   - `src/autoad_researcher/environments/adapters/__init__.py`
   - `src/autoad_researcher/environments/__init__.py`

4. 更新 `scripts/verify.sh`
   - 检查 `adapters/conda.py` 存在

5. 扩展测试
   - conda adapter selection
   - unsupported adapter kind
   - micromamba create command translation
   - unsupported conda action 拒绝

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_adapters.py -q` 通过，9 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 3.0C-5 仍缺 EnvironmentSnapshot 和更完整的 BuildResult 成功条件
- 3.0C-6 仍缺 verifier registry 与 revision loop

---

### 3.0C-5 Commit 4：EnvironmentSnapshot 与 BuildResult 一致性

**目标**: 补齐 EnvironmentSnapshot 合同、稳定环境 SHA，并约束 BuildResult 的 success/failed 状态一致性。

**操作**:

1. 新增 `src/autoad_researcher/environments/snapshot.py`
   - `InstalledPackage`
   - `AcceleratorSnapshot`
   - `EnvironmentSnapshot`
   - `environment_snapshot_sha256()`
   - `snapshot_from_plan()`

2. 更新 `EnvironmentBuildResult`
   - success 必须有 `snapshot_path`
   - success 不允许 `failure_code` / `failure_message`
   - failed 必须有失败字段
   - failed 不允许 `snapshot_path`

3. 更新 `run_environment_build_steps()`
   - build steps 全部成功后写出 `snapshot.json`
   - build steps 失败时不伪造 snapshot

4. 更新导出与 verify gate
   - `src/autoad_researcher/environments/__init__.py`
   - `scripts/verify.sh`

5. 新增和扩展测试
   - `tests/test_environment_snapshot.py`
   - `tests/test_environment_builder.py`

6. 修正一次测试暴露的问题
   - 初稿将 BuildResult validator 错放在 `CommandStepResult` 内
   - 已移动到 `EnvironmentBuildResult`

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_*.py -q` 通过，42 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- Snapshot 当前使用 plan-declared target facts；真实 package inventory / accelerator observation 将在 verifier 和 snapshot probe 后续提交中补强

---

### 3.0C-6 Commit 1：Environment Verifier Registry

**目标**: 将“环境是否可用”从主观判断推进到可扩展、确定性的验证 registry，并输出 `ValidationReport`。

**操作**:

1. 新增 `src/autoad_researcher/environments/validation.py`
   - `ValidationContext`
   - `ValidationResult`
   - `ValidationReport`
   - `VERIFIERS`
   - `validate_environment()`

2. Verifier 初版覆盖九类 validation kind
   - `runtime_version`
   - `package_inventory`
   - `python_import`
   - `command`
   - `file_exists`
   - `repository_clean`
   - `gpu_available`
   - `gpu_compute`
   - `project_smoke`

3. 设计边界
   - verifier 只消费 plan 声明和 context 观测值
   - 不做 PyTorch/CUDA 版本选择
   - required validation 失败会使 report failed
   - optional validation 失败只记录，不使 report failed
   - report 计算稳定 `report_sha256`

4. 更新导出与 verify gate
   - `src/autoad_researcher/environments/__init__.py`
   - `scripts/verify.sh`

5. 新增测试 `tests/test_environment_validation.py`
   - registry kind 完整性
   - runtime/import pass
   - required failure
   - package inventory mismatch
   - optional failure
   - repository clean + gpu compute
   - command + file exists

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_*.py -q` 通过，49 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 尚未将 verifier 集成进 Builder 成功路径
- 尚未实现 revision context 和 bounded revision loop

---

### 3.0C-6 Commit 2：Bounded EnvironmentPlan Revision Loop

**目标**: 建立环境失败后的结构化修订入口和有限修订循环，确保最多 revision 2 次，不允许无限自修。

**操作**:

1. 新增 `src/autoad_researcher/environments/revision.py`
   - `EnvironmentRevisionContext`
   - `EnvironmentAttemptOutcome`
   - `EnvironmentRevisionLoopResult`
   - `build_revision_context()`
   - `run_bounded_revision_loop()`

2. Revision loop 规则
   - attempt outcome 必须匹配当前 plan_id / revision
   - failed outcome 必须包含 failure_code / failure_message
   - replacement plan 必须 parent 指向当前 plan
   - replacement revision 必须递增 1
   - replacement revision 不得超过 `permissions.max_revision_count`
   - provider 返回 `None` 时停止并返回 failed

3. Revision context
   - 从 failed build result 提取 failed_step、failure_code、failure_message
   - 从 failed validation report 提取 failed_validations
   - 生成 suggested_evidence，不提供具体修复答案

4. 更新导出与 verify gate
   - `src/autoad_researcher/environments/__init__.py`
   - `scripts/verify.sh`

5. 新增 `tests/test_environment_revision.py`
   - 一次 revision 后成功
   - 达到 revision limit 后停止
   - provider 返回 None 后停止
   - bad parent 拒绝
   - build + validation failure context

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_*.py -q` 通过，54 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 下一步需要三类 fixture 的 build/validation 集成测试和 PyTorch/CUDA guide

---

### 3.0C-6 Commit 3：三类 Environment Fixture 离线集成测试

**目标**: 用 CPU uv、CUDA fixture、existing Python 三类 EnvironmentPlan fixture 验证 build step orchestration、snapshot、validation report 和失败证据链路。

**操作**:

1. 新增 `tests/test_environment_fixture_integration.py`
   - `python_cpu_uv.yaml`：fake runner build success + runtime/import validation
   - `python_cuda_uv.yaml`：fake runner build success + fake GPU context validation
   - `existing_python.yaml`：fake runner build success + runtime/import validation
   - build failure：首个失败 step 停止、保留 logs 和 `step_results.json`、不写 snapshot

2. 测试边界
   - 不真实安装 uv/pip/conda 环境
   - 不真实运行 CUDA/GPU
   - 只验证通用合同、证据和状态机

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_environment_*.py -q` 通过，58 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- PyTorch/CUDA planning guide 仍待补
- 后续工作包进入 AssetPlan / Runner 前，还需要更明确的阶段文档

---

### 3.0C-6 Commit 4：PyTorch/CUDA Environment Planning Guide

**目标**: 补齐 3.0C-6 要求的短指南，帮助 agent 生成 CUDA/PyTorch 环境计划，同时避免把版本矩阵或自动修复规则写进 Core。

**操作**:

1. 新增 `docs/agent-guides/pytorch-cuda-environment.md`
   - 区分 GPU driver、wheel CUDA runtime、system CUDA toolkit、compiler toolchain
   - 明确 evidence 要求
   - 明确 build planning 边界
   - 明确 validation planning 边界
   - 明确外部权重属于 AssetPlan，不属于环境验证

2. 更新 `scripts/verify.sh`
   - 检查 PyTorch/CUDA guide 文件存在

**结果**:
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 下一阶段进入 3.0D-1：通用 AssetPlan 与 Asset Manifest

---

### 3.0D-1 Commit 1：AssetPlan 与 AssetManifest 合同

**目标**: 建立通用外部资产计划和资产 manifest 合同，用于后续管理模型权重、checkpoint、tokenizer、index 等外部资产。

**操作**:

1. 新增 `src/autoad_researcher/assets/`
   - `models.py`
   - `io.py`
   - `__init__.py`

2. 新增合同模型
   - `AssetSource`
   - `AssetValidation`
   - `AssetRequirement`
   - `AssetPlan`
   - `AssetManifestEntry`
   - `AssetManifest`

3. 合同边界
   - `network_during_execution` 固定为 `False`
   - URL source 必须为 `https://`
   - local_path source 必须为安全相对路径
   - destination 必须位于 `assets/prepared`
   - asset_id 和 validation_id 不允许重复
   - failed manifest entry 必须有 failure_code
   - manifest SHA 计算时排除 `manifest_sha256`

4. 新增 IO / hash
   - `load_asset_plan()`
   - `write_asset_plan()`
   - `write_asset_manifest()`
   - `asset_plan_sha256()`
   - `asset_manifest_sha256()`

5. 更新 `scripts/verify.sh`
   - 检查 asset 合同模块存在

6. 新增 `tests/test_asset_contracts.py`
   - 合法 plan
   - execution network 拒绝
   - destination escape 拒绝
   - destination 非 assets/prepared 拒绝
   - http URL 拒绝
   - duplicate asset_id 拒绝
   - YAML load
   - plan JSON roundtrip
   - manifest hash 稳定
   - failed manifest entry failure_code
   - manifest JSON write

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_asset_contracts.py -q` 通过，11 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 下一提交实现受控 asset preparation 和 offline validation

---

### 3.0D-1 Commit 2：受控 Asset Preparation 与 Offline Validation

**目标**: 实现通用 Asset Builder，使外部资产可以在受控路径下准备、计算 SHA、离线验证并生成 `asset_manifest.json`。

**操作**:

1. 新增 `src/autoad_researcher/assets/builder.py`
   - `prepare_assets()`
   - `AssetFetcher`
   - `AssetProbe`

2. 受控准备逻辑
   - local_path source 从 `workspace_root` 解析
   - destination 写入 `run_dir/assets/prepared/...`
   - URL source 只有在 `network_during_prepare=True` 时允许
   - URL 下载支持 injected fetcher，测试不联网
   - manual/registry source 要求目标已存在
   - 防止 source/destination 路径逃逸
   - 已存在目标若 SHA 不匹配则失败，不覆盖

3. Offline validation
   - `sha256`
   - `file_exists`
   - `framework_load`
   - `cli_inspect`
   - `custom_probe`
   - probe 类验证通过 injected probe 执行，默认不伪造成功

4. Manifest 行为
   - required asset 失败记为 `failed`
   - optional asset 失败记为 `skipped`
   - prepared asset 保存 path、sha256、source、required、status
   - manifest SHA 稳定
   - 可写出 manifest JSON

5. 更新导出与 verify gate
   - `src/autoad_researcher/assets/__init__.py`
   - `scripts/verify.sh`

6. 新增 `tests/test_asset_builder.py`
   - local asset 成功
   - manifest 写出
   - SHA mismatch
   - existing destination 不覆盖
   - optional missing skipped
   - URL network permission
   - injected fetcher
   - required framework_load without probe fails
   - custom probe success

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_asset_contracts.py tests/test_asset_builder.py -q` 通过，20 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 下一阶段进入 3.0D-2：Controlled Experiment Runner

---

### 3.0D-2 Commit 1：Controlled Experiment Command Plan 与 Attempt Runner

**目标**: 建立通用实验命令计划和不可变 attempt runner，使实验执行具备 command SHA、stdout/stderr、output manifest、execution result 和失败分类。

**操作**:

1. 新增 `src/autoad_researcher/runner/models.py`
   - `ExperimentCommandPlan`
   - `ExperimentInputRefs`
   - `OutputManifestEntry`
   - `OutputManifest`
   - `ExperimentExecutionResult`

2. 新增 `src/autoad_researcher/runner/executor.py`
   - `experiment_command_sha256()`
   - `execute_experiment_attempt()`

3. Runner 行为
   - command 使用结构化 argv，不使用 shell 字符串
   - `network` 固定为 `False`
   - attempt output dir 已存在时拒绝
   - command SHA 与 input refs 不一致时 `preflight_failed`
   - timeout / 非零退出码分类为 `execution_failed`
   - repository fingerprint 变化分类为 `invalid_repository_mutation`
   - expected output 缺失分类为 `RUN_EXPECTED_OUTPUT_MISSING`
   - 成功时写出 `output_manifest.json` 和 `execution_result.json`

4. 更新 `src/autoad_researcher/runner/__init__.py`
   - 修正一次补丁错误：初稿把导出写进了 docstring，测试已暴露并修复

5. 更新 `scripts/verify.sh`
   - 检查 runner models/executor 存在

6. 新增 `tests/test_experiment_runner.py`
   - network=true 拒绝
   - shell metacharacter 拒绝
   - output dir 已存在拒绝
   - command hash mismatch
   - success evidence
   - nonzero exit
   - timeout
   - expected output missing
   - repository mutation

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_experiment_runner.py -q` 通过，9 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 下一提交继续补 metrics parser、scientific validity 和 attempt evidence summary

---

### 3.0D-3 Commit 1：Metrics Parser 与 Scientific Validity 合同

**目标**: 补齐真实 attempt 后置检查的基础能力：指标必须从结构化 source 文件解析，科研有效性必须基于 evidence 判断，不允许把缺指标或证据不足当成功。

**操作**:

1. 新增 `src/autoad_researcher/analysis/metrics.py`
   - `MetricParseSpec`
   - `ParsedMetric`
   - `MetricsReport`
   - `parse_metrics()`

2. Metrics Parser 行为
   - 只从 attempt 目录下的 JSON source 文件解析
   - 保存 source path 与 source SHA
   - 保存 dataset row、metric name、value、unit、required、parse status
   - required metric 缺失或非数值时 report failed
   - 不从 stdout/stderr 自然语言猜指标

3. 新增 `src/autoad_researcher/supervisor/validity.py`
   - `ValidityCheck`
   - `ScientificValidityReport`
   - `validate_scientific_contract()`

4. Validity 检查覆盖
   - execution success
   - repository fingerprint
   - environment / dataset / asset / command SHA
   - required metrics
   - category
   - baseline
   - seed
   - data leakage
   - execution network disabled by schema

5. 更新导出与 verify gate
   - `src/autoad_researcher/analysis/__init__.py`
   - `src/autoad_researcher/supervisor/__init__.py`
   - `scripts/verify.sh`

6. 新增 `tests/test_metrics_and_validity.py`
   - required metric parsed
   - missing required metric fails
   - non-numeric metric invalid
   - validity valid
   - missing required metric invalid
   - category missing insufficient evidence

7. 修正测试暴露的问题
   - `analysis/__init__.py` 和 `supervisor/__init__.py` 初稿导出被写进 docstring，已重写
   - `ParsedMetric.source_sha256` 缺省值修正为 `None`

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_metrics_and_validity.py -q` 通过，6 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 下一步需要 attempt summary / reproducibility report，并评估真实 PatchCore attempt 是否具备本机数据和运行条件

---

### 3.0D-4 Commit 1：Reproducibility Report 合同与比较器

**目标**: 建立 attempt_01 / attempt_02 的通用复现比较报告，覆盖 invariant、validity 和 required metrics tolerance。

**操作**:

1. 新增 `src/autoad_researcher/analysis/reproducibility.py`
   - `AttemptEvidenceSummary`
   - `InvariantCheck`
   - `MetricComparison`
   - `ReproducibilityReport`
   - `compare_attempts()`

2. 复现判定
   - 任一 validity 非 valid → `invalid`
   - required invariant 不一致 → `not_reproducible`
   - required metric 超出 tolerance → `not_reproducible`
   - 否则 → `reproducible`

3. Invariant 覆盖
   - repository fingerprint
   - case SHA
   - configuration SHA
   - environment SHA
   - dataset manifest SHA
   - asset manifest SHA
   - command SHA

4. 更新导出与 verify gate
   - `src/autoad_researcher/analysis/__init__.py`
   - `scripts/verify.sh`

5. 新增 `tests/test_reproducibility.py`
   - invariants + metric tolerance 内 → reproducible
   - environment 不同 → not_reproducible
   - metric tolerance 外 → not_reproducible
   - validity invalid → invalid

6. 修正测试暴露的问题
   - test helper 创建 raw 目录时增加 `parents=True`
   - missing metric comparison 不再使用 `inf`，保持模型有限数值约束

**结果**:
- `$HOME/.local/bin/uv run --extra dev pytest tests/test_reproducibility.py -q` 通过，4 passed
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 尚未生成真实 attempt_01 / attempt_02 精简 evidence
- 需要检查本机是否具备 PatchCore/MVTec bottle 真实运行条件

---

### 3.0D 真实 attempt readiness 收口

**目标**: 在进入 3.0E 或 3.1 前，明确真实 PatchCore/MVTec 双跑是否具备本地执行条件，并修补复现比较器的 required metric 对称性缺口。

**操作**:

1. 只读检查真实运行前置条件
   - GPU 可用：`nvidia-smi` 检测到 NVIDIA GeForce RTX 4090，driver 550.107.02
   - PatchCore 源码存在且 commit 为 `fcaa92f124fb1ad74a7acf56726decd4b27cbcad`
   - 该源码位于项目 workspace 外，且当前是 `refs/heads/main`，不满足 preflight 的 `workspace/repos` 与 detached HEAD 要求
   - `AUTOAD_INTERNAL_BENCHMARK_DATASET_ROOT` 未设置
   - 未发现 MVTec bottle 数据目录
   - 项目内 `workspace/repos`、`workspace/datasets`、`workspace/envs` 均不存在

2. 新增 readiness 证据
   - `evidence/experiments/internal_patchcore_mvtec_bottle_v1/readiness_report.json`
   - `docs/milestones/3.0D-real-attempt-readiness.md`
   - 结论为 `blocked`，明确禁止用 mock 结果生成 `attempt_01` / `attempt_02`

3. 修复 runner 类型定义
   - 将 `ExperimentRunner = callable` 改为 `Callable[[ResolvedCommand, Path], CommandExecutionOutput]`

4. 修复 reproducibility metric 比较
   - required metric 比较改为左右两侧 metric name 并集
   - 新增 `left_parse_status` / `right_parse_status`
   - 右侧独有 required metric 会使复现报告进入 `not_reproducible`

5. 更新 verify gate
   - 检查 readiness JSON 与 3.0D readiness 文档存在
   - 验证 readiness JSON 基本结构，且在无真实 preflight evidence 时不能标记 `attempt_01` 可执行

**结果**:
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 3.0D-3 / 3.0D-4 真实 attempt 仍被本地数据集、workspace repo、benchmark env 前置条件阻塞
- 不能封板 3.0；只能进入“等待真实运行条件补齐”的状态

---

### 3.0D 真实 attempt 前置条件推进：repo/env/asset 收窄

**目标**: 尽量补齐 3.0D-3 真实 attempt 的本地前置条件，并把剩余阻塞收窄到可证明的数据集问题。

**操作**:

1. 创建 ignored local workspace
   - `workspace/repos`
   - `workspace/datasets`
   - `workspace/envs`
   - `workspace/cache`

2. 准备 PatchCore repository
   - 克隆 `amazon-science/patchcore-inspection` 到 `workspace/repos/patchcore-inspection`
   - checkout 到 detached commit `fcaa92f124fb1ad74a7acf56726decd4b27cbcad`
   - repository preflight 通过

3. 准备 benchmark Python 环境
   - 使用 uv + Python `3.11.15`
   - 按 `configs/benchmarks/environments/patchcore_linux_gpu/requirements.lock.txt` 安装 66 个锁定依赖
   - environment preflight 通过：torch `2.5.1+cu124`、torchvision `0.20.1+cu124`、CUDA `12.4`、faiss `1.14.3`、timm `1.0.27`

4. 运行真实 preflight
   - `repository`: passed
   - `environment`: passed
   - `dataset`: failed，失败码 `DATASET_ROOT_ENV_MISSING`

5. 检查外部资产
   - PatchCore `wideresnet50` 会通过 torchvision `pretrained=True` 加载 ImageNet 权重
   - 实际权重文件为 `wide_resnet50_2-95faca4d.pth`
   - SHA256：`95faca4d11227dddf8633dbb5ff6c8a9003c1aa5b8945c73834b8007b10950b8`
   - 后续真实 attempt 必须通过 AssetPlan/AssetManifest 将该文件放入 attempt `TORCH_HOME`，不能在 execution 阶段联网下载

6. 核对 MVTec 数据来源
   - 官方页面要求填写 dataset 表单，并声明 CC BY-NC-SA 4.0 非商业条款
   - 公开文档中旧 mydrive 全量链接和 bottle 单类链接当前均返回 404
   - 本机未发现 `bottle/train/good`、`bottle/test`、`bottle/ground_truth`

7. 补齐 metrics parser 缺口
   - `MetricParseSpec` 新增 `source_format`
   - 支持从 PatchCore `results.csv` 按 row key / row value / metric column 解析真实指标
   - 新增 CSV 成功解析和缺列失败测试

**结果**:
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 真实 MVTec AD bottle 数据仍缺失；无数据集时不能运行 attempt_01 / attempt_02，也不能封板 3.0
- 数据集需由用户按许可流程提供到 `workspace/datasets`，或明确确认可使用的合法下载来源

---

### 3.0D 真实 attempt 执行入口收口

**目标**: 在数据集到位前补齐真实 PatchCore attempt 的可复用执行入口，避免后续临时拼命令。

**操作**:

1. 新增 subprocess experiment runner
   - `run_experiment_subprocess()`
   - 使用 `shell=False`
   - 捕获 stdout/stderr
   - 支持 timeout
   - 与现有 `execute_experiment_attempt()` 的 injected runner 签名一致

2. 新增内部 PatchCore attempt helpers
   - `build_patchcore_backbone_asset_plan()`
   - `build_patchcore_command_plan()`
   - `patchcore_metric_specs()`

3. 固定 PatchCore attempt 合同
   - `cwd`: `runs/{run_id}/{attempt}`
   - `TORCH_HOME`: `assets/prepared/torch`
   - `HF_HUB_OFFLINE=1`
   - `network=False`
   - expected output: `raw/autoad_internal_benchmark/internal_patchcore_mvtec_bottle_v1/results.csv`
   - command 只使用结构化 argv，不使用 shell 字符串

4. 补测试
   - subprocess runner 成功执行并写出 expected output
   - PatchCore asset plan 固定 wideresnet50 权重 SHA
   - PatchCore command plan 固定 offline env、dataset category 和 expected output
   - PatchCore metric specs 指向 CSV 结果源
   - 测试初稿使用 `python -c` 且包含 shell-like 分隔符，被 command plan 安全校验拒绝；已改为脚本文件输入，不放宽安全规则

5. 更新 verify gate
   - 检查 `src/autoad_researcher/benchmarks/patchcore_attempt.py` 存在

6. 修正循环导入
   - `runner/executor.py` 直接从 `environments.result` 导入 `CommandExecutionOutput` / `ResolvedCommand`
   - `benchmarks/__init__.py` 不再导出 PatchCore helper，避免导入 `benchmarks.hashing` 时触发 analysis/supervisor/runner 链式循环

**结果**:
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 仍缺真实 MVTec AD bottle 数据；执行入口就绪不等于 3.0D attempt 完成

---

### 3.0D 内部 PatchCore attempt 脚本化

**目标**: 将真实 attempt 的人工步骤收敛为一个受控脚本，并修正资产目录与 attempt 不可覆盖规则之间的冲突。

**操作**:

1. 修正 asset / attempt 目录关系
   - 权重资产放在 `runs/{run_id}/assets/prepared/...`
   - attempt cwd 仍为 `runs/{run_id}/{attempt}`
   - `TORCH_HOME` 改为 `../assets/prepared/torch`
   - 避免 asset preparation 预先创建 attempt 目录，从而破坏 runner 的 output dir 不可覆盖约束

2. 修正 repository after fingerprint
   - `execute_experiment_attempt()` 支持传入 `Callable[[], str]`
   - fingerprint probe 会在 runner 返回后执行
   - 新增测试确认 probe 在 runner 完成后才运行

3. 新增 `scripts/benchmark/run_internal_patchcore_attempt.py`
   - 执行 preflight
   - 写出 preflight evidence
   - 准备 backbone asset
   - 构造 `ExperimentInputRefs`
   - 调用 controlled runner
   - 解析 PatchCore CSV metrics
   - 写出 `validity_report.json`
   - 数据集缺失时只返回 `preflight_failed`，不生成成功 attempt evidence

4. 更新 verify gate
   - 检查内部 attempt runner 脚本存在

**结果**:
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 仍缺真实 MVTec AD bottle 数据；脚本会在 dataset preflight 阶段阻塞，不能绕过

---

### 3.0D 工程封板口径修正与 attempt 脚本收尾

**目标**: 根据 `f826c1f` 之后的 review，修复内部 PatchCore attempt 脚本的安全与证据问题，并把 MVTec 授权数据实跑从 3.0 Engineering Seal 中拆出。

**操作**:

1. 修复 `scripts/benchmark/run_internal_patchcore_attempt.py`
   - `run_id` 通过 `run_dir_path()` 校验后再生成 `runs/{run_id}` 路径
   - 新增 `--dataset-root`，并将同一份 resolved dataset root 同时用于 preflight 和 PatchCore command plan
   - command 中的 dataset path 从已校验的 `attempt_dir` 计算相对路径
   - attempt JSON 使用 `write_json_atomic()` 写入，拒绝覆盖
   - repository after fingerprint 只在 runner callback 中采集一次并复用
   - category / baseline 从 command args 推导，不再硬编码
   - 未实现 data leakage 检测时返回 `insufficient_evidence`，不伪造 passed

2. 补 schema 与配置
   - `BenchmarkDatasetAcquisition` 声明 MVTec AD 为 `user_provided`
   - 配置写明不自动下载、不再分发、用户必须接受许可

3. 补回归测试
   - 非法 `run_id` 不创建越界目录
   - preflight failed 只写 preflight evidence，不创建 attempt 目录
   - `--dataset-root` 同时驱动 preflight 和 command plan
   - required asset failed 时停止在 asset 阶段，不创建 attempt 目录
   - data leakage 未检测时 validity 为 `insufficient_evidence`

4. 修正文档口径
   - `docs/milestones/3.0D-real-attempt-readiness.md` 状态改为 `engineering_ready_dataset_user_provided`
   - `README.md` 和 `docs/internal_benchmark_case.md` 明确 MVTec AD 由用户提供，项目不下载、不提交、不再分发
   - `docs/3.0计划文档.md` 将 `3.0 Engineering Seal` 与 `MVTec Real Benchmark Evidence` 分离

**结果**:
- targeted tests 通过：`36 passed`

**遗留问题**:
- 待运行全量 `bash scripts/verify.sh`
- MVTec Real Benchmark Evidence 仍需用户提供授权数据后再补齐，不能用 mock 结果替代

---

### 3.0 Engineering Seal 文档提交

**目标**: 在 `fbffdf5` 工程实现完成后，补齐正式 3.0 seal 记录，并明确 3.1 的可启动范围与 Tool Foundation 缺口。

**操作**:

1. 新增 `docs/stages/3.0-final-report.md`
   - 记录 `stage: "3.0"`、`overall_status: sealed`
   - 记录工程实现提交 `fbffdf57ce86677979480e27ca690f41f487cab3`
   - 记录本地验证 `508 passed`
   - 记录 GitHub Actions run `27671468538`
   - 记录 MVTec real benchmark evidence 为 `pending_user_provided_dataset`，且 `blocks_step_3_1: false`
   - 将 `tool_foundation_status` 明确写为 `missing`

2. 更新 `docs/3.0计划文档.md`
   - 勾选 Seal 矩阵中的 pytest、verify、GitHub Actions、final report 和 sealed commit 记录
   - 增加 final report 路径和 sealed commit 解析规则

3. 纳入本地已有的 `docs/3.1开发计划文档.md`
   - 该文档是 3.1 R1-R3 与 Tool Foundation 并行建设的执行依据
   - 真实外部仓库 Full Mode 仍等待 Permission Engine、argv-based ProcessTool、Filesystem scope、Skill Loader、Evidence Middleware 和 State Attestor

**结果**:
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- sealed commit 的精确 SHA 只能在提交完成后由 Git 历史给出，不能自引用写入同一 commit 内容

---

### 3.0 Seal Commit SHA 回填

**目标**: 将纯文档 seal 提交的实际 SHA 回填到 3.0 final report 和计划文档。

**操作**:

1. 确认 seal 提交
   - commit: `d91f7088555b7faab7c1229fe8a7fbc2c889adea`
   - message: `chore: seal 3.0 engineering foundation`
   - GitHub Actions run `27672296068` 已通过

2. 更新记录
   - `docs/stages/3.0-final-report.md`
   - `docs/3.0计划文档.md`

**结果**:
- 待运行 `bash scripts/verify.sh`

**遗留问题**:
- 无
