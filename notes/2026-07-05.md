# 2026-07-05 Development Log

## Entry 1 — Restrict Server-Local Source Intake to Allowed Roots

### Goal

Add an allowlist boundary to server-local source intake so the Research Assistant can register known-safe local papers such as `/root/autodl-tmp/AI4S/2303.15140v2.pdf` without allowing arbitrary filesystem reads.

### Operations

- Added `get_allowed_local_source_roots()` in `src/autoad_researcher/ui/sources.py`.
- Default allowed root is `/root/autodl-tmp/AI4S`.
- Added `AUTOAD_ALLOWED_LOCAL_SOURCE_ROOTS` support with `:`-separated root paths.
- Updated `register_local_file_source()` so the resolved source path must live under an allowed root before copying.
- Kept browser upload and chat-input attachment flows unchanged; those still use in-memory uploaded files and do not depend on the server-local allowlist.
- Added tests for:
  - allowed local PDF registration;
  - default `/root/autodl-tmp/AI4S` root;
  - `AUTOAD_ALLOWED_LOCAL_SOURCE_ROOTS`;
  - allowlist-outside rejection;
  - symlink escape rejection;
  - directory and unsupported extension rejection;
  - browser upload and chat attachment compatibility through existing UI tests.
- Verified the real local paper path `/root/autodl-tmp/AI4S/2303.15140v2.pdf` can still be registered under the default allowlist.

### Result

Focused UI/source tests passed locally:

```text
$HOME/.local/bin/uv run pytest \
  tests/test_ui_sources.py \
  tests/test_ui_chat_prompts.py \
  tests/test_ui_research_chat_safety.py

73 passed
```

### Leftovers

- Run full `bash scripts/verify.sh`.
- Push with `bash scripts/verify_and_push.sh "fix: restrict server-local source intake to allowed roots"` if verification passes.
- Confirm GitHub Actions verify status after push.

## Entry 2 — ZG1 Task Naming and Task Picker UI

### Goal

Implement the first ZG1 task-management pass: allow users to create a new task with an optional human-readable name, rename an existing task without changing its artifact identity, and switch existing tasks from a sidebar picker instead of typing raw `run_id` values.

### Operations

- Extended `src/autoad_researcher/ui/task_profile.py` with:
  - safe task-name slug generation for internal `run_id`;
  - UI-created task profiles;
  - manual rename support that preserves the existing run directory;
  - one-level `runs/` task listing with profile/fallback metadata;
  - formatted task-picker labels.
- Updated `src/autoad_researcher/ui/app.py` so:
  - the sidebar lists existing tasks by display title and timestamp;
  - new tasks can be created from the sidebar or Run Config page;
  - existing task names can be renamed from the sidebar;
  - operational pages resolve artifacts from the active task picker selection;
  - task-scoped transient UI state is cleared when switching tasks.
- Added focused tests in `tests/test_ui_task_profile.py` for run_id generation, task profile creation, rename semantics, fallback listing, invalid profile fallback, ordering, and label formatting.

### Result

Focused verification passed locally:

```text
$HOME/.local/bin/uv run python -m compileall src/autoad_researcher/ui/app.py src/autoad_researcher/ui/task_profile.py
$HOME/.local/bin/uv run pytest tests/test_ui_task_profile.py

40 passed
```

### Leftovers

- Run full `bash scripts/verify.sh`.
- Push with `bash scripts/verify_and_push.sh "feat: add named task management UI"`.
- Confirm GitHub Actions verify status after push.

## Entry 3 — Harden Task Profile Fallback Handling

### Goal

Fix the ZG1 edge cases where a corrupt `ui_chat/task_profile.json` could still break the current task header or Research Assistant overview, and where legacy runs without a profile displayed differently in the task picker and current-task detail view.

### Operations

- Added `safe_load_task_profile()` as the UI-safe profile loading path.
- Updated task listing, task title lookup, task display info, rename recovery, and Research Assistant first-message naming checks to use the safe loader where fail-soft behavior is required.
- Kept strict `load_task_profile()` unchanged for callers/tests that need explicit parse or schema failures.
- Unified legacy/no-profile display so task picker and current-task detail both use the run directory name as the visible title.
- Added regression tests for corrupt profile fallback, legacy fallback display consistency, and Research Assistant overview behavior with an invalid profile.

### Result

Focused regression tests passed locally:

```text
$HOME/.local/bin/uv run pytest tests/test_ui_task_profile.py tests/test_ui_research_chat_safety.py

58 passed
```

### Leftovers

- Run full `bash scripts/verify.sh`.
- Push with `bash scripts/verify_and_push.sh "fix: harden task profile fallback handling"`.
- Confirm GitHub Actions verify status after push.

## Entry 4 — ZG1 Task Archive Instead of Destructive Delete

### Goal

Add a user-facing way to remove old tasks from the default task picker after reviewing Claude Code internals for session history, resume, rename, and clear behavior. The design should let users clean up the task list without physically deleting `runs/{run_id}` research artifacts.

### Operations

- Reviewed `references/coding-agents/claude-code-internals/claude-code-main` session/history patterns:
  - `/resume` lists persisted sessions and keeps session IDs as durable identity;
  - `/rename` stores display metadata separately from identity;
  - `/clear` creates a fresh session context instead of deleting historical logs;
  - prompt history uses skip/filter behavior for undone entries rather than rewriting the whole history file.
- Added non-destructive archive metadata at `runs/{run_id}/ui_chat/task_archive.json`.
- Extended task listing to hide archived tasks by default and include them when requested.
- Added sidebar controls:
  - checkbox to show archived tasks;
  - archive current task;
  - restore archived task.
- Kept artifact directories untouched; archive only changes picker visibility.
- Updated `docs/l3/ui_runbook.md` to describe task picker, rename, archive, and restore behavior.
- Added tests for archive marker writing, default hiding, restore, archived labels, corrupt archive metadata fallback, and current-task archive display info.

### Result

Focused UI task tests passed locally:

```text
$HOME/.local/bin/uv run python -m compileall src/autoad_researcher/ui/app.py src/autoad_researcher/ui/task_profile.py
$HOME/.local/bin/uv run pytest tests/test_ui_task_profile.py tests/test_ui_research_chat_safety.py

63 passed
```

### Leftovers

- Run full `bash scripts/verify.sh`.
- Push with `bash scripts/verify_and_push.sh "feat: archive tasks from task picker"`.
- Confirm GitHub Actions verify status after push.

## Entry 5 — Delete Archived Tasks by Moving Them to Trash

### Goal

Allow cleanup of many test tasks after they have been archived, while avoiding direct destructive deletion of `runs/{run_id}` artifacts.

### Operations

- Added `trash_archived_task()` in `src/autoad_researcher/ui/task_profile.py`.
- The delete flow requires an archive marker first; unarchived tasks are rejected.
- Archived task deletion moves the full run directory to `runs/.trash/{run_id}_{timestamp}`.
- Added collision handling for repeated trash target names.
- Updated the sidebar dangerous zone so users must:
  - show archived tasks;
  - select an archived task;
  - type the exact `run_id`;
  - click “删除已归档任务”.
- Updated `docs/l3/ui_runbook.md` with the cleanup flow for test tasks.
- Added tests for archived-task trash movement, unarchived rejection, target collision handling, and task-list cleanup after trashing.

### Result

Focused UI task tests passed locally:

```text
$HOME/.local/bin/uv run python -m compileall src/autoad_researcher/ui/app.py src/autoad_researcher/ui/task_profile.py
$HOME/.local/bin/uv run pytest tests/test_ui_task_profile.py tests/test_ui_research_chat_safety.py

66 passed
```

### Leftovers

- Run full `bash scripts/verify.sh`.
- Push with `bash scripts/verify_and_push.sh "feat: move archived tasks to trash"`.
- Confirm GitHub Actions verify status after push.

## Entry 6 — Simplify Archived Task Trash UX

### Goal

Remove the over-strict `run_id` typing confirmation from archived-task cleanup. Default generated IDs are long and not user-friendly, and the operation already requires the task to be archived and only moves the directory to `runs/.trash/`.

### Operations

- Removed `_delete_archived_task_confirm` UI state.
- Removed the sidebar text input that required users to type the full `run_id`.
- Renamed the action to “移到回收站”.
- Kept the underlying guard: only archived tasks can be moved to trash.
- Kept non-destructive behavior: the run directory is moved to `runs/.trash/{run_id}_{timestamp}`.
- Updated `docs/l3/ui_runbook.md` to describe the simpler cleanup flow.

### Result

Full verification passed locally:

```text
bash scripts/verify.sh

1738 passed
[verify] done.
```

### Leftovers

- Push with `bash scripts/verify_and_push.sh "fix: simplify archived task trash action"`.
- Confirm GitHub Actions verify status after push.

## Entry 7 — Make Archived Task Delete Physically Delete

### Goal

Correct the task cleanup semantics: in the UI, “delete” should mean physical deletion, not moving to `runs/.trash/`.

### Operations

- Replaced `trash_archived_task()` with `delete_archived_task()`.
- Updated the sidebar action from “移到回收站” to “删除已归档任务”.
- Kept the archive prerequisite so only explicitly archived tasks can be deleted.
- Changed deletion to physically remove `runs/{run_id}` with `shutil.rmtree()`.
- Updated tests to assert the task directory is removed and unarchived tasks are rejected.
- Updated `docs/l3/ui_runbook.md` so cleanup instructions no longer mention `.trash`.

### Result

Full verification passed locally:

```text
bash scripts/verify.sh

1737 passed
[verify] done.
```

### Leftovers

- Push with `bash scripts/verify_and_push.sh "fix: make archived task delete physical"`.
- Confirm GitHub Actions verify status after push.

## Entry 8 — Load UI API Key from Environment or Local Env File

### Goal

Avoid requiring users to re-enter the DeepSeek API key every time they open the Streamlit UI, while keeping the key out of git, logs, artifacts, and password widget state.

### Operations

- Added UI startup loading for `DEEPSEEK_API_KEY` from:
  - the process environment;
  - project-local `.env` when present.
- Kept `_api_key_raw` as the business session-state key so the Streamlit password widget remains decoupled from preflight and research chat behavior.
- Added source tracking for user-facing status text without displaying the secret value.
- Added a clear button that removes the key from the current Streamlit session and disables automatic reload for that session.
- Updated terminal reproduction commands to source `.env` and fail fast if `DEEPSEEK_API_KEY` is missing, instead of prompting with `read -s` each time.
- Updated L3 UI/demo docs to describe environment or `.env` based credentials.
- Added regression tests ensuring the UI can load from environment or `.env`, and generated terminal commands no longer prompt for the key.

### Result

Focused verification passed locally:

```text
$HOME/.local/bin/uv run python -m compileall src/autoad_researcher/ui/app.py
$HOME/.local/bin/uv run pytest tests/test_streamlit_api_key_state.py
git diff --check

4 passed
```

Full verification passed locally:

```text
bash scripts/verify.sh

1693 passed, 46 skipped
[verify] done.
```

### Leftovers

- Push with `bash scripts/verify_and_push.sh "fix: load ui api key from env file"`.
- Confirm GitHub Actions verify status after push.

## Entry 9 — Intent Alignment Alpha Grounding Repair

### Goal

Repair Alpha intent-alignment failures where Research Chat could overclaim source state, answer paper questions without parsed artifacts, lock vague "reproduce and use in my project" language into full reproduction too early, or blur task confirmation with execution approval.

### Operations

- Added `ResearchChatEvidenceContext` to partition Research Chat context into:
  - known facts;
  - candidate references;
  - uploaded but unparsed sources;
  - parsed paper evidence;
  - blocking gaps;
  - forbidden assumptions.
- Kept existing Source Intake schema/status names. Reference identifiers remain `user_provided_not_ingested` rather than adding a duplicate `reference_identifier` status.
- Added deterministic `response_guard` for:
  - paper-content claims without parsed paper artifacts;
  - repo-code claims without repo evidence;
  - execution promises without approval;
  - artifact-grounded questions without artifacts.
- Injected the structured evidence context into Research Chat system messages.
- Extended the intent prompt with explicit source-state, artifact-grounding, and ambiguous reproduction/transfer rules.
- Extended the deterministic assistant router/runtime with labels for reproduction/transfer ambiguity, method transfer, baseline reproduction, paper reproduction, and direct execution requests.
- Strengthened `ResearchTaskDraftV1` artifact creation by appending required boundary constraints through `constraints`, without changing the schema.
- Added `docs/assistant/intent_alignment_alpha_repair_report.md`.
- Added/extended tests for context building, response guarding, runtime routing, task boundary constraints, and Research Chat prompt injection.

### Result

Focused validation passed locally:

```text
$HOME/.local/bin/uv run python -m compileall src/autoad_researcher/assistant src/autoad_researcher/ui/research_chat.py src/autoad_researcher/ui/chat_prompts.py

$HOME/.local/bin/uv run pytest \
  tests/test_assistant_research_context_builder.py \
  tests/test_assistant_response_guard.py \
  tests/test_assistant_runtime_skeleton.py \
  tests/test_assistant_task_artifacts.py \
  tests/test_ui_chat_prompts.py

65 passed

$HOME/.local/bin/uv run pytest \
  tests/test_assistant_alpha_scenarios.py \
  tests/test_assistant_events.py \
  tests/test_assistant_session.py \
  tests/test_assistant_transition_policy.py \
  tests/test_assistant_probe.py \
  tests/test_ui_sources.py \
  tests/test_ui_research_chat_safety.py \
  tests/test_ui_intent_draft.py

110 passed
```

Full verification passed locally:

```text
bash scripts/verify.sh

1711 passed, 46 skipped
[verify] done.
```

### Leftovers

- Push with `bash scripts/verify_and_push.sh "fix: ground intent alignment alpha chat"`.
- Confirm GitHub Actions verify status after push.

## Entry 10 — Route Short PDF Parse Commands Before LLM Chat

### Goal

Fix the Alpha Research Chat failure where user messages like `读一下`, `读论文呀`, or a short confirmation after uploading one PDF fall through to normal LLM chat instead of triggering the paper-intelligence parse action.

### Operations

- Broadened parse action detection to include short natural language commands such as `读`, `看看`, and `打开`.
- Added short parse confirmation handling for single pending PDF context, including replies like `对啊`, `好的`, `可以`, and `开始`.
- Kept short confirmations without any pending PDF on the normal chat path to avoid global over-routing.
- Preserved explicit `sources/...pdf` routing and multi-PDF choice behavior.
- Added regression tests for:
  - `读一下` + single pending PDF;
  - `读论文呀` + single pending PDF;
  - `对啊` + single pending PDF;
  - `对啊` without pending PDF staying as chat.

### Result

Focused validation passed locally:

```text
$HOME/.local/bin/uv run python -m compileall src/autoad_researcher/ui/research_chat.py
$HOME/.local/bin/uv run pytest tests/test_ui_chat_prompts.py tests/test_ui_sources.py tests/test_ui_research_chat_safety.py

81 passed
```

Full verification passed locally:

```text
bash scripts/verify.sh

1715 passed, 46 skipped
[verify] done.
```

### Leftovers

- Push with `bash scripts/verify_and_push.sh "fix: route short pdf parse commands"`.
- Confirm GitHub Actions verify status after push.

## Entry 11 — Persist UI API Key Across Tasks with Local Env Save

### Goal

Fix the confusion where entering or switching tasks can still use a stale local API key, causing HTTP 401 errors such as an invalid masked key suffix. API keys should be user/machine-level UI credentials, not task-scoped artifacts.

### Operations

- Added `src/autoad_researcher/ui/api_key_store.py` for local credential loading/saving helpers.
- Kept API key loading order:
  - process environment `DEEPSEEK_API_KEY`;
  - project-local `.env`;
  - page manual input as current-session override.
- Added masked key display in the Run Config page so users can see source and suffix, e.g. `****ba28`, without exposing the full key.
- Added a Run Config button to save the current page/session key into project-local `.env` for future sessions.
- On save, also updates `os.environ["DEEPSEEK_API_KEY"]` for the current Streamlit process so new sessions in the same process do not keep using the stale key.
- Kept `.env` ignored and out of git.
- Updated the UI runbook with 401 troubleshooting guidance.
- Added tests for `.env` read/replace/append behavior, invalid save rejection, masked display, and UI source wiring.

### Result

Current local masked key source check:

```text
source=本地 .env mask=****ba28
```

Focused validation passed locally:

```text
$HOME/.local/bin/uv run python -m compileall src/autoad_researcher/ui/app.py src/autoad_researcher/ui/api_key_store.py
$HOME/.local/bin/uv run pytest tests/test_ui_api_key_store.py tests/test_streamlit_api_key_state.py
git diff --check

11 passed
```

Full verification passed locally:

```text
bash scripts/verify.sh

1722 passed, 46 skipped
[verify] done.
```

### Leftovers

- Push with `bash scripts/verify_and_push.sh "fix: persist ui api key in local env"`.
- Confirm GitHub Actions verify status after push.

## Entry 12 — Force Reparse Parsed PDFs from Research Chat

### Goal

Fix the Research Chat loop where a user asks to extract again after weak paper artifacts, but the request falls through to normal LLM chat and the assistant says it cannot run commands.

### Operations

- Added force-reparse intent detection for commands such as:
  - `你再提取一次`
  - `重新解析`
  - `重新提取`
  - `再解析`
  - `再读`
- Preserved the normal guard that already-parsed PDFs are not re-parsed for ordinary read requests.
- Allowed force reparse to route a single parsed PDF back to the `parse` action.
- Required user choice when multiple parsed PDFs exist.
- Kept `parsing` status protected from duplicate starts.
- Added regression tests for single parsed PDF force reparse, explicit parsed-path force reparse, and multiple parsed PDFs requiring choice.

### Result

Focused validation passed locally:

```text
$HOME/.local/bin/uv run python -m compileall src/autoad_researcher/ui/research_chat.py
$HOME/.local/bin/uv run pytest tests/test_ui_chat_prompts.py tests/test_ui_sources.py tests/test_ui_research_chat_safety.py
git diff --check

84 passed
```

Full verification passed locally:

```text
bash scripts/verify.sh

1725 passed, 46 skipped
[verify] done.
```

### Leftovers

- Push with `bash scripts/verify_and_push.sh "fix: allow paper artifact re-extraction"`.
- Confirm GitHub Actions verify status after push.

## Entry 13 — Intent Alignment Action Resolver Minimal Loop

### Goal

Implement the v1.4 minimal repair loop for Research Chat intent alignment without
rewriting core schemas, adding a database, or enabling clone/download/patch/real
experiment execution.

### Operations

- Read `/root/autodl-tmp/AI4S/参考/intent-alignment-repair-v1.4.md` and searched
  existing implementation with `rg` before editing.
- Added `src/autoad_researcher/assistant/intent_action.py` with:
  - `ResearchContextSnapshot`
  - `IntentSignal`
  - `ActionDecision`
  - material auto-action resolver
  - `ui_chat/research_context_snapshot.json`
  - `ui_chat/action_decisions.jsonl`
- Kept the existing source registry status enum unchanged; `parsing_in_progress`
  and `parsing_failed` are derived adapter statuses only.
- Routed Research Chat material actions through snapshot → signal → resolver →
  response mode before falling back to normal LLM chat.
- Added deterministic paper artifact quality checks so empty or garbled paper
  artifacts are not treated as usable parsed evidence.
- Kept repo URL/name as reference-only; no automatic clone or repository
  analysis is triggered.
- Kept execution requests blocked at intent alignment; no patch, benchmark, or
  pipeline execution is triggered.
- Added 15 P0 scenario tests plus one action-decision audit test.

### Result

Focused validation passed locally:

```text
$HOME/.local/bin/uv run pytest \
  tests/test_assistant_intent_action.py \
  tests/test_assistant_research_context_builder.py \
  tests/test_assistant_response_guard.py \
  tests/test_ui_chat_prompts.py -q

68 passed
```

Full verification passed locally:

```text
bash scripts/verify.sh

1741 passed, 46 skipped
[verify] done.
```

### Leftovers

- Push with `bash scripts/verify_and_push.sh "fix: add intent alignment action resolver"`.
- Confirm GitHub Actions verify status after push.

## Entry 14 — Close Intent Alignment Auto-Parse PR Gate Gap

### Goal

Make PR #1 pass the project verification gate before merging it into `main`.

### Operations

- Checked PR #1 local diff against `main`; it touched only:
  - `src/autoad_researcher/assistant/intent_action.py`
  - `src/autoad_researcher/ui/research_chat.py`
  - `tests/test_assistant_intent_action.py`
- Restored missing local `uv` executable with the official standalone installer because
  `scripts/verify.sh` could not find `uv` and the old `.venv` pointed at a missing
  Python installation.
- Ran `bash scripts/verify.sh` on the PR branch.
- Investigated the single failing test:
  `tests/test_ui_chat_prompts.py::TestChatInputAttachments::test_attachment_added_reply_names_files_without_paths`.
- Updated `build_attachment_added_reply()` so the attachment acknowledgement still
  names uploaded files without stored paths and also prompts the user with
  `读一下这个论文`.

### Result

- Initial verification reached pytest and failed with:

```text
1 failed, 1742 passed, 47 skipped
```

- Failure reason: the attachment-added reply omitted the expected next-step
  parse prompt.

### Leftovers

- Rerun full `bash scripts/verify.sh`.
- Push the PR branch with
  `bash scripts/verify_and_push.sh "fix: restore attachment parse prompt"`.
- Merge PR #1 into `main` after local verification passes.
- Confirm `main` status and GitHub Actions status after merge.

## Entry 15 — AutoAD Research Assistant v0.4 Code Baseline Read

### Goal

Execute Section 73 step 1 from `参考/用户意图/开发计划.md`: read the mandatory
real code definitions before implementing P0-1 through P0-9, and record the
current contracts without guessing identifiers, enum values, fields, or paths.

### Operations

- Read the project `AGENTS.md` and the v0.4 hard-constraint sections 54-73.
- Read the mandatory source list:
  - `src/autoad_researcher/ui/sources.py`
  - `src/autoad_researcher/assistant/intent_action.py`
  - `src/autoad_researcher/assistant/prompt_selector.py`
  - `src/autoad_researcher/assistant/prompt_registry.py`
  - `src/autoad_researcher/assistant/prompt_profiles.py`
  - `src/autoad_researcher/tools/providers.py`
  - `src/autoad_researcher/tools/registry.py`
  - `src/autoad_researcher/tools/contracts.py`
  - `src/autoad_researcher/core/events.py`
  - `src/autoad_researcher/paper_intelligence/orchestrator.py`
  - `src/autoad_researcher/paper_intelligence/evidence_models.py`
  - `src/autoad_researcher/repository_intelligence/acquisition.py`
  - `src/autoad_researcher/repository_intelligence/evidence_models.py`
  - current `paper/parse/parse_quality_report.json` generation logic
  - current `context/research_context_draft.json` generation logic
  - UI chat flow in `src/autoad_researcher/ui/research_chat.py`
- Also read the actual tool loading/execution boundary files needed to locate
  the backend ToolGuard insertion point:
  - `src/autoad_researcher/tools/deferred.py`
  - `src/autoad_researcher/tools/permissions.py`
  - `src/autoad_researcher/tools/process.py`
  - `src/autoad_researcher/tools/filesystem.py`
  - `src/autoad_researcher/research_context/models.py`
  - `src/autoad_researcher/assistant/research_context_builder.py`
- Found pre-existing uncommitted work before this v0.4 step:
  - `src/autoad_researcher/ui/research_chat.py`
  - `scripts/smoke/`
  These were temporarily stashed as `pre-v0.4-existing-worktree` so this first
  step can commit only the required baseline notes.

### Current Definitions Extracted

`ui/sources.py`:

- `SourceStatus` values are exactly:
  `uploaded_not_parsed`, `user_provided_not_ingested`, `parsing`, `parsed`,
  `failed`.
- `SourceKind` values are exactly:
  `paper_pdf`, `text`, `markdown`, `github_repo`, `arxiv_id`, `doi`, `url`.
- `source_references.json` currently defaults to:
  `{"schema_version": 1, "sources": []}`.
- `append_source_ref()` currently writes each source entry with:
  `source_id`, `kind`, `user_label`, `status`, `stored_path`, `created_at`.
- `update_source_status()` only updates `status`, optional `error_message`,
  and removes `error_message` when status is not `failed`.
- There is no current `intake_status`, `intake_error`,
  `active_parse_attempt_id`, or `parse_attempts`.

`assistant/intent_action.py`:

- `SourceDerivedStatus` values are:
  `reference_identifier`, `uploaded_not_parsed`, `parsing_in_progress`,
  `parsed`, `parsing_failed`.
- `SelectedAction` values are:
  `answer_directly`, `parse_uploaded_pdf`, `summarize_parsed_artifacts`,
  `refresh_research_context_snapshot`, `update_intent_draft`,
  `ask_blocking_gap`, `confirm_research_task`, `block_execution_request`.
- `ResponseMode` has 13 exact values:
  `empty_run_intake`, `reference_only_status`, `uploaded_not_parsed_status`,
  `uploaded_status_then_auto_parse`, `material_auto_parse_started`,
  `select_pdf_to_parse`, `parsing_in_progress_status`,
  `parsing_failed_status`, `parsed_artifact_summary`,
  `parsed_artifact_insufficient`, `execution_request_blocked`,
  `research_task_confirmed`, `answer_directly`.
- `ActionDecision` fields include `snapshot_sha256`, `selected_action`,
  `response_mode`, `needs_user_confirmation`, `reason`, `execution_status`,
  optional source/path/status/error fields, and user-visible message/error.
- `render_response_for_decision(snapshot, decision) -> str` currently returns
  deterministic strings. It must stay string-compatible.

Prompt layer:

- `PromptSelector` maps `AssistantMode` to prompt IDs and stages; it does not
  currently inject tools.
- `_MODE_TO_PROMPT` maps:
  `goal_alignment`, `material_alignment`, `artifact_processing`,
  `intent_structuring`, `task_confirmation`, `pipeline_ready`,
  `progress_reporting`.
- `PromptRegistry` is static and builds prompt text by prepending
  `GLOBAL_INVARIANTS_TEXT`.
- `PromptProfile` fields are `prompt_id`, `prompt_version`, `layer`, `title`,
  `description`, `system_prompt`, `io`, optional `assistant_stage`,
  `visibility`, `source_references`, and `changelog`.

Tool layer:

- `ToolSpec` fields are `name`, `description`, `input_schema`,
  `output_schema`, `read_only`, `destructive`, `concurrency_safe`, `deferred`,
  and `permission_category`.
- `ToolRegistry` is an immutable-style Pydantic model with `register()`,
  `get()`, `require()`, and `deferred_tool_names()`.
- Existing providers include `SecureWebFetchProvider`,
  `RecordedWebSearchProvider`, and `GitHubReadProvider`.
- `tools/deferred.py` defines `STAGE_ALLOWED_TOOLS`; `discovery` allows
  `web_search`, `web_fetch`, `github_read`; `acquisition` and `analysis`
  currently include `process`.
- The central actual process execution entry is
  `run_process_tool(request, permission_engine=...)` in `tools/process.py`.
- `PermissionEngine.decide()` is the central permission policy check before
  process/filesystem tool execution.

Events:

- `ALLOWED_EVENT_TYPES` currently contains:
  `run_created`, `stage_started`, `stage_completed`, `stage_failed`,
  `artifact_written`, `artifact_read`.
- `EventRecord` fields are `event_type`, `run_id`, `timestamp`, and `payload`.
  There is no `metadata` field; extra fields are forbidden.

Paper intelligence:

- `PaperIntelligenceOrchestrator.run()` currently rejects rerun if
  `paper/evidence_index.jsonl` exists.
- Current parse output writes directly to:
  `paper/parse/parser_manifest.json` and
  `paper/parse/parse_quality_report.json`.
- There is no `paper/parse/attempts/{parse_attempt_id}/` history yet.
- Failed parse currently returns `status=parse_failed` and does not write a
  failed attempt quality report.
- `parse_quality_report.json` current observed schema has:
  `schema_version`, `status`, `page_count`, `empty_pages`, `scanned_pages`,
  `ocr_pages`, `low_confidence_pages`, `garbled_pages`,
  `table_parse_warnings`, `formula_parse_warnings`,
  `figure_parse_warnings`, `fatal_errors`.
- `EvidenceIndexRecord` in `paper_intelligence/evidence_models.py` currently
  has `schema_version` and `evidence`. The paper evidence refs already include
  `parse_attempt_id`, but the index record itself does not.

Research context:

- `emit_context_artifacts()` writes `context/research_context_draft.json` and
  `context/context_readiness_report.json`; when ready it also writes
  `context/research_context.json` and `context/idea_transfer_handoff.json`.
- `ResearchContext` currently includes task, sources, optional paper/repository
  contexts, facts, gaps, conflicts, readiness, `evidence_index_refs`, optional
  `previous_context_id`, and `context_sha256`.
- `ContextFact` currently uses `evidence_ids`; it does not include structured
  `source_id`, `parse_attempt_id`, `artifact_path`, `evidence_type`, or
  `support_level`.

Repository acquisition:

- `GIT_ACQUISITION_ALLOWED` values are exactly:
  `init`, `clone`, `remote`, `fetch`, `checkout`, `status`, `rev-parse`,
  `symbolic-ref`.
- `RepositoryAcquisitionRunner.acquire()` writes repository source,
  attestation, acquisition tool calls, permission decisions, and evidence
  records. It uses `run_process_tool()` for controlled git commands.

Research Chat UI:

- `_run_paper_intelligence(run_id, pdf_path) -> dict[str, str]` delegates to
  `uv run autoad paper-intelligence --run-id ... --pdf ... --json`.
- `build_pdf_parse_action()` builds an `ActionDecision` through
  `build_research_context_snapshot()`, `infer_intent_signal()`, and
  `resolve_material_auto_action()`.
- `_execute_or_report_pdf_parse_action(run_dir, action) -> str` updates source
  status to `parsing`, calls `_run_paper_intelligence()`, then marks source
  `parsed` or `failed` and appends action decisions.
- `guard_research_chat_reply()` is already called after LLM chat replies.
- `save_transcript()` is used for UI transcript writes.

### Result

The first v0.4 step produced a concrete baseline for P0 implementation:

- Source registry has no parse attempt fields yet.
- Parse quality is active-only; no attempt history exists.
- Research context uses flat `evidence_ids`, not structured evidence refs.
- Response rendering is string-returning and must remain compatible.
- Prompt selection does not inject tools today.
- Backend tool execution is permission-gated through `PermissionEngine` and
  `run_process_tool()`, not through `PromptSelector`.

### Leftovers

- Run `bash scripts/verify.sh`.
- Run `bash scripts/verify_and_push.sh "docs: record v0.4 assistant baseline"`.
- Confirm git status, latest commits, and GitHub status.
- Restore or intentionally integrate the pre-existing stash before touching
  `research_chat.py` or `scripts/smoke/` in later P0/P2 work.
- Start P0-1 only after this notes-only baseline step is pushed.

## Entry 16 — P0-1 SourceReference v0.4 Compatibility Fields

### Goal

Implement Section 73 P0-1 for the source registry without changing existing
`SourceStatus` semantics, without adding a parallel `source_type` field, and
without writing virtual legacy attempts back to disk.

### Operations

- Extended `SourceKind` in `src/autoad_researcher/ui/sources.py` with:
  `webpage`, `user_text`, `local_repo`.
- Added `IntakeStatus`, `ParseAttemptStatus`, and `LEGACY_PARSE_ATTEMPT_ID`.
- Updated new source registry entries to include:
  `intake_status`, `intake_error`, `active_parse_attempt_id`,
  `parse_attempts`.
- Added read compatibility for legacy entries that lack `parse_attempts`:
  parsed legacy sources expose an in-memory `legacy_active` attempt pointing at
  `paper/parse/parse_quality_report.json`.
- Added disk-write filtering so `legacy_active` is not persisted during normal
  registry saves or status updates.
- Added `append_source_parse_attempt()` as an append-only helper; it filters the
  virtual legacy attempt and never replaces existing attempts.
- Added focused tests in `tests/test_ui_sources.py` for:
  - v0.4 fields on new entries;
  - new `SourceKind` values;
  - legacy virtual attempt read compatibility;
  - no disk write of `legacy_active` after status update;
  - append-only parse attempts.

### Result

Focused validation passed:

```text
$HOME/.local/bin/uv run python -m compileall -q src/autoad_researcher/ui/sources.py tests/test_ui_sources.py
$HOME/.local/bin/uv run pytest tests/test_ui_sources.py -q

32 passed
```

### Leftovers

- Run full `bash scripts/verify.sh`.
- Push with
  `bash scripts/verify_and_push.sh "feat: extend source registry for parse attempts"`.
- Confirm git status, latest commits, and GitHub status.
- Continue to P0-2 only after this step is pushed.

## Entry 17 — P0-2 Run-Local ParseAttempt IDs and History

### Goal

Implement Section 73 P0-2: create run-local monotonic parse attempt IDs,
serialize parse attempts with a run-local lock, create
`paper/parse/attempts/{parse_attempt_id}/`, and append parse attempt history to
`source_references.json` for UI-registered sources.

### Operations

- Added `ParseAttemptHandle` and `_ensure_parse_attempt()` in
  `src/autoad_researcher/paper_intelligence/orchestrator.py`.
- Added run-local ID generation by scanning all source registry
  `parse_attempts[]` and choosing `pa_000001`, `pa_000002`, ...
- Added `sources/.parse.lock` with `O_EXCL`; stale/occupied lock returns:
  `parse attempt in progress for this run, retry after completion`.
- Matched a paper-intelligence request back to a UI source by comparing
  `request.paper_pdf_path` to registry `stored_path`.
- For UI-registered sources:
  - created a `running` attempt record before parsing;
  - wrote parse output under `paper/parse/attempts/{pa_id}/`;
  - appended final attempt state after parsing;
  - kept active compatibility copies under `paper/parse/`.
- Preserved the old direct CLI rerun guard when no matching source registry
  entry exists, so existing non-UI behavior remains fail-closed.
- Added `update_source_parse_attempt()` to update a single attempt without
  replacing the attempt list.
- Added tests for:
  - registered source reparse producing `pa_000001` then `pa_000002`;
  - attempts not overwriting each other;
  - `.parse.lock` collision rejection;
  - old direct CLI rerun block still covered by existing tests.

### Result

Focused validation passed:

```text
$HOME/.local/bin/uv run pytest tests/test_paper_intelligence_behavior.py tests/test_ui_sources.py -q

67 passed
```

### Leftovers

- Run full `bash scripts/verify.sh`.
- Push with
  `bash scripts/verify_and_push.sh "feat: record paper parse attempts"`.
- Confirm git status, latest commits, and GitHub status.
- Continue to P0-3 only after this step is pushed.
