# 2026-07-13 Development Log

## Entry 1 - Establish canonical control-plane stores

Goal:
- Start the minimum AutoAD control plane with one per-run mutation lock, durable canonical V2 event/job stores, semantic contract hashes, strict corruption handling, and safe run-relative paths.

Operations:
- Added `core.control_plane` typed errors, `fcntl.flock` run/event locks, durable atomic JSON/JSONL I/O, domain-separated hashing, path containment, strict event/job models and stores, a UnitOfWork boundary, and a read-only store validator.
- Migrated the V2 event and PipelineJob services to compatibility facades over the canonical stores while keeping their current function APIs for the not-yet-migrated Worker.
- Added versioned confirmation-draft and confirmed-contract semantic hash projections that exclude derived readiness/discovery state.
- Added focused tests for concurrent job ID allocation, request/event idempotency conflicts, strict corruption failures, lock reentry, path escape/symlink rejection, derived-state-insensitive contract hashes, and store validation.

Result:
- Focused control-plane and existing V2 event/confirmation/evidence regression suite passed: `48 passed, 2 skipped`.
- The existing user modification to `AGENTS.md` remains untouched and excluded from task commits through a temporary index-only skip-worktree marker.

Leftovers:
- Run the full verification gate, commit/push Step 1, then migrate Worker claim/fencing/lease/attempt behavior in Step 2.

## Entry 2 - Fence Worker claims and persist attempt history

Goal:
- Migrate the embedded and standalone Worker onto the canonical PipelineJobStore with durable claim identity, fenced terminal writes, bounded lease recovery, dependency reconciliation, and per-run audit degradation.

Operations:
- Added strict claim, attempt-result, and transition models plus exclusive durable writes for immutable attempt artifacts.
- Extended PipelineJobStore with stable eligible ordering, durable `claim.json` creation before running state, token/attempt/lease fences, lease renewal and maximum wall-time enforcement, stale-input and expired-lease backoff, orphan-claim recovery, and historical dependency validation.
- Migrated Worker job processing away from direct JSONL parsing; authoritative corruption now stops only the affected run, while corrupt audit events produce `events/audit_health.json` and do not block authoritative job completion.
- Added focused coverage for stable scheduling independent of JSONL order, claim/AttemptResult invariants, incorrect-token rejection, orphan recovery, lease retry exhaustion, stale-input hashes, dependency cycles, and audit/authoritative corruption isolation.

Result:
- Focused control-plane, Worker, and V2 evidence-route regression suite passed: `47 passed, 2 skipped`.

Leftovers:
- Run the full verification gate and push Step 2; then implement the Session and three-layer Readiness materialization boundary in Step 3.

## Entry 3 - Materialize freshness-fenced experiment readiness

Goal:
- Add the single ExperimentSession projection and deterministic planning/implementation/execution readiness materialization without inferring unconfirmed repository, environment, dataset, or protocol facts.

Operations:
- Added strict Session, resolver snapshot, readiness fact/layer, materialization input, authorization, readiness artifact, and materialization outcome models.
- Added idempotent Session plus `experiment_prepare` Job creation bound to the immutable confirmed-contract semantic hash; the prepare Job ID remains stable across claim, retry, recovery, and projection repair.
- Added an explicit local resolver protocol with a five-second collection deadline, bounded run-relative reads, evidence path/hash checks, versioned input fingerprints, and fail-closed implementation/execution layers when no resolver is configured.
- Added two-snapshot freshness publication, immutable attempt input/candidate artifacts, canonical readiness publication/no-op semantics, Session transitions, AttemptResult publication, and fenced Job completion inside the run UnitOfWork.
- Added `assert_readiness_current()` and Worker dispatch for `experiment_prepare`, plus crash projection repair from canonical Job and Readiness state.
- Added regression coverage for one Session/Job, contract replacement rejection, default fail-closed readiness, verified resolver layers, stale candidate rejection, post-publication producer changes, and Session repair.

Result:
- Focused control-plane, Worker, intent-contract, confirmation, and V2 evidence regression suite passed: `105 passed, 2 skipped`.

Leftovers:
- Run the full verification gate and push Step 3; then implement confirmation recovery, materialization request concurrency, API/UI surfaces, and event reconciliation in Step 4.

## Entry 4 - Complete confirmation, request, API, UI, and audit recovery

Goal:
- Close the minimum control plane with an immutable confirmation Saga, deterministic materialization-request concurrency, experiment control APIs/UI, and rebuildable terminal audit events.

Operations:
- Replaced Event-derived confirmation state with a durable `contract_confirmation.json` projection and semantic draft/contract hashes; approve/reject is idempotent, contract wins recoverable projection conflicts, hash disagreement is authoritative corruption, and the same run cannot replace its confirmed contract.
- Approval now durably writes the contract and ensures the unique ExperimentSession/prepare Job before appending audit events; corrupt audit events mark `repair_required` without rolling back authorization.
- Added strict `materialization_requests.jsonl` storage. Same request ID and parameters replay the first result, different parameters conflict, queued/running Jobs persist a deterministic `not_scheduled/job_already_running` result, and completed/failed Jobs reuse the original Job with explicit retry-counter reset.
- Connected request force/control IDs through claim, stale/lease recovery, publish/no-op, technical failure, and terminal request completion/failure.
- Added GET Session/Readiness state plus materialize and retry APIs; added frontend state, readiness-layer display, rematerialize/retry actions, and synchronized all populated semantic authorization fields across confirmation text, Draft API, and modal.
- Added terminal Event reconciliation from Contract, Session, Job, Readiness, and Request authority, plus authoritative validation before per-run audit health handling in the Worker.
- Added tests for confirmation recovery/idempotency, corrupt-audit authorization durability, queued 409 replay, request conflicts, same-Job rematerialization/no-op, explicit retry, and audit reconstruction.

Result:
- Focused backend regression suite passed: `125 passed, 2 skipped`.
- Frontend `tsc -b && vite build` passed. `oxlint` reported only the existing `useAutoScroll.ts` dynamic dependency-list warning.

Leftovers:
- Run the full verification gate and push Step 4, confirm GitHub Actions, then perform the frozen acceptance audit and document any remaining limitations.


## Entry 5 - Restore contract dialogue and safe reply boundaries

Goal:
- Repair the manually reproduced V2 chat regression where an inconsistent TurnGate decision skipped durable contract creation, a numeric improvement target was lost, and internal planner/fallback text reached the user.

Operations:
- Normalized every validated `update_contract` action into one coherent contract-routing decision so NeedDiscovery and durable Draft persistence cannot be skipped by contradictory model booleans.
- Preserved explicit percentage targets such as `提升5%` as user-sourced success criteria while recording that the user did not specify absolute percentage points versus relative improvement.
- Removed every direct `next_reply_instruction` return path, reduced the TurnGate context visible to ReplyPlanner to an allowlist, and replaced the generic fallback's `HF-2`, `plan_only`, raw state, and agent terminology with user-facing language.
- Kept the four registered V2 prompts separate, added pause/cancel clarification and research-context preservation guidance, and added reviewed full-content SHA256 tests for all four profiles.
- Rendered confirmation summaries with user-facing Chinese labels and execution-mode descriptions instead of internal enum/stage terminology.
- Added regression coverage for the exact contradictory TurnGate flags, numeric-target precedence over generic LLM output, the reported multi-turn PatchCore/MVTec/image-level-AUROC conversation, durable pending confirmation, and planner-instruction isolation.
- Updated evidence-route regressions to verify the same no-false-reading boundary through user-facing language instead of internal planner terminology.

Result:
- Focused TurnGate, NeedDiscovery, intent-contract, reply-planner, and PromptRegistry suite passed: `88 passed`.
- Full project verification passed: `2072 passed, 48 skipped`.
- The existing user-owned `AGENTS.md` modification remains untouched and must be excluded from the commit.

Leftovers:
- Run the full verification gate, precisely stage this entry and its implementation without `AGENTS.md`, push `p0.3-control-plane`, and confirm GitHub Actions before starting automatic task naming.


## Entry 6 - Wire production task naming and selected chat model

Goal:
- Generate a human-readable task title from the first contract-bearing research turn in the production V2 chat flow, without overwriting manual names or changing the canonical run ID.
- Stop discarding `X-AutoAD-Model`; use the frontend-selected model consistently across the four V2 LLM stages and task-title generation.

Operations:
- Added an explicit `task_naming_eligible` result from the structured TurnGate contract-update branch, avoiding keyword-based research-message classification.
- Reused the existing first-message naming prompt and generator; no registered V2 prompt text or prompt profile was changed.
- Added a per-run locked compare-and-replace operation that accepts only the persisted UI placeholder and rechecks it after the naming LLM call, so a concurrent or earlier manual rename always wins.
- Connected the production chat route to the generator, preserved transcript persistence before best-effort naming, and refreshed the frontend task list after each successful chat turn.
- Propagated the selected chat model through SourceActionPlanner, TurnGate, NeedDiscovery, ReplyPlanner, and the task-profile generator instead of replacing it with a hard-coded model.
- Added regressions for placeholder replacement, manual-title protection, route/header model forwarding, selected-model use across the planner chain, immediate naming eligibility, and production persistence.

Result:
- Focused task-profile, chat-route, intent-contract, NeedDiscovery, TurnGate, and SourceActionPlanner suite passed: `140 passed`.
- Full project verification passed: `2076 passed, 48 skipped`.
- Frontend `tsc -b && vite build` passed; Vite reported only the existing mixed dynamic/static import advisory for `src/lib/mock.ts`.
- The user-owned `AGENTS.md` modification remains untouched and must be excluded from the commit.

Leftovers:
- Push this stage to `p0.3-control-plane` and confirm GitHub Actions before starting file/link source regression.


## Entry 7 - Regress production file and link source ingress

Goal:
- Exercise the browser-facing raw file upload contract and the production chat-link intake path without mutating an existing user run or relying on a live LLM.

Operations:
- Added an ASGI regression that sends the same raw request body and percent-encoded `X-AutoAD-Filename` header as the frontend upload client.
- Verified a Markdown filename containing Chinese characters and a space is preserved, the file is stored under the temporary run, and direct-upload Evidence is immediately readable.
- Added parameterized chat endpoint regressions for an ordinary webpage and a GitHub repository URL with server config and API-key fallback disabled.
- Verified the structured URL path registers exactly one Source and creates `web_fetch` plus `web_markitdown`, or `git_clone` plus `repo_summarize`, without a live model call.
- Kept production source code unchanged because no file/link ingress defect was reproduced.

Result:
- Focused API ingress, Evidence route, and SourceActionPlanner suite passed: `46 passed, 2 skipped`.
- Full project verification passed: `2079 passed, 48 skipped, 4 warnings`.
- The only warnings were the existing FastAPI `on_event` deprecation notices.
- The user-owned `AGENTS.md` modification remains untouched and must be excluded from the commit.

Leftovers:
- Push the regression commit, confirm GitHub Actions, then restart the backend and frontend services on the updated branch for manual testing.
