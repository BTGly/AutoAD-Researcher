# 2026-07-13 Development Log

## Entry 1 - Establish canonical control-plane stores

Goal:
- Start the minimum AutoAD control plane with one per-run mutation lock, durable canonical V2 event/job stores, semantic contract hashes, strict corruption handling, and safe run-relative paths.

Operations:
- Added `core.control_plane` typed errors, `fcntl.flock` run/event locks, durable atomic JSON/JSONL I/O, domain-separated hashing, path containment, strict event/job models and stores, a UnitOfWork boundary, and a read-only store validator.
- Migrated the V2 event and PipelineJob services to compatibility facades over the canonical stores while keeping their current function APIs for the not-yet-migrated Worker.
- Added versioned confirmation-draft and confirmed-contract semantic hash projections that exclude derived readiness/discovery state.
- Added focused tests for concurrent job ID allocation, request/event idempotency conflicts, strict corruption failures, lock reentry, path escape/symlink rejection, derived-state-insensitive contract hashes, and store validation.

Result:
- Focused control-plane and existing V2 event/confirmation/evidence regression suite passed: `48 passed, 2 skipped`.
- The existing user modification to `AGENTS.md` remains untouched and excluded from task commits through a temporary index-only skip-worktree marker.

Leftovers:
- Run the full verification gate, commit/push Step 1, then migrate Worker claim/fencing/lease/attempt behavior in Step 2.

## Entry 2 - Fence Worker claims and persist attempt history

Goal:
- Migrate the embedded and standalone Worker onto the canonical PipelineJobStore with durable claim identity, fenced terminal writes, bounded lease recovery, dependency reconciliation, and per-run audit degradation.

Operations:
- Added strict claim, attempt-result, and transition models plus exclusive durable writes for immutable attempt artifacts.
- Extended PipelineJobStore with stable eligible ordering, durable `claim.json` creation before running state, token/attempt/lease fences, lease renewal and maximum wall-time enforcement, stale-input and expired-lease backoff, orphan-claim recovery, and historical dependency validation.
- Migrated Worker job processing away from direct JSONL parsing; authoritative corruption now stops only the affected run, while corrupt audit events produce `events/audit_health.json` and do not block authoritative job completion.
- Added focused coverage for stable scheduling independent of JSONL order, claim/AttemptResult invariants, incorrect-token rejection, orphan recovery, lease retry exhaustion, stale-input hashes, dependency cycles, and audit/authoritative corruption isolation.

Result:
- Focused control-plane, Worker, and V2 evidence-route regression suite passed: `47 passed, 2 skipped`.

Leftovers:
- Run the full verification gate and push Step 2; then implement the Session and three-layer Readiness materialization boundary in Step 3.
