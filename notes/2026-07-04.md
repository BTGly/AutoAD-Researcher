# 2026-07-04 Development Log

## Entry 1 — AutoAD Assistant Prompt Architecture and Registry Foundation

### Goal

Start the prompt engineering architecture work by treating prompts as a versioned AutoAD subsystem rather than continuing to tune a single Research Assistant prompt. The first step is documentation plus a minimal prompt registry, without changing the existing Stage 3 pipeline or UI execution behavior.

### Operations

- Read local prompt architecture references:
  - `/root/autodl-tmp/AI4S/参考/input_intent_processing_architecture_v0_2.md`
  - `/root/autodl-tmp/AI4S/参考/target_autonomous_ad_research_loop_draft_v0_3.md`
  - `/root/autodl-tmp/AI4S/references/system-prompts/_INDEX.md`
  - system-prompt overview files for major LLMs, coding agents, multi-agent automation, and research/search agents.
- Added `docs/prompts/autoad_assistant_prompt_architecture.md` defining:
  - Layer 0 Global Invariants
  - Layer 1 Assistant State Prompts
  - Layer 2 Schema-Bound Research Task Draft Prompts
  - Layer 3 Pipeline Specialist Prompts
  - Layer 4 User-Facing Progress Prompts
  - Prompt Registry contract and prompt versioning rules
- Added `src/autoad_researcher/assistant/` with:
  - `prompt_io.py` for `AssistantStage`, prompt layers, visibility, and strict prompt IO contracts
  - `prompt_profiles.py` for `PromptProfile` and global invariant text
  - `prompt_registry.py` for the in-memory prompt registry and built-in profiles
  - `__init__.py` exports
- Mapped existing `chat_prompts.py` prompts into the new registry:
  - `assistant.collecting_goal.v1`
  - `assistant.understanding_intent.v1`
  - `assistant.run_explanation.v1`
  - `assistant.next_experiment.v1`
- Added planned profiles for:
  - `assistant.research_task_draft.v1`
  - `assistant.progress_digest.v1`
- Added `tests/test_assistant_prompt_registry.py` covering prompt ID uniqueness, existing prompt mapping, global invariant inheritance, schema-bound draft contract, progress digest boundaries, and unsafe artifact path rejection.

### Result

Focused prompt registry tests passed locally: `10 passed`.
Full verification passed locally with `1541 passed`.

### Leftovers

- Push with `bash scripts/verify_and_push.sh "feat: add assistant prompt architecture registry"`.
- Confirm GitHub Actions verify status after push.
- Future work: add `AutoADAssistantSession`, state-machine transition checks, material guidance prompts, and task draft builder skeleton.

## Entry 2 — Prompt Registry Refinement from System Prompt Reference Audit

### Goal

Refine the AutoAD Assistant prompt foundation after a closer read of mature system-prompt references. Keep the work limited to prompt architecture, registry profiles, documentation, and tests; do not change Stage 3 pipeline execution, LLM calls, UI behavior, or approval gates.

### Operations

- Reviewed representative system-prompt references under `references/system-prompts/OPENAI`, `references/system-prompts/XAI`, `references/system-prompts/ANTHROPIC`, `references/system-prompts/CURSOR`, `references/system-prompts/MANUS`, and `references/system-prompts/_INDEX.md`.
- Added `docs/prompts/system_prompt_reference_analysis.md` to record structural prompt-engineering patterns worth borrowing for AutoAD:
  - layered instruction blocks;
  - tool and capability boundaries;
  - project/workspace instruction precedence;
  - event stream vs planner separation;
  - user-facing vs internal output separation;
  - evidence/freshness rules;
  - memory and persistence boundaries.
- Updated `docs/prompts/autoad_assistant_prompt_architecture.md` to point at repository-tracked prompt reference analysis and remove unstable local scratch-reference paths.
- Split the legacy intent prompt mapping in `src/autoad_researcher/assistant/prompt_registry.py` into dedicated state profiles:
  - `assistant.collecting_goal.v1`
  - `assistant.guiding_materials.v1`
  - `assistant.understanding_intent.v1`
  - `assistant.confirming_task_draft.v1`
- Kept existing progress prompts mapped without changing runtime behavior:
  - `assistant.run_explanation.v1`
  - `assistant.next_experiment.v1`
- Added registry tests to verify prompt split semantics, repo-stable source references, and preservation of existing progress prompt mappings.

### Result

Focused prompt registry tests passed locally: `12 passed`.
Full verification passed locally: `1543 passed`.

### Leftovers

- Run `bash scripts/verify.sh`.
- Push with `bash scripts/verify_and_push.sh "feat: refine assistant prompt profiles from reference audit"` if verification passes.
- Confirm GitHub Actions verify status after push.

## Entry 3 — Accept Assistant Intent Alignment Round 1 Control Schemas

### Goal

Audit and preserve the existing Round 1 implementation for the AutoAD Assistant v0.5 intent-alignment plan before starting further development. Round 1 establishes the control schemas only: AssistantEvent, AutoADAssistantSession, TransitionPolicy, and ResearchTaskDraftV1.

### Operations

- Read `参考/意图对齐/autoad_assistant_next_plan_integrated_v0_5.md` and `参考/意图对齐/仓库事实核查报告.md` for exact v0.5 constraints.
- Inspected the untracked Round 1 files:
  - `src/autoad_researcher/assistant/draft_schema.py`
  - `src/autoad_researcher/assistant/events.py`
  - `src/autoad_researcher/assistant/session.py`
  - `src/autoad_researcher/assistant/transition_policy.py`
  - `tests/test_research_task_draft_schema.py`
  - `tests/test_assistant_events.py`
  - `tests/test_assistant_session.py`
  - `tests/test_assistant_transition_policy.py`
- Confirmed the implementation matches the v0.5 boundaries:
  - events are coarse envelopes, not user behavior enumerations;
  - session stores minimal control state, not the full conversation form;
  - transition policy does not call LLMs or pipeline code;
  - ResearchTaskDraftV1 uses five research-task elements and forbids method-level extra fields.

### Result

Focused Round 1 tests passed locally: `54 passed`.
Full verification passed locally: `1597 passed`.

### Leftovers

- Run `bash scripts/verify.sh`.
- Push Round 1 as a separate commit before starting Round 2.
- Continue with Round 2: `SessionStore`, `PromptSelector`, and Propose-don't-interrogate prompt rewrite.

## Entry 4 — Implement Assistant Intent Alignment Round 2 Store and Prompt Selection

### Goal

Implement v0.5 Round 2 for the AutoAD Assistant intent-alignment plan: persist minimal assistant control artifacts, select prompts by coarse assistant mode, and rewrite existing prompt profiles toward Propose-don't-interrogate behavior without adding user-behavior prompt enumeration or connecting the Stage 3 pipeline.

### Operations

- Added `docs/architecture/autoad_assistant_router_state_policy.md` as the repository-tracked v0.5 architecture guide.
- Added `src/autoad_researcher/assistant/session_store.py` with fixed assistant artifact paths:
  - `assistant/session.json`
  - `assistant/events.jsonl`
  - `assistant/transitions.jsonl`
- Added `src/autoad_researcher/assistant/prompt_selector.py` with explicit `AssistantMode` to `AssistantStage` and prompt profile mappings.
- Updated `src/autoad_researcher/assistant/prompt_registry.py` to encode the Round 2 behavior contract:
  - Do not interrogate. Propose first.
  - Use WhatWeKnow when available.
  - Ask only blocking questions.
  - Keep Goal vs Approach separated.
- Updated assistant exports in `src/autoad_researcher/assistant/__init__.py`.
- Added focused tests for SessionStore and PromptSelector, and updated prompt registry tests for `ResearchTaskDraftV1`.

### Result

Focused assistant tests passed locally: `82 passed`.
Full verification passed locally: `1613 passed`.

### Leftovers

- Run `bash scripts/verify.sh`.
- Push Round 2 as a separate commit if verification passes.
- Continue with Round 3: `silent_probe` / `WhatWeKnow` and stable fixture tests.

