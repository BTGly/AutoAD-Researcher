# 2026-07-25 开发日志

## 会话资料上下文与正式实验资料解耦

- 目标：让用户提供的本地仓库、数据集、论文或参数文件先进入当前对话的只读上下文，支持 Agent 调查和追问；不在探索阶段写入正式 source registry 或创建资料 Pipeline Job。
- 实现：新增 `context/session_context.json` 轻量记录，复用现有本地路径安全检查和有限结构扫描；消息中的绝对路径与结构化附件先由后端检查，当前 Run 的 `workspace` 自动允许，外部路径仍遵守 `AUTOAD_ALLOWED_LOCAL_SOURCE_ROOTS`。
- 对话：Orchestrator 合并确定性路径与模型路径指令并去重，receipt 明确显示 `context_attached`、`context_already_attached` 或结构化失败码；只读资料检查器可在会话上下文范围内复用已有 filesystem 工具。正式 URL/GitHub source intake 保持不变。
- 测试：新增会话上下文、幂等、路径提取和 workspace 安全回归；更新本地路径对话测试以断言不创建 source registry 和 dataset manifest job。定向回归 `68 passed`。
- 遗留：本次只提供探索层接入，尚未把会话上下文自动 promote 为正式实验 Source；本地验证后再决定实验准备入口的 promote 交互。

## UAT 对话边界修复

- 目标：修复真实 UAT 中 sibling Run workspace 被误拒、同轮路径重复报错、历史执行门禁劫持普通询问和研究目标的问题。
- 设计：保留 `AUTOAD_ALLOWED_LOCAL_SOURCE_ROOTS`，并额外允许 `AUTOAD_RUNS_ROOT` 下明确命名的 sibling `workspace/`；不放开整个 runs 根目录。为 Decision 增加 `current_turn_intent` 语义字段，上一轮摘要只作背景；Gate 依据当前回合意图清除陈旧动作或降级错误的 act 候选。
- 对话：当前回合路径先进入一个去重集合，模型再次返回同一路径不重复 attach；回答/讨论/准备不进入缺失 `input_task.yaml` 的正式执行门禁。对话转移记录持久化当前回合意图，便于审计和恢复。
- 测试：增加 sibling workspace 安全边界、同轮重复路径、研究目标和失败解释回归；定向回归 `71 passed`。
- 遗留：完整门禁和真实服务 UAT 仍待本轮验证；未提交、未推送。

## 本地路径 intake 与资料任务链路

- 目标：修复用户同时提供本地仓库、数据集等路径时只生成研究摘要、没有独立 Source/资料 Job/任务草案的问题；保持既有来源动作、实验执行和报告链路不变。
- 设计：复用现有 Decision、DialogueGate、Material Inspection Loop、Source registry 和 Pipeline Job 控制面。`local_path_sources` 是主字段，旧的 `local_path_source` 仅作兼容；每个路径独立检查、登记、排队并写入 action receipt，用户描述只作为提示，不作为类型事实。
- 本地资料：结构检查增加图像文件证据、递归深度和总扫描上限，MVTec 这类嵌套图像目录可映射为 dataset；每个 dataset 额外排队 `dataset_manifest` Job，Worker 生成独立 manifest。仓库仍走现有 acquire/summarize，混合目录仍保留通用 local_path 视图。
- 任务状态：资料部分成功时不丢弃其他 Source；有失败资料时仍可保存 `blocked_by_materials` 的 plan-only 任务草案，但确认接口明确拒绝，避免把“草案已保存”误报为可执行任务。
- 回复与 API：回复和 `ChatResponse` 暴露 receipt 驱动的资料状态；只有真实回执才能声明登记或 Job 排队，失败路径保留具体阻塞原因。
- 验证：基础 Python 语法编译通过；定向回归 `39 passed`；完整门禁第一次受机器瞬时负载影响，仅既有 attempt worker 非阻塞时间断言失败，隔离重跑 `1 passed`；随后完整 `scripts/verify.sh` 通过，`1986 passed, 2 skipped`，开发日志索引检查通过。

## 后台任务与主对话同步闭环

- 目标：让研究者提交的本地仓库、数据集等资料立即成为正式 Source 和可见 Pipeline Job；后台处理期间仍可继续对话；完成或失败结果以可恢复的助手消息进入主聊天和后续上下文。
- 实现：本地路径先保留受限只读会话检查，再以实际目录检查结果 promote 为 Source，并用稳定 idempotency key 创建仓库 acquire/summarize 或 dataset manifest Job；重复提交复用已有 Source/Job。
- 对话与事件：新增共享、带锁的 `chat/transcript.jsonl` store，HTTP chat 和 worker 均使用它；worker 在关键 Job 终态生成一次 `task_result` 或 `task_failure` 结构化助手消息，写入 `conversation.message.created` 事件。中间 acquisition Job 仅更新右侧任务卡，避免主聊天刷屏。
- 前端：WebSocket 收到持久对话事件后按 `message_id` 去重插入主聊天；刷新和任务切换从 transcript 恢复任务卡。Sources/Jobs/Evidence 仍复用现有 sidebar，任务消息显示关联 Job、产物和失败摘要。
- 上下文：持久 transcript 优先于浏览器传入的旧尾部；context builder 纳入 recent task messages，但失败消息不会被视为可用证据或科学结论。
- 验证：定向 Python 回归 `38 passed`，接口/事件回归 `11 passed`，前端 `npm run build` 通过；完整项目门禁待本条日志写入后执行。

## 后台通知闭环审计修正

- 目标：按“后台任务与主对话同步闭环”逐项审计，补齐首次实现中会影响真实协作体验的竞态、链路边界和恢复语义。
- 实现：补充 Decision 识别路径的正式 Source/Job promote；本地仓库 Job 图可补齐缺失的 successor 且保持稳定幂等键；未知 `local_path` 回执明确资料角色待确认，不伪造任务。
- 通知：报告中间阶段只更新任务侧栏，报告 bundle/PDF 才进入主对话；资料、环境、实验、报告失败保留精确原因与下一步，B_test 授权问题不会被表述为一般运行失败；worker 先持久化通知并写入 `conversation.message.created`，再追加 `job.completed`/`job.failed`。
- 并发与前端：transcript 改为内核释放的文件锁，避免 crash 遗留锁文件；切换 Run 时清空旧消息并按 `message_id` 合并 transcript 与实时 WebSocket 消息，避免初始加载竞态覆盖后台结果。
- 验证：新增并发 transcript、链式通知去重、环境/实验授权失败、worker 事件顺序、本地 Job 图补齐和 Playwright 任务卡刷新恢复测试；定向后端 `45 passed`、前端 lint/build、单独 Playwright 通过。完整门禁待本条日志写入后执行。
- CI 修正：首次推送的前端浏览器套件中，既有实验确认测试在 run 初始加载期间点击暂时禁用的发送按钮而超时；测试现改为等待按钮可交互。该问题与任务通知功能无关，但由新增的初始加载竞态回归暴露，需在下一次完整浏览器验证中确认。

## pytest 验证复用与慢用例可见性

- 目标：消除开发者先执行完整 `verify.sh`、再执行 `verify_and_push.sh` 时同一工作区重复运行完整 pytest 的等待，同时不降低本地或 CI 的全量测试门禁。
- 实现：新增 `scripts/pytest_verification_state.py`，对所有已跟踪和非忽略工作区文件的路径、权限和内容生成指纹，并将一次成功全量 pytest 的凭据保存在 Git 元数据目录。`verify_and_push.sh` 仍会执行完整 `verify.sh`，但仅在该指纹、pytest 命令和 Python 版本均完全相同时复用结果；任一文件变化、凭据缺失或无效都会回退到完整 pytest。CI 无本地凭据，故始终运行完整测试。
- 开发入口：新增 `scripts/test.sh`，可直接接收标准 pytest 文件或表达式选择器；完整 pytest 和本地入口均输出最慢 20 项耗时，供后续基于实际数据治理慢用例。未引入未经隔离测试的 xdist 并行，避免文件持久化、子进程和超时用例产生偶发失败。
- 验证：新增临时 Git 工作区回归，覆盖凭据不存在、匹配、已跟踪文件变更及未跟踪文件变更；Shell 语法与专用测试入口通过。完整门禁和复用门禁待本条日志写入后执行。
- CI 跟进：本提交首次 CI 的 `task-notifications` 浏览器测试把输入框显示误用为 WebSocket 订阅就绪，导致事件可能在 `onmessage` 赋值前丢失。fixture 现仅在实际投递给已订阅 socket 时返回成功，测试以该结果轮询；定向重复三次和完整前端套件均通过。最终 Python 门禁待此记录写入后执行。

## 显式本地路径与 Worker 授权统一

- 目标：修复本地资料在 Intake 阶段登记成功、后台 Worker 阶段却因环境白名单不同而失败的问题，并落实单用户部署下用户明确给出的路径可访问的产品前提。
- 实现：新增共享 resolve_local_source_path；聊天上下文、显式本地 Source 注册、local_repo_acquire 和 dataset_manifest Worker 均使用同一规范化路径。显式用户路径支持绝对路径、workspace/... 和服务进程 CWD 下的普通相对路径；低层默认白名单 API 保持不变。路径仍只读、限量扫描并写入 Source/context 回执。
- 对话：路径提取支持同一消息中的多个显式相对路径，并排除研究文本中的裸 /；orchestrator 去重键使用显式路径解析，避免确定性提取和模型 action 重复登记。保留跨轮次已登记路径复用。
- 测试：补充无 AUTOAD_ALLOWED_LOCAL_SOURCE_ROOTS 时 Dataset Worker 生成 manifest 的回归、显式相对路径提取和裸斜杠误识别回归；聚焦测试通过。
- 遗留：完整 scripts/verify.sh 结果待本条记录写入后执行；本次未提交或推送。

## 资料状态闭环与任务确认分层

- 目标：让 dataset manifest Worker 成功后同步完成 Source intake 状态，并让后台任务完成事件刷新侧栏；确认弹窗区分“只生成方案”和“进入执行”各自真正需要的条件。
- 实现：manifest 生成并写入元数据后，将 Source 更新为 `parsed` / `ok` 并清理旧 intake 错误；`job.completed` 统一复用侧栏刷新；任务确认先展示主指标状态，再选择执行模式，未确认主指标时仍允许 plan-only，但执行模式会明确提示阻塞条件。
- 交互：待采集仓库改为“正在等待采集”，避免把尚未完成的资料误表述为永久不可选；执行仓库提示补充下一步。
- 测试：补充 Worker 成功后的 Source 状态回归和通知事件触发侧栏重读的 Playwright 覆盖；Python 聚焦测试、前端 lint/build 和实验确认/任务通知 E2E 已执行，视觉快照更新后再跑完整门禁。
- 遗留：完整 `scripts/verify.sh` 待本条记录写入后执行；本次未提交或推送。

## 确认交互最终收敛

- 确认弹窗不再让用户选择逐步确认或确认后协助；确认当前草案后固定交给实验 Agent。
- 用户只需在底部选择“确认并交给实验 Agent”或“还需要细化实验草案”；主指标和执行仓库仅在对应条件下出现。
- 最新前端 lint/build 与实验确认、任务通知 E2E 已通过；完整 Python 门禁结果为 2030 passed, 2 skipped。

## 实验任务确认 readiness 门禁

- 目标：将“任务草案已持久化”与“可弹出确认并交给实验 Agent”分开，避免资料 Job 尚在处理、关键合同未确认或执行仓库不可绑定时出现确认弹窗。
- 后端：新增动态、只读的 `ExperimentTaskReadiness`。它基于当前摘要版本、未解决问题、任务引用 Source 的 intake 状态、关联资料 Job 终态以及实际 repository admission 计算确认资格；不改写 `pending_experiment_task.json`，资料后台完成后无需重新生成草案。
- 确认：`agent_assisted_after_approval` 在写入确认记录前重算 readiness；主指标和摘要仍沿用既有稳定冲突码，其他资料/仓库未就绪返回 `confirmation_not_ready`，因此不会创建 `input_task.yaml`、Session 或环境 Job。`plan_only` 保持原有非执行兼容语义。
- API/UI：聊天响应携带 readiness；新增 pending task readiness 查询接口。Run 刷新、聊天返回和 Job completed/failed WebSocket 事件都重新读取同一后端判定，只有 `ready=true` 才显示确认弹窗；弹窗仓库候选只显示已通过 admission 的 Source。缺主指标等情况不再由弹窗收集，保留在对话合同路径。
- 测试：新增资料 Job queued/running、failed、完成后 ready 和 readiness 路由回归；更新草案存在但不就绪的对话断言；前端确认 E2E 覆盖主指标未确认时不弹窗，并验证完成 Job 后刷新。定向 Python `40 passed`，前端 lint/build 与相关 Playwright `5 passed`，真实 FastAPI/Vite 全栈 `3 passed`。
