# 2026-07-25

### Session 1: 实验准备、adapter preflight 与 GPU 拓扑

- 目标：把真实训练 UAT 的准备过程从“用户填写一组内部 CLI 参数”改为 agent 调查、证据记录、阶段级 readiness 和后端门禁，并保留现有 Session/Attempt/Projection 兼容性。
- 操作：新增 `ExperimentPreparation` 与原子 sidecar；增加准备状态/调查/保存 API 和 Observatory 投影；复用 Repository Intelligence 做本地 baseline/reference 仓库调查；增加显式 adapter preflight 合同与真实 argv 检查；将 preflight 接入 baseline、candidate handoff 和 B_test；增加阶段级门禁；为 GPU 设备记录 UUID，并新增单卡、DDP/模型并行、独立单卡角色和未知拓扑的执行计划合同。
- 结果：准备层可以表达 MVTec 可运行、MPDD 仅阻断 MPDD 阶段；已有 adapter fixture 保持兼容；新增后端合同、preflight、GPU 拓扑和协调器测试通过，当前专项测试通过。
- 遗留：前端 baseline 高级契约表单仍需收缩为准备状态驱动的用户决策入口；需要补 full-stack/Playwright；还需更新独立 MVTec/MPDD UAT adapter 与重建包记录，之后再跑项目完整 verify 和推送。

### Session 2: 准备状态前端可读性收口

- 目标：让实验准备页只暴露用户可行动的展示信息，不把未知内部阶段 ID 当作用户文案。
- 操作：将当前阶段、可运行阶段和资产影响范围的未知项降级为“准备阶段”，保留后端提供的正式展示名；补充准备资产路径提交的浏览器回归覆盖。
- 结果：前端准备状态仍按阶段显示 MVTec 可运行、MPDD 不可运行，内部键名不会作为可见回退文案；待执行完整项目验证和推送。
- 遗留：UAT 脚本仍需按真实上游仓库接口拆分 adapter，并将 MPDD 缺失限制在 MPDD 阶段。

### Session 3: UAT adapter 与分阶段资产边界

- 目标：把真实 UAT 包改成 agent 调查后执行的 adapter 流程，明确 MVTec 可先行、MPDD 由用户补充且只阻断 MPDD 评价。
- 操作：增加 AnomalyCLIP/FAPrompt 独立 CLI adapter 矩阵和统一执行器；增加真实 `--help`、dataset mapping、checkpoint/log 检查；增加 MVTec baseline calibration；取消 UAT 脚本自动 clone；为单卡上游入口增加双独立 Attempt 并行和偶数 seed 换卡；更新用户提供资产、候选生成和 GPU 拓扑说明；重建 Git-safe ZIP 分片并更新尺寸、SHA256、central directory、CRC 和包内 SHA256 记录。
- 结果：AnomalyCLIP 的 MVTec 映射冻结为 `mvtec`，FAPrompt 冻结为 `mvtecad`，MPDD 评价映射为 `mpdd`；MPDD 缺失时运行协议会完成 MVTec calibration 后正常暂停；UAT 脚本通过 `bash -n`，工具通过 `py_compile`，新包 46 个成员全部 CRC 通过，包内 hash 校验通过。
- 遗留：需要执行项目完整验证并推送；GitHub Actions 需在推送后查询。
