# AutoAD-Researcher 开发日志 — 2026-06-16

---

### Step 2.5: Core cleanup — enable_events=False 测试

**目标**: 补充 `ArtifactStore(enable_events=False)` 的测试覆盖。另两项（Field(default_factory=dict)、AutAD typo）已在昨日修复。

**操作**:

1. 新增 `test_events_can_be_disabled`
   - 创建 `ArtifactStore(enable_events=False)`
   - 写入 + 读取 artifact
   - 断言 `events.jsonl` 不存在
   - 验证 `_record_artifact_written` 和 `_record_artifact_read` 的 None guard 生效

**结果**:
- 48 passed（+1）

**遗留问题**:
- 无

---

### Step 2.6: StageResult — harness 返回值结构化

**目标**: 让每个 harness stage 返回结构化结果，为后续 PipelineController 和 UI 展示提供前置基础。

**操作**:

1. 创建 `src/autoad_researcher/core/stage_result.py`
   - `StageStatus = Literal["success", "failed", "skipped", "not_implemented"]`
   - `StageResult(run_id, stage, status, artifacts, metadata)`
   - `extra="forbid"` + `Field(default_factory=dict/list)` 避免可变默认值

2. 更新 `core/__init__.py` — 导出 StageResult, StageStatus

3. 更新 `harness/base.py` — 抽象方法返回类型 `None` → `StageResult`

4. 更新 `harness/simple_pipeline.py`
   - 两个方法末尾返回 StageResult（status="success"）
   - metadata 含 `backend: "simple_pipeline"`

5. 更新 `harness/deepagents_backend.py`
   - 两个方法末尾返回 StageResult（status="success"）
   - metadata 含 `backend: "deepagents"`, `model`

6. 测试
   - `tests/test_stage_result.py` — 3 tests：最小字段、拒绝未知 status、默认值
   - `tests/test_simple_pipeline.py` — 补返回值断言（run_id, stage, status, artifacts, metadata）
   - `tests/test_harness_base.py` — 合法 run_id 测试补 status=="success" 断言

7. verify.sh core import 增加 StageResult 构造检查

**结果**:
- 51 passed（+3）

**关键设计**:
- artifacts 存文件名而非绝对路径，路径解析交给 ArtifactStore
- DeepAgentsHarness 不包裹 try/except，异常直接抛出（PipelineController 未来封装）
- extra="forbid" 确保不会静默接受拼写错误

**遗留问题**:
- 无

---

### Step 2.7: 阶段生命周期事件

**目标**: 让事件日志表达"阶段何时开始、何时完成"。

**操作**:

1. 扩展 `EventStore`（events.py）
   - 新增 event_type：`stage_started`, `stage_completed`, `stage_failed`
   - 新增方法：`record_stage_started()`、`record_stage_completed()`、`record_stage_failed()`

2. 更新 `SimplePipelineHarness` + `DeepAgentsHarness`
   - `__init__` 新增 `EventStore` 实例
   - 每个 stage：`record_stage_started` → artifact → `record_stage_completed`
   - DeepAgents 不包裹 try/except，失败事件留给 PipelineController

3. 测试
   - `tests/test_events.py` +3：stage_started/completed/failed
   - `tests/test_simple_pipeline.py`：事件顺序改为 6 条断言（stage_started/artifact_written/stage_completed × 2）

**结果**:
- 54 passed（+3）

**遗留问题**:
- 无

---

### Step 2.8: Minimal PipelineController

**目标**: 建立最小 pipeline 编排器，串联 experiment_planning + patch_planning。

**操作**:

1. 创建 `core/pipeline.py`
   - `PipelineResult(run_id, status, stages)` — 一次 pipeline 的结构化结果
   - `PipelineStatus = Literal["success", "failed"]`
   - `PipelineController(harness, runs_root)` — 依赖 AgentHarness 接口，不绑定具体实现

2. `PipelineController.run_planning_pipeline(run_id)`
   - 校验 run_id + 创建 run_dir
   - 写 `run_created` 事件（payload 含 pipeline name）
   - 依次调用 harness.run_experiment_planning() → harness.run_patch_planning()
   - 收集 StageResult 列表，返回 PipelineResult

3. 更新 `core/__init__.py` + `verify.sh` core import

4. 测试
   - `tests/test_pipeline_controller.py` — 3 tests：成功路径 / 事件顺序 / 非法 run_id

**结果**:
- 57 passed（+3）

**遗留问题**:
- 无

---

### Step 2.9: Pipeline failure handling

**目标**: 让 PipelineController 捕获 stage 异常，写 stage_failed 事件，返回失败 PipelineResult。

**操作**:

1. 扩展 `PipelineResult`
   - 新增 `failed_stage: str | None`、`error_type: str | None`、`error_message: str | None`
   - 成功路径仍兼容，三个失败字段默认为 None

2. 更新 `PipelineController.run_planning_pipeline()`
   - 每个 stage 调用包裹 try/except
   - 异常时调用 `_handle_stage_failure()`：写 `stage_failed` 事件 + 返回 `PipelineResult(status="failed")`
   - `_backend_name()` 取 harness 类名作为 backend 标识

3. 新增 fake harness（tests 内）
   - `FailingExperimentHarness` — experiment_planning 直接 RuntimeError
   - `FailingPatchHarness` — patch_planning 抛异常，保留已完成 experiment stage

4. 测试
   - `test_experiment_stage_failure_returns_failed_result` — stages=[], failed_stage="experiment_planning"
   - `test_patch_stage_failure_preserves_completed_stages` — 保留已完成 stage，failed_stage="patch_planning"

5. verify.sh core import 增加 `PipelineController`

**事件边界**:
```
Controller: run_created, stage_failed
Harness:    stage_started, stage_completed
ArtifactStore: artifact_written, artifact_read
```

**结果**:
- 59 passed（+2）

**遗留问题**:
- 无

---

### Step 2.10: CLI smoke entrypoint

**目标**: 提供 `uv run autoad smoke --run-id run_demo` 一键执行 planning pipeline。

**操作**:

1. 创建 `src/autoad_researcher/cli.py` + `__main__.py`
2. `pyproject.toml` 注册 `autoad` console script
3. 退出码：0=success, 1=failed, 2=参数错误
4. human / --json 两种输出模式
5. `tests/test_cli.py` — 4 tests
6. `verify.sh` 新增 CLI --help 检查

**结果**:
- 65 passed（+4）

**遗留问题**:
- 无

**关键设计边界**:
- Controller 管 run 级生命周期（run_created），Harness 管 stage 级，ArtifactStore 管 artifact 级
- 不 catch exception — 失败直接抛出，留给 Step 2.9
- 不接 DeepAgents 真实测试 — Controller 只依赖 AgentHarness 抽象接口
- PipelineResult extra="forbid"

**事件顺序**:
```
run_created → stage_started → artifact_written → stage_completed
            → stage_started → artifact_written → stage_completed
```

**遗留问题**:
- 无

---

### Step 2.11: Input Intake + Source Manifest

**目标**: 建立科研闭环的输入事实层。

**操作**:

1. 创建 `schemas/intake.py` — SourceKind, SourceEntry, SourceManifest, InputTask
2. ArtifactStore 增加 YAML 支持 — write_yaml/read_yaml/read_yaml_model, JSON/YAML 白名单分离
3. 创建 `core/intake.py` — InputIntake.persist()（写入前全部校验，避免部分写入）
4. 测试 — schemas 6 + intake 5 + artifact YAML 6 + 其他 = 85 passed（+20）

**关键设计**:
- InputIntake 当前独立于 PipelineController，不写 stage 事件
- baseline/dataset 等允许为空，留给 Intent Clarifier 补充
- 不下载/复制原始文件（SourceEntry.stored_path/sha256 预留）

**遗留问题**:
- 无

---

### 修复: 2.11.1 — 读取边界隔离 + 私有字段访问 + 文档同步

**操作**:

1. read_json/read_yaml 增加格式校验 — 读取端与写入端使用相同的 _validate_json/_validate_yaml
2. InputIntake 不再访问 ArtifactStore._runs_root — 改用 run_dir() 触发 run_id 校验
3. 移除 intake.py 中不再使用的 validate_run_id 导入
4. 错误消息改为操作无关：`JSON artifact required` / `YAML artifact required`
5. README 补齐 CLI smoke + Input Intake 说明

---

### Step 2.12: Paper Reader + Repository Reader contracts

**目标**: 建立论文和仓库读取的框架无关协议。

**操作**:

1. 创建 `schemas/readers.py`
   - `EvidenceReference` — 材料位置引用
   - `KnowledgeState` — yes/no/unknown 三态
   - `PaperSummary` — 论文结构化摘要（方法组件、数据假设、迁移点、未解决问题）
   - `RepositorySummary` — 仓库结构化摘要（入口、配置、可修改边界、protected_paths）

2. 创建 `readers/` 包
   - `base.py` — PaperReaderBackend / RepositoryReaderBackend ABC
   - `static.py` — StaticPaperReaderBackend / StaticRepositoryReaderBackend
   - `__init__.py` — 导出四个类

3. 创建 `core/readers.py`
   - `PaperReader` — 从 source_manifest 查 source → 校验 kind → 调 backend → 校验输出 → 写 artifact
   - `RepositoryReader` — 同上，kind=repository
   - 写入前校验 run_id/source_id 一致性，不匹配拒绝写入

4. ArtifactStore 白名单新增 `repo_summary.json`

5. 测试
   - `test_reader_schemas.py` — 10 tests
   - `test_readers.py` — 12 tests（success ×2、events、not found、wrong kind、run_id/source_id mismatch、exception propagate）

**结果**:
- 109 passed（+22）

**遗留问题**:
- 无

---

### Step 2.13: Evidence-based Intent Clarifier

**目标**: 基于已落盘事实生成澄清结果，汇总已知事实、识别缺口、生成问题。

**操作**:

1. 创建 `schemas/clarification.py` — ClarifiedTask, ClarificationContext, KnownFact, MissingInformation, ClarificationQuestion 等
2. 创建 `clarifiers/` 包 — IntentClarifierBackend ABC + RuleBasedIntentClarifierBackend
3. 创建 `core/clarification.py` — IntentClarifier core service
4. 测试 — schema 12 + rule_based 7 + clarifier 11 + artifacts 1 = 143 passed

**关键设计边界**:
- baseline/dataset/metric 只建议候选值，不自动选择
- user_idea 缺失不产生阻塞问题
- target_domain 缺失是阻塞问题
- 用户已设置的字段 backend 不能改写
- PaperSummary/RepositorySummary 可选（不存在时跳过）
- ClarifiedTask 的 status 与 missing/questions fields 自检一致性

**结果**:
- 143 passed（+30）

---

### Step 2.14: Idea Protocol + Idea Source Router

**目标**: 建立框架无关的 Idea 数据协议和确定性路由。

**操作**:

1. 创建 `schemas/ideas.py`
   - `IdeaMode` — direct_user_idea / idea_decomposition / multi_agent_exploration
   - `IdeaRouteDecision` — 路由决策（mode + requested_mode + reason）
   - `IdeaContext` — Idea backend 输入快照（含 clarified_task + paper/repo summary）
   - `IdeaCandidate` — 可验证的候选方案（安全 ID、插入点、成本、置信度、证据）
   - `IdeaGenerationResult` — 统一输出（1–3 candidates，ID 唯一，推荐一致性）

2. 扩展 `ArtifactReference` — 新增 `clarified_task.json` 和 `idea_context.json`

3. 创建 `ideation/` 包 — `IdeaGenerationBackend` ABC

4. 创建 `core/idea_router.py` — IdeaSourceRouter
   - blocking clarification → 拒绝路由
   - user_idea 为空 → multi_agent_exploration（默认）
   - user_idea 非空 → idea_decomposition（默认）
   - direct_user_idea → 只能显式请求

5. 测试 — 16 schema + 16 router

**结果**:
- 184 passed（+41）

---

### Step 2.14.1: 空字符串 idea 路由修正 + 证据强化 + 对称测试

**操作**:

1. `_has_user_idea()` — `bool(idea and idea.strip())`，默认与显式路由统一使用
2. `IdeaRouteDecision` validator — mode 必须匹配 requested_mode
3. 空/空白 idea 测试（4 个）
4. repo run_id/source_id 对称测试（2 个）
5. artifact 测试改为正式 schema round-trip

**结果**:
- 190 passed（+6）

---

### Step 2.15: DirectIdeaBackend + IdeaGenerator（地基最后一步）

**操作**:

1. 创建 `ideation/direct.py` — DirectIdeaBackend
   - 只支持 direct_user_idea
   - 忠实转录用户 idea，不虚构收益/成本
   - estimated_cost="unknown"，confidence=1.0 表示转录置信度

2. 创建 `core/idea_generation.py` — IdeaGenerator
   - 读取 idea_context → 调 backend → revalidate → 校验一致性 → 写 idea_candidates
   - `_revalidate_result()` 强制 model_validate 防 model_copy 绕过

3. `EstimatedIdeaCost` — 新增 "unknown"
4. `IdeaContext` 自校验 — clarified run_id、blocking、user_idea、paper/repo run/source

5. 测试 — 6 direct backend + 8 generator

**结果**:
- 204 passed（+14）

---

### Step 2.15.1: Direct 授权封板 + 文档同步

**操作**:

1. `IdeaRouteDecision` — direct_user_idea 必须 requested_mode="direct_user_idea"，阻止手工 JSON 绕过
2. README — 完整 artifact 链路 + Clarifier 正确描述
3. 路线 — 标记地基阶段 14 项完成，下一步改为 Step 3.1 真实纵向能力

**结果**:
- 205 passed（+1）

**地基阶段正式结束。**

---

### Step 3.0A: baseline 候选与确认来源协议

**目标**: baseline 候选来源（repo/paper/history/system）与确认来源（user_provided/user_confirmed）分离。候选不能冒充用户决定。

**操作**:

1. 创建 `schemas/decisions.py`
   - `DecisionCandidateSource` — repo_detected / paper_mentioned / history_detected / system_recommended
   - `DecisionConfirmationSource` — user_provided / user_confirmed
   - `DecisionEvidence` — 一条候选值的来源证据（source + rationale + references）
   - `DecisionCandidate` — 待确认候选（value + evidence list）
   - `ConfirmedDecision` — 已确认正式值（value + source + evidence）

2. 更新 `ClarifiedTask`
   - 新增 `baseline_candidates: list[DecisionCandidate]`
   - 新增 `baseline_decision: ConfirmedDecision | None`
   - 新增 `_validate_baseline_decision` validator：
     - baseline 非空必须有 decision
     - decision.value 必须与 baseline 一致
     - user_confirmed 必须匹配候选
     - 候选值大小写去重

3. 更新 `PaperSummary` — 新增 `compared_methods: list[str]`

4. 更新 rule-based clarifier
   - baseline 已提供 → ConfirmedDecision(source="user_provided")
   - 未提供 → 从 repo/paper 收集候选，多来源合并
   - 候选排序：repo_detected < paper_mentioned < history < system_recommended

5. Core 防线：task.baseline 非空时 backend 不能改写、必须返回 user_provided；为空时 backend 不能自动填 baseline

**结果**:
- 217 passed（+12）

**遗留问题**:
- dataset/metrics/compute_budget 的 provenance 协议后续按相同模式扩展

---

### Step 3.0A.2 + 3.0B-2: provenance sealing + benchmark locked

**3.0A.2 fixes** (already applied in previous commits):

1. `InputTask` blank baseline → `field_validator(mode="before")` normalizes to None
2. `_validate_candidate_references()` — candidate evidence source_id checked against actual context (repo_summary/paper_summary existence and source_id match)
3. `_revalidate_result()` — backend output forced re-validate via model_dump → model_validate

**3.0B-2 benchmark lock**:

1. Created `configs/benchmarks/internal_patchcore_mvtec_bottle_v1.yaml`
   - Amazon official PatchCore, commit `fcaa92f`
   - MVTec AD bottle, 3 AUROC metrics, 2 attempts, tolerance 0.005
   - scope: internal_benchmark_only

2. Created `docs/adr/0001-internal-benchmark-selection.md`
   - PatchCore over Anomalib v2.5.0 for first minimal benchmark
   - Risks recorded: Python 3.8 env, no upstream lockfile, CC BY-NC-SA 4.0 data

3. Created `docs/internal_benchmark_case.md` — status: locked_not_executed

**Result**:
- 256 passed

**下一步**: Step 3.0C — baseline 独立环境与证据采集

---

### Step 3.0C-1: benchmark runtime evidence contracts

**目标**: 建立 internal benchmark 的所有运行时证据模型。

**操作**:

1. 创建 `src/autoad_researcher/benchmarks/__init__.py` — internal-only 声明
2. 创建 `src/autoad_researcher/benchmarks/evidence.py`
   - `BenchmarkAttemptStatus` — 5 个合法状态
   - `BenchmarkFileFingerprint` — path + size + sha256
   - `BenchmarkRepositoryState` — 仓库前后状态（commit/dirty/fingerprint）
   - `BenchmarkEnvironmentSnapshot` — Python/CUDA/torch 版本
   - `BenchmarkWeightManifest` — 权重清单 + offline_load_verified
   - `BenchmarkDatasetManifest` — structural manifest (path+size only)
   - `BenchmarkCommandSpec` — shell=False, argv template, cwd, env, timeout
   - `BenchmarkMetricsResult` — parsed metrics with source SHA
   - `BenchmarkExecutionResult` — 完整 attempt 结果（所有 fingerprint SHA）
   - 全部 extra="forbid", str_strip_whitespace=True

3. 创建 `tests/test_benchmark_evidence.py` — 19 tests

**结果**:
- 275 passed（+19）

---

### Step 3.0C-1: benchmark runtime evidence contracts

**目标**: 建立 internal benchmark 的所有运行时证据模型（10 个模型）。

**操作**:

1. 创建 `src/autoad_researcher/benchmarks/__init__.py` + `evidence.py`
2. 定义 `BenchmarkAttemptStatus`（5 态）、`AllowedAttempt`、`BenchmarkMetricsStatus` 等枚举
3. 证据模型：FileFingerprint、RepositoryState、EnvironmentSnapshot、WeightManifest、DatasetManifest、CommandSpec、MetricsResult、PreflightCheck/Report、ExecutionResult
4. 全部 `extra="forbid"`, `str_strip_whitespace=True`

**结果**: 275 passed（+19）

---

### Step 3.0C-1.1: evidence invariants（hex、path、状态一致性）

**目标**: 从"字段容器"升级为有状态约束的证据模型。

**操作**:

1. `Sha256Hex` / `GitCommitSha` — pattern 强制小写 hex
2. Dataset manifest 新增 `files` + `manifest_strategy`，排序+唯一校验
3. Weight offline_verified → files 非空；weight 文件 size≥1
4. CUDA: accelerator=cuda→available=true→device_count≥1; !available→device_count=0
5. Metrics: NaN/Inf 拒绝；success→非空+required+dataset_row；failed→空
6. Success execution→9 SHA 全必填+exit_code=0+时间顺序
7. Command env allowlist（10 keys）
8. PreflightCheck/Report + passed 一致性

**结果**: 291 passed（+16）

---

### Step 3.0C-1.2: path safety + failure evidence + metrics ranges

**目标**: PurePosixPath 校验、parse_failed 可表达性、失败原因完整性、时区、duration、单位范围。

**操作**:

1. `_safe_relative_path(PurePosixPath)` — 拒绝 `..`、`/`、`\`、空、点，挂载到所有路径字段
2. metric_parse_failed 允许 source/source_sha256 为 None；success 必填
3. 所有非 success 状态强制 `failure_code`(CAPS) + `failure_message`；success 禁止设置
4. success 强制 tz-aware timestamps（utcoffset 不为 None）+ duration_seconds 必填
5. Metric 单位范围：ratio→[0,1]、percent→[0,100]、count→非负整数、seconds/bytes→非负
6. CUDA: accelerator=cuda→device_count≥1

**结果**: 309 passed（+18）

---

### Step 3.0C-1.3: close status contradictions

**目标**: 封堵 execution_failed exit_code=0、success repo 前后不一致、metric_parse_failed 缺执行证据。

**操作**:

1. `execution_failed` → exit_code≠0 or timed_out（exit_code=0 拒绝）
2. `success` → `repository_fingerprint_before == after`
3. 抽取 `_require_execution_context()` — success 和 metric_parse_failed 共用：
   exit_code=0、不超时、时区时间+utcoffset、duration、8 preflight SHA 必填
4. metric_parse_failed 允许 metrics_sha256=None

**最终状态矩阵**:

| 状态 | exit_code | 失败字段 | fingerprint | metrics_sha256 |
|---|---|---|---|---|
| success | =0 | 无 | 9 SHA + before==after | 必填 |
| execution_failed | ≠0/TIMEOUT | 必填 | 可选 | 可选 |
| metric_parse_failed | =0 | 必填 | 8 preflight 必填 | 可选 |
| preflight_failed | None | 必填 | 可选 | — |
| repo_mutation | 可选 | 必填 | before+after 必填 | — |

**结果**: 316 passed（+7）

**3.0C-1 证据契约封板。**

---

### Codex 项目记忆初始化与仓库巡检

**目标**: 在 Codex 重新参与开发前，将必须遵守的项目记忆写入仓库，并核对当前开发路线、门禁和日志规则。

**操作**:

1. 核对仓库状态
   - 当前 `main` 与 `origin/main` 同步
   - 最新历史已推进到 benchmark lock / evidence traceability 阶段

2. 读取关键入口
   - `CLAUDE.md`
   - `scripts/verify.sh`
   - `scripts/verify_and_push.sh`
   - `notes/development-log.md`
   - `docs/AutoAD_真实纵向闭环开发计划.md`
   - `docs/AutoAD_任务参数决策与来源协议.md`

3. 新增 `AGENTS.md`
   - 固化 Codex 工作流：读文件 → 小步修改 → 写日志 → verify → commit/push → 查 Actions
   - 固化不能猜字段、不能静默决定真实用户任务参数、不能提交密钥或真实 LLM 统计等边界
   - 指向当前主要计划文档和 benchmark 配置

**结果**:
- `bash scripts/verify.sh` 通过，391 passed

**遗留问题**:
- 无
