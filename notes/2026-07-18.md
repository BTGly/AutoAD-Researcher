# 2026-07-18

## Plan 05D — Deterministic Decision, Candidate, Approval, and Champion Promotion

### Goal

Complete the deterministic scientific decision and recoverable Champion path without creating a second Attempt, Job, Worker, Finalizer, or result authority.

### Operations

- Added `DecisionEngine` gates over the existing `OutcomeCard`, including protocol, runtime, implementation, comparability, guardrail, NoiseFloor, B_dev, and B_test decisions.
- Added immutable `CandidateSnapshot` and `PromotionApproval` artifacts.
- Added a single-coordinator write-ahead `PromotionJournal` with `prepared / committed / rolled_back / failed` states, optimistic trunk validation, idempotent replay, append-only `ChampionEvent`, and `current_by_contract.json` pointer.
- Added real `git merge --no-ff` callback boundary and `git revert` rollback boundary; the service never uses `git reset --hard`.
- Added pointer-failure recovery and duplicate transaction tests.

### Commits

- `ed48035` — deterministic promotion workflow.
- `0e49eb6` — promotion gates, replay, rollback, and recovery tests.

## Plan 05E — Structured Reflection and Scientific Coordinator Context

### Goal

Reuse the existing `CycleDecision` and `CognitiveCommit` foundations while adding specialist Reflection only for structured high-value or conflicting outcomes.

### Operations

- Added plan-defined Reflection triggers and a restricted `ReflectionAgentFactory` using the existing DeepAgents factory.
- Added `ReflectionService`, which writes derived hypotheses through `IdeaTreeStore` and facts/inferences/confidence through the existing `CognitiveCommitStore`.
- Added Champion-aware and assessment-aware ContextPack builders without changing Coordinator action permissions.
- Added explicit `scientific_evaluation_inputs.json` and rebuildable `scientific_assessment.json`; seed, split, checkpoint, command identity, and baseline metrics are caller-supplied and never inferred from paths or prose.
- Added `ScientificExecutorHandoffService`, which wraps the existing Executor handoff and persists only the assessment inputs for the existing Attempt.

### Commits

- `15cdff3` — structured Reflection service.
- `4b4d269` — Reflection integration tests.
- `e342185` / `a0cd4db` — Champion-aware scientific ContextPack and tests.
- `6d44c68` / `b052cca` — explicit scientific assessment inputs and tests.
- `bc5c631` — assessment overlay for Coordinator ContextPack.

## Plan 06 — Convergence, Strategy, Stop, Cost, and Failure Policy

### Operations

- Added tumbling-window ConvergenceMonitor, independent consecutive no-progress Stop counter, parent/duplicate signals, and lightweight structured stuck detectors.
- Added deterministic `StrategySelector.filter_and_rank()`: it validates caller-supplied skill directories and `SKILL.md`, task applicability, approval, safety, budget, active/disabled/repeated status, then returns eligible candidates for Coordinator choice.
- Added deterministic StopPolicy with explicit precedence and immutable terminal decision artifact.
- Added a rebuildable CognitiveCostSummary over the existing append-only CognitiveBudget ledger; no second cost authority was created.
- Added deterministic FailurePolicy for protocol violations, OOM bounded repair, NaN/Inf review, infrastructure retry caps, timeout-with-progress extension, and terminal failures.

### Commits

- `8080c0c` / `3dc5ac9` — convergence and stuck detection implementation/tests.
- `e61b838` / `d80445a` — strategy filtering and audit implementation/tests.
- `6086944` / `c79c9d5` — StopPolicy implementation/tests.
- `81fe548` / `f06b7a2` — cognitive cost summary implementation/tests.
- `8c65c59` / `2b8d8c4` — FailurePolicy implementation/tests.

## AD-AgentBench and Two-Cycle End-to-End Closure

### Operations

- Added a deterministic ten-case AD-AgentBench matrix covering effective improvement, implementation invalidity, valid regression, within-noise result, OOM, hang, Coordinator recovery, cheap batch, stagnation, and evaluation cheating.
- Added a real two-cycle control-plane E2E with temporary Git repository, baseline Attempt, locked NoiseFloor, two Coordinator/Executor iterations, worktree patches, PipelineJobs, Worker/Popen/Finalizer, scientific assessment, Reflection-derived child hypothesis, confirmatory B_test, human approval, real `git merge --no-ff`, Champion pointer, convergence, StopPolicy, and assessed ContextPack recovery.

### Commits

- `a28440e` / `bf6761e` — AD-AgentBench implementation/tests.
- `d50890b` — two-cycle experiment Agents E2E.

## Verification Status

- Full GitHub Actions `verify` succeeded at `d50890b` in run `29630442870`; the checkout, dependency installation, full repository verification script, and complete job all succeeded.
- The documentation/index follow-up at `99b5b75` also passed GitHub Actions `verify` in run `29630481534`.

## Plan 05 / Plan 06 Closure Record

### Status

- Plan 05A–05E are complete: frozen EvaluationContract, protocol SHA evidence, validity/comparability/effect, NoiseFloor, deterministic decision, Candidate/Champion promotion, and structured Reflection are connected.
- Plan 06 is complete for the first-version boundary: ConvergenceMonitor, lightweight stuck detection, existing CognitiveBudget summary, StrategySelector eligibility, deterministic StopPolicy, failure injection policy, AD-AgentBench, and two-cycle end-to-end acceptance are implemented.
- No second Agent runtime, PipelineJob, Attempt, Worker, Finalizer, OutcomeCard authority, EventStore, or CognitiveBudget ledger was introduced.

### Acceptance Evidence

- Ten AD-AgentBench scenarios replay deterministically.
- The two-cycle E2E reaches a confirmed Champion through actual temporary Git worktrees, Worker execution, scientific assessment, Reflection, B_test, approval, and a real `git merge --no-ff`.
- Protected protocol artifacts remain validated by the existing Finalizer path; invalid or non-comparable evidence cannot reach promotion.
- Coordinator checkpoint/cache state remains non-authoritative; assessed OutcomeCards, IdeaTree, CognitiveCommit, NoiseFloor, CandidateRegistry, and Champion pointer provide recovery evidence.

## Physical PatchCore Two-Cycle Rehearsal Gate

### Goal

Distinguish the verified deterministic control-plane E2E from a physical MVTec/PatchCore run. A real rehearsal must not start unless the pinned repository, dataset, environment, offline weight, and two user-provided scientific patches are all present and valid.

### Operations

- Added `PatchCoreTwoCycleReadinessChecker` for the pinned PatchCore repository commit, clean Git state, required MVTec bottle paths, executable benchmark Python, lockfile, offline backbone weight SHA-256, and two explicit patch files.
- Validates the two patches sequentially in a temporary detached Git worktree, so cycle 2 must apply after cycle 1 rather than independently against baseline.
- Rejects any patch that modifies the frozen evaluator/protected paths from the internal benchmark contract.
- Added `scripts/benchmark/check_patchcore_two_cycle_readiness.py`; it returns structured JSON and exits non-zero when blocked. It performs no training and does not claim a real experiment occurred.

### Commits and Verification

- `bc06493` — PatchCore two-cycle readiness model and checker.
- `ed1b172` — readiness CLI.
- `dac4dc5` — sequential patch, protected-path, and missing-prerequisite tests.
- GitHub Actions `verify` run `29630682634` succeeded for the complete repository.

### Remaining External Blockers

- A physical rehearsal still requires the user-licensed MVTec AD dataset at the configured root.
- The pinned PatchCore checkout, locked benchmark environment, offline WideResNet50 weight, and two concrete scientific patch files must be supplied on the execution host.
- Until the readiness CLI reports `ready`, the repository must report the real rehearsal as blocked rather than reuse fixture evidence.

## Plan 07H-A — Physical smoke protocol and command-contract alignment

### Goal

Create an independent, small PatchCore/MVTec bottle smoke protocol whose
frozen YAML parameters, generated command, expected CSV output, and metric
parser cannot diverge. The pre-existing internal regression benchmark and the
existing 05/06/07 plan documents remain unchanged.

### Operations

- Fetched `origin` with pruning; `git rev-list --left-right --count
  main...origin/main` returned `0 31`, so `main` fast-forwarded cleanly from
  `19ce498` to `b113944` before work began.
- Created the dedicated branch `agent/07h-physical-two-cycle`.
- Recorded read-only host facts: `nvidia-smi` reports GPU 0 as NVIDIA GeForce
  RTX 4090 (driver 550.107.02, 24564 MiB); `/root/autodl-tmp/mvtec/bottle`
  exists as a candidate user-provided dataset location. Its root, contents,
  and suitability remain to be established by the later physical readiness
  gate; no dataset was copied, linked, or used for training.
- Added `internal_patchcore_mvtec_bottle_smoke_v1.yaml`, separately freezing
  the 07H seed-0, GPU-0, 128-pixel, two-worker, offline, 30-minute smoke
  command contract.
- Added a strict config-driven PatchCore smoke command and metric-spec builder.
  It accepts no implicit/default command values, derives the expected CSV path
  from the frozen config, and rejects a mismatched evaluation output contract.
- Added command equality and malformed-contract regressions, and made the full
  verification gate validate the new smoke case.

### References

- PatchCore `bin/run_patchcore.py` command declarations and MVTec loader;
  repository-local source inspection only; reuse level: **REFER**.
- Existing AutoAD `benchmarks/patchcore_attempt.py` and benchmark config
  schemas; reuse level: **ADAPT**, no external code copied.

### Tests

- Initial `uv run ...` invocation did not start because `uv` is not on this
  shell's `PATH`; no tests or GPU work ran. The repository verification script
  already resolves the installed `/root/.local/bin/uv`, so the focused command
  used that exact executable.
- `/root/.local/bin/uv run --extra dev pytest -q tests/test_patchcore_smoke.py
  tests/test_patchcore_attempt.py tests/test_internal_benchmark_config.py
  tests/test_internal_benchmark_schema.py` — `34 passed`.
- `/root/.local/bin/uv run python scripts/benchmark/validate_case.py
  configs/benchmarks/internal_patchcore_mvtec_bottle_smoke_v1.yaml --json` —
  passed; case SHA-256:
  `faad414c83732280fd15bf78c5c05004989a1054ebeb65477334fe2353bf1a5b`.
- The first complete `bash scripts/verify.sh` run reached the pytest suite but
  reported one failure without printing its failure detail in the captured
  output. `/root/.local/bin/uv run --extra dev pytest --lf -vv` isolated
  `tests/test_experiment_attempt_job.py::test_gpu_capacity_failure_finalizes_attempt_without_starting_training`, which passed on rerun. A complete rerun is required before commit.
- A full `pytest -x -vv` run then produced the complete failure evidence: that
  existing GPU-capacity test monkeypatched `experiment.gpu.GpuAllocator`, while
  the Worker invokes the already imported symbol in
  `experiment.attempt_execution`. On a host with an allocatable RTX 4090 the
  replacement did not take effect and the fixture command entered `RUNNING`.
  The test now patches the actual invocation module; runtime allocation code is
  unchanged.
- `/root/.local/bin/uv run --extra dev pytest -q
  tests/test_experiment_attempt_job.py tests/test_patchcore_smoke.py` — `18
  passed` after that correction.
- `bash scripts/verify.sh` — passed after the correction (including the full
  pytest suite and the added smoke-case validation).

### Result

- Implementation is ready for focused tests and the required full verification
  gate. No GPU workload, real PatchCore command, or experiment Attempt has run.

### Leftovers

- 07H-B/C will define audited manifests and read-only B_dev/B_test projections.
- The physical readiness and per-cycle intervention-admission gates remain
  future work; the existing two-patch rehearsal checker is not used as the
  07H execution entrypoint.

## Plan 07H-B/C — Audited data projections and physical baseline gate

### Goal

Freeze an auditable, split-isolated MVTec bottle input for the physical 07H
baseline and admit that baseline only when deterministic machine, data,
environment, contract, and safety checks all pass. The gate must not require
pre-generated research patches and must never allocate a GPU or create an
Attempt by itself.

### Operations

- Read-only source inventory found 209 `train/good` images, test counts of 20
  good / 20 broken_large / 22 broken_small / 21 contamination, and matching
  anomaly-mask counts under `/root/autodl-tmp/mvtec/bottle`.
- Added deterministic source/train/B_dev/B_test manifests with relative paths,
  per-file SHA-256, sizes, labels, split, and anomaly-mask mapping. The fixed
  protocol selects the first 40 sorted training images and applies a
  label-derived deterministic seed-0 50/50 test split while retaining each
  label on both sides.
- Added idempotent, symlink-only read-only projection construction. Existing
  manifests, source hashes, contract evidence, and registered projection paths
  must all agree; mismatch, source mutation, missing/orphan masks, symlinked
  source entries, or unregistered projection files fail closed.
- Added a separate `PhysicalReadinessGate` and CLI. It validates 07H-A case
  SHA, source/projection evidence, RTX 4090 inventory, explicit caller-supplied
  GPU capacity thresholds, repository commit/cleanliness, locked Python,
  lockfile, offline weight, CUDA/PatchCore import, help invocation, command
  contract, protected hashes, existing leases, and baseline output collision.
- The CLI requires `--required-free-vram-mb` and `--maximum-used-vram-mb`; no
  unconfirmed capacity/occupancy threshold is invented.

### References

- PatchCore MVTec directory loader and command declarations; repository-local
  source inspection only; reuse level: **REFER**.
- Existing AutoAD benchmark hashing, environment lock, ResourceLease, and
  smoke-command contracts; reuse level: **ADAPT**, no external code copied.

### Tests

- The initial focused run did not collect the new tests because the project
  development environment has no `PIL` module. No data preparation or GPU
  work started. The implementation uses a standard-library PNG signature,
  chunk-boundary, CRC, and IDAT-decompression validator instead of adding a
  new dependency; focused tests are pending rerun.
- The first rerun collected the suite but exposed two gate defects before any
  physical action: the source-mutation test had made a PNG invalid rather than
  validly changing its SHA, and the data-preparation blocker serialized an
  exception containing an absolute temporary path. The regression now replaces
  the PNG with another valid payload and readiness records only the exception
  class for this boundary.
- `/root/.local/bin/uv run --extra dev pytest -q tests/test_patchcore_07h_data.py
  tests/test_patchcore_07h_readiness.py tests/test_patchcore_smoke.py` — `8
  passed`.
- The approved read-only source preparation completed for run `07h` without
  allocating a GPU. It wrote manifests and projections under `runs/07h/` with
  train SHA-256 `f4436ea1742f1723292eba4118b6c2725d3e54139b80866f537f92ec28470ad`,
  B_dev SHA-256 `2b980a11b6aa6658bbfd274b35cd64013e635a53e7e16fe7727ee99d7eb4de40`,
  and B_test SHA-256 `0927df5d4d288f1958f27a38ad18b64d172e9e736eb8171d66cfec248b377204`.
- Physical readiness has not been invoked because its GPU capacity thresholds
  are explicit required inputs and have not been supplied. It would also
  currently fail closed on the previously observed repository/weight blockers;
  no Attempt, ResourceLease, or PatchCore process was created.
- `bash scripts/verify.sh` — passed, including the full pytest suite and the
  07H data/readiness additions.

### Result

- Implementation is ready for deterministic tests, then the approved
  read-only data preparation and physical gate. No real GPU workload has run.

### Leftovers

- The actual baseline requires a `ready` gate plus user-confirmed GPU capacity
  thresholds. The first read-only host inspection found an external PatchCore
  checkout at `fcaa92f124fb1ad74a7acf56726decd4b27cbcad` and a missing default
  offline weight; neither is substituted for the frozen 07H assets.

## Plan 07H-D — Worker-owned seed-0 baseline admission

### Goal

Use the ready physical evidence to queue exactly one seed-0 PatchCore baseline
through `ExperimentAttempt -> PipelineJob -> Worker -> ResourceLease ->
Finalizer`, rather than invoking a standalone benchmark subprocess.

### Operations

- Added a Worker child-process adapter that executes the frozen PatchCore argv,
  persists command/protected-hash before-and-after evidence, parses the frozen
  CSV contract into `parsed_metrics.json` and `metrics.json`, and fails on a
  changed protected source or an out-of-range metric.
- Added a queue-only CLI that rejects non-ready evidence, freezes the Session,
  EvaluationContract, protected artifact report, and scientific comparison
  inputs before creating the baseline Attempt. It requests one GPU with 20 GB
  free capacity and a 1,800-second timeout.
- Worker process evidence now includes the command ID, assigned CUDA devices,
  and lease ID; it refreshes active lease heartbeats while the child is alive
  and writes a process-exit artifact.
- The existing Finalizer now emits a scientific assessment when the Attempt
  already has frozen scientific inputs.

### Tests

- `/root/.local/bin/uv run --extra dev pytest -q tests/test_patchcore_smoke.py
  tests/test_experiment_attempt_job.py tests/test_attempt_finalizer.py
  tests/test_scientific_assessment.py` — `27 passed`.
- `bash scripts/verify.sh` — passed, including the full pytest suite. No real
  baseline Attempt has been queued by this implementation batch.

### Physical attempt evidence and correction

- `attempt_000001` / `job_000001` was created only after the ready gate and
  was executed by the existing Worker. It allocated then released
  `lease_000001`, persisted PID/heartbeat/stdout/stderr/process-exit evidence,
  and left GPU 0 at 12 MiB used with no child process.
- It failed before PatchCore startup with `ModuleNotFoundError: pydantic`:
  the locked benchmark environment intentionally contains PatchCore
  dependencies, not AutoAD's control-plane dependencies. This is an adapter
  failure, not a training result; no retry is created and no threshold is
  relaxed.
- Corrected the Worker child adapter to use only the Python standard library.
  The queue step now freezes the exact PatchCore argv into a run artifact and
  binds that artifact's SHA-256 in the immutable command environment; the
  child rejects a mismatched command artifact before invoking PatchCore.
- The corrected adapter passed Python compilation, the 27 focused Worker/
  Finalizer/assessment tests, and is pending the required full verification
  before a second Attempt is admitted.
- A repaired baseline is intentionally a new Attempt keyed by its immutable
  command SHA-256. The failed first Attempt remains terminal evidence and is
  never overwritten or treated as a retry of a different command.

### 07H-D physical seed-0 outcome

- Re-ran `PhysicalReadinessGate` with the approved 20,000 MiB free / 1,000 MiB
  existing-use thresholds. It was `ready` with RTX 4090 GPU 0 at 12 MiB used,
  the frozen repository commit, locked environment, offline weight, manifests,
  and protected hashes all passing.
- `attempt_000002` / `job_000002` completed through the Worker chain on GPU 0.
  `lease_000002` was released; `process.json`, heartbeat, process-exit,
  stdout/stderr, exact command evidence, output manifest, and no-orphan-process
  check are retained under `runs/07h/attempts/attempt_000002/`.
- The frozen CSV parsed successfully: `instance_auroc=1.0`,
  `full_pixel_auroc=0.9793658025353316`, and
  `anomaly_pixel_auroc=0.9722287243867787`. OutcomeCard is
  `scientifically_evaluable` with `protocol_intact=true`; its deterministic
  scientific assessment is `COMPARABLE`.
- The first failed `attempt_000001` is retained as an adapter-environment
  failure; it did not start PatchCore, and its `lease_000001` is released.
- Do not begin seed-1/2 or an Agent intervention in this batch.

## Plan 07H-D2 — Multi-seed baseline admission

### Goal

Run seed 1 and seed 2 through the same Worker-owned physical path, preserve
every earlier Attempt, and derive a provisional NoiseFloor only from the three
real baseline metrics.

### Operations

- Extended the queue entrypoint with an explicit, bounded seed selector
  (`0`, `1`, or `2`). It changes only the frozen command's seed field and its
  Python hash seed; each seed gets its own immutable command artifact and SHA.
- D2 continues to bind the existing immutable EvaluationContract revision 0
  rather than rewriting seed-0 evidence. The contract records the seed-0
  baseline; cross-seed variability is represented only by the NoiseFloor,
  never as a direct candidate-comparison claim.

### Tests

- Python compilation and CLI argument validation passed.
- `/root/.local/bin/uv run --extra dev pytest -q tests/test_patchcore_smoke.py
  tests/test_experiment_attempt_job.py tests/test_attempt_finalizer.py` — `25 passed`.
- Full verification is the next gate before seed-1 is queued.

### Seed-1 evidence correction

- `attempt_000003` executed PatchCore with `--seed 1` (the immutable command
  artifact proves this) and completed cleanly, but its outer command ID and
  `PYTHONHASHSEED` were incorrectly left at seed 0. It is retained as a
  successful-but-not-counted protocol evidence artifact and is not used for
  NoiseFloor calibration.
- Corrected the outer command plan to derive both fields from the frozen seed.
  A new seed-1 Attempt will be created only after verification; seed-2 remains
  unqueued.
- Corrected seed-1 `attempt_000004` completed through `job_000004` with a
  released lease, complete Worker/Finalizer evidence, matching protected
  hashes, and no remaining PatchCore process. Its metrics are
  `instance_auroc=1.0`, `full_pixel_auroc=0.9793359910710664`, and
  `anomaly_pixel_auroc=0.9721886164196875`; it is the only seed-1 result that
  may enter the provisional NoiseFloor.
- Seed-2 `attempt_000005` completed through `job_000005` with command ID
  `baseline_seed_2_internal_patchcore_mvtec_bottle_smoke_v1`,
  `PYTHONHASHSEED=2`, released GPU lease, complete Finalizer evidence, and no
  residual PatchCore process. Its metrics are `instance_auroc=1.0`,
  `full_pixel_auroc=0.9791641030142731`, and
  `anomaly_pixel_auroc=0.9719572595666081`; it is valid NoiseFloor input.

## Plan 07H-A — Calibration provenance and effective-assessment boundary

### Goal

Repair the two identified evidence-semantic gaps without rewriting historical
Attempts, OutcomeCards, EvaluationContract v1, or the earlier NoiseFloor files.
Freeze a forward seed policy before any Agent intervention, and make scientific
decisions consume one explicit effective-assessment interface.

### Operations

- Added immutable `NoiseCalibrationProtocol` artifacts. They bind a base
  EvaluationContract hash, allowed seed set, approved invariant/variable fields,
  included/excluded Attempt IDs, and retrospective/prospective provenance.
- Added `PROVISIONAL_RETROSPECTIVE` to NoiseFloor status. Retrospective
  calibration cannot become `LOCKED` through sample count and is explicitly not
  promotion eligible.
- Added EvaluationContract schema v2 seed policy with
  `baseline_calibration_seeds`, `exploration_seed`, and an explicit
  confirmation-seed policy. Schema-v1 artifacts remain parseable and immutable.
- Added immutable `assessment_reconciliation.json` and
  `EffectiveScientificAssessment`. OutcomeCard remains the execution/protocol
  authority; ScientificAssessment is the only comparison/effect authority.
  DecisionEngine now accepts only the effective assessment, and Coordinator
  context removes comparison claims from unassessed raw cards.
- Added a 07H calibration CLI that validates completed Attempts, normalized
  command structure, seed/PYTHONHASHSEED agreement, input invariants, and
  protected hashes before it creates new artifacts.
- Ran it using only `attempt_000002`, `attempt_000004`, and `attempt_000005`;
  it preserved and explicitly excluded `attempt_000003`. It created the
  retrospective protocol, an immutable retrospective summary, three
  reconciliation artifacts, and EvaluationContract revision 1 / schema v2 with
  calibration seeds `[0, 1, 2]` and exploration seed `0`. No GPU process ran.

### Tests

- Added failure-first regressions for immutable retrospective calibration,
  OutcomeCard/assessment reconciliation, effective DecisionEngine inputs, and
  EvaluationContract v2 seed-policy validation.
- `/root/.local/bin/uv run --extra dev pytest -q tests/test_evaluation_contract.py
  tests/test_noise_floor.py tests/test_scientific_assessment.py
  tests/test_promotion.py tests/test_ad_agent_bench.py
  tests/e2e/test_experiment_agents_two_cycle.py` — `17 passed`.
- The dedicated calibration CLI completed successfully and emitted only new
  artifacts under ignored `runs/07h/`; full verification follows before commit.

### Result

- The valid three-seed baseline is now clearly labeled retrospective smoke
  calibration rather than a promotion-capable locked calibration. Historical
  seed-0 Contract and raw OutcomeCards remain unchanged.

### Leftovers

- Implement the thin InterventionAdmissionService and PatchCore07H executor
  adapter before Agent round 1. Then run the two real B_dev cycles using the
  v2 forward contract and effective-assessment decision boundary.

## Plan 07H-B1 — Executor non-empty patch and aggregate admission

### Goal

Close the executor path that could previously report `completed` for an empty
proposal, then add the narrow aggregate admission layer required before an
Executor result may create an ExperimentAttempt.

### Operations

- Made an empty `ExecutorProposal.edits` terminally
  `implementation_failed`; it can no longer pass handoff as a no-op patch.
- Added `InterventionAdmissionService` after the existing per-edit
  PreApply/PostApply gates. It checks the final worktree diff exactly matches
  Executor evidence, obeys the path policy, retains protected hashes, obeys
  non-empty `allowed_parameters`, writes a final patch SHA, and recomputes the
  command-plan SHA before an Attempt exists.
- Moved generic Adapter command construction until after Executor completion,
  so the plan is rebuilt from the final worktree state. Handoff replay reuses a
  previously immutable staging admission rather than re-executing an already
  applied edit.

### Tests

- Added a failure-first regression proving `edits=[]` is rejected.
- `/root/.local/bin/uv run --extra dev pytest -q tests/test_executor_agent.py
  tests/test_executor_handoff.py tests/e2e/test_experiment_agents_two_cycle.py`
  — `7 passed`.

### Leftovers

- B2 still needs the dedicated `PatchCore07HExecutorAdapter`, which will bind
  the admitted worktree to the frozen B_dev/GPU/offline-weight command rather
  than relying on the generic manifest adapter.

## Plan 07H-B2 — Dedicated PatchCore07H command adapter

### Goal

Provide the dedicated, explicit command construction needed for a real 07H
intervention. The adapter must not infer a parameter or silently reuse the
generic repository-manifest command.

### Operations

- Added `PatchCore07HExecutorAdapter` and frozen input schema. It accepts one
  explicit override only, rejects unsupported or unapproved parameters, and
  currently permits only the approved 07H parameters
  `coreset_sampling_ratio`, `target_embed_dimension`, `patchsize`, and
  `anomaly_scorer_num_nn`.
- It derives the actual argv from the frozen smoke-case builder and the final
  worktree entrypoint, writes a command artifact including the approved
  override, then binds that artifact and the rebuilt Worker command SHA.
- The generated plan remains GPU 0, seed 0, B_dev, frozen offline weight,
  network disabled, and 1,800-second bounded. It does not start a process.

### Tests

- Added failure-first tests for a permitted single override and rejected
  unapproved/multi-parameter overrides.
- `/root/.local/bin/uv run --extra dev pytest -q
  tests/test_patchcore_07h_executor_adapter.py tests/test_executor_agent.py
  tests/test_executor_handoff.py` — `8 passed`.

### Leftovers

- Wire this Adapter into the 07H handoff request so its admitted command,
  patch SHA, and scientific identities are created atomically before the first
  physical Agent Attempt.

## Plan 07H-B3 — PatchCore worktree intervention handoff

### Goal

Bind an Executor-produced, actually consumed scientific parameter patch to the
existing Attempt/Job/Worker path without modifying the frozen PatchCore source
checkout or using a bare subprocess.

### Operations

- Added `PatchCore07HInterventionHandoff`. It creates the existing isolated
  worktree, seeds a worktree-local `.autoad/patchcore_07h_intervention.json`
  through `git add -N`, and requires Executor to patch that tracked file.
- The dedicated Adapter reads that patched config, not a caller-side value, and
  rebuilds the real Worker command. Admission then verifies the final diff,
  parameter symbol, protected hashes, patch SHA, and rebuilt command SHA before
  calling `ExperimentAttemptService`.
- This preserves the critical chain: `worktree patch → admitted command artifact
  → ExperimentAttempt → PipelineJob → Worker → ResourceLease → Finalizer`.
  The bootstrap file is not placed in the baseline checkout and no GPU process
  was started.

### Tests

- Python compilation plus `/root/.local/bin/uv run --extra dev pytest -q
  tests/test_patchcore_07h_executor_adapter.py tests/test_executor_agent.py
  tests/test_executor_handoff.py` — `8 passed`.

### Leftovers

- Add an end-to-end handoff fixture, then use the authenticated Coordinator
  provider to create the first real 07H Idea/CognitiveCommit and execute the
  admitted B_dev Attempt.

## Plan 07H-E1 — Real Coordinator/Executor admission and bounded repair

### Goal

Run the first authenticated Coordinator and Executor steps against the physical
07H evidence, retaining all malformed model and control-plane outcomes instead
of converting them into synthetic scientific conclusions.

### Operations

- Created the 07H IdeaTree root and invoked the configured DeepSeek provider
  with the real three-seed evidence. Several raw outputs failed the strict
  CycleDecision schema, tree-ID, single-variable, or artifact-grounding gates;
  those raw responses are retained under ignored 07H runtime artifacts.
- A later schema-and-semantic repair produced a valid B_dev-only child idea:
  `patchsize` from `3` to `2`. `CompactCycleService` persisted
  `idea_000001` and `commit_000001` with the real assessment refs.
- The first authenticated Executor proposal was malformed and was retained as
  an implementation failure. Its one bounded repair passed the existing edit
  and admission gates, but Attempt creation exposed a pre-existing Session
  transition defect: completed baselines had not moved Session status beyond
  `READY_FOR_BASELINE`.
- Advanced the Session to `READY` only after verifying the three completed
  baseline artifacts, then created `attempt_000006`. Worker rejected it before
  GPU startup with `PROCESS_SPAWN_FAILED`: the new Adapter had emitted relative
  program/command/weight paths.
- Preserved `attempt_000006` and all staging evidence. Added a failure-first
  regression for relative Adapter inputs and now resolve program, command,
  repository, dataset, and weight paths before constructing Worker argv.

### Tests

- `/root/.local/bin/uv run --extra dev pytest -q
  tests/test_patchcore_07h_executor_adapter.py tests/test_executor_agent.py
  tests/test_executor_handoff.py` — `8 passed`.

### Leftovers

- Full verification/CI, then a new immutable repair Attempt with the corrected
  absolute command paths. The failed original Attempt and its Admission remain
  untouched.

## Plan 07H-E1 — Preserve virtualenv launcher in real intervention command

### Goal

Repair the physical Agent execution path without changing its admitted
scientific hypothesis, data projection, evaluator, or frozen source checkout.

### Operations

- `attempt_000007` was created through the admitted worktree →
  `ExperimentAttempt` → `PipelineJob` → `ResourceLease` → `Worker` chain and
  retained its complete Executor/Admission evidence. Its child process exited
  before GPU work because the Adapter had resolved the locked virtualenv's
  `bin/python` symlink to the base interpreter, which cannot import the
  virtualenv-installed `click` package.
- A one-shot worker invocation then exercised the restart boundary: with no
  in-memory `Popen` handle it correctly preserved the Attempt as
  `WORKER_LOST`, emitted the Finalizer evidence, and released `lease_000007`.
  `stderr.log` retains the underlying `ModuleNotFoundError`; no artifact was
  deleted or rewritten.
- `PatchCore07HExecutorAdapter` now makes the launcher path absolute while
  preserving its symlink. Repository, dataset, weight, command-file, and
  worktree paths remain resolved absolute paths because they are ordinary data
  paths rather than Python launchers.

### Tests

- Added a failure-first virtualenv-launcher regression: a symlinked
  `venv/bin/python` must remain the Worker program, rather than resolve to its
  base interpreter.
- `/root/.local/bin/uv run pytest tests/test_patchcore_07h_executor_adapter.py -q`
  — `3 passed`.

### Next

- Run repository verification and CI for the launcher repair, then create a
  fresh immutable bounded-repair Attempt. The failed `attempt_000006` and
  `attempt_000007` remain excluded from scientific comparison.
