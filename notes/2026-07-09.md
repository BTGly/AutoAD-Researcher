# 2026-07-09

## Entry 1 - Keep plain repository intake out of baseline drafts

Context:
- User tested `https://github.com/amazon-science/patchcore-inspection/；分析一下这个仓库，能clone`.
- The assistant reply said "baseline repository", the right-side draft inferred PatchCore/baseline repo, and clone used a URL contaminated by the Chinese semicolon.

Changes:
- Clean URL extraction now strips common Chinese trailing punctuation such as `；` across V2 source intake/planning and legacy chat URL extraction.
- Source intake replies now say "code repository" instead of "baseline repository".
- Offline structured source intake registers and queues jobs without writing a ResearchIntentContract draft.
- The right-side Chinese draft no longer infers baseline/baseline_repo from source-only repository URL transcripts when no contract exists.
- Clone failure explanations now classify truncated `tool_git_clone ... Cloning into` errors as transport/network-style clone failures and recommend user-provided mirror URLs or local zip/tar inputs.
- Sidebar job errors now distinguish timeout, TLS/network transfer interruption, truncated clone transport failures, and potentially dirty/inaccessible URLs.

Verification:
- `PATH=/root/.local/bin:$PATH uv run pytest -q tests/test_v2_source_action_planner.py tests/test_v2_turn_gate.py tests/test_v2_intent_contract.py` -> 51 passed.

## Entry 2 - Centralize source reference normalization

Context:
- User pointed out that URL/material handling should not be solved by adding more fixed punctuation/keyword rules.
- Existing project code already had multiple user-message material paths: legacy research chat URL intake/material requests, V2 source service, V2 source action planner, and UI source registry detection.

Changes:
- Added `autoad_researcher.source_normalizer` as the shared layer for concrete source references.
- Explicit git-remote-shaped URLs (`*.git`) are normalized structurally by host/owner/repo without a platform allowlist; typo-like repo names are never semantically corrected and must be validated by git acquisition.
- V2 source service, V2 source action planner, intent contract URL extraction, legacy research chat URL extraction, and UI source registry kind detection now share this normalizer.
- Added tests for Chinese text after repository URLs, typo preservation, mirror `.git` normalization, webpage classification, and multi-source extraction.

Verification:
- `PATH=/root/.local/bin:$PATH uv run pytest -q tests/test_source_normalizer.py tests/test_v2_source_action_planner.py tests/test_v2_intent_contract.py tests/test_ui_sources.py tests/e2e/test_e2e_research_assistant_v04.py` -> 102 passed.

## Entry 3 - Remove repository host allowlist from source normalization

Context:
- User asked whether Gitee/GitLab repository URLs can still clone smoothly.
- User objected that adding one more known host such as GitLab is still a brittle rule; code repository hosts are open-ended.
- Worker acquisition already has a `generic_shallow` path that can clone non-GitHub, credential-free git remotes without GitHub API metadata.

Changes:
- Removed the repository-host allowlist from source normalization.
- Explicit `.git` remotes from any HTTP(S) host are treated as repository sources without LLM.
- Non-`.git` HTTP(S) URLs remain webpages unless user/LLM intent routes them through a repository action; repository actions normalize the URL as a repo candidate without semantic correction.
- Added coverage that arbitrary `code.example.edu/.../repo.git` URLs become repository sources, while arbitrary non-`.git` URLs stay webpages until routed by repo intent.
- Worker coverage still proves non-GitHub repository sources bypass GitHub API metadata and use `generic_shallow` acquisition.

Verification:
- `PATH=/root/.local/bin:$PATH uv run pytest -q tests/test_source_normalizer.py tests/test_v2_source_action_planner.py tests/test_v2_intent_contract.py tests/test_v2_evidence_route.py::test_worker_git_clone_uses_generic_shallow_for_gitlab_url tests/test_v2_evidence_route.py::test_worker_git_clone_uses_repository_acquisition_runner tests/test_ui_sources.py tests/e2e/test_e2e_research_assistant_v04.py tests/test_ui_material_requests.py tests/test_material_subagents.py` -> 132 passed.

## Entry 4 - Start P0 prompt runtime architecture branch

Context:
- User approved starting the P0 prompt-engineering architecture optimization in small, pushable steps.
- Current base before work: branch `main`, clean worktree, latest commit `f1d56c8 fix: use schema-bound research draft prompt`.
- Created development branch `p0-prompt-runtime` for this work.
- Planning source: `/root/autodl-tmp/AI4S/notes/AutoAD-提示词工程参考对比分析-2026-07-09.md`.

P0 scope:
- Step 1: register V2 core prompts through the existing `PromptRegistry` / `PromptProfile` / `PromptIOContract`.
- Step 2: add lightweight V2 LLM trace records without saving API keys, `.env`, raw token usage, or sensitive provider data.
- Step 3: add low-frequency typed events while preserving existing `assistant.delta` / `assistant.done`.
- Step 4: strengthen visible reply streaming/parse robustness tests.

Explicit non-goals:
- No business-code changes in this preparation step.
- No memory, tool registry, research lifecycle, multi-agent, hooks, arbor/experiment-agent implementation, or frontend streaming rewrite in P0 Step 0.
- No changes to `ChatResponse` schema, `PermissionEngine`, or `research_assistant_tool_guard`.

Result:
- Preparation log recorded before verification.
- Next action is to run the repository verification gate, then commit and push this log-only preparation step.

Leftovers:
- Proceed to Step 1 only after Step 0 commit is pushed.

## Entry 5 - Reconfirm P0 prompt runtime Step 0 scope

Context:
- User approved starting P0 prompt-engineering architecture optimization with a strict one-step-at-a-time workflow.
- Current branch at start of this entry: `p0-prompt-runtime`.
- Latest local commit before this log-only step: `6257713 chore: start p0 prompt runtime branch`.
- Worktree status before this log-only step: clean.
- Push workflow reference: `/root/autodl-tmp/AI4S/notes/GitHub推送成功说明-2026-07-09.md`.
- Planning source rechecked: `/root/autodl-tmp/AI4S/notes/AutoAD-提示词工程参考对比分析-2026-07-09.md`.

P0 scope confirmed:
- Step 1: register V2 core prompts in the existing `PromptRegistry` / `PromptProfile` / `PromptIOContract`.
- Step 2: add lightweight V2 LLM trace records with redaction and no API key / `.env` / raw sensitive context persistence.
- Step 3: add low-frequency typed events while preserving `assistant.delta` and `assistant.done`.
- Step 4: strengthen visible reply streaming and parse robustness tests.

Explicit non-goals for P0:
- No arbor / experiment-agent prompt-engineering implementation.
- No memory, tool registry, research lifecycle, multi-agent, or hooks.
- No frontend streaming rewrite and no `ChatResponse` schema change.
- No permission relaxation; `PermissionEngine` and `research_assistant_tool_guard` remain authoritative.
- No API key, `.env`, token-bearing URL, raw token usage, or sensitive raw LLM context in logs, traces, Git, or user-visible replies.

Operations:
- Re-read project `AGENTS.md`, the external prompt-engineering analysis document, and the GitHub push note.
- Confirmed branch and repository state with `git status --short --branch`, `git branch -vv`, `git remote -v`, and recent log inspection.
- No business code changed in this preparation step.

Result:
- Step 0 scope and constraints are recorded before verification.

Leftovers:
- Run the repository verification gate, commit/push this log-only step, and stop before Step 1.

## Entry 6 - Register V2 core prompts in PromptRegistry

Context:
- Step 1 of P0 prompt runtime work: register V2 core inline prompts while preserving existing behavior.
- Planning source rechecked: `/root/autodl-tmp/AI4S/notes/AutoAD-提示词工程参考对比分析-2026-07-09.md`, especially Phase 0 / V2 prompt registration.
- Current branch: `p0-prompt-runtime`.

Changes:
- Added four V2 prompt profiles to the existing `PromptRegistry` / `PromptProfile` / `PromptIOContract`:
  - `assistant.v2.source_action_plan.v1` -> `SourceActionPlan`
  - `assistant.v2.turn_gate.v1` -> `TurnGateDecision`
  - `assistant.v2.need_discovery.v1` -> `RequiredNeedSpec`
  - `assistant.v2.reply_plan.v1` -> `V2ReplyPlanJSON`
- Added `PromptSelector` V2 component mapping so V2 call sites can resolve prompt IDs through the existing registry.
- Updated V2 `source_action_planner`, `turn_gate`, `need_discovery`, and `reply_planner` message builders to read the registered prompt text.
- Kept `include_global=False` for V2 runtime prompt rendering so the LLM-facing system prompt text stays equivalent to the previous inline prompt.
- Added tests confirming the four V2 prompt profiles are registered, have IO contracts and forbidden-output boundaries, and are used by the V2 message builders.

Non-goals preserved:
- No `ResearchOrchestratorV2` refactor.
- No `ChatResponse` schema change.
- No `assistant.delta` / `assistant.done` change.
- No `PermissionEngine` or `research_assistant_tool_guard` change.
- No LLM trace, typed events, memory, tool registry, research lifecycle, hooks, or arbor/experiment-agent implementation.

Verification before full gate:
- `PATH=/root/.local/bin:$PATH uv run pytest -q tests/test_assistant_prompt_registry.py tests/test_assistant_prompt_selector.py tests/test_v2_source_action_planner.py tests/test_v2_turn_gate.py tests/test_v2_intent_contract.py` -> 78 passed.

Result:
- V2 core prompts now have registry profiles and selector mapping while existing V2 behavior remains covered by tests.

Leftovers:
- Run the full repository verification gate, commit/push Step 1, and stop before Step 2.

## Entry 7 - Add redacted V2 LLM trace service

Context:
- Step 2 of P0 prompt runtime work: add lightweight trace records for V2 LLM calls.
- Planning source rechecked: `/root/autodl-tmp/AI4S/notes/AutoAD-提示词工程参考对比分析-2026-07-09.md`, especially Phase 1 / effective prompt trace.
- Current branch: `p0-prompt-runtime`.
- User risk notes rechecked:
  - `assistant.v2.reply_plan.v1` visibility remains a Step 4 streaming/leakage test risk, not a Step 2 schema change.
  - V2 prompt rendering intentionally stays `include_global=False`; trace must record `prompt_render_mode=profile_only` and `include_global=false`.

Changes:
- Added `assistant/v2/llm_trace_service.py` for redacted JSONL trace metadata under run-relative `assistant/llm_traces/llm_traces.jsonl`.
- Trace records include `trace_id`, `run_id`, `call_site`, `prompt_id`, `prompt_version`, `prompt_hash`, `prompt_render_mode`, `include_global`, `model`, `provider_url_host`, `messages_hash`, `raw_output_ref`, `raw_output_hash`, `parse_status`, `schema_validation`, `fallback_reason`, and `latency_ms`.
- Trace records store hashes and provider host only; they do not store API keys, `.env`, full provider URLs, message text, prompt text, or raw model output.
- Added optional `run_dir` threading for V2 LLM trace writing in:
  - `source_action_planner`
  - `turn_gate`
  - `need_discovery`
  - `reply_planner`
- `ResearchOrchestratorV2` and `build_contract_from_context` now pass `run_dir` to the existing V2 LLM call sites.
- Added `PromptSelector.profile_for_v2_component()` so call sites can record prompt id/version from the existing registry instead of duplicating mappings.
- Added tests for trace redaction, prompt id recording, fallback reason recording, provider host extraction, and `profile_only` / `include_global=false` render mode metadata.

Non-goals preserved:
- No `ChatResponse` schema change.
- No `assistant.delta` / `assistant.done` change.
- No typed events, memory, tool registry, research lifecycle, hooks, or arbor/experiment-agent implementation.
- No PermissionEngine or `research_assistant_tool_guard` change.
- No raw sensitive LLM context or token-bearing URL persistence.

Verification before full gate:
- `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_llm_trace_service.py tests/test_v2_source_action_planner.py tests/test_v2_turn_gate.py tests/test_v2_need_discovery.py tests/test_v2_intent_contract.py -q` -> 67 passed.

Result:
- V2 LLM calls now produce auditable, redacted trace metadata when a `run_dir` is available, while direct unit calls without `run_dir` retain old behavior.

Leftovers:
- Run the full repository verification gate, commit/push Step 2, and stop before Step 3.
- Step 4 should specifically test that `reply_plan` hidden/internal fields do not leak through visible streaming, regardless of profile visibility wording.

## Entry 8 - Persist low-frequency V2 typed events

Context:
- Step 3 of P0 prompt runtime work: add low-frequency typed events without replacing `assistant.delta` / `assistant.done`.
- Planning source rechecked: `/root/autodl-tmp/AI4S/notes/AutoAD-提示词工程参考对比分析-2026-07-09.md`, especially typed event stream notes and event naming.
- Current branch: `p0-prompt-runtime`.
- User risk notes rechecked:
  - Step 2 trace still stores metadata/hash only; no raw output artifact is added in this step.
  - `reply_planner` non-JSON passthrough leakage remains a Step 4 streaming/parse robustness target.

Changes:
- Extended existing V2 `event_service.py` with `append_typed_event()` for the P0 low-frequency event whitelist.
- Reused existing `events/events.jsonl` and WS replay path; added `event_to_ws_message()` so replay keeps flattening `{type, payload...}` consistently.
- Added persisted typed events:
  - `planner.source_action.decided`
  - `planner.turn_gate.decided`
  - `planner.need_discovery.decided`
  - `contract.draft.updated`
  - `contract.confirmation.requested`
  - `prompt.trace.created`
  - `schema.validation.failed`
- `prompt.trace.created` and `schema.validation.failed` include only redacted summary fields such as `trace_id`, `call_site`, `prompt_id`, parse/schema status, fallback reason, and latency.
- Kept `assistant.delta` and `assistant.done` as direct chat WS messages; helper functions were added only to lock/test their existing message shape.
- Typed event payloads intentionally avoid prompt text, messages, raw model output, full provider URLs, token-bearing values, and raw user context.

Non-goals preserved:
- No `ChatResponse` schema change.
- No high-frequency token delta persistence in `events.jsonl`.
- No frontend UI rewrite.
- No memory, tool registry, research lifecycle, hooks, multi-agent, or arbor/experiment-agent implementation.
- No PermissionEngine or `research_assistant_tool_guard` change.

Verification before full gate:
- `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_typed_events.py tests/test_v2_llm_trace_service.py tests/test_v2_ws_route.py tests/test_v2_chat_route.py tests/test_v2_source_action_planner.py tests/test_v2_intent_contract.py -q` -> 57 passed.

Result:
- V2 now persists low-frequency planner/contract/trace typed events and replays them through the existing WS event path while old chat streaming messages remain compatible.

Leftovers:
- Run the full repository verification gate, commit/push Step 3, and stop before Step 4.
- Step 4 should test `reply_to_user` ordering, chunk splitting, hidden/internal fields, non-JSON passthrough, fallback behavior, and `assistant.delta` compatibility.

## Entry 9 - Harden ReplyPlanner visible streaming tests

Context:
- Step 4 of P0 prompt runtime work: strengthen ReplyPlanner streaming and parse fallback robustness.
- Planning source rechecked: `/root/autodl-tmp/AI4S/notes/AutoAD-提示词工程参考对比分析-2026-07-09.md`, especially the `reply_to_user` ordering and streaming robustness risks.
- Current branch: `p0-prompt-runtime`.
- Step 3 risk notes rechecked:
  - WS replay currently flattens payload to top-level messages for compatibility; no format refactor in Step 4.
  - All replayable events must remain treated as potentially user-visible and must not contain raw prompt/messages/output or token-bearing values.

Changes:
- Added tests that `reply_to_user` need not be the first JSON key for final replies to stay user-visible and internal-field-free.
- Added split-chunk streaming coverage where hidden fields arrive before `reply_to_user`; `assistant.delta` receives only the visible reply text.
- Added tests for hidden/internal fields in LLM output, including malformed JSON and non-JSON control-style payloads.
- Added a small ReplyPlanner guard: if an otherwise non-JSON reply contains obvious internal control fields such as `contract_updates` or `missing_required_fields`, it uses the existing safe fallback instead of returning the raw text.
- Kept ordinary non-JSON passthrough behavior for replies that do not look like internal control payloads.
- Reconfirmed `assistant.delta` and `assistant.done` message shapes.

Non-goals preserved:
- No `ChatResponse` schema change.
- No prompt registry, trace schema, typed event format, frontend UI, memory, tool registry, research lifecycle, hooks, multi-agent, or arbor/experiment-agent implementation.
- No PermissionEngine or `research_assistant_tool_guard` change.

Verification before full gate:
- `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_intent_contract.py tests/test_v2_typed_events.py tests/test_v2_chat_route.py -q` -> 47 passed.
- `PATH=/root/.local/bin:$PATH uv run pytest tests/test_v2_intent_contract.py tests/test_v2_llm_trace_service.py tests/test_v2_typed_events.py tests/test_v2_chat_route.py tests/test_assistant_prompt_selector.py -q` -> 61 passed.

Result:
- ReplyPlanner now has regression coverage for key-order changes, chunk splitting, hidden/internal fields, malformed JSON, internal-looking non-JSON passthrough, and legacy chat streaming message compatibility.

Leftovers:
- Run the full repository verification gate, commit/push Step 4, and stop.
