# AutoAD-Researcher Step 3.6–3.7 开发计划：Patch Planning, Approval & Controlled Application

> 文档状态：修订稿 v1.5.1（v1.5 targeted_revision_required，3 P0 + 4 P1 已修复）
> 更新日期：2026-06-17
> 阶段名称：**补丁规划、人工审批与受控应用**
> 上游输入：3.5 Experiment Planning Handoff（`experiment_planning_handoff.json`）
> 下游阶段：Step 3.8 Runner Intake & Experiment Execution Integration → Step 3.9 Results Analysis
> 3.6 本身：**只读、只规划、不修改仓库文件**
> 3.7 本身：**审批后写入、不执行完整实验、不宣称实验成功**

---

# 0. 阶段定义与主流程

```
3.5 Experiment Planning Handoff
        ↓
3.6 Narrow Repository Read（仅读取已确认范围）
        ↓
3.6 RepositoryChangePlan（文件级修改计划）
        ↓
3.6 Conflict Analysis + Workspace Layout
        ↓
3.6 Patch Materialization（生成真实代码载荷）
        ↓
3.6 PatchPayload Manifest + Proposed Patch Diff
        ↓
3.6 Deterministic Payload Validation
        ↓
3.7 Approval Request
  ├── 用户审批 exact change IDs + exact payload SHA
  ├── approve / revise / reject
  └── 审批绑定 patch_payload_manifest_sha256 + proposed_patch_diff_sha256
        ↓
3.7 Application Preflight
  ├── plan SHA / payload manifest SHA / diff SHA 三方绑定
  ├── repository fingerprint 匹配
  ├── workspace 存在
  └── validation report passed
        ↓
3.7 Controlled Patch Application
        ↓
3.7 Local Validation（受控命令执行）
        ↓
patch_applied_and_local_validations_passed
        ↓
eligible_for_runner_intake
        ↓
3.8 Runner Intake & Experiment Execution Integration
```

**合并写一份计划是合理的，但必须保留"只读规划"（3.6）和"审批后写入"（3.7）两道强制边界。**

核心不变量：

```
approved_change_ids 是审批权威粒度
approved_paths 从 approved_change_ids 确定性派生
用户审批绑定的是确切代码 payload（patch_payload_manifest_sha256 + proposed_patch_diff_sha256）
policy allow scope 缺失 → preflight blocked（default-deny，无例外）
deny 永远不能被用户审批覆盖
3.7 成功终态只能是 patch_applied_and_local_validations_passed
Handoff 状态为 eligible_for_runner_intake，不是 runner_ready 或 experiment_ready
Change model 分离 change_kind 和 target_mode
任何影响 canonical SHA 的变化 → 旧审批自动失效 → 重新审批
```

---

# 1. 上游 Contract

3.6 不直接拼接 `ExperimentPlan` + `ImplementationVariant`，而是消费单一 handoff：

```python
class ExperimentPlanningHandoff(BaseModel):
    handoff_id: str
    run_id: str
    experiment_plan_sha256: str
    selected_variant_ids: list[str]
    variant_artifact_sha256: str
    repository_context_sha256: str
    # 引用 3.5 artifacts，不内嵌
```

3.6 按需通过 artifact ref 读取具体内容。避免 3.4 与 3.5 版本错位。

---

# 2. 输出 Artifacts

## 3.6 输出

| Artifact | 说明 |
|---|---|
| `repository_change_plan.json` | 唯一文件级修改事实源 |
| `patch_conflict_analysis.json` | 多 Variant 冲突检测 |
| `patch_plan_validation_report.json` | 确定性验证报告 |
| `patch_payload_manifest.json` | 所有 PatchPayload 的清单 + SHA |
| `proposed_patch.diff` | 用户审批前可见的统一 diff |
| `patch_payload_validation_report.json` | payload 级确定性验证 |

## 3.7 输出

| Artifact | 说明 |
|---|---|
| `approval_request.json` | 审批请求（绑定 plan + payload + diff 三个 SHA） |
| `approval_record.json` | 审批决策 |
| `patch_application_manifest.json` | 唯一 canonical 写入记录（含 changed_files 子表） |
| `patch.diff` | 实际应用的 unified diff |
| `post_patch_validation_report.json` | 局部验证报告 |
| `rollback_manifest.json` | 回滚记录 |
| `patch_runner_handoff.json` | 交给 Runner Intake |

`changed_files_manifest.json` 不独立存在—是 `patch_application_manifest.json` 的 `changed_files` 字段，不可单独编辑。

---

# 3. 核心数据模型

## 3.1 PlannedRepositoryChange

```python
class PlannedRepositoryChange(BaseModel):
    change_id: str
    workspace_id: str
    change_kind: Literal["create", "modify", "delete", "rename", "configuration_only", "test_only"]
    target_mode: Literal["existing_target", "new_target"]
    hook_id: str | None = None
    existing_symbol_id: str | None = None
    proposed_symbol: str | None = None
    payload_id: str | None = None          # 关联 PatchPayload
    repository_path: str                   # validate_workspace_path 已校验
    rename_target_path: str | None = None
    variant_ids: list[str]
    rationale: str
    symbol_delta: SymbolContractDelta | None
    interface_delta: InterfaceContractDelta | None
    target_collision_policy: Literal["must_not_exist", "replace_existing"] = "must_not_exist"
```

**关键规则**：
- `change_kind` 与 `target_mode` 解耦
- `create` → `target_mode=new_target`；文件已存在 + `must_not_exist` → 失败
- `modify/delete/rename` → `target_mode=existing_target`
- `rename_target_path` 必填；目标已存在 + `must_not_exist` → 失败

## 3.2 PatchPayload

```python
class PatchPayload(BaseModel):
    payload_id: str
    change_id: str
    payload_kind: Literal["unified_diff", "full_after_content"]
    before_sha256: str | None
    target_path: str
    payload_artifact_id: str         # ArtifactStore identifier
    payload_media_type: Literal["text/x-diff", "application/octet-stream"]
    payload_size_bytes: int
    payload_sha256: str

class PatchPayloadManifest(BaseModel):
    """所有 payload 的清单，整体计算 SHA 供审批绑定。"""
    manifest_id: str
    run_id: str
    patch_plan_sha256: str
    workspace_id: str
    payloads: list[PatchPayload]
    proposed_diff_artifact_id: str
    proposed_diff_sha256: str
    manifest_sha256: str

class PatchPayloadValidationIssue(BaseModel):
    issue_id: str
    payload_id: str | None
    change_id: str | None
    category: Literal[
        "payload_sha_mismatch", "before_sha_mismatch",
        "undeclared_path", "undeclared_file_creation",
        "file_mode_change", "symlink_change", "submodule_change",
        "unsupported_payload_kind",
    ]
    description: str
    resolution: Literal["regenerate", "blocked"]

class PatchPayloadValidationReport(BaseModel):
    report_id: str
    patch_plan_sha256: str
    payload_manifest_sha256: str
    status: Literal["passed", "failed"]
    issues: list[PatchPayloadValidationIssue]
    validated_at: datetime
```

MVP 只保留 `unified_diff` 和 `full_after_content`。Payload artifact 通过 ArtifactStore 解析，不接受任意本地路径。

## 3.3 PatchPayload 生产者：PatchMaterializer

```
PatchMaterializer / CodeSynthesisAgent
  输入: RepositoryChangePlan + narrow repository context
  输出: PatchPayload + proposed_patch.diff + PatchPayloadManifest
  权限: 只读仓库、只写 artifact，不写工作树
```

`DeterministicPatchPayloadValidator` 检查：
- `payload.change_id` 存在且 `target_path` 与 change 匹配
- `payload_sha256` 匹配
- `before_sha256` 匹配当前文件
- diff 只触碰当前 workspace 中 RepositoryChangePlan 声明的路径
- diff 不新增未声明文件
- diff 不修改文件模式、symlink 或 submodule

## 3.4 ApprovalRequest（per-workspace）

每个 workspace 独立审批，绑定 workspace_id：

```python
class ApprovalRequest(BaseModel):
    approval_request_id: str
    run_id: str
    workspace_id: str                              # 审批作用域

    patch_plan_sha256: str
    patch_payload_manifest_sha256: str              # 该 workspace 的 manifest SHA
    proposed_patch_diff_sha256: str                  # 该 workspace 的 diff SHA
    patch_payload_validation_report_sha256: str      # Payload 已验证的证明
    patch_plan_validation_report_sha256: str
    repository_before_fingerprint: str
    selected_variant_ids: list[str]
    overall_risk_level: Literal["low", "medium", "high"]
    workspace_summary: WorkspaceApprovalSummary
    internal_validation_steps: list[InternalValidationStep] = Field(default_factory=list)
    external_validation_commands: list[ExternalValidationCommand] = Field(default_factory=list)
    created_at: datetime
```

用户批准的是**确定的代码内容**，而不仅是"打算改哪个 symbol"。

## 3.5 ApprovalDecision（判别联合，per-workspace）

全量审批和部分审批使用不同模型，Preflight 按类型验证对应 SHA。
`approve_all` = 当前 workspace 中全部 non-blocked change IDs。

```python
class FullApprovalDecision(BaseModel):
    decision: Literal["approve_all"]
    decision_id: str
    approval_request_id: str
    workspace_id: str                                 # 审批作用域
    patch_plan_sha256: str
    payload_manifest_sha256: str                       # 该 workspace manifest
    approved_diff_sha256: str                          # 该 workspace diff
    approved_change_ids: list[str]                     # = 该 workspace 全部 non-blocked
    approved_paths: list[str]
    approved_ask_paths: list[str]
    approved_internal_step_ids: list[str]
    approved_external_command_ids: list[str]
    approved_collision_change_ids: list[str]            # replace_existing 的显式审批
    user_evidence_id: str
    decided_at: datetime

class PartialApprovalDecision(BaseModel):
    decision: Literal["approve_partial"]
    decision_id: str
    approval_request_id: str
    workspace_id: str
    patch_plan_sha256: str
    payload_manifest_sha256: str
    approval_patch_bundle_sha256: str                   # subset bundle SHA
    approved_change_ids: list[str]                      # 用户选择的子集
    rejected_change_ids: list[str]
    approved_paths: list[str]
    approved_ask_paths: list[str]
    approved_internal_step_ids: list[str]
    approved_external_command_ids: list[str]
    approved_collision_change_ids: list[str]
    user_evidence_id: str
    decided_at: datetime

class RejectDecision(BaseModel):
    decision: Literal["reject", "revise"]
    decision_id: str
    workspace_id: str
    approval_request_id: str
    patch_plan_sha256: str
    reason: str
    user_evidence_id: str
    decided_at: datetime

ApprovalDecision = Union[FullApprovalDecision, PartialApprovalDecision, RejectDecision]
```

## 3.6 ApprovalPatchBundle

`approve_partial` 时，用户选择的 change 子集必须生成独立的 subset diff：

```python
class ApprovalPatchBundle(BaseModel):
    """部分审批时，批准的 change IDs 与对应的 subset diff 一起打包。"""
    bundle_id: str
    patch_plan_sha256: str
    workspace_id: str

    approved_change_ids: list[str]         # 用户选择的子集
    approved_payload_ids: list[str]        # 对应 payload

    approved_diff_artifact_id: str         # subset-specific diff
    approved_diff_sha256: str

    payload_manifest_sha256: str           # 原始全量 manifest（不变）
    bundle_sha256: str                     # 本 bundle 的 canonical SHA
```

流程：

```
用户选择部分 changes
→ 生成 subset-specific diff（只含批准变更）
→ 计算 ApprovalPatchBundle SHA
→ 用户批准该 bundle
→ 3.7 只能应用该 bundle 中的 change IDs + diff
→ 全量 diff SHA 与 部分 change IDs 不再共用一个审批对象
```

`ApprovalPatchBundleValidator` 确定性验证：

```text
- bundle.approved_payload_ids == manifest 中 change_id ∈ approved_change_ids 的全部 payload IDs
- 无缺失 payload / 无额外 payload
- 无跨 workspace change（bundle.workspace_id == manifest.workspace_id）
- subset diff SHA（approved_diff_sha256）与投影结果一致
- subset diff 不包含未批准路径
- bundle_sha256 == canonical SHA(bundle)
```

## 3.7 审批失效规则

任何影响 canonical SHA 的变化 → 旧审批自动失效：

```
payload 生成后
workspace 重排后
conflict resolution 后
validation command 变化后
→ plan_changed → approval_invalidated → approval_pending
```

---

# 4. Path 安全模型（严格 default-deny）

```
1. path 或 ancestor 在 deny → DENY
2. change_id 未在 approved_change_ids → DENY
3. path 未从 approved_change_ids 派生 → DENY
4. path 在 ask-list 且未显式批准 → DENY
5. allow scope 缺失 → DENY
6. path 不在 allow scope → DENY
→ ALLOW
```

`allow scope` 必须显式配置。未配置 → preflight blocked，不进入写入。

## 路径逃逸防护

```
模型级: validate_workspace_path（拒绝 ..、绝对路径）
运行时: resolve() + 确认在 repository_root 内
```

---

# 5. 创建与重命名冲突策略

```
create + target_mode=new_target
  ├── 文件不存在 → 创建
  └── 文件已存在
      ├── target_collision_policy=must_not_exist → patch_application_failed
      └── target_collision_policy=replace_existing → 需单独审批 → 保存 before_blob

rename + 目标路径
  ├── 目标不存在 → 正常 rename
  ├── 目标已存在 → 默认失败
  └── target_collision_policy=replace_existing → 单独审批 → 保存 target_before_blob
```

---

# 6. Narrow Repository Read Contracts

```python
class SymbolQuery(BaseModel):
    symbol_name: str
    container_path: str
    query_reason: str

class NarrowRepositoryReadRequest(BaseModel):
    repository_source_id: str
    repository_commit: str
    allowed_paths: list[str]
    requested_paths: list[str]
    requested_symbols: list[SymbolQuery]
    max_files: int = 20
    max_bytes: int = 524288
    max_symbol_searches: int = 10
    purpose: Literal["patch_planning"]
    originating_variant_ids: list[str]

class RepositoryScopeExpansionRequired(BaseModel):
    status: Literal["return_to_3_1"]
    cause_variant_ids: list[str]
    cause_change_ids: list[str]
    cause_hook_ids: list[str]
    missing_paths: list[str]
    missing_symbols: list[str]
    required_evidence_types: list[str]
    current_repository_source_id: str
    current_repository_commit: str
    current_scope_sha256: str
    reason: str
    completion_criteria: list[str]
```

超出已确认范围 → `return_to_3_1`，不扩大读取。

---

# 7. Workspace 共享 Change 方案

当前 `workspace_id` 是单值。多 workspace 共享 change (如公共 `adapter.py`) 由布局阶段克隆实例：

```python
class WorkspaceChangeBinding(BaseModel):
    workspace_id: str
    source_change_id: str          # 原始 change ID
    workspace_change_id: str       # 该 workspace 内的克隆 ID

def clone_shared_changes(plan, bindings) -> RepositoryChangePlan:
    """为每个 workspace 创建独立的 change 实例。"""
    # 每个 workspace 克隆后拥有独立的审批、应用、diff 和 manifest
```

每个 workspace 都拥有独立的 change 实例，保证审批和应用一一对应。

---

# 8. 验证与受控执行

## 8.1 内部确定性检查（直接函数调用，非 subprocess）

以下检查由 AutoAD Core 直接执行受信任 Python 函数，不使用 `subprocess.run` 或 `python -c`：

```python
class InternalValidationStep(BaseModel):
    step_id: Literal[
        "ast_parse",
        "diff_integrity",
        "path_containment",
        "import_declaration_scan",
    ]
    target_artifact_ids: list[str]
    required: bool = True

INTERNAL_VALIDATION_STEPS = [
    InternalValidationStep(step_id="ast_parse", target_artifact_ids=[], required=True),
    InternalValidationStep(step_id="diff_integrity", target_artifact_ids=[], required=True),
    InternalValidationStep(step_id="path_containment", target_artifact_ids=[], required=True),
    InternalValidationStep(step_id="import_declaration_scan", target_artifact_ids=[], required=False),
]
```

对应实现函数：

```text
validate_ast(artifact_ref)       # AST.parse，不执行代码
validate_diff_integrity(diff)    # difflib 文本比对
validate_path_containment(paths) # 路径 containment
scan_import_declarations(paths)  # 静态 import 声明扫描
```

内部检查不通过 subprocess，不涉及 `argv` 或 `executable`，不存在 `python -c` 风险。

## 8.2 外部受控命令（仅限静态分析工具）

3.7 外部命令只允许 ruff 的只读模式：

```python
class ExternalValidationCommand(BaseModel):
    command_id: str
    template_id: Literal["ruff_check_no_fix", "ruff_format_check"]
    resolved_argv: list[str]          # 审批固化
    working_directory: str            # workspace 内
    required: bool = True

EXTERNAL_TEMPLATES = {
    "ruff_check_no_fix": ExternalValidationCommand(
        command_id="cmd_ruff_check", template_id="ruff_check_no_fix",
        resolved_argv=["ruff", "check", "--no-fix", "--no-unsafe-fixes"],
        working_directory=".", required=True,
    ),
    "ruff_format_check": ExternalValidationCommand(
        command_id="cmd_ruff_format", template_id="ruff_format_check",
        resolved_argv=["ruff", "format", "--check"],
        working_directory=".", required=True,
    ),
}
```

ruff 约束：
- `ruff check` 必须带 `--no-fix` / `--no-unsafe-fixes`
- `ruff format` 必须带 `--check`（禁止无 `--check`）

以下工具**禁止在 3.7 中执行**，全部移交 3.8 Runner sandbox：
`python`、`python3`、`pytest`、`mypy`、`pylint`、`compileall`、`pip`、`curl`

## 8.3 范围边界

```
3.7 允许:
  - AST.parse() 语法检查（内部函数，不执行代码）
  - difflib diff 完整性校验（内部函数）
  - 路径 containment 检查（内部函数）
  - import 声明扫描（内部函数，不实际导入）
  - ruff check（--no-fix / --no-unsafe-fixes）
  - ruff format --check

3.7 禁止（全部移交 3.8 Runner sandbox）:
  - 任何 python -c / python <script.py>
  - pytest / mypy / pylint（均导入并执行仓库代码）
  - compileall（写入 __pycache__，产生 dirty state）
  - 任何 shell 或 subprocess 网络操作
```

---

# 9. Rollback 方案

## 主策略：隔离 worktree + 销毁

```
separate worktree → 失败时销毁 worktree（最优）
```

## 辅助策略：artifact blob 恢复

```python
class FileSnapshotRef(BaseModel):
    path: str
    blob_sha256: str
    blob_artifact_ref: str
    file_mode: int | None = None
    symlink_target: str | None = None
```

回滚校验：
- `fingerprint == repository_before_fingerprint` → `rolled_back`
- `fingerprint != repository_before_fingerprint` → `rollback_failed`

---

# 10. 应用规则

```
modify/delete/rename + 文件不存在 → patch_application_failed
create + 文件已存在 + must_not_exist → patch_application_failed
rename + 目标已存在 → patch_application_failed（除非显式 replace_target）
逐 change try/except → 异常收集到 failed_changes
部分写入 → patch_application_partial_failure（可审计）
全部失败 → patch_application_failed
```

---

# 11. Preflight

在打开任何文件前强制检查。按四组验证：

```
A. Common binding（approve_all 和 approve_partial 均适用）

  1. decision.approval_request_id == request.approval_request_id
  2. decision.workspace_id == request.workspace_id == manifest.workspace_id
  3. compute_canonical_plan_sha256(plan) == plan.patch_plan_sha256
  4. decision.patch_plan_sha256 == plan.patch_plan_sha256
     == request.patch_plan_sha256
  5. decision.payload_manifest_sha256 == manifest.manifest_sha256
     == request.patch_payload_manifest_sha256
  6. _fingerprint(repository_root) == plan.repository_fingerprint
  7. run_id == plan.run_id
  8. workspace_id ∈ plan.workspace_plans
  9. PatchPlanValidationReport:
     - sha(report) == request.patch_plan_validation_report_sha256
     - report.run_id == plan.run_id
     - report.plan_sha256 == plan.patch_plan_sha256
     - report.status == passed, report.issues == []
 10. PatchPayloadValidationReport:
     - sha(report) == request.patch_payload_validation_report_sha256
     - report.patch_plan_sha256 == plan.patch_plan_sha256
     - report.payload_manifest_sha256 == manifest.manifest_sha256
     - report.status == passed, report.issues == []
 11. derive_paths(decision.approved_change_ids) == decision.approved_paths

B. Full approval binding

 12. decision.approved_diff_sha256 == manifest.proposed_diff_sha256
 13. sha(diff_artifact) == decision.approved_diff_sha256
 14. approved_change_ids == workspace 全部 non-blocked changes

C. Partial approval binding

 15. decision.approval_patch_bundle_sha256 == canonical_sha(bundle)
 16. decision.workspace_id == bundle.workspace_id
 17. set(decision.approved_change_ids) == set(bundle.approved_change_ids)
 18. sha(bundle.approved_diff_artifact) == bundle.approved_diff_sha256

D. Collision and policy binding

 19. approved_collision_change_ids ⊆ approved_change_ids
 20. 所有 target_collision_policy=replace_existing 且目标当前存在的 change
     ∈ approved_collision_change_ids
 21. 无碰撞策略的 change ∉ approved_collision_change_ids
```

---

# 12. 状态机

```
planning_initialized
→ repository_attested
→ patch_planning
→ patch_payload_generation
→ patch_payload_validating
→ approval_pending
    ├── approve → patch_applying
    ├── revise → patch_planning（或 payload 重生成）
    └── reject → rejected

payload/workspace/conflict 变化 → plan_changed → approval_invalidated → approval_pending

patch_applying
    ├── full success → patch_applied
    ├── partial → patch_application_partial_failure
    ├── failed → patch_application_failed
    └── stale → replan_required

patch_applied → post_patch_validating
    ├── passed → patch_applied_and_local_validations_passed
    └── failed → patch_applied_but_local_validation_failed

patch_applied_and_local_validations_passed → eligible_for_runner_intake

recovery: repair_or_rollback_pending → rollback_running → rolled_back | rollback_failed
```

---

# 13. 负向测试矩阵

封板前必须覆盖：

```
[ ] payload SHA 不匹配 → blocked
[ ] payload 修改未审批路径 → payload validation 失败
[ ] diff 包含额外文件 → payload validation 失败
[ ] create 覆盖已有文件 → application_failed
[ ] rename 目标冲突 → application_failed
[ ] modify/delete 文件不存在 → application_failed
[ ] 同文件多 change 逆序回滚 → 恢复原始内容
[ ] rollback fingerprint 不匹配 → rollback_failed
[ ] symlink escape → containment blocked
[ ] allow scope 缺失 → preflight blocked
[ ] ask 路径未批准 → write denied
[ ] required command 未批准 → validation failed
[ ] 非法 executable → validation failed
[ ] validation 产生 __pycache__ → dirty state 检测
[ ] workspace shared change 克隆 → 两个 workspace 独立应用
[ ] payload 变化 → 旧审批失效 → reapproval_required
[ ] plan SHA 变化 → preflight blocked
[ ] stale repository fingerprint → preflight blocked
```

---

# 14. 当前实现状态

| 模块 | 状态 |
|---|---|
| 核心 schemas（30+ 模型） | 已实现（`PatchPayloadManifest`、`PayloadValidationReport`、`ApprovalPatchBundle`、`ValidationCommandTemplate`、`target_collision_policy` 为 contract-only） |
| PatchPlannerAgent | 已实现 |
| PatchConflictAnalyzer（path + symbol） | 已实现 |
| Deterministic Validator | 已实现 |
| Approval Protocol | legacy implementation / v1.5.1 contracts pending |
| ControlledPatchApplicator | experimental（real-write disabled） — preflight/rollback/rename 骨架可用 |
| Permission Profiles | 已实现 |
| Canonical SHA | 已实现 |
| apply_workspace_layout | 已实现 |
| PatchPayload schema（contract） | 已实现 |
| PatchMaterializer | **未实现** — contract 已定义 |
| DeterministicPatchPayloadValidator | **未实现** |
| WorkspaceChangeBinding / clone_shared_changes | **未实现** |
| InternalValidationStep | contract only |
| FileSnapshotRef + artifact blob 存储 | **未实现** |
| NarrowRepositoryRead 集成 | **未实现**（contract 已定义） |
| ExperimentPlanningHandoff adapter | **未实现** |

---

# 15. 遗留问题

| 问题 | 优先级 | 说明 |
|---|---|---|
| PatchMaterializer 未实现 | P0 | PatchPayload contract 已定义，需产生真实代码载荷的 agent |
| Payload Validator 未实现 | P0 | 需检查 payload SHA、before_sha256 匹配、路径范围、diff 内容 |
| WorkspaceChangeBinding 未实现 | P1 | shared change 跨 workspace 克隆 |
| ExternalValidationCommand templates | contract only |
| FileSnapshotRef 未实现 | P1 | rollback 仍用 inline base64，大文件需 artifact ref |
| ast_edit_script 暂缓 | P2 | MVP 只保留 unified_diff / full_after_content |

---

# 16. 封板条件

## 16.1 开发计划封板条件（Design Plan Seal）

```
[ ] 数据模型闭合（PlannedRepositoryChange, PatchPayload, PatchPayloadManifest,
      ApprovalRequest, Full/PartialApprovalDecision, ApprovalPatchBundle,
      PatchExecutionResult, PatchRunnerHandoff）
[ ] 状态机闭合（planning → approval → application → validation → handoff）
[ ] 权限边界闭合（3.6 read-only, 3.7 controlled write, default-deny）
[ ] 审批合同闭合（per-workspace, Full vs Partial, Payload binding, collision approval）
[ ] 内部检查与外部命令分离（InternalValidationStep vs ExternalValidationCommand）
[ ] 无合同矛盾（所有 Literal、model_validator、Preflight 规则自洽）
[ ] 负向测试矩阵完整（18 项）
```

## 16.2 实现封板条件（Implementation Seal）

```
[ ] PatchMaterializer 实现（RepositoryChangePlan → PatchPayload → proposed_patch.diff）
[ ] DeterministicPatchPayloadValidator 实现
[ ] InternalValidationStep 四函数实现
[ ] ExternalValidationCommand ruff 模板实现
[ ] create/rename collision policy 实现
[ ] NarrowRepositoryRead 集成到 3.6 流程
[ ] WorkspaceChangeBinding + clone_shared_changes 实现
[ ] Approval contract 从 legacy 升级到 v1.5
[ ] Applicator 解除 experimental / real-write disabled
[ ] 负向测试矩阵 18 项全部通过
```

---

## 版本修订历史

### v1.0 → v1.1

| v1.0 问题 | v1.1 修正 |
|---|---|
| P0-1: 下游 3.8/3.9 名称冲突 | 改为 3.8 Runner Intake & Experiment Execution, 3.9 Results Analysis |
| P0-2: 审批仅绑定 plan SHA | 增加 `patch_payload_manifest_sha256` + `proposed_patch_diff_sha256` 绑定 |
| P0-3: PatchPayload 生产者未定义 | 新增 PatchMaterializer + DeterministicPatchPayloadValidator |
| P0-4: "scope 未配置 → ALLOW" 与 default-deny 矛盾 | 改为 "scope 缺失 → DENY" |
| P0-5: create+文件已存在 → 覆写 | 改为 `target_collision_policy`（must_not_exist / replace_existing） |
| P0-6: ValidationCommand 无执行策略 | 新增 executable allowlist + shell=False + network_deny |
| P0-7: Rollback inline base64 不适合大文件 | 新增 FileSnapshotRef + blob_artifact_ref；失败回滚标记 rollback_failed |
| P0-8: Narrow Repository Read 缺 contract | 补充 `NarrowRepositoryReadRequest` + `RepositoryScopeExpansionRequired` |
| P0-9: 多 workspace shared change 未解决 | 新增 `WorkspaceChangeBinding` + `clone_shared_changes()` |
| P0-10: 审批失效流程不完整 | 明确 payload/workspace/conflict/command 变化 → 旧审批失效 |
| P1-1: 上游拼接多 artifact | 改为单一 `ExperimentPlanningHandoff` |
| P1-2: "3.7 最终状态只能是 passed" 措辞 | 改为 "3.7 成功终态只能是 patch_applied_and_local_validations_passed" |
| P1-3: changed_files_manifest 独立文件 | 归入 manifest.changed_files 子表 |
| P1-4: 缺少负向测试计划 | 新增第 13 章负向测试矩阵（18 项） |

### v1.1 → v1.2

| v1.1 问题 | v1.2 修正 |
|---|---|
| P0-1: 部分审批 + 全量 diff SHA 歧义 | 新增 `ApprovalPatchBundle`（subset diff + change IDs 打包） |
| P0-2: Payload Manifest/Report 缺正式 schema | 新增 `PatchPayloadManifest` / `PatchPayloadValidationIssue` / `PatchPayloadValidationReport` |
| P0-3: executable allowlist 不防 `python -c` | 新增 `ValidationCommandTemplate`（fixed_prefix_argv 锁定） |
| P1-1: `existing_target_policy` 命名 | 改为 `target_collision_policy` |
| P1-2: `payload_artifact_ref` 任意路径 | 改为 `payload_artifact_id`（ArtifactStore identifier） |
| P1-3: rollback_failed 未进入 schema | 状态机 + PatchExecutionResult 支持 `rollback_failed` |
| Code: `ast_edit_script` 仍在 schema | 从 `payload_kind` Literal 中移除 |
| Code: rollback fingerprint 不匹配仍返 rolled_back | 改为 `rolled_back` vs `rollback_failed` 分支 |

### v1.2 -> v1.3

| v1.2 问题 | v1.3 修正 |
|---|---|
| P0-1: ApprovalDecision 未绑定 bundle | FullApprovalDecision / PartialApprovalDecision / RejectDecision 判别联合；Preflight 按类型验证 |
| P0-2: Preflight 缺 Payload Validation | 九项校验（新增 Payload manifest SHA + Payload Validation Report passed） |
| P0-3: Sandbox deferred 与安全承诺矛盾 | 3.7 MVP 只允许不导入仓库代码的静态检查；pytest/mypy/pylint/compileall 移交 3.8 |
| P1-1: Manifest workspace 作用域 | 每 workspace 一个 Manifest + 一个 proposed diff |
| P1-2: ArtifactStore 缺 media_type | 增加 payload_media_type / payload_size_bytes |
| P1-3: 实现状态过于乐观 | 新增 schema 标为 contract-only |
| Code: rollback_failed 未进 schema | 加入 PatchExecutionResult.overall_status Literal；applicator 返回 rollback_failed |

### v1.3 -> v1.4

| v1.3 问题 | v1.4 修正 |
|---|---|
| P0-1: 审批未明确 workspace 作用域 | ApprovalRequest + Full/PartialApprovalDecision 均增加 workspace_id；approve_all = 当前 workspace 全部 non-blocked |
| P0-2: Payload Validation Report SHA 未进模型 | ApprovalRequest 强制字段 `patch_payload_validation_report_sha256`；Preflight 校验报告内容与 SHA |
| P0-3: ValidationCommand 禁止/允许矛盾 | 删除所有执行型模板（pytest/mypy/pylint/compileall）；只保留静态检查模板；ruff 强制 --no-fix / --check |
| P0-4: Partial bundle 缺投影验证 | 新增 ApprovalPatchBundleValidator 确定性规则（payload ID 映射、diff 一致性、workspace 匹配） |
| P1-1: media_type 在历史但不在模型 | PatchPayload 增加 payload_media_type + payload_size_bytes |
| P1-2: replace_existing 审批缺合同 | Full/PartialApprovalDecision 增加 approved_collision_change_ids |
| P1-3: 实现状态过度声明 | Approval Protocol | legacy implementation / v1.5.1 contracts pending |
| P1-4: 封板条件措辞 | executable allowlist 强制 -> static template registry + execution-capable delegated to 3.8 |

### v1.4 -> v1.5

| v1.4 问题 | v1.5 修正 |
|---|---|
| P0-1: ValidationCommand 内部检查与 python -c 禁令矛盾 | 分离 InternalValidationStep（直接函数调用）与 ExternalValidationCommand（仅 ruff）；删除所有 python -c/subprocess 包装的内部检查 |
| P0-2: Decision-Bundle 逐字段一致性未进入 Preflight | Preflight 20 项校验（含 decision.workspace_id==bundle.workspace_id、approved_change_ids 集合相等、derive_paths 一致性等） |
| P0-3: Payload Validator 引用了尚不存在的"批准路径" | 改为"当前 workspace 中 RepositoryChangePlan 声明的路径"；批准子集约束放入 Bundle Validator |
| P1-1: RejectDecision 缺 workspace 绑定 | 增加 workspace_id / approval_request_id / patch_plan_sha256 |
| P1-2: collision approval 缺精确规则 | 补充 replace_existing change 强制 ∈ approved_collision_change_ids；无 collision 的 change 不得进入 |
| P1-3: workspace_summaries 列表可跨 workspace | 改为 workspace_summary: WorkspaceApprovalSummary \| None |
| P1-4: 计划封板与实现封板未区分 | 拆为 16.1（Design Plan Seal）和 16.2（Implementation Seal） |

### v1.5 -> v1.5.1

| v1.5 问题 | v1.5.1 修正 |
|---|---|
| P0-1: 旧版 ValidationCommand + EXECUTABLE_ALLOWLIST 未删除 | 彻底删除；ApprovalRequest 改为 internal_validation_steps + external_validation_commands；Decision 改为 approved_internal_step_ids + approved_external_command_ids |
| P0-2: Full/Partial Decision 缺 approval_request_id | 增加；SHA 字段统一为 patch_plan_sha256 |
| P0-3: Preflight 23 项平铺 + 缺 artifact SHA 校验 | 重组为 A/B/C/D 四组 21 项；增加 sha(report) 校验 |
| P1-1: workspace_summary 可选 | 改为必填 WorkspaceApprovalSummary |
| P1-2: replace_target 命名不统一 | 统一为 target_collision_policy=replace_existing |
| P1-3: 测试矩阵未覆盖 v1.5 新合同 | 扩展至 30 项 |
| P1-4: 实现状态术语过时 | 更新为 InternalValidationStep/ExternalValidationCommand/v1.5.1 |
