# 3.0D Real Attempt Readiness

Project date: 2026-06-17

## Status

`blocked`

The 3.0D generic contracts are in place for asset preparation, immutable
attempt execution, metrics parsing, scientific validity, and reproducibility
comparison. The real PatchCore/MVTec attempt is not executable yet because the
local benchmark prerequisites are not in the required preflight locations.

## Checked Facts

| Check | Result |
|---|---|
| GPU availability | Passed: NVIDIA GeForce RTX 4090, driver 550.107.02 |
| PatchCore source availability | Passed: source exists outside the project workspace at commit `fcaa92f124fb1ad74a7acf56726decd4b27cbcad` |
| Repository workspace boundary | Failed: `workspace/repos` is absent |
| Repository detached HEAD | Failed: discovered source is on `refs/heads/main` |
| Dataset root environment | Failed: `AUTOAD_INTERNAL_BENCHMARK_DATASET_ROOT` is not set |
| Dataset workspace boundary | Failed: `workspace/datasets` is absent |
| Benchmark Python environment | Failed: `workspace/envs` is absent |

## Gate Decision

Do not create `attempt_01` or `attempt_02` evidence from mocks. The 3.0 final
seal remains blocked until a real preflight passes and two independent real
attempts produce parsed required metrics and a reproducibility report.

## Next Required Actions

1. Create ignored local directories: `workspace/repos`, `workspace/datasets`,
   and `workspace/envs`.
2. Put a clean detached checkout of `amazon-science/patchcore-inspection` at
   commit `fcaa92f124fb1ad74a7acf56726decd4b27cbcad` under `workspace/repos`.
3. Put MVTec AD bottle data under `workspace/datasets` and set
   `AUTOAD_INTERNAL_BENCHMARK_DATASET_ROOT` to that dataset root.
4. Build the locked benchmark Python environment under `workspace/envs` using
   `configs/benchmarks/environments/patchcore_linux_gpu/requirements.lock.txt`.
5. Run `scripts/benchmark/preflight.py` for `attempt_01`; only execute the
   real controlled attempt after preflight passes.
