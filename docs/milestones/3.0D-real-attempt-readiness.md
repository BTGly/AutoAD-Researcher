# 3.0D Real Attempt Readiness

Project date: 2026-06-17

## Status

`blocked`

The 3.0D generic contracts are in place for asset preparation, immutable
attempt execution, metrics parsing, scientific validity, and reproducibility
comparison. Repository and benchmark Python preflight now pass locally. The real
PatchCore/MVTec attempt is still blocked by the missing MVTec AD bottle dataset.

## Checked Facts

| Check | Result |
|---|---|
| GPU availability | Passed: NVIDIA GeForce RTX 4090, driver 550.107.02 |
| PatchCore source availability | Passed: `workspace/repos/patchcore-inspection` is at detached commit `fcaa92f124fb1ad74a7acf56726decd4b27cbcad` |
| Repository workspace boundary | Passed: repository preflight passed |
| Repository detached HEAD | Passed: repository preflight passed |
| Dataset root environment | Failed: `AUTOAD_INTERNAL_BENCHMARK_DATASET_ROOT` is not set |
| Dataset workspace boundary | Failed: no local MVTec AD bottle directory was found under `workspace/datasets` |
| Benchmark Python environment | Passed: environment preflight passed with Python 3.11.15, torch 2.5.1+cu124, torchvision 0.20.1+cu124, CUDA 12.4, faiss 1.14.3, timm 1.0.27 |
| Backbone weight asset | Passed: `wide_resnet50_2-95faca4d.pth` identified with SHA256 `95faca4d11227dddf8633dbb5ff6c8a9003c1aa5b8945c73834b8007b10950b8` |
| MVTec download path | Blocked: the official MVTec page requires filling in a dataset form and states CC BY-NC-SA 4.0 non-commercial terms; previously published mydrive links now return 404 |

## Gate Decision

Do not create `attempt_01` or `attempt_02` evidence from mocks. The 3.0 final
seal remains blocked until the real MVTec AD bottle data is available under the
workspace dataset boundary, preflight passes, and two independent real attempts
produce parsed required metrics and a reproducibility report.

## Next Required Actions

1. Put MVTec AD bottle data under `workspace/datasets` and set
   `AUTOAD_INTERNAL_BENCHMARK_DATASET_ROOT` to that dataset root.
2. Copy `wide_resnet50_2-95faca4d.pth` into the attempt `TORCH_HOME` through
   the AssetPlan/AssetManifest path before execution.
3. Run `scripts/benchmark/preflight.py` for `attempt_01`; only execute the
   real controlled attempt after preflight passes.
4. Parse PatchCore `results.csv` with the CSV metric parser and validate the
   scientific contract.
