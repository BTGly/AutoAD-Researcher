# AutoAD-Researcher Step 3.10 开发计划：End-to-End Acceptance & Demo

> 文档状态：草稿 v0.1
> 更新日期：2026-06-18
> 阶段名称：**End-to-End Acceptance & Demo / 系统验收与演示**
> 前置条件：Step 3.1–3.9 全部代码实现完成、对应测试通过
> 核心原则：**验收必须使用真实成果物，不得使用 fixture 替代**

---

# 0. 阶段定义

Step 3.10 不是新功能阶段，而是**第三阶段（3.1–3.9）的系统验收门**。只有通过本步骤的验收，才能声明：

```
Stage 3 Vertical Research Loop accepted and sealed
```

验收分为三层：

| 层 | 内容 | 判定标准 |
|----|------|----------|
| L1 构件验收 | 每个阶段的 artifact、handoff、SHA 链独立可验证 | 所有 handoff 有正式 schema、SHA 双向闭合 |
| L2 流程验收 | PipelineController 或 Orchestrator 按正确顺序调用各阶段，异常时 fail-closed | 一次 `run()` 调用产出完整 artifact 树 |
| L3 真实任务验收 | 一条真实任务（PDF + repo + patch + command + metric + report）走通 | 验收清单全部完成 |

L1 和 L2 可以在任何环境执行。L3 必须在配备 GPU 的 AutoDL 容器内执行。

---

# 1. 前置条件

## 1.1 各阶段实现确认

| 阶段 | 要求 | 检查方法 |
|------|------|----------|
| 3.0 Tool Foundation | ProcessTool / Filesystem / Permission Engine 全部通过安全检查 | `test_process_tool.py` `test_filesystem_tools.py` `test_permission_engine.py` |
| 3.1 Repository Intelligence | 可扫描指定 commit 的 Git 仓库，产出 7 个 artifact | `test_repository_intelligence_*` (12 files) |
| 3.2 Paper Intelligence + Context | 可解析指定 PDF，产出 research_context.json | `test_paper_intelligence_behavior.py` |
| 3.4 Idea & Transfer Design | 从 research_context 产出 1–3 Variant + handoff | `test_transfer_design.py` |
| 3.5 Experiment Planner | 从 3.4 handoff 产出含 9 个 artifact 的实验计划包 | `test_experiment_planning_schemas.py` (49 tests) |
| 3.6 Patch Planner | 从实验计划产出文件级 RepositoryChangePlan + PatchPayload | `test_patch_planning_behavior.py` |
| 3.7 Patch Applicator | 审批后受控应用 patch，产出 PatchRunnerHandoff v2 | `test_patch_runner_handoff_v2.py` (15 tests) |
| 3.8 Runner Intake | 从 PatchRunnerHandoff v2 接收 intake，执行实验，产出 AttemptRecord/ResourceUsageReport | `test_experiment_runner.py` `test_patchcore_attempt.py` |
| 3.9 Results Analysis | 从 execution 产出 PairedMetricObservation、ValidityReport、BundleBudgetAssessment、FinalReport | `test_metrics_and_validity.py` `test_reproducibility.py` |

## 1.2 编排器要求

`pipeline/orchestrator.py` 中的 `Orchestrator.run()` 必须按以下顺序串联全部阶段：

```
0. Intake (input_task.yaml + source_manifest.json)
    ↓
1. Repository Intelligence  →  repo_summary.json  (7 artifacts)
    ↓
2. Paper Intelligence       →  paper_summary.json (7 artifacts)
    ↓
3. Context Assembly         →  research_context.json
    ↓
4. Idea & Transfer Design   →  idea_transfer_design_handoff.json
    ↓  [用户确认 Variant]
5. Experiment Planner       →  experiment_planner_handoff.json  (9 artifacts)
    ↓  [用户确认实验计划]
6. Patch Planner            →  patch_plan.json + patch.diff
    ↓  [用户审批 Patch]
7. Patch Applicator         →  PatchRunnerHandoff v2
    ↓
8. Runner Intake + Execute  →  ExecutionUnitRecord + AttemptRecord + ResourceUsageReport
    ↓
9. Results Analysis         →  PairedMetricObservation + ValidityReport + BudgetAssessment
    ↓
10. Report Generator        →  FinalReport + comparison_report.md
```

各阶段间通过 ArtifactStore 传递，handoff 只包含 SHA 引用，不允许传递裸数据。

## 1.3 必须实现的 3.8/3.9 合同（从 v2.12 计划到代码）

当前 3.8/3.9 代码（`runner/`, `analysis/`, `supervisor/`）尚未完全对齐 v2.12 计划。验收前必须实现：

### 3.8 missing contracts

| 合同 | 计划位置 | 实现要求 |
|------|----------|----------|
| `RunnerIntakeRequest` | 3.8 计划 §1 | 包含 `patch_runner_handoff_ref: ArtifactReferenceV2` |
| `ExecutionUnitRecord` | 3.8 计划 §4 | terminal_reason 闭包、final_status 派生、attempt 约束 |
| `AttemptRecord` | 3.8 计划 §4 | unit_id、outcome、四组 ref、resolved_bindings |
| `AttemptOutcome` | 3.8 计划 §4 | 从 artifact 内容确定性派生，非手写 |
| `ResourceUsageReport` (updated) | 3.8 计划 §4 | gpu_count_used + actual_gpu_hours computed |
| `validate_resolution_presence()` | 3.8 计划 §5 | ref↔resolved 双向等价约束 |
| `validate_attempt_record_against_artifacts()` | 3.8 计划 §5 | SHA/run_id/attempt/command 身份绑定 |
| `derive_attempt_outcome()` | 3.8 计划 §5 | 从 ResolvedArtifact 内容派生 outcome |
| `derive_execution_status()` | 3.8 计划 §5 | 3.0 ExperimentExecutionResult 适配 |
| `derive_terminal_reason()` | 3.8 计划 §5 | outcome → terminal_reason → final_status |
| `compute_identity_match()` | 3.8 计划 §5 | AttemptIdentitySnapshot 确定性比较 |

### 3.9 missing contracts

| 合同 | 计划位置 | 实现要求 |
|------|----------|----------|
| `PairedMetricObservation` | 3.9 计划 §1 | baseline/variant metric ref、pair_validity_status、delta 重算 |
| `validate_observation_against_metric_artifacts()` | 3.9 计划 §2 | SHA/seed/provenance/pair_validity 全部校验 |
| `AggregatedMetricComparison` | 3.9 计划 §2 | mean/status/seed_count 聚合 |
| `derive_pair_validity()` | 3.9 计划 §2 | 组合两侧 ScientificValidityReport + ParsedMetric |
| `VariantResourceAggregate` (updated) | 3.9 计划 §2 | per_unit_actual_gpu_hours dict + total computed |
| `BaselineResourceAggregate` (updated) | 3.9 计划 §2 | 同上，无 variant_id |
| `BundleResourceAggregate` | 3.9 计划 §2 | baseline + per_variant，total computed + max_unit computed |
| `BundleBudgetAssessment` | 3.9 计划 §2 | missing_unit_ids/missing_variant_ids 覆盖校验 |
| `VariantBudgetAssessment` (updated) | 3.9 计划 §2 | per-experiment only，不涉及 total |
| `determine_budget_assessment()` | 3.9 计划 §2 | per-variant per-experiment 判定 |
| `determine_bundle_budget_assessment()` | 3.9 计划 §2 | bundle total vs max_total_gpu_hours |
| `validate_bundle_resource_coverage()` | 3.9 计划 §2 | 预期 unit IDs 与 actual GPU-hours 覆盖比较 |
| `ResourceComparisonReport` (updated) | 3.9 计划 §2 | 含 bundle + bundle_budget_assessment 字段 + 一致性校验 |
| `FactGenerator` / `ReportFacts` | 3.9 计划 §2 | 确定性预生成报告事实 |
| `FinalReport` / `ReportSection` | 3.9 计划 §2 | 结构化最终报告 |

---

# 2. E2E 验收场景

## 2.1 固定任务

验收使用内部 benchmark 任务：

| 参数 | 值 |
|------|-----|
| 论文 | **"Towards Total Recall in Industrial Anomaly Detection"** (PatchCore, CVPR 2022) |
| Baseline 仓库 | `workspace/repos/patchcore-inspection/` (固定 commit `7f5e9a4`) |
| Dataset | MVTec AD **bottle** 类别 |
| 目标 Variant | 修改 backbone 或特征融合层（具体由 3.4 Transfer Design 产出） |
| GPU | 单卡 NVIDIA (H100/L40S/A100) |
| 环境 | `configs/benchmarks/environments/patchcore_linux_gpu/` |

## 2.2 验收路径

```
真实 PDF papers/patchcore.pdf
  → 3.2 Paper Intelligence 解析
  → 与 3.1 Repository Intelligence 结果汇入 Research Context
  → 3.4 Transfer Design 生成 1–3 Variant
  → 用户选择 1 个 Variant
  → 3.5 Experiment Planner 生成实验计划（含 smoke + 1 seed full）
  → 用户确认计划
  → 3.6 Patch Planner 生成代码修改
  → 用户审批 patch
  → 3.7 Patch Applicator 应用 patch 到 workspace
  → 3.8 Runner Intake + 执行 smoke test
  → 3.9 Analysis 解析指标、评估有效性、生成对比报告
  → 最终报告输出到 runs/{run_id}/final_report.json
```

---

# 3. 验收标准

## 3.1 必须产出物

| # | 产出物 | 格式 | 验证点 |
|---|--------|------|--------|
| 1 | `stage3_acceptance_manifest.json` | JSON | 包含所有 stage + handoff SHA + artifact 列表 |
| 2 | `end_to_end_run_report.json` | JSON | run_id、状态、各阶段结果、失败原因 |
| 3 | `artifact_chain_validation.json` | JSON | 每对 handoff 的 SHA 双向闭合验证 |
| 4 | `security_gate_report.json` | JSON | ProcessTool 白名单、路径逃逸检测、权限审计 |
| 5 | `scientific_validity_report.json` | JSON | ScientificValidityReport 合同 |
| 6 | `comparison_report.md` | Markdown | 人类可读的 baseline-vs-variant 对比 |
| 7 | `final_report.json` | JSON | 结构化最终报告（含结论 + next_run_proposals） |
| 8 | `demo_script.md` | Markdown | 演示脚本（含预期输出和截图指引） |
| 9 | `demo_runbook.md` | Markdown | 从零开始的安装/运行/排错手册 |
| 10 | `release_candidate_report.md` | Markdown | 封板判断 + 已知问题 + P1 清单 |

## 3.2 必须满足的条件

### 不可用 fixture 替代的要素

- [ ] 一个**真实 PDF**（`papers/patchcore.pdf`）
- [ ] 一个**真实 Git 仓库**和固定 commit（`workspace/repos/patchcore-inspection/`）
- [ ] 一次**真实用户确认**（Variant 选择 + 实验计划确认 + Patch 审批）
- [ ] 一个**真实代码修改**（patch.diff 应用到仓库）
- [ ] 一次**真实受控命令执行**（`subprocess.run(shell=False)` 经 PermissionEngine 门控）
- [ ] 一个**真实指标文件**（从 smoke 执行输出的 JSON/CSV 解析）
- [ ] 一个成功、失败或部分成功的**正式报告**（含 conclusion）
- [ ] **完整 artifact SHA 链**（intake → 各阶段 handoff → final report，SHA 双向闭合）
- [ ] 最终 head 的 **GitHub Actions 证据**（verify job success）

### 允许使用 fixture 的要素

- [ ] 论文 PDF 提前下载到 `papers/`（避免网络波动）
- [ ] 仓库提前 clone 到 `workspace/repos/`（固定 commit）
- [ ] 环境锁提前生成（`configs/benchmarks/environments/patchcore_linux_gpu/requirements.lock.txt`）
- [ ] 测试数据提前下载（MVTec AD bottle → `workspace/datasets/`）

## 3.3 验收流程

```
Phase 1: L1 构件验收（~30 min）
  逐个验证每个阶段的 handoff SHA 链 + schema 合规
  可并行执行

Phase 2: L2 流程验收（~15 min）
  Orchestrator.run() 调用全部阶段
  验证 artifact 树完整、异常时 fail-closed

Phase 3: L3 真实任务验收（~60 min）
  准备：确认 PDF + 仓库 + 数据集 + 环境锁
  执行：一条完整 run
  检查：所有验收产出物
  判定：通过 / 有条件通过 / 不通过
```

---

# 4. 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| GPU 环境不兼容 | 中 | 高 | 提前生成环境锁 + 使用已有 lockfile |
| PDF 解析精度不足导致 Transfer 失败 | 中 | 高 | 3.2 维护 MinerU 精度 fixture；3.3 Context Repair 可弥补 |
| 真实 patch 导致仓库损坏 | 低 | 高 | 3.7 在 workspace 副本上操作，不从 original 修改 |
| smoke test 执行超时 | 中 | 中 | 3.8 在实验计划中设定 timeout；Pipeline fail-closed |
| 3.8/3.9 新合同未完成 | 高 | 高 | 验收前先完成合同实现 + 测试 |
| Orchestrator 未就绪 | 高 | 高 | 可先用 `PipelineController` 扩展，逐步加入更多阶段 |

---

# 5. 时间线估算

| 工作项 | 估算 |
|--------|------|
| 3.8 新合同实现（10 validator + schema update） | 2–3 天 |
| 3.9 新合同实现（15 validator + aggregator + report） | 3–4 天 |
| Orchestrator 实现（串联 3.1→3.9） | 1–2 天 |
| L1 构件验收测试 | 0.5 天 |
| L2 流程验收测试 | 0.5 天 |
| L3 真实任务验收执行 | 1 天 |
| 文档 + demo script/runbook 编写 | 1 天 |
| **合计** | **9–12 天** |

---

# 6. 封板判定

```
L1 构件验收:  [ ] 未开始  [ ] 通过  [ ] 有条件通过  [ ] 不通过
L2 流程验收:  [ ] 未开始  [ ] 通过  [ ] 有条件通过  [ ] 不通过
L3 真实验收:  [ ] 未开始  [ ] 通过  [ ] 有条件通过  [ ] 不通过

Stage 3 final: [ ] accepted  [ ] rejected  [ ] conditioned
```

最终封板声明：

```text
Stage 3 Vertical Research Loop accepted and sealed.
Commit: {sha}
Date: {date}
Verification: {GitHub Actions run URL}

Acceptance authority: {reviewer}
```

---

# A. 附录：产出物示例结构

## `stage3_acceptance_manifest.json`

```json
{
  "schema_version": 1,
  "run_id": "e2e_acceptance_001",
  "stages": [
    {
      "stage": "repository_intelligence",
      "status": "success",
      "handoff_sha256": "abc...",
      "artifacts": ["repo_summary.json", "repo_structure.json", "..."]
    },
    {
      "stage": "paper_intelligence",
      "status": "success",
      "handoff_sha256": "def...",
      "artifacts": ["paper_summary.json", "method_components.json", "..."]
    }
  ],
  "final_handoff_sha256": "fff...",
  "verification": {
    "sha_chain_closed": true,
    "all_stages_completed": true,
    "failed_stage": null
  }
}
```

## `artifact_chain_validation.json`

```json
{
  "schema_version": 1,
  "run_id": "e2e_acceptance_001",
  "bindings": [
    {
      "upstream_stage": "idea_transfer_design",
      "upstream_handoff_sha256": "abc...",
      "downstream_stage": "experiment_planning",
      "downstream_input_ref_sha256": "abc...",
      "match": true
    }
  ],
  "all_match": true
}
```
