# AutoAD-Researcher Step 3.8 开发计划：Experiment Execution Integration

> 文档状态：终审稿 v2.5（canonical + binding_id + PatchRunnerHandoff v2 已落地）
> 更新日期：2026-06-18
> 上游：3.5 ExperimentPlannerHandoff + 3.7 PatchRunnerHandoff v2（`schemas/patch_planning.py`）
> 下游：3.9 Results Analysis & Final Report

---

# 0. 阶段定义

```
ExperimentPlannerHandoff (3.5) + PatchRunnerHandoff v2 (3.7, schema_version=2)
    ↓
Runner Intake Validator → RunnerIntakeReport
    ↓
Matrix → Immutable ExecutionUnitPlans
    ↓
每个 ExecutionUnit:
  → ExperimentCommandPlan (3.0) → Preflight (3.0)
  → Attempt 01: AttemptIdentitySnapshot → ResourceUsageReport →
    MetricsReport → ScientificValidityReport → RetryDecision
  → Attempt 02 (仅 retry_same_command 且 identity 匹配)
    ↓
ExecutionUnitRecord + ExecutionManifest
    ↓
ExperimentExecutionHandoff → 3.9
```

## 0.1 关键原则

- `ExecutionUnitPlan` 不可变，SHA 一经计算永不改变
- 运行时解析结果入 `AttemptRecord`，不入 Plan
- 计划态 binding 有唯一 `binding_id`
- Retry = 失败分类允许 + identity 匹配

---

# 1. 3.7 Handoff（已落地 `schemas/patch_planning.py`）

```python
class PatchRunnerHandoff(BaseModel):
    schema_version: Literal[2] = 2
    status: Literal["eligible_for_runner_intake"]
    run_id: str; repository_before_commit: str
    approved_patch_plan_sha256: str; selected_variant_ids: list[str]
    experiment_bundle_ref: str
    baseline_workspace_ref: BaselineWorkspaceRef
    variant_workspaces: list[VariantWorkspaceHandoff]
    next_stage: Literal["runner_intake"]

class BaselineWorkspaceRef(BaseModel):
    workspace_id: str; repository_fingerprint: str
    repository_commit: str; repository_validation_ref: ArtifactReference

class VariantWorkspaceHandoff(BaseModel):
    workspace_id: str; variant_ids: list[str]
    repository_fingerprint: str; patch_diff_sha256: str
    local_validation_report_sha256: str
    patch_application_manifest_ref: ArtifactReference
    post_patch_validation_report_ref: ArtifactReference
```

---

# 2. WorkspaceExecutionRef + Intake

```python
class WorkspaceExecutionRef(BaseModel):
    workspace_id: str
    subject_type: Literal["baseline", "variant"]
    variant_ids: list[str]
    repository_fingerprint: str; repository_commit: str
    patch_diff_sha256: str | None
    local_validation_report_sha256: str | None
    patch_runner_handoff_ref: ArtifactReference | None

    @model_validator(mode="after")
    def _validate(self):
        if self.subject_type == "baseline":
            assert not self.variant_ids
            assert self.patch_diff_sha256 is None
            assert self.local_validation_report_sha256 is None
            assert self.patch_runner_handoff_ref is None
        else:
            assert self.variant_ids
            assert self.patch_diff_sha256 is not None
            assert self.local_validation_report_sha256 is not None
            assert self.patch_runner_handoff_ref is not None
        return self

class RunnerIntakeRequest(BaseModel):
    experiment_planner_handoff_sha256: str; experiment_matrix_sha256: str
    shared_protocol_fingerprint: str; statistical_analysis_plan_sha256: str
    operational_guard_policy_sha256: str; workspace_refs: list[WorkspaceExecutionRef]

class RunnerIntakeReport(BaseModel):
    status: Literal["eligible", "blocked", "needs_revalidation"]
    checks: list[IntakeCheck]; report_sha256: str
```

整体 Validator：恰好 1 baseline workspace；workspace_id 不重复；variant 不跨 workspace 重复。

---

# 3. 计划态 vs 运行时态

```python
class PlannedArtifactBinding(BaseModel):
    """唯一 binding_id，在整个 ExecutionUnitPlan 集合中唯一。"""
    binding_id: str
    consumer_requirement_id: str
    producer_unit_id: str
    producer_production_id: str

class PlannedArtifactProduction(BaseModel):
    production_id: str; artifact_type: str; description: str

class ExecutionUnitPlan(BaseModel):
    """不可变。"""
    unit_id: str; matrix_entry_id: str
    subject_type: Literal["baseline", "variant"]
    variant_id: str | None
    stage: Literal["baseline", "fit", "smoke", "full"]
    seed: int | None  # 事实源: MatrixEntry.seed
    workspace_id: str
    command_plan: ExperimentCommandPlan; input_refs: ExperimentInputRefs
    metric_parse_specs: list[MetricParseSpec]; guard_policy_entry_id: str
    depends_on_unit_ids: list[str]
    required_input_artifacts: list[PlannedArtifactBinding]
    expected_output_artifacts: list[PlannedArtifactProduction]

class ResolvedArtifactBinding(BaseModel):
    binding_id: str  # 对应 PlannedArtifactBinding.binding_id
    artifact_ref: ArtifactReference; artifact_sha256: str
```

运行时：

```python
class AttemptIdentitySnapshot(BaseModel):
    execution_unit_plan_sha256: str; command_sha256: str
    input_refs_sha256: str; workspace_repository_fingerprint: str

class AttemptRecord(BaseModel):
    attempt_id: str; attempt_index: int
    identity: AttemptIdentitySnapshot
    execution_result_ref: ArtifactReference | None
    metrics_report_ref: ArtifactReference | None
    validity_report_ref: ArtifactReference | None
    resource_usage_ref: ArtifactReference | None
    resolved_bindings: list[ResolvedArtifactBinding]
    produced_artifacts: list[ProducedArtifactRecord]
    started_at: datetime; completed_at: datetime | None
```

ResourceUsageReport：`measured/partially_measured/not_available` 三态 nullable fields + `model_validator`。（同 v2.4）

RetryDecision + RetryIdentity（同 v2.4）。

---

# 4. ExecutionUnitRecord + Manifest + Handoff

```python
class ExecutionUnitRecord(BaseModel):
    unit_id: str; matrix_entry_id: str
    variant_id: str | None; seed: int | None; stage: str; workspace_id: str
    final_status: ExecutionUnitStatus
    final_attempt_id: str | None
    attempts: list[AttemptRecord]
    # 所有 ref 由 final_attempt_id 指向的 AttemptRecord 派生

class MatrixCoverageReport(BaseModel):
    total_matrix_entries: int; executable_entries: int; executed_entries: int
    skipped_entry_ids: list[str]; missing_unit_ids: list[str]

class ExecutionManifest(BaseModel):
    run_id: str; experiment_matrix_sha256: str; protocol_fingerprint: str
    workspace_refs_sha256: str; operational_guard_policy_sha256: str
    runner_intake_report_ref: ArtifactReference
    unit_records: list[ExecutionUnitRecord]
    completed_unit_count: int; failed_unit_count: int; blocked_unit_count: int
    retry_decisions: list[RetryDecision]; matrix_coverage: MatrixCoverageReport

class ExperimentExecutionHandoff(BaseModel):
    run_id: str
    execution_manifest_ref: ArtifactReference
    execution_unit_plans_sha256: str; experiment_matrix_sha256: str
    statistical_analysis_plan_sha256: str; protocol_fingerprint: str
    runner_intake_report_ref: ArtifactReference
    resource_budget_ref: ArtifactReference; budget_decision_ref: ArtifactReference
    workspace_refs: list[WorkspaceExecutionRef]
    completed_unit_ids: list[str]; failed_unit_ids: list[str]; blocked_unit_ids: list[str]
    overall_status: Literal["completed", "partially_completed", "failed", "blocked"]
    next_stage: Literal["3.9_results_analysis"]
```

---

# 5. 状态机 + Resume（同 v2.4）

---

# 6. 测试

```
[ ] binding_id 在 all ExecutionUnitPlans 中唯一
[ ] baseline workspace 四字段=None；variant 四字段必填
[ ] workspace 集合: 1 baseline + variant 不跨 workspace 重复
[ ] ExecutionUnitPlan 不含运行时字段
[ ] measured/partially_measured/not_available 三态字段一致性
[ ] partially_measured 也必须要求 measurement_tool
[ ] validity invalid → do_not_retry
[ ] identity 不匹配 → 拒绝 retry
[ ] ExecutionUnitRecord 所有 ref 由 final_attempt_id 派生
[ ] final_status 由 attempts + final_attempt_id 重算
[ ] Handoff completed/failed/blocked IDs 与 Manifest 一致
[ ] PatchRunnerHandoff v2 schema_version=2 能通过 pytest
```
